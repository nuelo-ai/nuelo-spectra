---
phase: 39-api-key-management-ui-deployment-mode
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/api_v1/__init__.py
  - admin-frontend/src/hooks/useApiKeys.ts
  - admin-frontend/src/lib/admin-api-client.ts
  - admin-frontend/src/components/users/UserApiKeysTab.tsx
autonomous: true
requirements: [APIKEY-06, APIKEY-08, APIINFRA-03]
gap_closure: true

must_haves:
  truths:
    - "GET /api/v1/health returns JSON with status healthy"
    - "Revoking API key in admin UI updates list immediately without page refresh"
    - "Admin API key list displays as table with columns"
  artifacts:
    - path: "backend/app/routers/api_v1/__init__.py"
      provides: "Correct /api/v1 prefix for API v1 router"
      contains: 'prefix="/api/v1"'
    - path: "admin-frontend/src/hooks/useApiKeys.ts"
      provides: "Safe 204 handling in revoke mutation"
    - path: "admin-frontend/src/components/users/UserApiKeysTab.tsx"
      provides: "Table layout for API key list"
  key_links:
    - from: "admin-frontend/src/hooks/useApiKeys.ts"
      to: "backend DELETE /api/admin/users/{user_id}/api-keys/{key_id}"
      via: "204 No Content response handling"
      pattern: "res\\.ok.*204|onSettled"
---

<objective>
Fix three UAT gaps: (1) health endpoint 404 from wrong router prefix, (2) admin revoke error toast from unsafe 204 handling, (3) admin key list from cards to table layout.

Purpose: Close major and minor UAT issues in backend routing and admin frontend.
Output: Working /api/v1/health endpoint, error-free admin revoke flow, table-formatted key list.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-api-key-management-ui-deployment-mode/39-01-SUMMARY.md
@.planning/phases/39-api-key-management-ui-deployment-mode/39-03-SUMMARY.md
@.planning/phases/39-api-key-management-ui-deployment-mode/39-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix API v1 router prefix and admin revoke 204 handling</name>
  <files>
    backend/app/routers/api_v1/__init__.py
    admin-frontend/src/hooks/useApiKeys.ts
    admin-frontend/src/lib/admin-api-client.ts
  </files>
  <action>
    1. In `backend/app/routers/api_v1/__init__.py`: Change `APIRouter(prefix="/v1")` to `APIRouter(prefix="/api/v1")`. Update the module docstring to match. The router is mounted via `app.include_router(api_v1_router)` with no prefix in main.py, so the prefix must be on the router itself.

    2. In `admin-frontend/src/hooks/useApiKeys.ts` — fix `useRevokeUserApiKey`:
       - The backend DELETE returns 204 No Content (empty body). Current code calls `res.json()` on error path which throws on empty body.
       - Fix the mutationFn: After `adminApiClient.delete(...)`, check `if (!res.ok)`. In the error branch, wrap `res.json()` in try/catch — if parsing fails, throw `new Error("Failed to revoke API key")`.
       - Also: handle success path explicitly — if `res.ok` (including 204), just return (do NOT call res.json() on success either, since 204 has no body).
       - Move `queryClient.invalidateQueries` from `onSuccess` to `onSettled` so cache refreshes even if the mutation threw after the server already processed the request.

    3. In `admin-frontend/src/lib/admin-api-client.ts` — fix the `delete` method:
       - The `fetchWithAdminAuth` function unconditionally sets `Content-Type: application/json` even for body-less requests.
       - Update: Only set `Content-Type: application/json` when `options.body` is defined. Move the Content-Type assignment inside a conditional: `if (options.body) { headers["Content-Type"] = "application/json"; }` and remove it from the default headers object.
       - Also ensure the `delete` method only passes `body: JSON.stringify(body)` when body is provided (it already does this with the ternary, but verify).
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev && grep -q 'prefix="/api/v1"' backend/app/routers/api_v1/__init__.py && grep -q 'onSettled' admin-frontend/src/hooks/useApiKeys.ts && echo "PASS"</automated>
    <manual>GET http://localhost:8000/api/v1/health should return 200 with {status: "healthy"}. Revoking a key in admin UI should show success toast and update list immediately.</manual>
  </verify>
  <done>Health endpoint accessible at /api/v1/health (not /v1/health). Admin revoke handles 204 without error toast and invalidates cache via onSettled.</done>
</task>

<task type="auto">
  <name>Task 2: Convert admin API key list from cards to table layout</name>
  <files>admin-frontend/src/components/users/UserApiKeysTab.tsx</files>
  <action>
    Replace the stacked card div layout (the `space-y-3` div with mapped `rounded-lg border p-3` divs) with a proper Table component using shadcn/ui Table, TableHeader, TableBody, TableRow, TableHead, TableCell from `@/components/ui/table` (already exists in admin-frontend).

    Table columns: Name (with status badge and Admin badge inline), Key Prefix, Created, Last Used, Credit Usage, Actions (revoke button).

    Specifics:
    - Import Table, TableHeader, TableBody, TableRow, TableHead, TableCell from "@/components/ui/table"
    - Table header row with columns: Name, Key Prefix, Created, Last Used, Credits, Actions
    - Each key row:
      - Name cell: key name (with line-through if revoked) + Active/Revoked Badge + Admin Badge (if created_by_admin_id)
      - Key Prefix cell: monospace `{key.key_prefix}...`
      - Created cell: formatted date
      - Last Used cell: formatted date or "Never"
      - Credits cell: `{key.total_credits_used.toFixed(1)}`
      - Actions cell: Revoke button (Trash2 icon) if active, or Revoked date if revoked_at
    - Revoked rows: add `opacity-50` to the TableRow
    - Keep all existing dialog/state logic unchanged (create dialog, one-time key display, revoke confirmation)
    - Keep the empty state rendering unchanged
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev && grep -q 'TableHeader' admin-frontend/src/components/users/UserApiKeysTab.tsx && grep -q 'TableBody' admin-frontend/src/components/users/UserApiKeysTab.tsx && echo "PASS"</automated>
    <manual>Admin API Keys tab shows keys in a table with proper column headers instead of stacked cards.</manual>
  </verify>
  <done>Admin API key list renders as a table with Name, Key Prefix, Created, Last Used, Credits, and Actions columns. Revoked keys show dimmed rows with strikethrough names.</done>
</task>

</tasks>

<verification>
- `GET /api/v1/health` returns 200 JSON (not 404)
- Admin revoke flow: click Revoke -> confirm -> success toast, list updates, no error
- Admin key list: table layout with 6 columns
</verification>

<success_criteria>
All three UAT gaps (tests 1, 4, 6) resolved: health endpoint reachable at /api/v1/health, admin revoke works without error toast and updates immediately, admin key list displayed as table.
</success_criteria>

<output>
After completion, create `.planning/phases/39-api-key-management-ui-deployment-mode/39-04-SUMMARY.md`
</output>
