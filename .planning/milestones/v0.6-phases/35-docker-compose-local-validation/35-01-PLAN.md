---
phase: 35-docker-compose-local-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yaml
  - .env.docker.example
  - .gitignore
autonomous: false
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Developer runs `docker compose up --build` and all four containers (db, backend, public-frontend, admin-frontend) start successfully"
    - "Backend on port 8000, public frontend on port 3000, admin frontend on port 3001, PostgreSQL on port 5432"
    - "Backend container waits for PostgreSQL healthcheck before starting (depends_on with service_healthy condition)"
    - "File uploads persist across docker compose down && docker compose up (uploads_data named volume)"
    - "SPECTRA_MODE=dev for local development, BACKEND_URL=http://backend:8000 for inter-service networking"
    - "CORS_ORIGINS includes both http://localhost:3000 and http://localhost:3001"
  artifacts:
    - path: "compose.yaml"
      provides: "Docker Compose orchestration for full Spectra stack"
      contains: "services:"
    - path: ".env.docker.example"
      provides: "Template environment file for Docker Compose backend secrets"
      contains: "SECRET_KEY"
    - path: ".gitignore"
      provides: "Gitignore entry for .env.docker"
      contains: ".env.docker"
  key_links:
    - from: "compose.yaml"
      to: "Dockerfile.backend"
      via: "build.dockerfile reference"
      pattern: "dockerfile: Dockerfile.backend"
    - from: "compose.yaml"
      to: "Dockerfile.frontend"
      via: "build.dockerfile reference"
      pattern: "dockerfile: Dockerfile.frontend"
    - from: "compose.yaml"
      to: "Dockerfile.admin"
      via: "build.dockerfile reference"
      pattern: "dockerfile: Dockerfile.admin"
    - from: "compose.yaml"
      to: ".env.docker"
      via: "env_file reference"
      pattern: "env_file.*\\.env\\.docker"
    - from: "compose.yaml backend service"
      to: "compose.yaml db service"
      via: "depends_on with healthcheck condition"
      pattern: "condition: service_healthy"
---

<objective>
Create the Docker Compose configuration and environment template that orchestrates the full Spectra stack (PostgreSQL, backend, public frontend, admin frontend) for local development.

Purpose: Enable developers to bring up the entire Spectra application with a single `docker compose up` command, with correct service ordering, persistent volumes, and proper inter-service networking.
Output: compose.yaml, .env.docker.example template, .gitignore update
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-docker-compose-local-validation/35-RESEARCH.md
@Dockerfile.backend
@Dockerfile.frontend
@Dockerfile.admin
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compose.yaml and .env.docker.example</name>
  <files>compose.yaml, .env.docker.example, .gitignore</files>
  <action>
Create `compose.yaml` at project root (NOT `docker-compose.yml` -- use modern naming, no `version:` field) with 4 services and 2 named volumes:

**db service:**
- Image: `postgres:16-alpine`
- Ports: `5432:5432`
- Environment: `POSTGRES_USER=spectra`, `POSTGRES_PASSWORD=spectra`, `POSTGRES_DB=spectra`
- Volume: `postgres_data:/var/lib/postgresql/data`
- Healthcheck: `pg_isready -U spectra -d spectra` with interval 5s, timeout 5s, retries 5, start_period 10s

**backend service:**
- Build: context `.`, dockerfile `Dockerfile.backend`
- Ports: `8000:8000`
- Volume: `uploads_data:/app/uploads`
- `env_file: .env.docker` (loads secrets from the actual .env.docker file)
- Inline environment overrides (these take precedence over env_file):
  - `DATABASE_URL: postgresql+asyncpg://spectra:spectra@db:5432/spectra`
  - `SPECTRA_MODE: dev`
- `depends_on: db` with `condition: service_healthy`

**public-frontend service:**
- Build: context `.`, dockerfile `Dockerfile.frontend`
- Ports: `3000:3000`
- Environment: `BACKEND_URL: http://backend:8000`
- `depends_on: backend` with `condition: service_healthy`

**admin-frontend service:**
- Build: context `.`, dockerfile `Dockerfile.admin`
- Ports: `3001:3000` (maps host 3001 to container internal 3000 -- do NOT set PORT env var)
- Environment: `BACKEND_URL: http://backend:8000`
- `depends_on: backend` with `condition: service_healthy`

**Named volumes:**
- `postgres_data` with `driver: local`
- `uploads_data` with `driver: local`

Add a comment header explaining usage: `docker compose up --build`, `docker compose down`, `docker compose down -v`.

Create `.env.docker.example` with ALL backend env vars that need values:
- SECRET_KEY=local-dev-secret-key-change-in-production
- ALGORITHM=HS256
- ACCESS_TOKEN_EXPIRE_MINUTES=30
- REFRESH_TOKEN_EXPIRE_DAYS=30
- FRONTEND_URL=http://localhost:3000
- CORS_ORIGINS=["http://localhost:3000","http://localhost:3001"]
- ADMIN_CORS_ORIGIN=http://localhost:3001
- ANTHROPIC_API_KEY= (blank, user fills in)
- OPENAI_API_KEY= (blank)
- GOOGLE_API_KEY= (blank)
- E2B_API_KEY= (blank)
- TAVILY_API_KEY= (blank)
- SMTP_HOST= (blank)
- SMTP_PORT=587
- SMTP_USER= (blank)
- SMTP_PASS= (blank)

Add comment at top: "Copy to .env.docker and fill in API keys".

Update `.gitignore`: Add `.env.docker` entry (the actual secrets file). The `.env.docker.example` template IS committed.
  </action>
  <verify>
Validate compose syntax: `docker compose -f compose.yaml config --quiet` (requires Docker Desktop running; if not available, verify YAML is valid).
Verify `.env.docker.example` exists and contains SECRET_KEY, CORS_ORIGINS, ANTHROPIC_API_KEY fields.
Verify `.gitignore` contains `.env.docker` entry.
  </verify>
  <done>
compose.yaml exists with 4 services (db, backend, public-frontend, admin-frontend) and 2 named volumes (postgres_data, uploads_data). Backend depends_on db with service_healthy condition. Both frontends depend_on backend with service_healthy condition. Admin frontend maps 3001:3000. SPECTRA_MODE=dev set on backend. BACKEND_URL=http://backend:8000 set on both frontends. .env.docker.example committed as template. .env.docker gitignored.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full stack runs with docker compose up</name>
  <files>compose.yaml</files>
  <action>
Human verification step. Claude has completed all automated work in Task 1. The user now verifies the full stack works end-to-end by running docker compose up and testing each service.
  </action>
  <verify>User confirms all services start and respond correctly.</verify>
  <done>User types "approved" confirming the stack works.</done>
  <what-built>
Complete Docker Compose configuration for the Spectra stack. All four services (PostgreSQL, backend, public frontend, admin frontend) should start with a single command and communicate correctly.
  </what-built>
  <how-to-verify>
1. Copy the env template: `cp .env.docker.example .env.docker`
2. Fill in at least one LLM API key in `.env.docker` (or leave blank if just testing startup)
3. Build and start: `docker compose up --build`
4. Wait for all containers to report healthy (watch logs for backend "Uvicorn running" message)
5. Verify services respond:
   - Backend: `curl http://localhost:8000/health` should return `{"status":"ok"}`
   - Public frontend: open `http://localhost:3000` in browser -- should show login page
   - Admin frontend: open `http://localhost:3001` in browser -- should show admin login page
   - Version endpoint: `curl http://localhost:8000/version` should return `{"version":"dev","environment":"dev"}`
6. Check backend logs show correct startup: `docker compose logs backend | head -30`
   - Should see pg_isready wait, then Alembic migration, then Uvicorn start
   - Should see SPECTRA_MODE=dev logged
7. Verify all containers healthy: `docker compose ps` -- all should show "healthy" status
8. (Optional) Test file upload persistence:
   - Upload a file via http://localhost:3000
   - Run `docker compose down` then `docker compose up`
   - Verify the file still appears
9. Teardown when done: `docker compose down`
  </how-to-verify>
  <resume-signal>Type "approved" if all services start and respond correctly, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- `docker compose config --quiet` passes without errors
- All 4 services defined: db, backend, public-frontend, admin-frontend
- PostgreSQL healthcheck uses pg_isready
- Backend depends_on db with condition: service_healthy
- Both frontends depend_on backend with condition: service_healthy
- uploads_data volume mounted at /app/uploads in backend
- postgres_data volume mounted at /var/lib/postgresql/data in db
- SPECTRA_MODE=dev set on backend
- BACKEND_URL=http://backend:8000 set on both frontends
- Admin frontend maps host port 3001 to container port 3000
- .env.docker gitignored, .env.docker.example committed
</verification>

<success_criteria>
All four containers start successfully with `docker compose up --build`. Backend waits for PostgreSQL health before starting. Public frontend accessible on localhost:3000, admin frontend on localhost:3001, backend on localhost:8000. Named volumes declared for data persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/35-docker-compose-local-validation/35-01-SUMMARY.md`
</output>
