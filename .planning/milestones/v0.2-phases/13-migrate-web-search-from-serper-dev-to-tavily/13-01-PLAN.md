---
phase: 13-migrate-web-search-from-serper-dev-to-tavily
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/search.py
  - backend/app/agents/tools/web_search.py
  - backend/app/config.py
  - backend/app/routers/search.py
  - backend/app/services/agent_service.py
  - backend/.env.example
  - backend/pyproject.toml
autonomous: true

must_haves:
  truths:
    - "Data Analysis Agent receives Tavily's synthesized answer in its LLM context when web search is used"
    - "Search results include source URLs in clickable title + URL format for DataCard display"
    - "GET /search/config returns same response shape using tavily_api_key instead of serper_api_key"
    - "System gracefully degrades when Tavily API key is missing, quota exceeded, or API unavailable"
    - "Zero references to Serper.dev remain in the codebase after migration"
    - "SEARCH_DEPTH is configurable via .env (basic/advanced)"
    - "Web search queries are logged with credits_used for cost tracking (SEARCH-07)"
  artifacts:
    - path: "backend/app/services/search.py"
      provides: "Tavily-backed SearchService with AsyncTavilyClient"
      contains: "AsyncTavilyClient"
    - path: "backend/app/agents/tools/web_search.py"
      provides: "search_web tool returning Tavily answer + source URLs"
      contains: "Web search answer for"
    - path: "backend/app/config.py"
      provides: "tavily_api_key and search_depth settings"
      contains: "tavily_api_key"
    - path: "backend/pyproject.toml"
      provides: "tavily-python dependency"
      contains: "tavily-python"
  key_links:
    - from: "backend/app/services/search.py"
      to: "tavily AsyncTavilyClient"
      via: "SDK import and search() call"
      pattern: "AsyncTavilyClient"
    - from: "backend/app/agents/tools/web_search.py"
      to: "backend/app/services/search.py"
      via: "SearchService.from_settings() factory"
      pattern: "SearchService\\.from_settings"
    - from: "backend/app/agents/tools/web_search.py"
      to: "backend/app/agents/data_analysis.py"
      via: "Tool output string flows through search_results_text into _generate_analysis_with_search"
      pattern: "Web search answer for"
    - from: "backend/app/routers/search.py"
      to: "backend/app/config.py"
      via: "settings.tavily_api_key check"
      pattern: "settings\\.tavily_api_key"
---

<objective>
Migrate web search provider from Serper.dev to Tavily API across the entire backend.

Purpose: Replace Serper.dev (returns URL links only) with Tavily API (returns synthesized answer + URLs) so the Data Analysis Agent gets actual content context for higher-quality analysis. This is a clean provider swap -- no fallback pattern, no frontend changes.

Output: Fully functional Tavily-backed web search with synthesized answers flowing into agent context, all Serper.dev references removed.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-migrate-web-search-from-serper-dev-to-tavily/13-RESEARCH.md

# Source files being modified
@backend/app/services/search.py
@backend/app/agents/tools/web_search.py
@backend/app/config.py
@backend/app/routers/search.py
@backend/app/services/agent_service.py
@backend/.env.example
@backend/pyproject.toml

# Downstream consumer (unchanged but verify compatibility)
@backend/app/agents/data_analysis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SearchService for Tavily and update config/dependencies</name>
  <files>
    backend/app/services/search.py
    backend/app/config.py
    backend/pyproject.toml
    backend/.env.example
  </files>
  <action>
    **1. Add tavily-python dependency to pyproject.toml:**
    - Add `"tavily-python>=0.7.0"` to the `dependencies` list (after `e2b-code-interpreter`)
    - Run `cd backend && pip install tavily-python` to install

    **2. Update Settings in config.py:**
    - Remove: `serper_api_key: str = ""`
    - Add: `tavily_api_key: str = ""`
    - Add: `search_depth: str = "basic"` (after tavily_api_key; valid values: "basic" or "advanced")
    - Change: `search_num_results: int = 5` → `search_num_results: int = 3` (user decision: 3 focused results)
    - Keep all other search settings unchanged (search_max_per_query, search_daily_quota, search_timeout)

    **3. Fully rewrite backend/app/services/search.py:**
    Replace the entire file with a Tavily-backed implementation. Key changes:

    - Module docstring: "Web search service using Tavily API."
    - Import `AsyncTavilyClient` from `tavily`
    - Remove: `httpx` import, `BASE_URL`, `X-API-KEY` header, `blocked_domains`, `organic[]` parsing
    - `SearchResult` dataclass: fields are `title: str` and `url: str` (was `link` and `position`)
    - `SearchResponse` dataclass: add `answer: str | None` field (Tavily's synthesized answer)
    - `SearchService.__init__`: accept `api_key`, `max_results=3`, `search_depth="basic"`, `timeout=10.0`. Create `self.client = AsyncTavilyClient(api_key=api_key)`. Store max_results, search_depth, timeout.
    - `SearchService.from_settings()`: use `settings.tavily_api_key` (not serper). Return None if empty. Pass `settings.search_depth` to constructor.
    - `SearchService.search()`: call `await self.client.search(query=query, search_depth=self.search_depth, topic="general", max_results=self.max_results, include_answer=True, timeout=self.timeout)`. Extract `response.get("answer")` and `response.get("results", [])`. Map results to `SearchResult(title=r.get("title",""), url=r.get("url",""))`. Return `SearchResponse(query, results, answer, success=True)`.
    - Error handling: single `except Exception` block (SDK handles HTTP errors internally). Log error and return `SearchResponse(query, [], None, False, str(e))`.
    - Logging: keep `logger = logging.getLogger("spectra.search")` and structured JSON logging. Add `"has_answer": answer is not None` and `"credits_used": 2 if self.search_depth == "advanced" else 1` to the success log dict. This satisfies SEARCH-07 (cost tracking): Tavily charges 2 credits for advanced depth, 1 for basic.

    Use the exact code from 13-RESEARCH.md Example 1 as the reference implementation.

    **4. Update backend/.env.example:**
    - Find and replace the `SERPER_API_KEY` line with:
      ```
      # Web Search (Tavily)
      TAVILY_API_KEY=your-tavily-api-key
      SEARCH_DEPTH=basic
      ```
    - If there is no existing SERPER_API_KEY line in .env.example, add the Tavily section after the Sandbox section or at the end of the file.
    - Ensure no SERPER references remain in .env.example.
  </action>
  <verify>
    1. `cd backend && python -c "from app.services.search import SearchService, SearchResult, SearchResponse; print('OK')"` succeeds
    2. `cd backend && python -c "from app.config import get_settings; s = get_settings(); print(f'tavily_key={bool(s.tavily_api_key)}, depth={s.search_depth}, num={s.search_num_results}')"` prints `tavily_key=..., depth=basic, num=3`
    3. `cd backend && python -c "from app.config import get_settings; s = get_settings(); assert not hasattr(s, 'serper_api_key'), 'serper still exists'; print('serper removed')"` prints `serper removed`
    4. `grep -ri "serper" backend/app/services/search.py backend/app/config.py backend/pyproject.toml backend/.env.example` returns no matches
  </verify>
  <done>
    SearchService uses AsyncTavilyClient with include_answer=True, max_results=3, configurable search_depth. SearchResponse includes answer field. Config uses tavily_api_key and search_depth. tavily-python added to dependencies. No Serper references in modified files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tool output format, router key check, and remove all Serper references</name>
  <files>
    backend/app/agents/tools/web_search.py
    backend/app/routers/search.py
    backend/app/services/agent_service.py
  </files>
  <action>
    **1. Update search_web tool in backend/app/agents/tools/web_search.py:**

    Update the module docstring: replace "Serper.dev API calls" with "Tavily Search API".

    Update the `@tool` function docstring: replace "Google Search" with "Tavily Search".

    Change the return format to include Tavily's synthesized answer:
    - After `result = await service.search(query)`, check `result.success`
    - Update the no-results check: `if not result.results and not result.answer:` (both empty means no useful data)
    - Build formatted output:
      ```python
      formatted = ""
      if result.answer:
          formatted += f"Web search answer for '{query}':\n{result.answer}\n\n"
      if result.results:
          formatted += "Sources:\n"
          for r in result.results:
              formatted += f"- {r.title}: {r.url}\n"
      ```
    - Note: source format `"- {title}: {url}\n"` MUST be preserved exactly (regex in data_analysis.py `_extract_sources_from_tool_response` depends on this pattern)
    - Note: `r.link` is now `r.url` (SearchResult field renamed in Task 1)

    **2. Update router key check in backend/app/routers/search.py:**
    - Line 38: change `settings.serper_api_key` → `settings.tavily_api_key`
    - Update docstring comment on line 31: change "Whether Serper API key is set" → "Whether Tavily API key is set"

    **3. Clean Serper references in backend/app/services/agent_service.py:**
    - Line 215: change comment `# Track search quota per user query (not per Serper API call)` → `# Track search quota per user query (not per API call)`
    - Line 419: change comment `# Track search quota per user query (not per Serper API call)` → `# Track search quota per user query (not per API call)`
    - Line 473: change docstring `Track search quota per user query (not per Serper API call).` → `Track search quota per user query (not per API call).`

    **4. Final Serper cleanup verification:**
    Run `grep -ri "serper" backend/` and verify ZERO matches. If any remain, fix them.

    **5. Verify source extraction compatibility:**
    Confirm that `_extract_sources_from_tool_response()` in `data_analysis.py` uses regex `r"^- (.+?):\s+(https?://\S+)$"` and that the new tool output format `"- {title}: {url}\n"` matches this pattern. The Tavily answer text appears above the Sources section and should NOT contain lines matching this pattern. No changes needed to data_analysis.py.

    **6. Verify Tavily answer flows into agent LLM context (locked decision):**
    Confirm the end-to-end answer injection path is intact — this is a critical locked decision. Trace the data flow:
    1. `search_web` tool returns a string starting with `"Web search answer for '{query}':\n{answer}\n\n"` (from step 1 above)
    2. In `da_response_node` (data_analysis.py lines 182-187), `ToolMessage.content` is concatenated into `search_results_text`
    3. `_generate_analysis_with_search` (data_analysis.py line 255-258) appends `search_results_text` (which now contains Tavily's synthesized answer) directly into the system prompt under `**Web Search Results (incorporate relevant findings and cite sources):**`
    4. This means Tavily's answer text is injected into the agent's LLM context as part of the system prompt — not displayed separately in the UI.
    Verify by reading `data_analysis.py` lines 182-187 and 253-258 to confirm `search_results_text` is still appended to the system prompt. No code changes needed — just verify the existing wiring is compatible with the new tool output format.
  </action>
  <verify>
    1. `grep -ri "serper" backend/` returns zero matches
    2. `cd backend && python -c "from app.agents.tools.web_search import search_web; print(f'Tool: {search_web.name}')"` prints `Tool: search_web`
    3. `cd backend && python -c "from app.routers.search import router; print('Router OK')"` prints `Router OK`
    4. `cd backend && python -c "
    import re
    # Simulate new tool output format
    output = '''Web search answer for 'test query':
    This is the synthesized answer from Tavily.

    Sources:
    - Example Page: https://example.com/page
    - Another Result: https://another.com/result
    '''
    pattern = re.compile(r'^- (.+?):\s+(https?://\S+)$', re.MULTILINE)
    matches = pattern.findall(output)
    assert len(matches) == 2, f'Expected 2 sources, got {len(matches)}'
    print(f'Source extraction OK: {matches}')
    "` prints `Source extraction OK` with 2 extracted sources
    5. Verify Tavily answer injection into agent context: `cd backend && python -c "
    import inspect
    from app.agents.data_analysis import _generate_analysis_with_search
    src = inspect.getsource(_generate_analysis_with_search)
    assert 'search_results_text' in src, 'search_results_text not found in _generate_analysis_with_search'
    assert 'Web Search Results' in src, 'Web Search Results prompt section missing'
    print('Answer injection path verified: search_results_text -> system prompt')
    "`
    6. Run existing tests: `cd backend && python -m pytest tests/ -x -q 2>&1 | tail -5` — all tests pass
  </verify>
  <done>
    search_web tool returns Tavily synthesized answer + source URLs in format compatible with _extract_sources_from_tool_response regex. Router checks tavily_api_key. All Serper references removed from entire backend codebase. Existing tests pass.
  </done>
</task>

</tasks>

<verification>
1. **Import chain**: `python -c "from app.services.search import SearchService; from app.agents.tools.web_search import search_web; from app.routers.search import router; print('All imports OK')"` in backend dir
2. **Zero Serper references**: `grep -ri "serper" backend/` returns empty
3. **Config shape**: GET /search/config returns `{configured, enabled, daily_quota, used_today, quota_exceeded}` (unchanged shape)
4. **Source format compatibility**: Regex `r"^- (.+?):\s+(https?://\S+)$"` matches `"- Title: https://url"` lines in new tool output
5. **Existing tests**: `cd backend && python -m pytest tests/ -x -q` — all pass with no regressions
6. **Tavily answer in tool output**: Tool output starts with `"Web search answer for '{query}':\n{answer}"` followed by `"Sources:\n- Title: URL\n"` lines
7. **Cost tracking (SEARCH-07)**: `grep -n "credits_used" backend/app/services/search.py` shows credits_used field in success log
8. **Answer flows into agent context**: `_generate_analysis_with_search` in data_analysis.py appends `search_results_text` (containing Tavily answer) to system prompt
</verification>

<success_criteria>
- SearchService uses Tavily AsyncTavilyClient with include_answer=True, max_results=3, search_depth from env
- search_web tool returns synthesized answer + source URLs (not just bare links)
- tavily_api_key replaces serper_api_key everywhere (config, router, .env.example)
- search_depth configurable in .env (basic/advanced), defaults to basic
- search_num_results defaults to 3 (was 5)
- tavily-python>=0.7.0 in pyproject.toml dependencies
- Source URL format "- Title: URL" preserved for regex extraction compatibility
- Search success log includes credits_used field (1 for basic, 2 for advanced) for SEARCH-07 cost tracking
- data_analysis.py unchanged (Tavily answer flows through existing search_results_text -> _generate_analysis_with_search system prompt path)
- Zero "serper" references anywhere in backend/
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-migrate-web-search-from-serper-dev-to-tavily/13-01-SUMMARY.md`
</output>
