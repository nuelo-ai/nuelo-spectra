---
phase: 07-multi-llm-provider-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - backend/tests/test_llm_providers.py
  - backend/tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "Test scenarios exist for each LLM provider: Anthropic, OpenAI, Google, Ollama, OpenRouter"
    - "Tests verify factory creates correct LLM class for each provider"
    - "Tests verify per-agent config loading (provider, model, temperature)"
    - "Tests verify startup validation catches invalid configurations"
    - "Tests can run without live API keys (mocked/unit tests)"
  artifacts:
    - path: "backend/tests/test_llm_providers.py"
      provides: "Comprehensive test scenarios for multi-provider LLM infrastructure"
      contains: "test_ollama"
    - path: "backend/tests/conftest.py"
      provides: "Test fixtures for LLM provider testing (may be extended)"
      contains: "mock"
  key_links:
    - from: "backend/tests/test_llm_providers.py"
      to: "backend/app/agents/llm_factory.py"
      via: "Tests factory creates correct provider instances"
      pattern: "get_llm"
    - from: "backend/tests/test_llm_providers.py"
      to: "backend/app/agents/config.py"
      via: "Tests per-agent config loading functions"
      pattern: "get_agent_provider|get_agent_model|get_agent_temperature"
---

<objective>
Create test scenarios for the multi-LLM provider infrastructure covering all 5 providers (Anthropic, OpenAI, Google, Ollama, OpenRouter), per-agent configuration, and startup validation.

Purpose: Satisfy requirement LLM-06 ("System includes well-defined test scenarios for each provider"). Tests verify the infrastructure works correctly without requiring live API connections.
Output: Test file `backend/tests/test_llm_providers.py` with unit tests for factory, config, and validation.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multi-llm-provider-infrastructure/07-CONTEXT.md
@.planning/phases/07-multi-llm-provider-infrastructure/07-01-SUMMARY.md
@.planning/phases/07-multi-llm-provider-infrastructure/07-02-SUMMARY.md
@.planning/phases/07-multi-llm-provider-infrastructure/07-03-SUMMARY.md

Key existing files to reference:
@backend/app/agents/llm_factory.py
@backend/app/agents/config.py
@backend/app/config/prompts.yaml
@backend/app/config/llm_providers.yaml
@backend/app/main.py
@backend/app/routers/health.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LLM provider test scenarios</name>
  <files>
    backend/tests/test_llm_providers.py
    backend/tests/conftest.py
  </files>
  <action>
    Create `backend/tests/test_llm_providers.py` with comprehensive test scenarios for LLM-06. All tests must run WITHOUT live API keys — use `unittest.mock.patch` to mock provider constructors and HTTP calls.

    **Test Group 1: LLM Factory (`get_llm`) — one test per provider**

    ```python
    class TestLLMFactory:
    ```

    - `test_factory_creates_anthropic()` — calls `get_llm("anthropic", "claude-sonnet-4-20250514", "test-key")`, asserts returns `ChatAnthropic` instance. Mock `ChatAnthropic` constructor.
    - `test_factory_creates_openai()` — calls `get_llm("openai", "gpt-4o", "test-key")`, asserts returns `ChatOpenAI` instance. Mock constructor.
    - `test_factory_creates_google()` — calls `get_llm("google", "gemini-2.0-flash", "test-key")`, asserts returns `ChatGoogleGenerativeAI` instance. Mock constructor.
    - `test_factory_creates_ollama()` — calls `get_llm("ollama", "llama3.1", "", base_url="http://localhost:11434")`, asserts returns `ChatOllama` instance. Mock constructor. Verify `base_url` is passed to constructor.
    - `test_factory_creates_openrouter()` — calls `get_llm("openrouter", "anthropic/claude-3.5-sonnet", "test-key")`, asserts returns `ChatOpenAI` instance with `base_url="https://openrouter.ai/api/v1"`. Mock constructor. Verify `default_headers` include `HTTP-Referer` and `X-Title`.
    - `test_factory_rejects_unknown_provider()` — calls `get_llm("unknown", "model", "key")`, asserts raises `ValueError` with message listing supported providers.
    - `test_factory_passes_temperature()` — calls `get_llm("anthropic", "model", "key", temperature=0.7)`, verify temperature is passed to constructor kwargs.

    **Test Group 2: Per-Agent Config Loading**

    ```python
    class TestAgentConfig:
    ```

    - `test_get_agent_provider_explicit()` — mock prompts.yaml with agent having explicit `provider: ollama`, assert `get_agent_provider("agent_name")` returns `"ollama"`.
    - `test_get_agent_provider_fallback_to_default()` — mock prompts.yaml with agent having NO provider field, assert `get_agent_provider("agent_name")` returns default provider from registry.
    - `test_get_agent_model()` — mock prompts.yaml with agent having `model: llama3.1`, assert `get_agent_model("agent_name")` returns `"llama3.1"`.
    - `test_get_agent_model_missing_raises()` — mock prompts.yaml with agent having NO model field, assert `get_agent_model("agent_name")` raises `KeyError`.
    - `test_get_agent_temperature_explicit()` — mock prompts.yaml with agent having `temperature: 0.7`, assert `get_agent_temperature("agent_name")` returns `0.7`.
    - `test_get_agent_temperature_default()` — mock prompts.yaml with agent having NO temperature field, assert `get_agent_temperature("agent_name")` returns `0.0`.
    - `test_get_default_provider()` — mock registry with anthropic marked default, assert returns `"anthropic"`.
    - `test_get_default_provider_missing_raises()` — mock registry with NO default marked, assert raises `ValueError`.

    **Test Group 3: Provider Registry**

    ```python
    class TestProviderRegistry:
    ```

    - `test_load_provider_registry_returns_5_providers()` — load actual `llm_providers.yaml`, assert 5 providers present: anthropic, google, openai, ollama, openrouter.
    - `test_registry_has_one_default()` — load actual registry, assert exactly one provider has `default: true`.
    - `test_registry_anthropic_is_default()` — assert anthropic is the default provider.

    **Test Group 4: Startup Validation**

    ```python
    class TestStartupValidation:
    ```

    - `test_validation_catches_unknown_provider_reference()` — mock prompts.yaml with agent referencing provider not in registry, assert validation raises with clear message including agent name and provider name.
    - `test_validation_catches_missing_api_key()` — mock settings with empty anthropic API key, mock an agent using anthropic, assert validation fails with message about `ANTHROPIC_API_KEY`.
    - `test_validation_catches_ollama_unreachable()` — mock httpx to raise `ConnectError` for Ollama URL, assert validation fails with message about checking Ollama is running.
    - `test_validation_catches_openrouter_invalid_key()` — mock httpx to return 401 for OpenRouter, assert validation fails with message about `OPENROUTER_API_KEY`.
    - `test_validation_skips_unused_providers()` — mock all agents using anthropic only, assert validation does NOT test ollama/openrouter connectivity (only active providers validated).

    **Test Group 5: Error Classification**

    ```python
    class TestErrorClassification:
    ```

    - `test_classify_connection_error()` — assert `classify_llm_error(ConnectionError(...), "ollama")` returns `"network_error"`.
    - `test_classify_timeout_error()` — assert `classify_llm_error(TimeoutError(...), "anthropic")` returns `"network_error"`.
    - `test_classify_auth_error()` — assert `classify_llm_error(Exception("401 Unauthorized"), "openai")` returns `"auth_error"`.
    - `test_classify_rate_limit()` — assert `classify_llm_error(Exception("429 rate limit"), "anthropic")` returns `"rate_limit"`.
    - `test_classify_model_not_found()` — assert `classify_llm_error(Exception("model not found"), "openrouter")` returns `"model_not_found"`.

    **Test Group 6: Health Endpoint**

    ```python
    class TestHealthEndpoint:
    ```

    - `test_health_llm_endpoint_exists()` — assert `/health/llm` route is registered on the health router.
    - `test_health_returns_provider_status()` — mock httpx responses for providers, call endpoint, assert response contains `status` and `providers` fields.
    - `test_health_caches_results()` — call endpoint twice within 60 seconds, assert httpx only called once (cache hit).

    **Mocking strategy:** Use `unittest.mock.patch` to mock:
    - LangChain provider constructors (ChatAnthropic, ChatOpenAI, ChatOllama, ChatGoogleGenerativeAI)
    - `httpx.AsyncClient` for connectivity tests
    - `load_prompts()` and `load_provider_registry()` caches (clear LRU cache before tests)

    **Fixture in conftest.py:** If `backend/tests/conftest.py` exists, extend it. If not, create it. Add fixture to clear LRU caches between tests:
    ```python
    @pytest.fixture(autouse=True)
    def clear_config_caches():
        """Clear LRU caches between tests to prevent state leakage."""
        from app.agents.config import load_prompts, load_provider_registry
        load_prompts.cache_clear()
        load_provider_registry.cache_clear()
        yield
        load_prompts.cache_clear()
        load_provider_registry.cache_clear()
    ```
  </action>
  <verify>
    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run pytest tests/test_llm_providers.py -v` — all tests should pass.

    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run pytest tests/test_llm_providers.py -v --co` — should list all test scenarios (at least 25 tests covering all 5 providers).

    Verify no tests require live API keys:
    `grep -n 'ANTHROPIC_API_KEY\|OPENAI_API_KEY\|GOOGLE_API_KEY\|OPENROUTER_API_KEY' backend/tests/test_llm_providers.py` — should find NO unguarded env var references (all should be mocked).
  </verify>
  <done>
    Test file exists at `backend/tests/test_llm_providers.py` with test scenarios covering: LLM factory (all 5 providers + temperature), per-agent config loading (provider/model/temperature with defaults), provider registry validation, startup validation (missing keys, unreachable providers, unknown references), error classification (5 error types), and health endpoint. All tests pass without live API keys (fully mocked). Satisfies LLM-06.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_llm_providers.py -v` passes all tests
2. Tests cover all 5 providers (Anthropic, OpenAI, Google, Ollama, OpenRouter)
3. Tests verify factory, config, validation, error classification, and health endpoint
4. No tests require live API keys
5. At least 25 test scenarios defined
</verification>

<success_criteria>
- Test scenarios exist for each LLM provider (LLM-06 satisfied)
- Factory tests verify correct class instantiation per provider
- Config tests verify provider/model/temperature loading and defaults
- Validation tests verify fail-fast catches misconfigurations
- All tests run in CI without API keys (fully mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-llm-provider-infrastructure/07-04-SUMMARY.md`
</output>
