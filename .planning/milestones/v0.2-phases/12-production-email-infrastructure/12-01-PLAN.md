---
phase: 12-production-email-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/models/password_reset.py
  - backend/app/models/__init__.py
  - backend/app/services/email.py
  - backend/app/templates/email/password_reset.html
  - backend/app/templates/email/password_reset.txt
  - backend/alembic/env.py
  - backend/alembic/versions/xxx_add_password_reset_tokens.py
  - backend/pyproject.toml
  - backend/.env.example
autonomous: true

must_haves:
  truths:
    - "SMTP email service sends password reset emails when SMTP is configured"
    - "Email service falls back to console logging when SMTP is not configured"
    - "Password reset tokens are stored as SHA-256 hashes in PostgreSQL"
    - "Reset emails include HTML with plain text fallback (multipart MIME)"
    - "Reset email has personalized greeting using first name"
  artifacts:
    - path: "backend/app/config.py"
      provides: "SMTP settings (smtp_host, smtp_port, smtp_user, smtp_pass, smtp_from_email, smtp_from_name)"
      contains: "smtp_host"
    - path: "backend/app/models/password_reset.py"
      provides: "PasswordResetToken SQLAlchemy model with token_hash, is_used, is_active, expires_at"
      contains: "class PasswordResetToken"
    - path: "backend/app/services/email.py"
      provides: "Async SMTP email sending with Jinja2 templates and dev-mode fallback"
      contains: "aiosmtplib"
    - path: "backend/app/templates/email/password_reset.html"
      provides: "Professional HTML email template with Spectra branding and reset button"
      min_lines: 20
    - path: "backend/app/templates/email/password_reset.txt"
      provides: "Plain text email fallback"
      min_lines: 5
  key_links:
    - from: "backend/app/services/email.py"
      to: "backend/app/templates/email/password_reset.html"
      via: "Jinja2 FileSystemLoader"
      pattern: "FileSystemLoader"
    - from: "backend/app/services/email.py"
      to: "backend/app/config.py"
      via: "Settings import for SMTP config"
      pattern: "settings\\.smtp_"
    - from: "backend/app/models/password_reset.py"
      to: "backend/app/models/base.py"
      via: "SQLAlchemy Base inheritance"
      pattern: "class PasswordResetToken\\(Base\\)"
---

<objective>
Build the SMTP email infrastructure and database-backed password reset token system.

Purpose: Replace Mailgun HTTP API with standard SMTP via aiosmtplib, replace JWT-based tokens with database-backed single-use tokens, and create professional Jinja2 email templates. This is the foundation layer that Plan 02 will wire into the auth endpoints.

Output: Working email service, PasswordResetToken model with migration, HTML/text email templates, updated config with SMTP settings.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-production-email-infrastructure/12-RESEARCH.md

@backend/app/config.py
@backend/app/services/email.py
@backend/app/models/__init__.py
@backend/app/models/base.py
@backend/app/models/user.py
@backend/alembic/env.py
@backend/pyproject.toml
@backend/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: SMTP config, PasswordResetToken model, migration, and dependency</name>
  <files>
    backend/app/config.py
    backend/app/models/password_reset.py
    backend/app/models/__init__.py
    backend/alembic/env.py
    backend/pyproject.toml
    backend/.env.example
  </files>
  <action>
    1. **backend/app/config.py** -- Update Settings class:
       - Remove `email_service_api_key: str = ""` and `email_from: str = "noreply@spectra.app"` (under # Email section)
       - Add new SMTP settings in their place:
         ```
         # SMTP Email
         smtp_host: str = ""
         smtp_port: int = 587
         smtp_user: str = ""
         smtp_pass: str = ""
         smtp_from_email: str = "noreply@spectra.app"
         smtp_from_name: str = "Spectra"
         ```

    2. **backend/app/models/password_reset.py** -- Create new file with PasswordResetToken model:
       - Fields: `id` (UUID PK, default uuid4), `email` (String(255), indexed), `token_hash` (String(128), unique, indexed), `is_used` (Boolean, default False), `is_active` (Boolean, default True), `expires_at` (DateTime with timezone), `created_at` (DateTime with timezone, default utcnow)
       - Composite index on (email, is_active) for efficient lookups: `Index("ix_password_reset_tokens_email_active", "email", "is_active")`
       - Use same patterns as existing models (User, SearchQuota): import Base from app.models.base, use Mapped/mapped_column
       - Import: `from datetime import datetime, timezone` / `from uuid import UUID, uuid4` / `import secrets` / `from sqlalchemy import String, DateTime, Boolean, Index` / `from sqlalchemy.orm import Mapped, mapped_column`

    3. **backend/app/models/__init__.py** -- Add import:
       - Add `from app.models.password_reset import PasswordResetToken`
       - Add `PasswordResetToken` to `__all__` list

    4. **backend/alembic/env.py** -- Add model import (line 13):
       - Add `password_reset` to the import line: `from app.models import user, file, chat_message, search_quota, password_reset`

    5. **backend/pyproject.toml** -- Add aiosmtplib dependency:
       - Add `"aiosmtplib>=5.1.0",` to the dependencies list
       - Also add `"jinja2>=3.1.0",` explicitly (already installed as transitive dep, but we now import it directly)

    6. **backend/.env.example** -- Replace email config section:
       - Remove `EMAIL_SERVICE_API_KEY=your-email-api-key` and `EMAIL_FROM=noreply@spectra.app`
       - Add SMTP section with comments explaining common providers:
         ```
         # SMTP Email Configuration
         # Leave SMTP_HOST empty for dev mode (console logging)
         # Gmail: SMTP_HOST=smtp.gmail.com SMTP_PORT=587
         # SendGrid: SMTP_HOST=smtp.sendgrid.net SMTP_PORT=587
         SMTP_HOST=
         SMTP_PORT=587
         SMTP_USER=
         SMTP_PASS=
         SMTP_FROM_EMAIL=noreply@spectra.app
         SMTP_FROM_NAME=Spectra
         ```

    7. **Run migration** -- Generate and apply Alembic migration:
       - `cd backend && uv run alembic revision --autogenerate -m "add password_reset_tokens table"`
       - `cd backend && uv run alembic upgrade head`
       - Verify the generated migration creates `password_reset_tokens` table with correct columns, unique index on `token_hash`, and composite index on `(email, is_active)`

    8. **Install dependency** -- `cd backend && uv add "aiosmtplib>=5.1.0"` (jinja2 already present, but add to pyproject.toml manually for explicitness)
  </action>
  <verify>
    - `cd backend && uv run python -c "from app.models.password_reset import PasswordResetToken; print('Model OK')"` succeeds
    - `cd backend && uv run python -c "from app.config import get_settings; s = get_settings(); print(s.smtp_host, s.smtp_port)"` prints empty string and 587
    - `cd backend && uv run python -c "import aiosmtplib; print(aiosmtplib.__version__)"` prints version >= 5.1.0
    - `cd backend && uv run alembic heads` shows the new migration as head
    - Migration file exists in backend/alembic/versions/ with "password_reset_tokens" in filename
  </verify>
  <done>
    PasswordResetToken model created with all required fields (token_hash, is_used, is_active, expires_at), Alembic migration generated and applied, SMTP settings in config.py (smtp_host/port/user/pass/from_email/from_name), old email_service_api_key/email_from removed, aiosmtplib installed, .env.example updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Email service rewrite and Jinja2 templates</name>
  <files>
    backend/app/services/email.py
    backend/app/templates/email/password_reset.html
    backend/app/templates/email/password_reset.txt
  </files>
  <action>
    1. **backend/app/templates/email/password_reset.html** -- Create Jinja2 HTML template:
       - Create directories: `backend/app/templates/email/`
       - Minimal branding per locked decision: "Spectra" app name in header, styled reset button, no logo, no footer links
       - Personalized greeting: "Hi {{ first_name }},"
       - Professional, concise tone -- straight to the point
       - Styled reset button (inline CSS -- blue #2563eb background, white text, rounded corners, 600 font-weight)
       - Fallback plain-text link below button: "Or copy and paste this link: {{ reset_link }}"
       - Expiry notice: "This link expires in {{ expiry_minutes }} minutes."
       - Security note: "If you did not request this, you can safely ignore this email."
       - All CSS inline on elements (no `<style>` block -- maximum email client compat)
       - Use `select_autoescape(["html"])` in Jinja2 env for XSS safety

    2. **backend/app/templates/email/password_reset.txt** -- Create plain text template:
       - Same content as HTML but plain text
       - Format: greeting, explanation, link URL, expiry notice, ignore notice
       - Variables: `{{ first_name }}`, `{{ reset_link }}`, `{{ expiry_minutes }}`

    3. **backend/app/services/email.py** -- Full rewrite replacing Mailgun with aiosmtplib + Jinja2:
       - Remove httpx import and `_DEV_PLACEHOLDERS` set
       - Add imports: `aiosmtplib`, `email.message.EmailMessage`, `jinja2.Environment`, `jinja2.FileSystemLoader`, `jinja2.select_autoescape`, `pathlib.Path`, `hashlib`, `secrets`
       - Initialize Jinja2 environment at module level: `_template_dir = Path(__file__).parent.parent / "templates" / "email"`, `_jinja_env = Environment(loader=FileSystemLoader(str(_template_dir)), autoescape=select_autoescape(["html"]))`
       - Add helper: `def is_smtp_configured(settings: Settings) -> bool:` returns `bool(settings.smtp_host and settings.smtp_user and settings.smtp_pass)`
       - Add helper: `def create_reset_token() -> tuple[str, str]:` generates `secrets.token_urlsafe(32)` as raw_token, computes `hashlib.sha256(raw_token.encode()).hexdigest()` as token_hash, returns `(raw_token, token_hash)`
       - Add helper: `def verify_token_hash(raw_token: str, stored_hash: str) -> bool:` computes sha256 of raw_token and uses `secrets.compare_digest()` for constant-time comparison
       - Rewrite `send_password_reset_email(to_email, first_name, reset_link, settings)` -- note NEW signature adds `first_name: str | None` parameter:
         - If not SMTP configured: console log (dev mode) with reset link, return True
         - If SMTP configured: render HTML and text templates with Jinja2, build `EmailMessage` with multipart (set_content for text, add_alternative for HTML), set Subject/From/To headers, send via `aiosmtplib.send()` with `hostname`, `port`, `username`, `password`, `start_tls=True`
         - Structured JSON logging: `logger.info("Password reset email sent", extra={"email": to_email, "event": "email_sent"})` on success, `logger.error("Failed to send password reset email", extra={"email": to_email, "event": "email_failed", "error": str(e)})` on failure -- metadata only, no email content per locked decision
         - From header format: `f"{settings.smtp_from_name} <{settings.smtp_from_email}>"`
         - Subject: "Spectra - Reset Your Password"
       - Add `async def validate_smtp_connection(settings: Settings) -> bool:` for startup validation (used by Plan 02):
         - If not configured: log info "SMTP not configured - email will use console logging (dev mode)", return False
         - If configured: create `aiosmtplib.SMTP(hostname=..., port=..., start_tls=True, timeout=10)`, connect, login, quit, return True
         - On any exception: log warning with error details, return False (never block startup per locked decision)
  </action>
  <verify>
    - `cd backend && uv run python -c "from app.services.email import send_password_reset_email, is_smtp_configured, create_reset_token, verify_token_hash, validate_smtp_connection; print('All imports OK')"` succeeds
    - `cd backend && uv run python -c "from app.services.email import create_reset_token, verify_token_hash; raw, h = create_reset_token(); assert verify_token_hash(raw, h); assert not verify_token_hash('wrong', h); print('Token OK')"` passes
    - `cd backend && uv run python -c "from pathlib import Path; p = Path('app/templates/email/password_reset.html'); assert p.exists(); assert '{{ first_name }}' in p.read_text(); print('HTML template OK')"` passes
    - `cd backend && uv run python -c "from pathlib import Path; p = Path('app/templates/email/password_reset.txt'); assert p.exists(); assert '{{ reset_link }}' in p.read_text(); print('Text template OK')"` passes
  </verify>
  <done>
    Email service fully rewritten with aiosmtplib + Jinja2 replacing Mailgun + f-strings. HTML template has minimal Spectra branding, personalized greeting, styled reset button, inline CSS. Plain text fallback included. Dev-mode console logging fallback works when SMTP not configured. Token generation uses secrets.token_urlsafe(32) with SHA-256 hashing. All email logging is structured JSON with metadata only.
  </done>
</task>

</tasks>

<verification>
- All new imports resolve without errors
- PasswordResetToken model is registered with SQLAlchemy metadata
- Alembic migration creates password_reset_tokens table
- Email templates render correctly with Jinja2
- Token generation produces cryptographically secure tokens
- Token hash verification uses constant-time comparison
- SMTP config settings load from environment variables
</verification>

<success_criteria>
- aiosmtplib installed and importable
- PasswordResetToken model with migration applied to database
- Email service sends via SMTP when configured, falls back to console when not
- Professional HTML + plain text email templates with personalized greeting
- Secure token generation (secrets.token_urlsafe) and hashing (SHA-256)
- Old Mailgun code completely removed from email.py
- Old email_service_api_key/email_from removed from config.py
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-email-infrastructure/12-01-SUMMARY.md`
</output>
