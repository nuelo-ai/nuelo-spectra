---
phase: 10-smart-query-suggestions
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - frontend/src/types/file.ts
  - frontend/src/types/chat.ts
  - frontend/src/components/chat/QuerySuggestions.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/components/chat/DataCard.tsx
autonomous: false

must_haves:
  truths:
    - "User sees 5-6 query suggestions grouped by category when opening new chat tab with empty state"
    - "Greeting text reads 'What would you like to know about your data?'"
    - "Suggestions display as clickable chips/pills with category headers"
    - "Clicking a suggestion auto-sends the query immediately (default behavior)"
    - "After clicking one suggestion, remaining chips fade out with animation"
    - "Follow-up suggestions appear at bottom of each DataCard after analysis completes"
    - "Clicking follow-up suggestion auto-sends (same behavior as initial)"
    - "Existing files with no suggestions show fallback empty state"
  artifacts:
    - path: "frontend/src/components/chat/QuerySuggestions.tsx"
      provides: "Reusable suggestion chips component with category grouping and fade animation"
      min_lines: 50
    - path: "frontend/src/components/chat/ChatInterface.tsx"
      provides: "QuerySuggestions rendered in empty state, connected to handleSend"
      contains: "QuerySuggestions"
    - path: "frontend/src/components/chat/DataCard.tsx"
      provides: "Follow-up suggestion chips at bottom of card"
      contains: "follow_up_suggestions"
    - path: "frontend/src/types/file.ts"
      provides: "FileSummaryResponse with query_suggestions and suggestion_auto_send"
      contains: "query_suggestions"
  key_links:
    - from: "frontend/src/components/chat/QuerySuggestions.tsx"
      to: "frontend/src/components/chat/ChatInterface.tsx"
      via: "onSelect callback triggers handleSend"
      pattern: "onSelect.*handleSend"
    - from: "frontend/src/components/chat/ChatInterface.tsx"
      to: "frontend/src/hooks/useFileManager.ts"
      via: "useFileSummary provides query_suggestions data"
      pattern: "useFileSummary"
    - from: "frontend/src/components/chat/DataCard.tsx"
      to: "frontend/src/components/chat/ChatInterface.tsx"
      via: "onFollowUpClick callback triggers handleSend"
      pattern: "onFollowUpClick"
---

<objective>
Build the complete frontend for Smart Query Suggestions: QuerySuggestions component with chips/pills grouped by categories, ChatInterface integration for empty state, DataCard follow-up suggestion chips, and human verification of the full feature.

Purpose: Delivers the user-facing experience where suggestions help users start interacting with data (reducing blank-page intimidation) and continue exploring after each analysis.
Output: New QuerySuggestions component, updated ChatInterface and DataCard, complete suggestion UX.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-smart-query-suggestions/10-CONTEXT.md
@.planning/phases/10-smart-query-suggestions/10-RESEARCH.md
@.planning/phases/10-smart-query-suggestions/10-01-SUMMARY.md

@frontend/src/components/chat/ChatInterface.tsx
@frontend/src/components/chat/DataCard.tsx
@frontend/src/hooks/useFileManager.ts
@frontend/src/types/file.ts
@frontend/src/types/chat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuerySuggestions component and ChatInterface empty state integration</name>
  <files>
    frontend/src/types/file.ts
    frontend/src/components/chat/QuerySuggestions.tsx
    frontend/src/components/chat/ChatInterface.tsx
  </files>
  <action>
    1. **File types** (`frontend/src/types/file.ts`): Extend `FileSummaryResponse` to add:
       ```typescript
       query_suggestions: {
         categories: Array<{
           name: string;
           suggestions: string[];
         }>;
       } | null;
       suggestion_auto_send: boolean;
       ```

    2. **QuerySuggestions component** (`frontend/src/components/chat/QuerySuggestions.tsx`): Create new component:
       - Props: `categories: Array<{name: string; suggestions: string[]}>`, `onSelect: (suggestion: string) => void`, `autoSend: boolean` (default true).
       - State: `selected: string | null`, `visible: boolean` (default true).
       - On chip click: set `selected` to the clicked suggestion text, call `onSelect(suggestion)`. Start fade-out of remaining chips. After 300ms, set `visible` to false.
       - If `!visible`, return null.
       - Layout: Centered flex column with max-w-2xl mx-auto.
       - Greeting: `<p>` with "What would you like to know about your data?" in `text-muted-foreground text-lg font-medium text-center`.
       - For each category: `<div>` with category name as `<h3>` in `text-sm font-medium text-muted-foreground text-center uppercase tracking-wide`, then flex-wrap justify-center gap-2 container for chips.
       - Each chip: `<button>` with classes `px-4 py-2 rounded-full border text-sm cursor-pointer hover:bg-accent hover:border-primary/30 transition-all duration-300 bg-background`. When selected and not this chip: `opacity-0`. When this chip is selected: `bg-primary text-primary-foreground border-primary`.
       - Suggestion text may contain **bold** markers for column names. Use inline ReactMarkdown with minimal config (`components={{ p: ({children}) => <>{children}</> }}`) to render bold text inside chips. Import ReactMarkdown.
       - Add `"use client"` directive at top.

    3. **ChatInterface integration** (`frontend/src/components/chat/ChatInterface.tsx`):
       - Import `QuerySuggestions` from `./QuerySuggestions`.
       - Import `useFileSummary` from `@/hooks/useFileManager`.
       - Call `useFileSummary(fileId)` to get summary data (the hook already exists and polls -- suggestions arrive alongside data_summary).
       - Replace the empty state block (lines 264-280, the `showEmptyState && (...)` block) with:
         - If `summaryData?.query_suggestions?.categories?.length > 0`: render `<QuerySuggestions categories={summaryData.query_suggestions.categories} onSelect={handleSend} autoSend={summaryData.suggestion_auto_send ?? true} />`.
         - Else (no suggestions -- either pre-Phase 10 file or suggestions generation failed): keep the existing fallback empty state with the chat bubble icon and "Ask a question about your data" text.
       - The `QuerySuggestions` `onSelect` should call `handleSend` directly when `autoSend` is true (which it does by default -- `handleSend` is the callback). When `autoSend` is false, instead of calling `handleSend`, the suggestion text should be placed into the chat input. To support this: `ChatInput` needs a way to set its value externally. The simplest approach: add a `ref` or `defaultValue` prop to `ChatInput`, OR use a state variable in ChatInterface. Implementation choice: add a `initialValue` state in ChatInterface, pass it to ChatInput. When `autoSend` is false, `onSelect` sets `initialValue` instead of calling `handleSend`. Check ChatInput's implementation to determine the best approach. If ChatInput uses internal state for the input value, add an `initialValue` prop that sets it via useEffect.
       - Ensure that when `isStreaming` becomes true (after handleSend), the QuerySuggestions component is hidden (the existing `showEmptyState` logic handles this since `isStreaming` makes `showEmptyState` false).
  </action>
  <verify>
    1. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx tsc --noEmit` -- no TypeScript errors.
    2. `QuerySuggestions.tsx` exists and exports the component.
    3. `ChatInterface.tsx` imports and renders QuerySuggestions in empty state.
    4. `FileSummaryResponse` type includes `query_suggestions` and `suggestion_auto_send`.
  </verify>
  <done>
    - QuerySuggestions component renders grouped chips with category headers
    - Greeting text "What would you like to know about your data?" displayed above chips
    - Chips styled as rounded pills with hover state
    - Bold column names rendered inside chips via ReactMarkdown
    - Click triggers handleSend (auto-send) or populates input (non-auto-send)
    - Clicked chip highlights, other chips fade out over 300ms
    - ChatInterface shows QuerySuggestions in empty state when suggestions available
    - Falls back to existing empty state for files without suggestions
  </done>
</task>

<task type="auto">
  <name>Task 2: DataCard follow-up suggestions and stream event type updates</name>
  <files>
    frontend/src/types/chat.ts
    frontend/src/components/chat/DataCard.tsx
    frontend/src/components/chat/ChatInterface.tsx
  </files>
  <action>
    1. **Chat types** (`frontend/src/types/chat.ts`): No new StreamEventType needed -- `follow_up_suggestions` arrives as a field in `node_complete` events (already handled by the generic `data?: Record<string, any>` field). No changes needed to StreamEvent type unless you want to add explicit typing.

    2. **DataCard component** (`frontend/src/components/chat/DataCard.tsx`):
       - Add `followUpSuggestions?: string[]` and `onFollowUpClick?: (suggestion: string) => void` to `DataCardProps` interface.
       - At the bottom of `CollapsibleContent`, after the Analysis section (after the markdown download button area, before the closing `</CollapsibleContent>`), add a follow-up suggestions section:
         - Only render when `followUpSuggestions && followUpSuggestions.length > 0 && !isStreaming`.
         - Container: `<div className="space-y-2 pt-3 border-t border-border/50">`.
         - Header: `<h4 className="text-xs font-medium text-muted-foreground">Continue exploring</h4>`.
         - Chips container: `<div className="flex flex-wrap gap-2">`.
         - Each chip: `<button onClick={() => onFollowUpClick?.(suggestion)} className="px-3 py-1.5 rounded-full border text-xs hover:bg-accent hover:border-primary/30 transition-all duration-200 bg-background cursor-pointer">`.
         - No ReactMarkdown needed for follow-ups (they are simpler, shorter text without bold markers).

    3. **ChatInterface wiring** (`frontend/src/components/chat/ChatInterface.tsx`):
       - In the message list rendering (the `messages.map()` block), when rendering `ChatMessage` for assistant messages that have structured data (DataCard), extract `follow_up_suggestions` from `message.metadata_json.follow_up_suggestions` and pass it to the DataCard.
       - Find where ChatMessage renders DataCard for assistant messages. This is in `ChatMessage.tsx` -- check if DataCard is rendered there or in ChatInterface. Looking at the code, ChatMessage is a separate component. The DataCard is rendered from ChatMessage when metadata contains structured data.
       - **Two integration points:**
         a. **Historical messages**: `ChatMessage` component renders DataCard for messages with `metadata_json.generated_code`. Pass `followUpSuggestions` from `metadata_json.follow_up_suggestions` and an `onFollowUpClick` callback. Since ChatMessage is a child component, add an `onFollowUpClick` prop to ChatMessage and thread it through. In ChatInterface, pass `onFollowUpClick={handleSend}` to each ChatMessage.
         b. **Streaming DataCard**: In the streaming section of ChatInterface (the `getStreamingDataCard()` function and its DataCard rendering around line 350), extract `follow_up_suggestions` from the data_analysis node_complete event. Add it to the streaming DataCard props once the stream completes. During active streaming, follow-ups are not shown (per the `!isStreaming` guard in DataCard).
       - Read `ChatMessage.tsx` to understand how it renders DataCard, then wire through the `onFollowUpClick` and `followUpSuggestions` props appropriately.
  </action>
  <verify>
    1. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx tsc --noEmit` -- no TypeScript errors.
    2. DataCard.tsx accepts followUpSuggestions and onFollowUpClick props.
    3. ChatInterface passes handleSend as onFollowUpClick callback.
  </verify>
  <done>
    - DataCard renders follow-up suggestion chips at bottom after analysis section
    - Follow-up chips only show when not streaming and suggestions exist
    - Follow-up chips styled consistently with initial suggestion chips (rounded pill, smaller)
    - Clicking follow-up chip calls handleSend (auto-sends query)
    - Follow-up suggestions extracted from metadata_json for historical messages
    - Follow-up suggestions extracted from stream events for just-completed analysis
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Smart Query Suggestions feature</name>
  <files>none</files>
  <action>
    CHECKPOINT: Human verification of the complete Smart Query Suggestions feature.

    **What was built:** Complete Smart Query Suggestions feature with initial suggestions on empty chat state and follow-up suggestions on Data Cards. Backend generates suggestions via LLM during onboarding and data analysis. Frontend displays them as clickable chips.

    **How to verify:**

    Prerequisites: Start backend (`cd backend && uvicorn app.main:app --reload`) and frontend (`cd frontend && npm run dev`).

    Test 1 - Initial suggestions on new file upload:
    1. Upload a new CSV/Excel file via the UI.
    2. Wait for onboarding to complete (data summary appears).
    3. Open a new chat tab for that file.
    4. Verify: "What would you like to know about your data?" greeting appears.
    5. Verify: 5-6 suggestion chips appear grouped under 2-4 category headers.
    6. Verify: Suggestions use real column names (check they match the uploaded file).
    7. Verify: Chips are styled as rounded pills.

    Test 2 - Suggestion click auto-sends:
    1. Click any suggestion chip.
    2. Verify: The query is immediately sent (streaming starts, no intermediate step).
    3. Verify: The clicked chip briefly highlights, then all chips fade out.
    4. Verify: A DataCard appears with the analysis result.

    Test 3 - Follow-up suggestions on DataCard:
    1. After the DataCard from Test 2 renders completely.
    2. Verify: 2-3 follow-up suggestion chips appear at the bottom of the DataCard.
    3. Verify: "Continue exploring" header appears above the follow-up chips.
    4. Click a follow-up chip.
    5. Verify: The follow-up query is auto-sent (new analysis starts).

    Test 4 - Existing file backward compatibility:
    1. If you have files uploaded before this phase, open their chat tab.
    2. Verify: The old empty state ("Ask a question about your data") appears (no crash).
    3. Verify: No JavaScript console errors related to null suggestions.

    Test 5 - Page refresh persistence:
    1. After having a conversation with DataCards.
    2. Refresh the page (F5 or Cmd+R).
    3. Verify: Follow-up suggestions still appear on previously rendered DataCards.
    4. Verify: Initial suggestions appear again if you open a new empty chat tab.

    Resume signal: Type "approved" or describe issues found in each test.
  </action>
  <verify>Human confirms all 5 test scenarios pass.</verify>
  <done>All Smart Query Suggestions features verified working: initial suggestions, auto-send, follow-up suggestions, backward compatibility, and persistence.</done>
</task>

</tasks>

<verification>
1. QuerySuggestions component renders with proper chip/pill styling and category grouping
2. ChatInterface shows suggestions in empty state, falls back gracefully for files without suggestions
3. Click auto-sends by default with fade-out animation
4. DataCard shows follow-up chips after analysis
5. Follow-up click triggers new analysis
6. No TypeScript compilation errors
7. Backward compatible with existing files (NULL suggestions handled)
8. Suggestions persist across page refresh (stored in DB)
</verification>

<success_criteria>
- New chat tabs show 5-6 LLM-generated suggestions grouped by categories
- Greeting reads "What would you like to know about your data?"
- Clicking suggestion auto-sends query with zero friction
- Chips fade out after selection
- DataCards show 2-3 follow-up suggestions after analysis completes
- All suggestion interactions work consistently (initial and follow-up)
- No regressions in existing chat functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10-smart-query-suggestions/10-02-SUMMARY.md`
</output>
