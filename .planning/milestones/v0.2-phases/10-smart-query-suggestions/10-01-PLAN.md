---
phase: 10-smart-query-suggestions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/xxx_add_query_suggestions_to_files.py
  - backend/app/models/file.py
  - backend/app/schemas/file.py
  - backend/app/agents/onboarding.py
  - backend/app/services/agent_service.py
  - backend/app/config.py
  - backend/app/config/prompts.yaml
  - backend/app/routers/files.py
  - backend/app/agents/data_analysis.py
  - backend/app/agents/state.py
autonomous: true

must_haves:
  truths:
    - "Onboarding Agent generates both data_summary and query_suggestions in a single LLM call"
    - "query_suggestions stored as JSON in files table alongside data_summary"
    - "GET /files/{id}/summary returns query_suggestions and suggestion_auto_send config"
    - "Data Analysis Agent returns 2-3 follow_up_suggestions with each analysis"
    - "follow_up_suggestions flows through stream events to frontend"
    - "Existing files with NULL suggestions do not break any endpoint"
  artifacts:
    - path: "backend/alembic/versions/xxx_add_query_suggestions_to_files.py"
      provides: "Alembic migration adding query_suggestions JSON column"
      contains: "query_suggestions"
    - path: "backend/app/models/file.py"
      provides: "File model with query_suggestions column"
      contains: "query_suggestions"
    - path: "backend/app/agents/onboarding.py"
      provides: "Extended OnboardingResult with query_suggestions, JSON parsing with fallback"
      contains: "query_suggestions"
    - path: "backend/app/config/prompts.yaml"
      provides: "Extended onboarding prompt for JSON output with suggestions, extended data_analysis prompt for follow-ups"
      contains: "suggestions"
    - path: "backend/app/agents/data_analysis.py"
      provides: "Data Analysis Agent returning follow_up_suggestions"
      contains: "follow_up_suggestions"
    - path: "backend/app/services/agent_service.py"
      provides: "Saves query_suggestions in run_onboarding, follow_up_suggestions in stream allowlist"
      contains: "follow_up_suggestions"
  key_links:
    - from: "backend/app/agents/onboarding.py"
      to: "backend/app/services/agent_service.py"
      via: "OnboardingResult.query_suggestions saved to file_record.query_suggestions"
      pattern: "file_record\\.query_suggestions"
    - from: "backend/app/routers/files.py"
      to: "frontend (via GET /files/{id}/summary)"
      via: "FileSummaryResponse includes query_suggestions and suggestion_auto_send"
      pattern: "query_suggestions"
    - from: "backend/app/agents/data_analysis.py"
      to: "backend/app/services/agent_service.py"
      via: "follow_up_suggestions in state dict flows through node_complete stream event"
      pattern: "follow_up_suggestions"
---

<objective>
Implement all backend infrastructure for Smart Query Suggestions: database schema, onboarding prompt extension for LLM-generated initial suggestions, Data Analysis Agent extension for follow-up suggestions, and API response changes.

Purpose: Provides the data layer and LLM integration needed for the frontend to display initial suggestions on empty chat state and follow-up suggestions on Data Cards.
Output: Database migration, extended onboarding prompt producing JSON with suggestions, extended data_analysis prompt producing follow-ups, updated API responses.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-smart-query-suggestions/10-CONTEXT.md
@.planning/phases/10-smart-query-suggestions/10-RESEARCH.md

@backend/app/models/file.py
@backend/app/schemas/file.py
@backend/app/agents/onboarding.py
@backend/app/agents/data_analysis.py
@backend/app/agents/state.py
@backend/app/services/agent_service.py
@backend/app/config/prompts.yaml
@backend/app/config.py
@backend/app/routers/files.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration, model, schema, onboarding prompt, and agent_service for initial suggestions</name>
  <files>
    backend/alembic/versions/xxx_add_query_suggestions_to_files.py
    backend/app/models/file.py
    backend/app/schemas/file.py
    backend/app/agents/onboarding.py
    backend/app/services/agent_service.py
    backend/app/config.py
    backend/app/config/prompts.yaml
    backend/app/routers/files.py
  </files>
  <action>
    1. **Database migration**: Create Alembic migration adding `query_suggestions` column (JSON, nullable) to `files` table. Run: `cd backend && alembic revision --autogenerate -m "add_query_suggestions_to_files"` then `alembic upgrade head`.

    2. **File model** (`backend/app/models/file.py`): Add `query_suggestions: Mapped[dict | None] = mapped_column(JSON, nullable=True)` after the `data_summary` field. Import `JSON` from sqlalchemy.

    3. **Settings** (`backend/app/config.py`): Add `suggestion_auto_send: bool = True` setting in the Settings class under a "# Suggestions" comment block after the Session Memory section.

    4. **Onboarding Agent** (`backend/app/agents/onboarding.py`):
       - Add `query_suggestions: dict | None = None` field to `OnboardingResult` dataclass.
       - Modify `generate_summary()` signature to return `tuple[str, dict | None]` (summary, suggestions).
       - After LLM invocation, parse `response.content` as JSON with `json.loads()` inside try/except:
         - On success: extract `parsed.get("summary", response.content)` and `parsed.get("suggestions", None)`.
         - On `json.JSONDecodeError`: fallback to `(response.content, None)` -- treat entire response as summary, no suggestions.
       - Return the tuple `(summary, suggestions)`.
       - Update `run()` method to unpack the tuple and pass `query_suggestions` to `OnboardingResult`.

    5. **Prompts YAML** (`backend/app/config/prompts.yaml`): Replace the onboarding `system_prompt` with the extended version that requests JSON output. The new prompt must:
       - Request ONLY valid JSON with `"summary"` and `"suggestions"` keys.
       - Include the exact JSON schema example with categories array.
       - Keep existing summary instructions (Structure & Types, Data Quality, Semantic Understanding, Sample Preview).
       - Add suggestions instructions: 5-6 suggestions in 2-4 meaningful categories per dataset (LLM decides categories -- NOT fixed General/Benchmarking/Trend per CONTEXT.md override of REQUIREMENTS.md), use real column names wrapped in **bold**, mix simple and advanced, 8-15 words each, explain intent not just operation.
       - Increase `max_tokens` from 1500 to 3000.

    6. **Agent service** (`backend/app/services/agent_service.py`): In `run_onboarding()`, after `result = await agent.run(...)`, add `file_record.query_suggestions = result.query_suggestions` alongside the existing `file_record.data_summary = result.data_summary` line.

    7. **File schemas** (`backend/app/schemas/file.py`): Add `query_suggestions: dict | None = None` to `FileDetailResponse`. (FileUploadResponse does not need it -- suggestions aren't ready at upload time.)

    8. **Files router** (`backend/app/routers/files.py`):
       - Update the `FileSummaryResponse` model (defined in the router at line 169) to add `query_suggestions: dict | None = None` and `suggestion_auto_send: bool = True`.
       - Update the `get_file_summary` endpoint to include `query_suggestions=file.query_suggestions` and `suggestion_auto_send=get_settings().suggestion_auto_send` in the response. Import `get_settings` from `app.config`.
  </action>
  <verify>
    1. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && alembic upgrade head` succeeds (migration applies cleanly).
    2. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "from app.models.file import File; print('query_suggestions' in File.__table__.columns)"` prints True.
    3. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "from app.agents.onboarding import OnboardingResult; r = OnboardingResult(data_summary='test', data_profile={}, sample_data='[]', query_suggestions={'categories': []}); print(r.query_suggestions)"` prints the dict.
    4. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "from app.config import get_settings; print(get_settings().suggestion_auto_send)"` prints True.
  </verify>
  <done>
    - Alembic migration adds query_suggestions JSON column to files table
    - File model has query_suggestions mapped column
    - OnboardingResult includes query_suggestions field
    - generate_summary() parses JSON response and extracts suggestions with fallback
    - Onboarding prompt requests structured JSON with summary + categorized suggestions
    - max_tokens increased to 3000
    - run_onboarding() saves query_suggestions to file record
    - GET /files/{id}/summary returns query_suggestions and suggestion_auto_send
    - suggestion_auto_send configurable via .env
  </done>
</task>

<task type="auto">
  <name>Task 2: Data Analysis Agent follow-up suggestions and stream event wiring</name>
  <files>
    backend/app/config/prompts.yaml
    backend/app/agents/data_analysis.py
    backend/app/agents/state.py
    backend/app/services/agent_service.py
  </files>
  <action>
    1. **ChatAgentState** (`backend/app/agents/state.py`): Add `follow_up_suggestions: list[str]` field with docstring "Follow-up suggestion queries generated by Data Analysis Agent for continued exploration."

    2. **Prompts YAML** (`backend/app/config/prompts.yaml`): Extend the `data_analysis` system_prompt to request JSON output. The modified prompt should:
       - Wrap the existing analysis instructions in a JSON output format.
       - Request the LLM to return ONLY valid JSON with two keys: `"analysis"` (the existing analysis text) and `"follow_up_suggestions"` (array of 2-3 short follow-up query strings).
       - Follow-up instructions: "Suggest 2-3 natural next-step queries based on the analysis just completed. Each should be 8-15 words, feel like a natural continuation ('Now filter to show only Q4', 'Compare these totals across regions'). Use real column names from the data."
       - Keep existing placeholders: `{user_query}`, `{executed_code}`, `{execution_result}`.
       - Increase `max_tokens` from 2000 to 2500 to accommodate JSON overhead.

    3. **Data Analysis Agent** (`backend/app/agents/data_analysis.py`): In the STANDARD MODE section (after line 176 `response = await llm.ainvoke(messages)`):
       - Parse `response.content` as JSON with `json.loads()` inside try/except.
       - On success: extract `parsed.get("analysis", response.content)` and `parsed.get("follow_up_suggestions", [])`.
       - On `json.JSONDecodeError`: fallback to `(response.content, [])`.
       - Import `json` at top of file.
       - Update the return dict to include `"follow_up_suggestions": follow_ups`.
       - For MEMORY_SUFFICIENT mode: also add `"follow_up_suggestions": []` to the return dict (no follow-ups for memory-only responses since they don't involve code execution).

    4. **Agent service stream allowlist** (`backend/app/services/agent_service.py`): In `run_chat_query_stream()`, find the node_complete event filter (approximately line 384-388) which currently filters to `("generated_code", "execution_result", "analysis", "final_response", "error", "routing_decision")`. Add `"follow_up_suggestions"` to this tuple.

    5. **Agent service metadata** (`backend/app/services/agent_service.py`): In `run_chat_query_stream()`, in the save_db section where assistant message metadata_json is built (approximately line 416-426), add `"follow_up_suggestions": final_state.get("follow_up_suggestions", [])` to the metadata_json dict so follow-up suggestions persist across page refreshes.
  </action>
  <verify>
    1. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "from app.agents.state import ChatAgentState; print('follow_up_suggestions' in ChatAgentState.__annotations__)"` prints True.
    2. Grep for `follow_up_suggestions` in `agent_service.py` shows it in both the stream allowlist tuple and the metadata_json dict.
    3. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -m pytest tests/ -x -q` -- all existing tests pass (no regressions).
  </verify>
  <done>
    - ChatAgentState has follow_up_suggestions field
    - Data Analysis Agent standard mode parses JSON and returns follow_up_suggestions with graceful fallback
    - Data Analysis Agent memory mode returns empty follow_up_suggestions
    - data_analysis prompt requests JSON with analysis + follow_up_suggestions
    - follow_up_suggestions included in stream event allowlist (flows to frontend)
    - follow_up_suggestions persisted in chat_messages.metadata_json for page refresh
    - All existing tests pass
  </done>
</task>

</tasks>

<verification>
1. Alembic migration applies and rolls back cleanly
2. OnboardingResult carries query_suggestions
3. GET /files/{id}/summary includes query_suggestions and suggestion_auto_send
4. Data Analysis Agent returns follow_up_suggestions in state
5. follow_up_suggestions flows through stream events (in allowlist)
6. follow_up_suggestions persisted in chat message metadata
7. All existing tests pass (no regressions from prompt changes)
</verification>

<success_criteria>
- Database has query_suggestions JSON column on files table
- Onboarding prompt generates categorized suggestions in single LLM call (no separate call)
- API serves suggestions and auto_send config
- Data Analysis Agent generates follow-up suggestions
- Stream events carry follow_up_suggestions to frontend
- All 99 existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-smart-query-suggestions/10-01-SUMMARY.md`
</output>
