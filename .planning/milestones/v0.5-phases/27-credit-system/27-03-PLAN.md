---
phase: 27-credit-system
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - backend/app/routers/chat.py
  - backend/app/services/auth.py
autonomous: true

must_haves:
  truths:
    - "Sending a chat message (both stream and non-stream) deducts credits before agent execution"
    - "A user with insufficient credits gets HTTP 402 with error message containing next reset date"
    - "Unlimited-class users can send messages without balance deduction (transaction still logged)"
    - "New user registration creates a UserCredit row with balance set to their tier's credit allocation"
  artifacts:
    - path: "backend/app/routers/chat.py"
      provides: "Credit gate before agent invocation in both query endpoints"
      contains: "CreditService.deduct_credit"
    - path: "backend/app/services/auth.py"
      provides: "UserCredit row creation during user registration"
      contains: "UserCredit"
  key_links:
    - from: "backend/app/routers/chat.py"
      to: "backend/app/services/credit.py"
      via: "deduct_credit call before agent_service invocation"
      pattern: "CreditService\\.deduct_credit"
    - from: "backend/app/services/auth.py"
      to: "backend/app/models/user_credit.py"
      via: "UserCredit row creation in create_user"
      pattern: "UserCredit"
---

<objective>
Integrate credit deduction into the chat flow (pre-send gate) and create UserCredit rows for new user registrations. This wires the CreditService into the live application.

Purpose: Makes credits functional -- users are actually charged per message, blocked when out of credits, and initialized with credits on signup. Satisfies CREDIT-03 (atomic deduction before agent) and CREDIT-04 (out-of-credits blocking).
Output: 2 modified files integrating credit logic into existing flows.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-credit-system/27-RESEARCH.md
@.planning/phases/27-credit-system/27-CONTEXT.md
@.planning/phases/27-credit-system/27-01-SUMMARY.md

@backend/app/routers/chat.py
@backend/app/services/auth.py
@backend/app/services/credit.py
@backend/app/schemas/credit.py
@backend/app/models/user_credit.py
@backend/app/models/user.py
@backend/app/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add credit deduction gate to chat query endpoints</name>
  <files>backend/app/routers/chat.py</files>
  <action>
Modify `backend/app/routers/chat.py` to add credit deduction BEFORE agent execution in both `query_with_ai` and `stream_query` endpoints.

At the top of the file, add imports:
```python
from decimal import Decimal
from app.services.credit import CreditService
from app.services.user_class import get_class_config
```

**In `query_with_ai` (non-streaming endpoint):**
- BEFORE the `agent_service.run_chat_query()` call, add credit deduction.
- Get credit cost: For now hardcode `Decimal("1.0")` as the default cost. Add a TODO comment: "Phase 28: read default_credit_cost from platform_settings".
- Check if user's class is unlimited: `class_config = get_class_config(current_user.user_class)`. If `class_config and class_config.get("reset_policy") == "unlimited"`, still call deduct (the service handles unlimited logic -- logs but doesn't deduct).
- Call `deduction = await CreditService.deduct_credit(db, current_user.id, credit_cost)`.
- If `not deduction.success`: raise `HTTPException(status_code=status.HTTP_402_PAYMENT_REQUIRED, detail={"error": "insufficient_credits", "message": deduction.error_message, "balance": float(deduction.balance), "next_reset": deduction.next_reset.isoformat() if deduction.next_reset else None})`.
- If success: `await db.commit()` to commit the credit deduction transaction BEFORE running the agent. The agent uses the request-scoped session for validation but saves results with a fresh session (existing pattern from agent_service._save_stream_result). Credit deduction MUST be committed before agent runs because agent execution can be long and may fail.

**In `stream_query` (SSE streaming endpoint):**
- AFTER the file ownership check (`file = await FileService.get_user_file(...)`) but BEFORE the `async def event_generator()`, add the same credit deduction logic.
- Same pattern: deduct, check success, commit deduction, then proceed to streaming.
- On failure, raise HTTPException 402 (same as above). This happens before SSE starts, so the error is returned as a normal HTTP response.
- IMPORTANT: The credit deduction uses the request-scoped `db` session. This is fine because it happens BEFORE streaming starts (per research: "Deduct credits at the START of the request, using the request-scoped session. The deduction is fast.").

Per locked decision: "deduct before agent runs, no refund on failure" -- this matches LLM billing patterns. The deduction commits independently of agent success.

Per locked decision: "Silent deduction per message -- balance updates without showing per-message cost" -- the 402 response only fires when balance is insufficient. Successful deductions are silent (no extra response headers or fields about cost).
  </action>
  <verify>
Read chat.py and verify: (1) CreditService.deduct_credit is called before agent_service.run_chat_query and before event_generator in stream_query, (2) HTTP 402 is raised on insufficient credits, (3) db.commit() is called after successful deduction before agent runs.
  </verify>
  <done>Both chat query endpoints (stream and non-stream) deduct credits atomically before agent execution. Insufficient credits return HTTP 402 with next reset date. Deductions commit independently of agent success.</done>
</task>

<task type="auto">
  <name>Task 2: Create UserCredit row during user registration</name>
  <files>backend/app/services/auth.py</files>
  <action>
Modify `backend/app/services/auth.py` to create a UserCredit row when a new user registers.

Add imports at top:
```python
from decimal import Decimal
from app.models.user_credit import UserCredit
from app.services.user_class import get_class_config, get_default_class
```

In the `create_user` function, AFTER creating the User object and BEFORE `await db.commit()`:

1. Get the default class: `default_class = get_default_class()` (returns "free" for now, Phase 28 makes configurable).
2. Set `user.user_class = default_class` (ensure user gets the default class, not hardcoded "free" in the model default -- though they match for now, this makes intent explicit).
3. Get tier config: `class_config = get_class_config(default_class)`.
4. Determine initial balance:
   - If class_config exists: `initial_balance = Decimal(str(class_config.get("credits", 0)))`.
   - If class_config is None (shouldn't happen with valid config): `initial_balance = Decimal("0")`.
   - For unlimited users (reset_policy == "unlimited"): set balance to `Decimal("-1")` (sentinel value per research recommendation).
5. Create UserCredit: `credit = UserCredit(user_id=user.id, balance=initial_balance)`.
6. `db.add(credit)`.

This solves Pitfall 6 from research: "Missing UserCredit row for new users." New users get credits immediately on signup.

Also: The existing users were backfilled with balance=0 in the Phase 26 migration. Those existing users will need a data migration to set balance to their tier allocation. Add a TODO comment noting: "TODO: Existing users have balance=0 from migration backfill. Consider a one-time script or migration to set balance to tier allocation."
  </action>
  <verify>
Read auth.py and verify: (1) UserCredit import exists, (2) UserCredit row is created in create_user function, (3) Initial balance is set from tier config, (4) db.add(credit) is called before commit.
  </verify>
  <done>New user registration creates a UserCredit row with balance initialized from their tier's credit allocation. Default class assignment is explicit via get_default_class().</done>
</task>

</tasks>

<verification>
- Credit deduction happens BEFORE agent execution (not after)
- HTTP 402 returned for insufficient credits with structured error detail
- Credit deduction committed independently (db.commit before agent runs)
- Unlimited users pass through (CreditService handles logging)
- New users get UserCredit row with correct initial balance
- No float arithmetic in credit code (all Decimal)
- Error message matches locked decision: "You're out of credits. Credits reset on [date]."
</verification>

<success_criteria>
- Both stream_query and query_with_ai have credit gates
- Registration creates UserCredit row
- HTTP 402 response includes error, message, balance, next_reset fields
- Deduction commits before agent invocation
</success_criteria>

<output>
After completion, create `.planning/phases/27-credit-system/27-03-SUMMARY.md`
</output>
