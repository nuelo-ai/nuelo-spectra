---
phase: 31-dashboard-admin-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - admin-frontend/package.json
  - admin-frontend/tsconfig.json
  - admin-frontend/next.config.ts
  - admin-frontend/postcss.config.mjs
  - admin-frontend/eslint.config.mjs
  - admin-frontend/components.json
  - admin-frontend/src/app/globals.css
  - admin-frontend/src/app/layout.tsx
  - admin-frontend/src/app/page.tsx
  - admin-frontend/src/app/(auth)/layout.tsx
  - admin-frontend/src/app/(auth)/login/page.tsx
  - admin-frontend/src/app/(admin)/layout.tsx
  - admin-frontend/src/lib/admin-api-client.ts
  - admin-frontend/src/lib/query-client.ts
  - admin-frontend/src/lib/utils.ts
  - admin-frontend/src/hooks/useAdminAuth.tsx
  - admin-frontend/src/stores/sidebarStore.ts
  - admin-frontend/src/components/layout/AdminSidebar.tsx
  - admin-frontend/src/components/layout/AdminHeader.tsx
  - admin-frontend/src/components/providers.tsx
  - admin-frontend/src/types/admin-auth.ts
autonomous: true
requirements:
  - ARCH-06
  - ARCH-07
  - ARCH-09

must_haves:
  truths:
    - "admin-frontend/ is a standalone Next.js application separate from frontend/"
    - "Running npm run dev in admin-frontend starts on port 3001"
    - "Admin can log in via /login page and is redirected to /dashboard"
    - "Admin shell has a collapsible left sidebar with navigation grouped into Overview, People, Platform sections"
    - "Admin API client intercepts X-Admin-Token response header for sliding window session renewal"
    - "Unauthenticated access redirects to /login"
  artifacts:
    - path: "admin-frontend/package.json"
      provides: "Admin frontend package with matching stack"
      contains: "next"
    - path: "admin-frontend/src/lib/admin-api-client.ts"
      provides: "Admin API client with X-Admin-Token interception"
      contains: "X-Admin-Token"
    - path: "admin-frontend/src/hooks/useAdminAuth.tsx"
      provides: "Admin auth context with login/logout"
      contains: "AdminAuthProvider"
    - path: "admin-frontend/src/components/layout/AdminSidebar.tsx"
      provides: "Collapsible admin sidebar with navigation"
      contains: "AdminSidebar"
  key_links:
    - from: "admin-frontend/src/lib/admin-api-client.ts"
      to: "backend admin auth endpoints"
      via: "fetch with Authorization header and X-Admin-Token interception"
      pattern: "X-Admin-Token"
    - from: "admin-frontend/src/hooks/useAdminAuth.tsx"
      to: "admin-frontend/src/lib/admin-api-client.ts"
      via: "imports adminApiClient for login and /auth/me calls"
      pattern: "adminApiClient"
    - from: "admin-frontend/src/app/(admin)/layout.tsx"
      to: "admin-frontend/src/hooks/useAdminAuth.tsx"
      via: "useAdminAuth for route protection"
      pattern: "useAdminAuth"
---

<objective>
Scaffold the admin-frontend/ Next.js application with the complete authentication flow, admin API client, and shell layout (sidebar + header).

Purpose: This is the foundation for all admin frontend pages. It establishes the separate Next.js application (ARCH-06), installs the matching stack (ARCH-07), configures port 3001 (ARCH-09), and creates the admin auth flow and navigation shell that all subsequent pages will live inside.

Output: A working admin-frontend/ application with login page, protected admin shell with sidebar navigation, and API client infrastructure.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard-admin-frontend/31-RESEARCH.md
@frontend/package.json
@frontend/components.json
@frontend/next.config.ts
@frontend/postcss.config.mjs
@frontend/src/app/globals.css
@frontend/src/app/layout.tsx
@frontend/src/lib/api-client.ts
@frontend/src/lib/query-client.ts
@frontend/src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold admin-frontend Next.js app with dependencies and config</name>
  <files>
    admin-frontend/package.json
    admin-frontend/tsconfig.json
    admin-frontend/next.config.ts
    admin-frontend/postcss.config.mjs
    admin-frontend/eslint.config.mjs
    admin-frontend/components.json
    admin-frontend/src/app/globals.css
    admin-frontend/src/app/layout.tsx
    admin-frontend/src/app/page.tsx
    admin-frontend/src/lib/utils.ts
  </files>
  <action>
    **MANDATORY: Use /frontend-design skill for all layout and design implementation.**

    Create admin-frontend/ directory. Scaffold manually (NOT create-next-app to avoid unnecessary boilerplate):

    1. Create `package.json` with:
       - name: "admin-frontend"
       - scripts: { "dev": "next dev --port 3001", "build": "next build", "start": "next start --port 3001", "lint": "eslint" }
       - Dependencies matching frontend/package.json versions exactly: next@16.1.6, react@19.2.3, react-dom@19.2.3, @tanstack/react-query@^5.90.20, @tanstack/react-table@^8.21.3, zustand@^5.0.11, radix-ui@^1.4.3, lucide-react@^0.563.0, next-themes@^0.4.6, sonner@^2.0.7, class-variance-authority@^0.7.1, clsx@^2.1.1, tailwind-merge@^3.4.0
       - ADD recharts@^2 (new for admin)
       - devDependencies matching: tailwindcss@^4, @tailwindcss/postcss@^4, tw-animate-css@^1.4.0, @types/node@^20, @types/react@^19, @types/react-dom@^19, typescript@^5, eslint@^9, eslint-config-next@16.1.6

    2. Copy from frontend/: tsconfig.json, postcss.config.mjs, eslint.config.mjs (adjust paths if needed)

    3. Create `next.config.ts` with API rewrites: /api/:path* -> http://localhost:8000/:path*

    4. Create `components.json` matching frontend: style=new-york, rsc=true, tsx=true, baseColor=neutral, cssVariables=true, aliases same (@/components, @/lib, etc.)

    5. Create `src/app/globals.css` with:
       - Same Tailwind v4 imports (@import "tailwindcss", @plugin, @import "tw-animate-css")
       - Same @custom-variant dark
       - Same @theme inline block (copy from frontend)
       - DISTINCT admin theme: In :root, use an indigo/blue accent for --primary (e.g., oklch(0.45 0.2 260)) instead of the neutral black. In .dark, use Nord Frost blue (#81A1C1) for --primary instead of #D8DEE9. Keep all other Nord colors identical to frontend.
       - Same @layer base and skeleton utilities

    6. Create `src/app/layout.tsx` as root layout with Geist font imports, metadata (title: "Spectra Admin"), body wrapper

    7. Create `src/app/page.tsx` that redirects to /login (using redirect from next/navigation)

    8. Create `src/lib/utils.ts` with cn() utility (copy from frontend)

    9. Run `npm install` in admin-frontend/

    10. Run `npx shadcn@latest init` (if needed, or manually configure) then add required UI components:
        `npx shadcn@latest add button card input label checkbox dialog dropdown-menu table badge avatar separator scroll-area skeleton tooltip sheet tabs select`
        (These cover all pages: dashboard cards, data tables, forms, modals, etc.)
  </action>
  <verify>
    - `cd admin-frontend && npm run build` completes without errors
    - `npm run dev` starts on port 3001
    - Visit http://localhost:3001 -- should redirect to /login (which will 404 until Task 2)
    - shadcn/ui components exist in admin-frontend/src/components/ui/
  </verify>
  <done>admin-frontend/ is a working Next.js 16 application on port 3001 with full matching stack plus Recharts, admin-distinct theme, and all shadcn/ui primitives installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin API client, auth context, login page, and admin shell layout</name>
  <files>
    admin-frontend/src/lib/admin-api-client.ts
    admin-frontend/src/lib/query-client.ts
    admin-frontend/src/hooks/useAdminAuth.tsx
    admin-frontend/src/components/providers.tsx
    admin-frontend/src/types/admin-auth.ts
    admin-frontend/src/stores/sidebarStore.ts
    admin-frontend/src/app/(auth)/layout.tsx
    admin-frontend/src/app/(auth)/login/page.tsx
    admin-frontend/src/app/(admin)/layout.tsx
    admin-frontend/src/components/layout/AdminSidebar.tsx
    admin-frontend/src/components/layout/AdminHeader.tsx
    admin-frontend/src/app/layout.tsx
  </files>
  <action>
    **MANDATORY: Use /frontend-design skill for all layout and design implementation.**

    1. Create `src/types/admin-auth.ts`:
       - `AdminUser`: { id: string, email: string, first_name: string, last_name: string, is_admin: boolean, created_at: string }
       - `AdminLoginRequest`: { email: string, password: string }
       - `AdminLoginResponse`: { access_token: string, token_type: string }

    2. Create `src/lib/admin-api-client.ts` following research Pattern 1:
       - localStorage key: "spectra_admin_token"
       - `setAdminToken(token)`, `getAdminToken()`, `clearAdminToken()` helpers
       - `fetchWithAdminAuth(path, options)`: sets Authorization Bearer header, intercepts X-Admin-Token response header to update stored token, redirects to /login on 401
       - `adminApiClient` object with get, post, patch, put, delete methods
       - Use relative paths (e.g., `/api/admin/dashboard`) so Next.js rewrites proxy to backend

    3. Create `src/lib/query-client.ts`: TanStack Query client with sensible defaults (staleTime: 30s, retry: 1)

    4. Create `src/hooks/useAdminAuth.tsx`:
       - React context with AdminAuthProvider
       - State: user (AdminUser | null), isLoading (boolean), isAuthenticated (boolean)
       - `login(email, password)`: POST /api/admin/auth/login, store token via setAdminToken, then GET /api/admin/auth/me to load user profile
       - `logout()`: clearAdminToken, set user to null, redirect to /login
       - On mount: check if token exists in localStorage, if so GET /api/admin/auth/me to verify session; if 401, clear and redirect
       - Export useAdminAuth hook

    5. Create `src/components/providers.tsx`:
       - Wraps children with QueryClientProvider, AdminAuthProvider, ThemeProvider (from next-themes), Toaster (from sonner)

    6. Update `src/app/layout.tsx` to wrap body with Providers component

    7. Create `src/app/(auth)/layout.tsx`:
       - Centered layout for login page (flex items-center justify-center min-h-screen)

    8. Create `src/app/(auth)/login/page.tsx`:
       - "use client" component
       - Admin login form: email input, password input, "Sign in to Admin" button
       - Uses useAdminAuth().login on submit
       - On successful login, redirect to /dashboard
       - Show error toast on failure (sonner)
       - Distinctive admin branding: "Spectra Admin" title, indigo/blue accent on button
       - Loading state on submit button

    9. Create `src/stores/sidebarStore.ts`:
       - Zustand store with `isCollapsed` boolean and `toggle()` action
       - Default: not collapsed

    10. Create `src/components/layout/AdminSidebar.tsx`:
        - Fixed left sidebar with icon + label navigation
        - Sections per locked decision:
          - **Overview:** Dashboard (LayoutDashboard icon)
          - **People:** Users (Users icon), Invitations (Mail icon)
          - **Platform:** Credits (CreditCard icon), Settings (Settings icon), Audit Log (ScrollText icon)
        - Section headers in muted text
        - Active route highlighted with admin accent color
        - Collapse button at bottom (ChevronLeft/ChevronRight) -- when collapsed, show icons only
        - Uses sidebarStore for collapse state
        - Width: 256px expanded, 64px collapsed, smooth CSS transition

    11. Create `src/components/layout/AdminHeader.tsx`:
        - Top header bar in admin shell
        - Shows current page title (derived from route)
        - Right side: theme toggle (dark/light), admin user avatar/name dropdown with Logout option

    12. Create `src/app/(admin)/layout.tsx`:
        - "use client" -- wraps children with admin shell
        - Uses useAdminAuth() -- if not authenticated and not loading, redirect to /login
        - Renders: AdminSidebar + main content area (AdminHeader + {children})
        - Main content area adjusts margin-left based on sidebar collapse state

    13. Create placeholder page files for routing (just "Coming soon" text):
        - `src/app/(admin)/dashboard/page.tsx`
        - `src/app/(admin)/users/page.tsx`
        - `src/app/(admin)/users/[userId]/page.tsx`
        - `src/app/(admin)/invitations/page.tsx`
        - `src/app/(admin)/credits/page.tsx`
        - `src/app/(admin)/settings/page.tsx`
        - `src/app/(admin)/audit-log/page.tsx`

    Update root `src/app/page.tsx` to redirect to /dashboard (if authenticated) or /login (if not).
  </action>
  <verify>
    - `npm run build` in admin-frontend completes without errors
    - Start backend (SPECTRA_MODE=dev) and admin-frontend
    - Visit http://localhost:3001 -- redirects to /login
    - Log in with admin credentials -- redirects to /dashboard
    - Sidebar shows all navigation sections, clicking each navigates to correct route
    - Collapsing sidebar animates smoothly to icon-only mode
    - Theme toggle switches between light and dark mode
    - Logging out redirects to /login and clears token
    - Refreshing the page while logged in stays authenticated (token persisted in localStorage)
  </verify>
  <done>Admin frontend has working login flow, route protection, collapsible sidebar navigation, header with theme toggle and logout, and placeholder pages for all routes.</done>
</task>

</tasks>

<verification>
1. admin-frontend/ is a standalone Next.js app with its own package.json (ARCH-06)
2. Stack matches public frontend exactly: Next.js 16.1.6, React 19.2.3, TanStack Query v5, Zustand v5, shadcn/ui, plus Recharts (ARCH-07)
3. `npm run dev` starts on port 3001 (ARCH-09)
4. Admin API client intercepts X-Admin-Token for sliding window session
5. Login page authenticates against admin auth endpoint
6. Admin shell has collapsible sidebar with correct navigation sections
7. Route protection redirects unauthenticated users to /login
</verification>

<success_criteria>
- admin-frontend/ exists as a standalone Next.js application separate from frontend/
- Port 3001 is configured in package.json scripts
- Admin login works end-to-end (login -> dashboard redirect)
- Sidebar navigation routes to all admin pages
- X-Admin-Token header is intercepted on every API response
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-admin-frontend/31-02-SUMMARY.md`
</output>
