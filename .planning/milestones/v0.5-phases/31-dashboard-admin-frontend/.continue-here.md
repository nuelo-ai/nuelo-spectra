---
phase: 31-dashboard-admin-frontend
task: UAT v2 test 12 of 19
total_tasks: 19
status: in_progress
last_updated: 2026-02-17T22:17:02Z
---

<current_state>
Running UAT v2 for phase 31 gap closure (plans 31-06, 31-07, 31-08). Tests 1-11 complete (all passed, some with inline fixes). Currently presenting Test 12: Settings Tier Dropdown. UAT file is at `.planning/phases/31-dashboard-admin-frontend/31-UAT-v2.md`.
</current_state>

<completed_work>

- Phase 31 execution: All 8 plans complete (31-01 through 31-08)
- Phase 31 verification: 23/23 must-haves passed
- UAT v2 Tests 1-11: All passed

Inline fixes made during UAT:
1. `tier.id` → `tier.name` in useTiers, SettingsForm, UserDetailTabs (backend returns `name` not `id`)
2. `Number()` wrap on all `.toFixed()` calls for NUMERIC fields (UserDetailTabs, CreditOverview, UserTable, UserSection)
3. ChallengeCodeDialog infinite loop fix (useRef for onFetchChallenge instead of useEffect dep)
4. Admin login `last_login_at` update (was missing from admin auth endpoint)
5. Credit balance URL prefix `/credits/balance` → `/api/credits/balance`
6. Credit adjust password moved from inline form to confirmation modal (UX improvement per user request)
7. Credit adjust/reset wrong password: 401 → 403 to prevent logout on incorrect confirmation password
</completed_work>

<remaining_work>

- Test 12: Settings Tier Dropdown — awaiting user response
- Test 13: Settings Credit Reset Policy Display
- Test 14: Audit Log Column Sorting
- Test 15: Credit Balance in Public Sidebar
- Test 16: Chat Credit Error Message
- Test 17: Invite Registration Page Access
- Test 18: Invite Registration Form Fields
- Test 19: Password Reset Page Access
</remaining_work>

<decisions_made>

- Used 403 (not 401) for wrong confirmation password — 401 triggers auth interceptor logout
- Credit adjust password collected via modal dialog after clicking Adjust, not inline in form
- All NUMERIC fields from backend need Number() coercion before .toFixed() — backend serializes Decimal as string
- TierInfo interface uses `name` field (matching backend TierSummaryResponse), not `id`
</decisions_made>

<blockers>
None — all tests passing so far, remaining tests are straightforward verifications.
</blockers>

<context>
UAT v2 covers the gap closure deliverables from plans 31-06/07/08. The previous UAT (31-UAT.md) found 16 issues across phases 26-31, which were diagnosed and fixed by the gap closure plans. This UAT v2 validates those fixes. Several runtime bugs were caught and fixed inline during testing (NUMERIC string coercion, tier.id mismatch, 401 vs 403 for password confirmation). 8 tests remain, all related to settings, audit log, public frontend credit display, and invite/reset-password auth guard.
</context>

<next_action>
Resume UAT v2 from Test 12 (Settings Tier Dropdown). Read `.planning/phases/31-dashboard-admin-frontend/31-UAT-v2.md` to see full test list and progress. Present Test 12 checkpoint to user.
</next_action>
