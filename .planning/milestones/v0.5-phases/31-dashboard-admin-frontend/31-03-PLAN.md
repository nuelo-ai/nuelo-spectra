---
phase: 31-dashboard-admin-frontend
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - admin-frontend/src/app/(admin)/dashboard/page.tsx
  - admin-frontend/src/hooks/useDashboard.ts
  - admin-frontend/src/types/dashboard.ts
  - admin-frontend/src/components/dashboard/MetricCard.tsx
  - admin-frontend/src/components/dashboard/TrendChart.tsx
  - admin-frontend/src/components/dashboard/CreditDistributionChart.tsx
  - admin-frontend/src/components/dashboard/LowCreditTable.tsx
  - admin-frontend/src/components/dashboard/DashboardSkeleton.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "Dashboard displays 6 metric cards: total users (with active/inactive breakdown), active today, new signups, total sessions, messages sent, credit balance"
    - "Dashboard renders two side-by-side Recharts trend charts: signups over time (left) and messages over time (right)"
    - "Dashboard shows credit distribution by tier and a low-credit users table below the charts"
    - "Dashboard auto-refreshes data every 60 seconds"
  artifacts:
    - path: "admin-frontend/src/app/(admin)/dashboard/page.tsx"
      provides: "Dashboard page layout with all sections"
      contains: "MetricCard"
    - path: "admin-frontend/src/components/dashboard/TrendChart.tsx"
      provides: "Recharts LineChart for time-series data"
      contains: "LineChart"
    - path: "admin-frontend/src/hooks/useDashboard.ts"
      provides: "TanStack Query hook for dashboard metrics"
      contains: "useDashboardMetrics"
  key_links:
    - from: "admin-frontend/src/hooks/useDashboard.ts"
      to: "/api/admin/dashboard"
      via: "adminApiClient.get"
      pattern: "adminApiClient.*dashboard"
    - from: "admin-frontend/src/app/(admin)/dashboard/page.tsx"
      to: "admin-frontend/src/hooks/useDashboard.ts"
      via: "useDashboardMetrics hook"
      pattern: "useDashboardMetrics"
---

<objective>
Build the admin dashboard page with metric cards, trend charts, and credit section.

Purpose: The dashboard is the admin's landing page -- a single-page overview of platform health. It renders the 6 metric cards (DASH-01 through DASH-06), two Recharts trend charts (DASH-07), and a credit section with tier distribution chart and low-credit users table, all powered by the dashboard metrics endpoint created in Plan 31-01.

Output: A fully functional dashboard page with real data, charts, and auto-refresh.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard-admin-frontend/31-RESEARCH.md
@.planning/phases/31-dashboard-admin-frontend/31-01-SUMMARY.md
@.planning/phases/31-dashboard-admin-frontend/31-02-SUMMARY.md
@admin-frontend/src/lib/admin-api-client.ts
@admin-frontend/src/app/(admin)/layout.tsx
@admin-frontend/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard data hook, types, and metric card components</name>
  <files>
    admin-frontend/src/types/dashboard.ts
    admin-frontend/src/hooks/useDashboard.ts
    admin-frontend/src/components/dashboard/MetricCard.tsx
    admin-frontend/src/components/dashboard/DashboardSkeleton.tsx
  </files>
  <action>
    **MANDATORY: Use /frontend-design skill for all layout and design implementation.**

    1. Create `src/types/dashboard.ts`:
       - `TrendDataPoint`: { date: string, count: number }
       - `CreditDistributionItem`: { tier: string, user_count: number, total_credits: number }
       - `LowCreditUser`: { user_id: string, email: string, name: string, balance: number, tier: string }
       - `CreditSummary`: { total_used: number, total_remaining: number }
       - `DashboardMetrics`: { total_users: number, active_users: number, inactive_users: number, active_today: number, signups_today: number, signups_this_week: number, signups_this_month: number, total_sessions: number, total_files: number, total_messages: number, credit_summary: CreditSummary, signup_trend: TrendDataPoint[], message_trend: TrendDataPoint[], credit_distribution: CreditDistributionItem[], low_credit_users: LowCreditUser[] }

    2. Create `src/hooks/useDashboard.ts`:
       - `useDashboardMetrics(days?: number)` using TanStack Query:
         - queryKey: ["admin", "dashboard", days]
         - queryFn: adminApiClient.get(`/api/admin/dashboard?days=${days || 30}`)
         - staleTime: 30 * 1000 (30 seconds)
         - refetchInterval: 60 * 1000 (auto-refresh every 60 seconds)
         - Returns typed DashboardMetrics

    3. Create `src/components/dashboard/MetricCard.tsx`:
       - Props: title (string), value (string|number), subtitle (string, optional), icon (ReactNode), trend (optional: { value: number, isPositive: boolean })
       - Uses shadcn Card component
       - Layout: icon top-right, value large and bold, title as muted label above value, subtitle below
       - If trend provided, show colored arrow + percentage (green up, red down)
       - Professional Stripe-style card design

    4. Create `src/components/dashboard/DashboardSkeleton.tsx`:
       - Skeleton loading state matching dashboard layout
       - 6 skeleton cards in a row, 2 skeleton chart areas, skeleton table
       - Uses shadcn Skeleton component
  </action>
  <verify>
    - TypeScript compiles with no errors (`npm run build`)
    - MetricCard renders correctly in isolation (visual check)
  </verify>
  <done>Dashboard types, data fetching hook, metric card component, and loading skeleton are created.</done>
</task>

<task type="auto">
  <name>Task 2: Create trend charts, credit section, and assemble dashboard page</name>
  <files>
    admin-frontend/src/components/dashboard/TrendChart.tsx
    admin-frontend/src/components/dashboard/CreditDistributionChart.tsx
    admin-frontend/src/components/dashboard/LowCreditTable.tsx
    admin-frontend/src/app/(admin)/dashboard/page.tsx
  </files>
  <action>
    **MANDATORY: Use /frontend-design skill for all layout and design implementation.**

    1. Create `src/components/dashboard/TrendChart.tsx`:
       - Props: title (string), data (TrendDataPoint[]), color (string), dataKey (string, default "count")
       - Uses Recharts: ResponsiveContainer (width=100%, height=300), LineChart or AreaChart
       - XAxis with dataKey="date" (formatted as short date), YAxis, CartesianGrid (strokeDasharray="3 3"), Tooltip
       - Theme-aware: use CSS variables for grid/text colors (check document class for dark mode)
       - Wrapped in shadcn Card with CardHeader (title) and CardContent
       - Optional time range selector (7d/30d/90d tabs) -- Claude's discretion on exact UI

    2. Create `src/components/dashboard/CreditDistributionChart.tsx`:
       - Props: data (CreditDistributionItem[])
       - Uses Recharts: PieChart or BarChart showing credit distribution by tier
       - Each tier gets a distinct color from the chart color palette (--chart-1 through --chart-5)
       - Shows tier name, user count, and total credits
       - Wrapped in Card

    3. Create `src/components/dashboard/LowCreditTable.tsx`:
       - Props: users (LowCreditUser[])
       - Simple table showing users with low credit balances
       - Columns: User (email + name), Tier (badge), Balance (formatted number, red if < 5)
       - Uses shadcn Table component
       - Wrapped in Card with header "Low Credit Users"
       - If no low-credit users, show "All users have sufficient credits" message

    4. Assemble `src/app/(admin)/dashboard/page.tsx`:
       - "use client" component
       - Uses useDashboardMetrics() hook
       - Shows DashboardSkeleton while loading
       - Layout per locked decision:
         - **Row 1 (hero):** 6 MetricCards in a responsive grid (grid-cols-2 md:grid-cols-3 lg:grid-cols-6)
           1. Total Users (Users icon) -- subtitle: "{active} active / {inactive} inactive"
           2. Active Today (UserCheck icon) -- value from active_today
           3. New Signups (UserPlus icon) -- subtitle: "today: {n} / week: {n} / month: {n}"
           4. Total Sessions (MessageSquare icon) -- value from total_sessions
           5. Messages Sent (Send icon) -- value from total_messages
           6. Credit Balance (CreditCard icon) -- subtitle: "{used} used / {remaining} remaining"
         - **Row 2 (charts):** 2 TrendCharts side-by-side in grid-cols-1 lg:grid-cols-2
           - Left: "Signups Over Time" with signup_trend data
           - Right: "Messages Over Time" with message_trend data
         - **Row 3 (credits):** grid-cols-1 lg:grid-cols-2
           - Left: CreditDistributionChart with credit_distribution data
           - Right: LowCreditTable with low_credit_users data
       - Error state: show error message with retry button
       - Page title set to "Dashboard" (used by AdminHeader)
  </action>
  <verify>
    - Start backend (dev mode) and admin-frontend
    - Log in as admin, navigate to /dashboard
    - Verify 6 metric cards display with real data
    - Verify two trend charts render with Recharts (line/area charts with axes, tooltips)
    - Verify credit distribution chart and low-credit users table render
    - Verify auto-refresh: wait 60 seconds, check data refreshes (or manually trigger via React Query devtools)
    - Verify dark mode: toggle theme, charts and cards should adapt
    - Verify loading state: skeleton appears on initial load
  </verify>
  <done>Dashboard page shows all 6 metrics, 2 trend charts, credit distribution, and low-credit users. Data auto-refreshes. Dark mode works.</done>
</task>

</tasks>

<verification>
1. DASH-01: Dashboard shows total users with active vs inactive count
2. DASH-02: Dashboard shows new signups (today/week/month)
3. DASH-03: Dashboard shows total chat sessions
4. DASH-04: Dashboard shows total files uploaded
5. DASH-05: Dashboard shows total messages sent
6. DASH-06: Dashboard shows credit consumption summary (used/remaining)
7. DASH-07: Dashboard displays Recharts trend charts for signups and messages over time
8. Charts are theme-aware (light and dark mode)
9. Dashboard auto-refreshes every 60 seconds
</verification>

<success_criteria>
- All 6 metric cards display real aggregated data from the backend
- Two Recharts trend charts render time-series data side-by-side
- Credit section shows tier distribution and low-credit users
- Dashboard loads with skeleton, refreshes automatically, and supports dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-admin-frontend/31-03-SUMMARY.md`
</output>
