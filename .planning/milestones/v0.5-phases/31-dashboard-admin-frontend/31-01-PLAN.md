---
phase: 31-dashboard-admin-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/admin/dashboard.py
  - backend/app/routers/admin/audit.py
  - backend/app/routers/admin/__init__.py
  - backend/app/schemas/admin_dashboard.py
  - backend/app/schemas/admin_audit.py
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "GET /api/admin/dashboard returns aggregated platform metrics including user counts, signup counts, session/file/message totals, credit summary, and time-series trend data"
    - "GET /api/admin/audit-log returns paginated audit log entries with optional filters for action, admin_id, and target_type"
    - "GET /api/admin/auth/me returns the current admin user profile when called with a valid admin JWT"
  artifacts:
    - path: "backend/app/routers/admin/dashboard.py"
      provides: "Dashboard metrics aggregation endpoint"
      contains: "get_dashboard_metrics"
    - path: "backend/app/routers/admin/audit.py"
      provides: "Audit log listing endpoint"
      contains: "list_audit_logs"
    - path: "backend/app/schemas/admin_dashboard.py"
      provides: "Pydantic schemas for dashboard response"
      contains: "DashboardMetricsResponse"
  key_links:
    - from: "backend/app/routers/admin/dashboard.py"
      to: "backend/app/models/"
      via: "SQLAlchemy queries on User, ChatSession, ChatMessage, File, UserCredit tables"
      pattern: "select.*func\\.count"
    - from: "backend/app/routers/admin/__init__.py"
      to: "backend/app/routers/admin/dashboard.py"
      via: "admin_router.include_router"
      pattern: "include_router.*dashboard"
---

<objective>
Create the backend API endpoints that the admin frontend dashboard and audit log pages will consume.

Purpose: The admin frontend needs three endpoints that do not yet exist: (1) a dashboard metrics aggregation endpoint returning all DASH-01 through DASH-07 data in a single response, (2) a paginated audit log listing endpoint, and (3) an admin /auth/me endpoint for session verification. Without these, the frontend has no data to display.

Output: Three new endpoints registered on the admin router, with Pydantic response schemas.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard-admin-frontend/31-RESEARCH.md
@backend/app/routers/admin/__init__.py
@backend/app/routers/admin/auth.py
@backend/app/dependencies.py
@backend/app/models/user.py
@backend/app/models/chat_session.py
@backend/app/models/chat_message.py
@backend/app/models/file.py
@backend/app/models/admin_audit_log.py
@backend/app/models/user_credit.py
@backend/app/models/credit_transaction.py
@backend/app/schemas/admin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard metrics endpoint and admin /auth/me</name>
  <files>
    backend/app/routers/admin/dashboard.py
    backend/app/schemas/admin_dashboard.py
    backend/app/routers/admin/auth.py
    backend/app/routers/admin/__init__.py
  </files>
  <action>
    Create `backend/app/schemas/admin_dashboard.py` with Pydantic response models:
    - `DashboardMetricsResponse` with fields: total_users (int), active_users (int), inactive_users (int), active_today (int), signups_today (int), signups_this_week (int), signups_this_month (int), total_sessions (int), total_files (int), total_messages (int), credit_summary (dict with total_used: float, total_remaining: float), signup_trend (list of {date: str, count: int}), message_trend (list of {date: str, count: int}), credit_distribution (list of {tier: str, user_count: int, total_credits: float}), low_credit_users (list of {user_id: str, email: str, name: str, balance: float, tier: str})

    Create `backend/app/routers/admin/dashboard.py`:
    - `GET ""` endpoint (prefix="/dashboard") protected by `CurrentAdmin` dependency
    - Accept `days: int = Query(default=30, ge=7, le=90)` for trend time range
    - Query aggregations:
      - DASH-01: COUNT users grouped by is_active; COUNT users with last_login_at >= today_start for "active today"
      - DASH-02: COUNT users with created_at >= today_start / week_start / month_start
      - DASH-03: COUNT chat_sessions
      - DASH-04: COUNT files
      - DASH-05: COUNT chat_messages
      - DASH-06: SUM credit_transactions where type='deduction' for total_used; SUM user_credits.balance for total_remaining
      - DASH-07: Time-series: group users by DATE(created_at) for signup_trend; group chat_messages by DATE(created_at) for message_trend; both filtered to last `days` days
      - Credit distribution: group user_credits by user.user_class with COUNT and SUM balance
      - Low credit users: users where user_credits.balance < 10 (or 10% of tier allocation), ordered by balance ASC, limit 20
    - Return `DashboardMetricsResponse`

    Add `GET /me` endpoint to existing `backend/app/routers/admin/auth.py`:
    - Protected by `CurrentAdmin` dependency
    - Returns admin user profile: id, email, first_name, last_name, is_admin, created_at
    - Create `AdminMeResponse` schema in `backend/app/schemas/admin.py` (or admin_dashboard.py)

    Update `backend/app/routers/admin/__init__.py`:
    - Import and include the new dashboard router
  </action>
  <verify>
    Start backend in dev mode. Verify:
    - `curl -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:8000/api/admin/dashboard` returns 200 with all metric fields
    - `curl -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:8000/api/admin/auth/me` returns 200 with admin profile
    - Unauthenticated requests return 401/403
  </verify>
  <done>Dashboard metrics endpoint returns all DASH-01 through DASH-07 data. Admin /auth/me returns admin profile.</done>
</task>

<task type="auto">
  <name>Task 2: Create audit log listing endpoint</name>
  <files>
    backend/app/routers/admin/audit.py
    backend/app/schemas/admin_audit.py
    backend/app/routers/admin/__init__.py
  </files>
  <action>
    Create `backend/app/schemas/admin_audit.py` with:
    - `AuditLogEntry`: id (UUID), admin_id (UUID), admin_email (str), action (str), target_type (str|None), target_id (str|None), details (dict|None), created_at (datetime)
    - `AuditLogListResponse`: items (list[AuditLogEntry]), total (int), page (int), page_size (int), total_pages (int)

    Create `backend/app/routers/admin/audit.py`:
    - `GET ""` endpoint (prefix="/audit-log") protected by `CurrentAdmin` dependency
    - Query parameters: page (int, default=1), page_size (int, default=50, max=100), action (str|None), admin_id (UUID|None), target_type (str|None), date_from (datetime|None), date_to (datetime|None)
    - Query AdminAuditLog table with optional filters, JOIN with users table to get admin_email
    - Order by created_at DESC
    - Return paginated response with total count

    Update `backend/app/routers/admin/__init__.py`:
    - Import and include the audit router
  </action>
  <verify>
    - `curl -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:8000/api/admin/audit-log` returns 200 with paginated entries
    - `curl -H "Authorization: Bearer $ADMIN_TOKEN" "http://localhost:8000/api/admin/audit-log?action=admin_login&page=1&page_size=10"` returns filtered results
    - Verify total count and pagination math are correct
  </verify>
  <done>Audit log endpoint returns paginated, filterable audit entries with admin email resolution.</done>
</task>

</tasks>

<verification>
1. All three new endpoints (dashboard, audit-log, auth/me) respond with 200 and correct schema
2. Dashboard endpoint aggregates real data from database tables
3. Audit log supports pagination and all filter parameters
4. Endpoints are protected by CurrentAdmin dependency (401 without token, 403 for non-admin)
5. Backend starts without import errors
</verification>

<success_criteria>
- GET /api/admin/dashboard returns complete metrics matching DASH-01 through DASH-07 requirements
- GET /api/admin/audit-log returns paginated audit entries
- GET /api/admin/auth/me returns admin user profile
- All endpoints are admin-protected
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-admin-frontend/31-01-SUMMARY.md`
</output>
