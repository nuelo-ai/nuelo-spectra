---
phase: 42-analysis-workspace-pulse-detection
plan: 03
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - pulse-mockup/src/app/workspace/collections/[id]/page.tsx
  - pulse-mockup/src/app/workspace/collections/[id]/layout.tsx
  - pulse-mockup/src/components/workspace/file-table.tsx
  - pulse-mockup/src/components/workspace/file-upload-zone.tsx
  - pulse-mockup/src/components/workspace/sticky-action-bar.tsx
  - pulse-mockup/src/components/workspace/detection-loading.tsx
  - pulse-mockup/src/components/workspace/data-summary-panel.tsx
autonomous: true
requirements:
  - PULSE-04
  - PULSE-05
  - PULSE-08

must_haves:
  truths:
    - "Reviewer can see a Collection detail page with a file upload/dropzone area and a file list table with checkboxes"
    - "Reviewer can see file rows with name, type icon, size, upload date, and profiling status badges"
    - "Reviewer can see a sticky bottom action bar with selected file count, credit cost estimate, and Run Detection button"
    - "Reviewer can see Run Detection button is disabled with hint text when no files are selected"
    - "Reviewer can see a pre-action credit cost estimate in the sticky bar"
    - "Reviewer can see a full-page loading state with animated step stages after clicking Run Detection"
    - "Reviewer can click a file row to open a data summary side panel"
  artifacts:
    - path: "pulse-mockup/src/app/workspace/collections/[id]/page.tsx"
      provides: "Collection detail page with file management"
      min_lines: 60
    - path: "pulse-mockup/src/components/workspace/sticky-action-bar.tsx"
      provides: "Sticky bottom bar with credit estimate and Run Detection"
      min_lines: 30
    - path: "pulse-mockup/src/components/workspace/detection-loading.tsx"
      provides: "Full-page animated loading with step stages"
      min_lines: 50
    - path: "pulse-mockup/src/components/workspace/file-table.tsx"
      provides: "File list table with checkboxes and status badges"
      min_lines: 50
  key_links:
    - from: "pulse-mockup/src/components/workspace/sticky-action-bar.tsx"
      to: "pulse-mockup/src/lib/mock-data.ts"
      via: "Uses COST_PER_FILE for credit estimate calculation"
      pattern: "COST_PER_FILE"
    - from: "pulse-mockup/src/components/workspace/detection-loading.tsx"
      to: "/workspace/collections/[id]/signals"
      via: "Auto-navigates to signals page after loading completes"
      pattern: "router.push.*signals"
---

<objective>
Build the Collection detail page with file management, Run Detection sticky action bar with credit display, and the animated detection loading state.

Purpose: The core interaction flow — reviewer sees how files are managed, how detection cost is communicated, and what the loading experience looks like during the 15-30s detection process.
Output: Fully interactive Collection detail page at /workspace/collections/[id] with file table, sticky bar, and loading transition.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-analysis-workspace-pulse-detection/42-CONTEXT.md
@.planning/phases/42-analysis-workspace-pulse-detection/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collection detail page with file table and data summary panel</name>
  <files>
    pulse-mockup/src/app/workspace/collections/[id]/page.tsx
    pulse-mockup/src/app/workspace/collections/[id]/layout.tsx
    pulse-mockup/src/components/workspace/file-table.tsx
    pulse-mockup/src/components/workspace/file-upload-zone.tsx
    pulse-mockup/src/components/workspace/data-summary-panel.tsx
  </files>
  <action>
    1. Create src/app/workspace/collections/[id]/layout.tsx:
       - Simple pass-through layout (children only) — the app shell is already applied by workspace/layout.tsx

    2. Create src/components/workspace/file-upload-zone.tsx:
       - Dropzone area at the top of the page with dashed border, upload icon (lucide Upload), and text "Drop files here or click to upload"
       - Styled as a card-like zone with hover state (border color change)
       - On click/drop: since this is a mockup, show a brief visual feedback (e.g., flash the zone) but don't actually process — the mock files are already in the table
       - Subtle helper text below: "Supported: CSV, Excel, JSON"

    3. Create src/components/workspace/file-table.tsx:
       - Uses shadcn Table component
       - Columns: checkbox (for selection), file name with type icon (lucide FileSpreadsheet for CSV/Excel, FileJson for JSON), file size (formatted KB/MB), upload date, profiling status
       - Profiling status badges: "Ready" (green badge), "Profiling..." (amber badge + small spinner/pulse animation), "Error" (red badge)
       - Checkbox column: individual row checkboxes + header "select all" checkbox
       - Row click (on file name, not checkbox): triggers data summary panel open
       - Exposes selectedFileIds state via callback prop
       - Imports mock files from mock-data.ts

    4. Create src/components/workspace/data-summary-panel.tsx:
       - Uses shadcn Sheet component (slides in from right side)
       - Shows: file name, file type, size, upload date
       - Mock "Data Summary" section: column count, row count, data types detected, a few sample column names — all hardcoded mock content
       - Close button in header
       - Panel width: ~400px

    5. Create src/app/workspace/collections/[id]/page.tsx:
       - Page header: collection name (from mock data, matched by route param id), status badge, created date
       - Below header: FileUploadZone component
       - Below upload zone: FileTable component with mock files
       - DataSummaryPanel (Sheet) controlled by state — opens when a file row name is clicked
       - Manages selectedFileIds state from FileTable
       - Manages detection loading state (for Task 2)
       - When detection loading is active: renders DetectionLoading component INSTEAD of the normal page content (full-page replacement per CONTEXT.md)
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Collection detail page shows collection name/status header, file dropzone, file table with checkboxes and status badges. Clicking a file name opens the data summary side panel. File selection state is tracked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sticky action bar with credit estimate and detection loading state</name>
  <files>
    pulse-mockup/src/components/workspace/sticky-action-bar.tsx
    pulse-mockup/src/components/workspace/detection-loading.tsx
  </files>
  <action>
    1. Create src/components/workspace/sticky-action-bar.tsx:
       - Fixed/sticky bar at the bottom of the page content area (not viewport bottom — stays within the main content column, above any page padding)
       - Left side: "{N} files selected" text
       - Center: credit cost estimate — "~{N * COST_PER_FILE} credits for {N} files selected" using the COST_PER_FILE constant from mock-data
       - Right side: "Run Detection" button (primary variant, lucide Zap icon)
       - When no files selected: button is disabled, shows tooltip or helper text "Select at least 1 file to run detection"
       - When files selected: button is enabled, estimate updates dynamically based on selectedFileIds count
       - Visual: semi-transparent dark background with backdrop blur, subtle top border, rounded corners
       - Props: selectedCount (number), onRunDetection callback

    2. Create src/components/workspace/detection-loading.tsx:
       - Full-page replacement content (replaces the collection detail body)
       - Animated step stages displayed vertically or as a horizontal stepper:
         - Step 1: "Analyzing files" (FileSearch icon)
         - Step 2: "Detecting patterns" (Search icon)
         - Step 3: "Scoring signals" (BarChart3 icon)
         - Step 4: "Finalizing results" (CheckCircle icon)
       - Animation: steps progress sequentially with ~3-4s per step using setTimeout/setInterval
         - Current step: highlighted with accent color + spinning/pulse indicator
         - Completed steps: checkmark + muted success color
         - Upcoming steps: dimmed/muted
       - Below the stepper: "Estimated time: 15-30 seconds" text
       - A subtle progress bar spanning the full width showing overall progress
       - After all steps complete (after ~12-15s total), auto-navigate to /workspace/collections/{id}/signals using router.push
       - Collection ID passed as prop

    3. Wire into collection detail page.tsx:
       - StickyActionBar rendered at page bottom, receives selectedFileIds.length and onRunDetection callback
       - onRunDetection sets isDetecting state to true
       - When isDetecting is true: page renders DetectionLoading instead of the file table content
       - DetectionLoading receives the collection ID for navigation
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Sticky action bar shows at page bottom with file count, credit estimate ("~10 credits for 5 files selected"), and Run Detection button. Button disabled with hint when no files selected. Clicking Run Detection triggers full-page loading animation with 4 sequential steps and auto-navigates to signals page after completion.
  </done>
</task>

</tasks>

<verification>
- /workspace/collections/1 renders collection name, status, file dropzone, and file table
- File table shows checkboxes, file names with icons, size, date, profiling status badges
- Selecting files updates the sticky bar count and credit estimate
- Run Detection button disabled when no files selected
- Clicking Run Detection shows animated 4-step loading state
- Loading auto-navigates to /workspace/collections/1/signals after ~12-15s
- Clicking a file name opens the data summary side panel from the right
</verification>

<success_criteria>
- Collection detail page renders file management UI with upload zone and file table
- File selection via checkboxes works with select-all
- Sticky action bar displays dynamic credit estimate based on selected files
- Run Detection disabled state with hint text when no files selected
- Detection loading shows animated step progression with time estimate
- Auto-navigation to signals page after loading completes
- Data summary side panel opens on file row click
</success_criteria>

<output>
After completion, create `.planning/phases/42-analysis-workspace-pulse-detection/42-03-SUMMARY.md`
</output>
