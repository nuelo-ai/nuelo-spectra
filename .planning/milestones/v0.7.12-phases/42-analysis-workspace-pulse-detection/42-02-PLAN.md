---
phase: 42-analysis-workspace-pulse-detection
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - pulse-mockup/src/app/workspace/page.tsx
  - pulse-mockup/src/components/workspace/collection-card.tsx
  - pulse-mockup/src/components/workspace/collection-list.tsx
  - pulse-mockup/src/components/workspace/create-collection-dialog.tsx
  - pulse-mockup/src/components/workspace/empty-state.tsx
autonomous: true
requirements:
  - PULSE-02
  - PULSE-03

must_haves:
  truths:
    - "Reviewer can see a Collection list page with collection cards showing name, status badge, created date, and signal count"
    - "Reviewer can see a Create New Collection button in the header area"
    - "Reviewer can see an empty state CTA when no collections exist"
    - "Reviewer can open a Create Collection dialog with name and optional description fields"
  artifacts:
    - path: "pulse-mockup/src/app/workspace/page.tsx"
      provides: "Collection list page at /workspace"
      min_lines: 30
    - path: "pulse-mockup/src/components/workspace/collection-card.tsx"
      provides: "Card component for a single collection"
      min_lines: 30
    - path: "pulse-mockup/src/components/workspace/create-collection-dialog.tsx"
      provides: "Modal dialog for creating a new collection"
      min_lines: 40
  key_links:
    - from: "pulse-mockup/src/app/workspace/page.tsx"
      to: "pulse-mockup/src/lib/mock-data.ts"
      via: "Imports mock collections array"
      pattern: "import.*mock-data"
    - from: "pulse-mockup/src/components/workspace/collection-card.tsx"
      to: "/workspace/collections/[id]"
      via: "Next.js Link to collection detail"
      pattern: "href.*collections"
---

<objective>
Build the Collection list page and Create Collection flow at /workspace.

Purpose: The entry point to the Analysis Workspace — reviewer sees all collections as cards and can trigger the Create New Collection dialog.
Output: Fully styled /workspace page with collection cards grid and create collection modal.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-analysis-workspace-pulse-detection/42-CONTEXT.md
@.planning/phases/42-analysis-workspace-pulse-detection/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collection list page with cards and empty state</name>
  <files>
    pulse-mockup/src/app/workspace/page.tsx
    pulse-mockup/src/components/workspace/collection-card.tsx
    pulse-mockup/src/components/workspace/collection-list.tsx
    pulse-mockup/src/components/workspace/empty-state.tsx
  </files>
  <action>
    1. Create src/components/workspace/collection-card.tsx:
       - Accepts a Collection object (from mock-data types)
       - Renders as a shadcn Card with hover effect (subtle border highlight or shadow lift)
       - Shows: collection name (bold title), status badge (active = green/teal, archived = gray), created date (formatted relative or absolute), signal count with a small icon
       - Files count shown as secondary info
       - Entire card is clickable — wraps in Next.js Link to /workspace/collections/{id}
       - Hex.tech aesthetic: dark card surface, subtle border, clean typography

    2. Create src/components/workspace/empty-state.tsx:
       - Centered layout with illustration area (use a lucide-react icon like BarChart3 or Activity at large size with muted color)
       - Heading: "No collections yet"
       - Subtext: "Create your first collection to start detecting signals in your data"
       - CTA button: "Create Collection" (primary styled) — triggers the create dialog

    3. Create src/components/workspace/collection-list.tsx:
       - Receives collections array as prop
       - If empty: renders EmptyState
       - If populated: renders responsive grid of CollectionCard components (3 columns on lg, 2 on md, 1 on sm)
       - Grid gap and padding consistent with Hex.tech aesthetic

    4. Create src/app/workspace/page.tsx:
       - Page header area with: title "Analysis Workspace" (or "Collections"), description text "Manage your data collections and detect signals", and a "Create Collection" button (top right) that opens the create dialog
       - Imports mock collections from mock-data.ts
       - Renders CollectionList with the mock collections
       - Manages dialog open state for Create Collection
       - Passes dialog trigger to both the header button and the empty state CTA
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    /workspace page renders collection cards in a responsive grid with name, status badge (active/archived), created date, and signal count. Empty state shows CTA when no collections. Header has "Create Collection" button.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Collection dialog with form</name>
  <files>
    pulse-mockup/src/components/workspace/create-collection-dialog.tsx
  </files>
  <action>
    1. Create src/components/workspace/create-collection-dialog.tsx:
       - Uses shadcn Dialog component
       - Controlled open/onOpenChange props
       - Dialog content:
         - Title: "Create New Collection"
         - Form with: name input (required, placeholder "e.g., Q3 Sales Analysis"), description textarea (optional, placeholder "Describe the purpose of this collection...")
         - Footer: Cancel button (ghost variant) + Create button (primary variant)
       - On "Create" click: since this is a mockup, simulate creation by navigating to /workspace/collections/1 (or first mock collection ID) using next/navigation router.push — this simulates the "redirect to collection detail after create" behavior per CONTEXT.md
       - Form validation: Create button disabled when name is empty
       - Styling: dialog backdrop blur, dark surface, consistent with app theme

    2. Wire the dialog into workspace/page.tsx:
       - Dialog state managed in page component
       - Header "Create Collection" button and empty state CTA both set dialog open to true
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Create Collection dialog opens from both header button and empty state CTA. Shows name (required) and description (optional) fields. Create button disabled when name empty. Clicking Create navigates to collection detail page.
  </done>
</task>

</tasks>

<verification>
- /workspace shows collection cards in responsive grid
- Each card shows name, status badge, created date, signal count
- Cards are clickable and link to /workspace/collections/[id]
- "Create Collection" button in header opens dialog
- Dialog has name + description fields, Create disabled when empty
- Clicking Create navigates to a collection detail page
- Empty state renders when collections array is empty (can test by temporarily clearing mock data)
</verification>

<success_criteria>
- Collection list page at /workspace renders all mock collections as styled cards
- Status badges visually distinguish active (green/teal) from archived (gray)
- Create Collection dialog works from header button
- Empty state with CTA displays when no collections exist
- Navigation to collection detail works from both card click and post-create redirect
</success_criteria>

<output>
After completion, create `.planning/phases/42-analysis-workspace-pulse-detection/42-02-SUMMARY.md`
</output>
