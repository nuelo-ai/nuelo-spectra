---
phase: 42-analysis-workspace-pulse-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pulse-mockup/package.json
  - pulse-mockup/tsconfig.json
  - pulse-mockup/next.config.ts
  - pulse-mockup/tailwind.config.ts
  - pulse-mockup/postcss.config.mjs
  - pulse-mockup/src/app/layout.tsx
  - pulse-mockup/src/app/globals.css
  - pulse-mockup/src/app/page.tsx
  - pulse-mockup/src/app/providers.tsx
  - pulse-mockup/src/app/workspace/layout.tsx
  - pulse-mockup/src/components/layout/sidebar.tsx
  - pulse-mockup/src/components/layout/header.tsx
  - pulse-mockup/src/components/layout/app-shell.tsx
  - pulse-mockup/src/components/ui/*.tsx
  - pulse-mockup/src/lib/utils.ts
  - pulse-mockup/src/lib/mock-data.ts
  - pulse-mockup/components.json
autonomous: true
requirements:
  - PULSE-01
  - PULSE-08

must_haves:
  truths:
    - "Reviewer can open the app and see a dark analytical UI with left sidebar navigation and top header"
    - "Reviewer can see Spectra branding and nav items: Chat, Analysis Workspace (active), Files, API, Settings"
    - "Reviewer can toggle between dark and light theme"
    - "Reviewer can see a credit balance indicator in the header area"
    - "Navigating to /workspace shows the workspace layout shell with sidebar active on Analysis Workspace"
  artifacts:
    - path: "pulse-mockup/package.json"
      provides: "Next.js app with all dependencies"
      contains: "next"
    - path: "pulse-mockup/src/components/layout/sidebar.tsx"
      provides: "Left sidebar navigation with Spectra nav items"
      min_lines: 40
    - path: "pulse-mockup/src/components/layout/header.tsx"
      provides: "Top header with credit balance and theme toggle"
      min_lines: 20
    - path: "pulse-mockup/src/app/workspace/layout.tsx"
      provides: "Workspace route layout"
      min_lines: 10
    - path: "pulse-mockup/src/lib/mock-data.ts"
      provides: "Shared mock data constants for collections, files, signals"
      min_lines: 80
  key_links:
    - from: "pulse-mockup/src/app/layout.tsx"
      to: "pulse-mockup/src/components/layout/app-shell.tsx"
      via: "Root layout wraps AppShell"
      pattern: "AppShell"
    - from: "pulse-mockup/src/app/providers.tsx"
      to: "next-themes"
      via: "ThemeProvider wrapping app"
      pattern: "ThemeProvider"
---

<objective>
Scaffold the pulse-mockup Next.js app from scratch and build the app shell (sidebar + header + theme toggle + credit indicator).

Purpose: Establishes the standalone mockup application with the Hex.tech-inspired dark analytical design system, navigation structure, and shared mock data that all subsequent plans build upon.
Output: Running Next.js app at pulse-mockup/ with full app shell, dark/light theme, and workspace route structure.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-analysis-workspace-pulse-detection/42-CONTEXT.md

Reference for shadcn/ui setup patterns (DO NOT import from these — re-install fresh):
@frontend/package.json
@frontend/src/app/globals.css
@frontend/src/app/layout.tsx
@frontend/src/app/providers.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js app and install dependencies</name>
  <files>
    pulse-mockup/package.json
    pulse-mockup/tsconfig.json
    pulse-mockup/next.config.ts
    pulse-mockup/tailwind.config.ts
    pulse-mockup/postcss.config.mjs
    pulse-mockup/src/app/layout.tsx
    pulse-mockup/src/app/globals.css
    pulse-mockup/src/app/page.tsx
    pulse-mockup/src/app/providers.tsx
    pulse-mockup/src/lib/utils.ts
    pulse-mockup/components.json
  </files>
  <action>
    1. Create pulse-mockup/ directory at repo root. Run `npx create-next-app@latest pulse-mockup --typescript --tailwind --eslint --app --src-dir --no-import-alias --use-npm` (answer defaults). If interactive prompts block, create manually:
       - package.json with: next@16, react@19, react-dom@19, typescript@5, tailwindcss@4, @tailwindcss/postcss@4
       - Standard tsconfig.json, next.config.ts, postcss.config.mjs

    2. Install additional dependencies:
       ```
       cd pulse-mockup && npm install next-themes lucide-react class-variance-authority clsx tailwind-merge recharts plotly.js-dist-min sonner @tailwindcss/typography
       ```

    3. Initialize shadcn/ui: `npx shadcn@latest init` — select New York style, slate base color, CSS variables yes. Then install components:
       ```
       npx shadcn@latest add button card badge dialog input textarea table scroll-area skeleton progress sheet separator dropdown-menu tooltip tabs avatar
       ```

    4. Set up globals.css with a Hex.tech-inspired dark color system using CSS variables. The dark theme should be the default. Key design tokens:
       - Background: very dark blue-gray (#0a0e1a or similar)
       - Card surfaces: slightly lighter (#111827 range)
       - Borders: subtle (#1e293b range)
       - Primary accent: blue (#3b82f6 range)
       - Text: white/gray hierarchy (primary #f8fafc, secondary #94a3b8, muted #64748b)
       - Severity colors: red (#ef4444), amber (#f59e0b), green (#22c55e)
       - Light theme: invert appropriately — white backgrounds, dark text, same accent colors

    5. Create src/app/providers.tsx wrapping ThemeProvider from next-themes (attribute="class", defaultTheme="dark", enableSystem=false).

    6. Create src/app/layout.tsx: html with suppressHydrationWarning, body wraps Providers. Use Inter or Geist font from next/font.

    7. Create src/app/page.tsx: redirect to /workspace using next/navigation redirect().

    8. Create src/lib/utils.ts with the standard cn() helper (clsx + tailwind-merge).
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>pulse-mockup/ exists with all dependencies installed, shadcn/ui components available, dark theme CSS variables defined, app builds successfully, root redirects to /workspace</done>
</task>

<task type="auto">
  <name>Task 2: Build app shell (sidebar + header + credit indicator) and mock data</name>
  <files>
    pulse-mockup/src/components/layout/sidebar.tsx
    pulse-mockup/src/components/layout/header.tsx
    pulse-mockup/src/components/layout/app-shell.tsx
    pulse-mockup/src/app/workspace/layout.tsx
    pulse-mockup/src/lib/mock-data.ts
  </files>
  <action>
    1. Create src/lib/mock-data.ts with TypeScript types and static data for the ENTIRE phase (all plans will import from here):
       - Collection type: { id, name, description, status: 'active'|'archived', createdAt, signalCount, filesCount, creditsUsed }
       - FileItem type: { id, name, type, size, uploadDate, profilingStatus: 'profiling'|'ready'|'error' }
       - Signal type: { id, title, description, severity: 'critical'|'warning'|'info', category, statisticalEvidence, chartType: 'line'|'bar'|'scatter', chartData: any[] }
       - Create 4-5 mock collections (mix of active/archived, varying signal counts)
       - Create 6-8 mock files (CSV, Excel types, all 'ready' status, plus 1 'profiling')
       - Create 6-8 mock signals with realistic analytical titles (e.g., "Revenue Anomaly Detected Q3", "Customer Churn Rate Spike", "Seasonal Demand Pattern"), varied severities, categories (Anomaly, Trend, Correlation, Opportunity), and realistic chart data arrays for Recharts
       - CREDIT_BALANCE constant: 47 (or similar realistic number)
       - COST_PER_FILE constant: 2 (for credit estimate calculation)

    2. Create src/components/layout/sidebar.tsx:
       - ~240px fixed width left sidebar
       - Spectra logo/wordmark at top (styled text or SVG — "Spectra" with a subtle accent, NOT a logo file)
       - Navigation items with lucide-react icons: Chat (MessageSquare), Analysis Workspace (Activity — this is the active item), Files (FolderOpen), API (Code), Settings (Settings)
       - Active item: highlighted background + accent color left border
       - Each nav item is a Next.js Link to mock routes (Chat → /chat, Workspace → /workspace, Files → /files, etc.) — only /workspace routes are real, others can be stub pages or just href="#"
       - Bottom section: user avatar placeholder + name "Demo User"
       - Hex.tech aesthetic: dark surface, subtle borders, clean spacing

    3. Create src/components/layout/header.tsx:
       - Top header bar spanning the content area (right of sidebar)
       - Left: breadcrumb or page title area (dynamic via props or context)
       - Right: credit balance indicator showing coin/zap icon + "{CREDIT_BALANCE} credits" with accent styling, theme toggle button (Sun/Moon icons from lucide-react using next-themes useTheme), and a notification bell icon (static, decorative)

    4. Create src/components/layout/app-shell.tsx:
       - Composes Sidebar + Header + main content area
       - Layout: sidebar fixed left, header fixed top-right, scrollable content area below header
       - Pass children to the content area
       - Responsive: on mobile (<768px), sidebar collapses to icons-only or hamburger menu (Claude's discretion on exact approach)

    5. Create src/app/workspace/layout.tsx:
       - Wraps children in AppShell
       - This ensures all /workspace/* routes get the shell

    6. Update src/app/layout.tsx to NOT include the shell (shell only applies to workspace routes via the workspace layout).
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>App shell renders with dark Hex.tech-inspired sidebar (5 nav items, Spectra branding), header with credit balance indicator and theme toggle, workspace layout wraps content area. Mock data module exports typed collections, files, signals, and credit constants. Theme toggle switches between dark and light modes.</done>
</task>

</tasks>

<verification>
- `cd pulse-mockup && npm run dev` starts without errors
- Visiting http://localhost:3000 redirects to /workspace
- /workspace shows app shell: dark sidebar with 5 nav items, header with credit balance and theme toggle
- Clicking theme toggle switches between dark and light
- Analysis Workspace nav item is visually active/highlighted
- Mock data module exports are importable with correct TypeScript types
</verification>

<success_criteria>
- Standalone Next.js app at pulse-mockup/ with all dependencies
- Hex.tech-inspired dark analytical design system with dark/light toggle
- App shell with sidebar navigation (Chat, Analysis Workspace, Files, API, Settings)
- Credit balance indicator visible in header
- Workspace route structure in place (/workspace, /workspace/collections/[id], /workspace/collections/[id]/signals)
- Shared mock data module with typed collections, files, signals
</success_criteria>

<output>
After completion, create `.planning/phases/42-analysis-workspace-pulse-detection/42-01-SUMMARY.md`
</output>
