---
phase: 19-v03-gap-closure
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/(dashboard)/my-files/page.tsx
  - frontend/src/components/session/WelcomeScreen.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/components/file/FileLinkingDropdown.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Upload modal width matches info modal width (sm:max-w-4xl) across all dialog instances"
    - "Markdown in upload analysis results renders without cramping or visual breakage"
  artifacts:
    - path: "frontend/src/app/(dashboard)/my-files/page.tsx"
      provides: "Upload dialog with consistent modal width"
      contains: "sm:max-w-4xl"
    - path: "frontend/src/components/session/WelcomeScreen.tsx"
      provides: "Upload dialogs with consistent modal width"
      contains: "sm:max-w-4xl"
    - path: "frontend/src/components/chat/ChatInterface.tsx"
      provides: "Drag-drop upload dialog with consistent modal width"
      contains: "sm:max-w-4xl"
    - path: "frontend/src/components/file/FileLinkingDropdown.tsx"
      provides: "Paperclip upload dialog with consistent modal width"
      contains: "sm:max-w-4xl"
  key_links: []
---

<objective>
Fix upload modal width to match info modal width across all 4 files (5 dialog instances).

Purpose: User reported upload modal is too narrow compared to the (i) info modal, and markdown appears cramped/broken due to the narrow container. The fix is a simple CSS class change from `sm:max-w-lg` (512px) to `sm:max-w-4xl max-h-[85vh] overflow-y-auto` (896px), matching FileContextModal, FileInfoModal, and FileSidebar.
Output: All upload dialogs use consistent width matching info modals.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/file/FileContextModal.tsx (reference: line 60, sm:max-w-4xl max-h-[85vh] overflow-y-auto)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen all upload DialogContent to sm:max-w-4xl</name>
  <files>
    frontend/src/app/(dashboard)/my-files/page.tsx
    frontend/src/components/session/WelcomeScreen.tsx
    frontend/src/components/chat/ChatInterface.tsx
    frontend/src/components/file/FileLinkingDropdown.tsx
  </files>
  <action>
In each file, find every `DialogContent` with `className="sm:max-w-lg"` and replace with `className="sm:max-w-4xl max-h-[85vh] overflow-y-auto"`.

Specific locations:

1. **frontend/src/app/(dashboard)/my-files/page.tsx** line 149:
   - Change `<DialogContent className="sm:max-w-lg">` to `<DialogContent className="sm:max-w-4xl max-h-[85vh] overflow-y-auto">`

2. **frontend/src/components/session/WelcomeScreen.tsx** line 385 (pre-session upload dialog):
   - Change `<DialogContent className="sm:max-w-lg">` to `<DialogContent className="sm:max-w-4xl max-h-[85vh] overflow-y-auto">`

3. **frontend/src/components/session/WelcomeScreen.tsx** line 409 (drag-drop upload dialog):
   - Change `<DialogContent className="sm:max-w-lg">` to `<DialogContent className="sm:max-w-4xl max-h-[85vh] overflow-y-auto">`

4. **frontend/src/components/chat/ChatInterface.tsx** line 343 (drag-drop upload dialog):
   - Change `<DialogContent className="sm:max-w-lg">` to `<DialogContent className="sm:max-w-4xl max-h-[85vh] overflow-y-auto">`

5. **frontend/src/components/file/FileLinkingDropdown.tsx** line 187 (paperclip upload dialog):
   - Change `<DialogContent className="sm:max-w-lg">` to `<DialogContent className="sm:max-w-4xl max-h-[85vh] overflow-y-auto">`

This matches the existing pattern in FileContextModal.tsx (line 60), FileInfoModal.tsx (line 54), and FileSidebar.tsx (line 190).
  </action>
  <verify>
Run: `grep -rn "sm:max-w-lg" frontend/src/` — should return ZERO results (all instances replaced).
Run: `grep -rn "sm:max-w-4xl" frontend/src/` — should show all dialog instances using the wider width.
Run: `cd frontend && npx next build --no-lint 2>&1 | tail -5` — should compile without errors (or use `npx tsc --noEmit`).
  </verify>
  <done>All 5 DialogContent instances across 4 files use sm:max-w-4xl max-h-[85vh] overflow-y-auto. No sm:max-w-lg remains in the codebase. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `grep -rn "sm:max-w-lg" frontend/src/` returns zero results
- `grep -rn "sm:max-w-4xl" frontend/src/` shows 7+ instances (4 existing + 5 changed = some overlap since some files already had it)
- Frontend compiles without errors
</verification>

<success_criteria>
All upload dialog modals render at 896px width (same as info modals), providing adequate space for markdown analysis results.
</success_criteria>

<output>
After completion, create `.planning/phases/19-v03-gap-closure/19-06-SUMMARY.md`
</output>
