---
phase: 14-database-foundation-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/chat_session.py
  - backend/app/models/chat_message.py
  - backend/app/models/file.py
  - backend/app/models/user.py
  - backend/app/models/__init__.py
  - backend/app/schemas/chat.py
  - backend/app/schemas/chat_session.py
autonomous: true

must_haves:
  truths:
    - "ChatSession model exists with id, user_id, title, created_at, updated_at fields"
    - "session_files association table exists with composite PK (session_id, file_id)"
    - "ChatMessage model has session_id FK (nullable for migration compatibility)"
    - "File model has bidirectional sessions relationship via session_files"
    - "User model has chat_sessions relationship"
    - "Pydantic schemas exist for ChatSession CRUD operations"
  artifacts:
    - path: "backend/app/models/chat_session.py"
      provides: "ChatSession ORM model and session_files association table"
      contains: "class ChatSession"
    - path: "backend/app/models/chat_message.py"
      provides: "Updated ChatMessage with session_id FK"
      contains: "session_id"
    - path: "backend/app/models/file.py"
      provides: "File model with sessions M2M relationship"
      contains: "sessions"
    - path: "backend/app/models/user.py"
      provides: "User model with chat_sessions relationship"
      contains: "chat_sessions"
    - path: "backend/app/schemas/chat_session.py"
      provides: "Pydantic schemas for session CRUD"
      contains: "ChatSessionCreate"
  key_links:
    - from: "backend/app/models/chat_session.py"
      to: "backend/app/models/file.py"
      via: "session_files association table with back_populates"
      pattern: "secondary=session_files.*back_populates"
    - from: "backend/app/models/chat_session.py"
      to: "backend/app/models/chat_message.py"
      via: "one-to-many relationship with cascade delete-orphan"
      pattern: "cascade.*delete-orphan"
    - from: "backend/app/models/chat_message.py"
      to: "backend/app/models/chat_session.py"
      via: "session_id FK with back_populates"
      pattern: "session_id.*ForeignKey.*chat_sessions"
---

<objective>
Create the ChatSession SQLAlchemy model, session_files association table, update existing models (ChatMessage, File, User) with new relationships, and create Pydantic schemas for session CRUD.

Purpose: Establishes the data model foundation that all other Phase 14 plans depend on. Models define the ORM layer; schemas define the API contract. Without these, migrations cannot be written and services cannot be built.

Output: 6 files (1 new model, 3 updated models, 1 updated __init__, 1 new schema file, 1 updated schema file)
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-database-foundation-migration/14-RESEARCH.md

@backend/app/models/base.py
@backend/app/models/chat_message.py
@backend/app/models/file.py
@backend/app/models/user.py
@backend/app/models/__init__.py
@backend/app/schemas/chat.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatSession model with session_files association table and update existing models</name>
  <files>
    backend/app/models/chat_session.py
    backend/app/models/chat_message.py
    backend/app/models/file.py
    backend/app/models/user.py
    backend/app/models/__init__.py
  </files>
  <action>
    Create `backend/app/models/chat_session.py` with:

    1. **session_files association table** (SQLAlchemy Core Table, NOT a class):
       - `session_id`: UUID FK to `chat_sessions.id` with `ondelete="CASCADE"`, primary_key=True
       - `file_id`: UUID FK to `files.id` with `ondelete="CASCADE"`, primary_key=True
       - Register on `Base.metadata`

    2. **ChatSession class** (inherits from Base):
       - `__tablename__ = "chat_sessions"`
       - `id`: Mapped[UUID], primary_key, default=uuid4
       - `user_id`: Mapped[UUID], FK to `users.id` with `ondelete="CASCADE"`, index=True, nullable=False
       - `title`: Mapped[str], String(255)
       - `created_at`: Mapped[datetime], DateTime(timezone=True), default=lambda: datetime.now(timezone.utc)
       - `updated_at`: Mapped[datetime], DateTime(timezone=True), default + onupdate=lambda: datetime.now(timezone.utc)
       - Relationships:
         - `user`: Mapped["User"], back_populates="chat_sessions"
         - `files`: Mapped[list["File"]], secondary=session_files, back_populates="sessions" (NO cascade - deleting session should NOT delete files)
         - `messages`: Mapped[list["ChatMessage"]], back_populates="session", cascade="all, delete-orphan", order_by="ChatMessage.created_at"
       - Use TYPE_CHECKING imports for User, File, ChatMessage to avoid circular imports

    Update `backend/app/models/chat_message.py`:
    - Add `session_id`: Mapped[UUID | None], FK to `chat_sessions.id` with `ondelete="CASCADE"`, index=True, **nullable=True** (nullable for migration compatibility - will be made NOT NULL after data migration)
    - Add `session`: Mapped["ChatSession" | None], relationship back_populates="messages"
    - Keep existing `file_id` and `file` relationship intact (backward compatibility during migration, file_id stays as-is for now)
    - Add TYPE_CHECKING import for ChatSession
    - Update docstring to note session_id is the new primary relationship

    Update `backend/app/models/file.py`:
    - Add import: `from app.models.chat_session import session_files`
    - Add `sessions`: Mapped[list["ChatSession"]], secondary=session_files, back_populates="files" (NO cascade)
    - Add TYPE_CHECKING import for ChatSession
    - Keep existing `chat_messages` relationship (backward compatibility)

    Update `backend/app/models/user.py`:
    - Add `chat_sessions`: Mapped[list["ChatSession"]], back_populates="user", cascade="all, delete-orphan"
    - Add TYPE_CHECKING import for ChatSession

    Update `backend/app/models/__init__.py`:
    - Import ChatSession and session_files
    - Add to __all__

    IMPORTANT: Follow existing code style exactly (look at how File, User, ChatMessage are structured). Use Mapped[] type annotations (SQLAlchemy 2.0 style). Use TYPE_CHECKING guard for all cross-model imports.
  </action>
  <verify>
    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "from app.models import Base, ChatSession, session_files; print('ChatSession:', ChatSession.__tablename__); print('session_files columns:', [c.name for c in session_files.columns])"`

    Expected output: ChatSession: chat_sessions, session_files columns: ['session_id', 'file_id']

    Also run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "from app.models import ChatMessage; print('session_id' in [c.name for c in ChatMessage.__table__.columns])"`

    Expected: True
  </verify>
  <done>
    ChatSession model importable with correct table name, session_files has composite PK with both FK columns, ChatMessage has nullable session_id FK, File has sessions relationship, User has chat_sessions relationship. All models import cleanly without circular import errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ChatSession Pydantic schemas and update ChatMessage schemas</name>
  <files>
    backend/app/schemas/chat_session.py
    backend/app/schemas/chat.py
  </files>
  <action>
    Create `backend/app/schemas/chat_session.py` with these Pydantic v2 schemas:

    1. **ChatSessionCreate** (BaseModel):
       - `title`: str (optional, default "New Chat" - will be auto-generated from first message later in Phase 18)

    2. **ChatSessionUpdate** (BaseModel):
       - `title`: str | None = None

    3. **ChatSessionFileLink** (BaseModel):
       - `file_id`: UUID

    4. **ChatSessionResponse** (BaseModel):
       - `id`: UUID
       - `user_id`: UUID
       - `title`: str
       - `created_at`: datetime
       - `updated_at`: datetime
       - `file_count`: int = 0 (computed field or default)
       - `model_config = ConfigDict(from_attributes=True)`

    5. **ChatSessionDetail** (ChatSessionResponse):
       - Extends ChatSessionResponse
       - `files`: list[FileBasicInfo] = [] (basic file info: id, original_filename, file_type, created_at)

    6. **FileBasicInfo** (BaseModel):
       - `id`: UUID
       - `original_filename`: str
       - `file_type`: str
       - `created_at`: datetime
       - `model_config = ConfigDict(from_attributes=True)`

    7. **ChatSessionList** (BaseModel):
       - `sessions`: list[ChatSessionResponse]
       - `total`: int

    Update `backend/app/schemas/chat.py`:
    - In **ChatMessageResponse**: add `session_id: UUID | None = None` field (nullable because old messages may not have session_id until migration runs)
    - Keep `file_id: UUID` as-is for backward compatibility

    Follow existing Pydantic v2 patterns from chat.py: use `model_config = ConfigDict(from_attributes=True)`, field_validator where needed, proper type annotations.
  </action>
  <verify>
    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "from app.schemas.chat_session import ChatSessionCreate, ChatSessionResponse, ChatSessionDetail, ChatSessionList, ChatSessionFileLink; print('All schemas importable'); s = ChatSessionCreate(); print('Default title:', s.title)"`

    Expected: All schemas importable, Default title: New Chat

    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "from app.schemas.chat import ChatMessageResponse; print('session_id' in ChatMessageResponse.model_fields)"`

    Expected: True
  </verify>
  <done>
    ChatSessionCreate, ChatSessionUpdate, ChatSessionResponse, ChatSessionDetail, ChatSessionList, ChatSessionFileLink, FileBasicInfo schemas all importable and validate correctly. ChatMessageResponse includes optional session_id field.
  </done>
</task>

</tasks>

<verification>
- All models import without circular dependency errors: `python -c "from app.models import Base, User, File, ChatMessage, ChatSession"`
- ChatSession has correct relationships: files (M2M), messages (O2M), user (M2O)
- session_files association table has composite PK
- ChatMessage.session_id is nullable (migration compatibility)
- File.sessions and ChatSession.files are bidirectional
- All Pydantic schemas validate with sample data
</verification>

<success_criteria>
- ChatSession SQLAlchemy model exists with all required fields and relationships
- session_files association table defined as Core Table with CASCADE deletes on both FKs
- ChatMessage has nullable session_id FK pointing to chat_sessions
- File and User models updated with new relationships
- Pydantic schemas cover create, update, response, detail, and list operations
- No import errors across the models package
</success_criteria>

<output>
After completion, create `.planning/phases/14-database-foundation-migration/14-01-SUMMARY.md`
</output>
