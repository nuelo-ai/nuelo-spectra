---
phase: 06-frontend-ui-interactive-data-cards
plan: 16
type: execute
wave: 2
depends_on: ["06-15"]
files_modified:
  - .planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md
autonomous: false

must_haves:
  truths:
    - "Upload dialog shows analysis text rendered as formatted markdown (not raw text)"
    - "Upload dialog modal is large enough for comfortable reading"
    - "User can provide optional context textarea in upload ready state (FILE-05)"
    - "File info modal has feedback form to refine AI understanding (FILE-06)"
    - "Continue to Chat button works: closes dialog, populates sidebar, opens tab"
    - "Sidebar file list shows uploaded files without 5-second delay"
    - "All previously-passed UAT tests (password reset, profile update) still pass"
  artifacts:
    - path: ".planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md"
      provides: "Updated UAT retest with definitive results for all 20 tests"
      contains: "status:"
  key_links:
    - from: "06-15-SUMMARY.md"
      to: "UAT-RETEST Tests 2-3"
      via: "Markdown rendering, modal sizing, sidebar refetch fix, FILE-05, FILE-06"
      pattern: "result: pass"
---

<objective>
Verify that plan 06-15 fixes resolve ALL remaining UAT failures and re-test the complete test suite.

Purpose: Plan 06-15 addresses five root causes: (1) raw markdown rendering via react-markdown, (2) modal sizing via max-w-4xl and max-h-[60vh], (3) FILE-05 user context textarea in upload flow, (4) FILE-06 feedback form in file info modal, (5) sidebar double-refetch conflict removal. This plan verifies those fixes work in actual user testing and records definitive results for all 20 UAT tests.

Output: Updated 06-UAT-RETEST.md with pass/fail results for all tests.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

## Fix applied in prior plan
@.planning/phases/06-frontend-ui-interactive-data-cards/06-15-SUMMARY.md

## Current test state
@.planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks -- verify services running and 06-15 fixes deployed</name>
  <files></files>
  <action>
Before asking user to test, verify everything is ready:

1. Check backend is running on port 8000:
   ```bash
   curl -s http://localhost:8000/health | head -20
   ```
   If not running, start it:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && .venv/bin/uvicorn app.main:app --reload --port 8000 &
   ```

2. Check frontend is running on port 3000:
   ```bash
   curl -s http://localhost:3000 | head -5
   ```
   If not running, start it:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npm run dev &
   ```

3. Verify 06-15 fixes are deployed by checking source code:
   a. react-markdown in package.json: `grep "react-markdown" frontend/package.json`
   b. ReactMarkdown used in FileUploadZone.tsx: `grep "ReactMarkdown" frontend/src/components/file/FileUploadZone.tsx`
   c. ReactMarkdown used in FileInfoModal.tsx: `grep "ReactMarkdown" frontend/src/components/file/FileInfoModal.tsx`
   d. max-w-4xl in FileInfoModal: `grep "max-w-4xl" frontend/src/components/file/FileInfoModal.tsx`
   e. No premature refetch in useUploadFile: `grep -A3 "onSuccess" frontend/src/hooks/useFileManager.ts | head -8` -- should NOT contain refetchQueries
   f. useUpdateFileContext hook exists: `grep "useUpdateFileContext" frontend/src/hooks/useFileManager.ts`
   g. FILE-05 textarea in FileUploadZone: `grep "userContextInput\|Add context" frontend/src/components/file/FileUploadZone.tsx`
   h. FILE-06 feedback form in FileInfoModal: `grep "feedbackInput\|Refine AI" frontend/src/components/file/FileInfoModal.tsx`

4. Quick smoke test -- verify app builds without errors:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx tsc --noEmit
   ```

If any check fails, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
All checks pass: backend running, frontend running, all 06-15 code fixes confirmed in source, TypeScript compiles.
  </verify>
  <done>Infrastructure verified. Plan 06-15 fixes confirmed deployed. Services running and responsive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 06-15 made five fixes to the upload flow and file management:
1. Installed react-markdown -- analysis text now renders as formatted markdown (headings, bold, lists)
2. Enlarged modal -- FileInfoModal now max-w-4xl, analysis container now max-h-[60vh]
3. FILE-05 -- Added optional "Add context about your data" textarea in upload ready state
4. FILE-06 -- Added "Refine AI Understanding" feedback form in file info modal
5. Removed double-refetch -- sidebar should populate quickly after Continue to Chat
  </what-built>
  <how-to-verify>
Please test the following in order. Report each as "Test X: pass" or "Test X: issue - [description]".

**TEST 2: File Upload Analysis Display (previously failing)**
  1. Navigate to /dashboard
  2. Click "Upload File" in sidebar
  3. Upload a CSV or Excel file
  4. Wait for progress: Uploading -> Analyzing -> Ready
  5. CHECK: Is the analysis text rendered as formatted markdown (with headings, bold text, bullet lists)? NOT raw text with # and * characters?
  6. CHECK: Is the analysis display area large enough to read comfortably (not cramped)?
  7. CHECK: Do you see an optional "Add context about your data" textarea below the analysis?
  8. CHECK: Do you see the "Continue to Chat" button?
  Expected: Formatted markdown, large readable area, context textarea present, Continue button visible.

**TEST 3: Sidebar File List Population (previously failing)**
  1. From Test 2, click "Continue to Chat"
  2. CHECK: Does the dialog close promptly (not hanging for 5+ seconds)?
  3. CHECK: Does the uploaded file appear in the sidebar file list within 1-2 seconds?
  4. CHECK: Does a new tab open with the file name?
  Expected: Dialog closes quickly, file appears in sidebar, tab opens.

**TEST 4: File Tab Opens After Upload**
  1. Verify a tab opened from Test 3
  2. CHECK: Tab shows file name in tab bar
  3. CHECK: ChatInterface is visible in the main content area
  Expected: File tab with chat interface ready.

**TEST 5: File Info Modal (FILE-06 feedback)**
  1. In the sidebar, click the info icon (i) next to the uploaded file
  2. CHECK: Does a modal open showing the AI analysis as formatted markdown?
  3. CHECK: Is the modal large enough for comfortable reading?
  4. CHECK: Do you see a "Refine AI Understanding" section with a textarea and "Save Feedback" button?
  5. (Optional) Type some feedback and click Save -- does it show "Saved!" confirmation?
  Expected: Large modal, markdown-rendered analysis, feedback form present.

**TEST 6: Delete File**
  1. Click the trash icon next to a file in the sidebar
  2. CHECK: Confirmation dialog appears
  3. Click Delete
  4. CHECK: File removed from sidebar, tab closes
  Expected: Clean delete with UI update.

**TEST 8: AI Chat Response Streaming** (if Tests 2-4 pass)
  1. With a file tab open, type "What columns are in this data?" and press Enter
  2. CHECK: Do you see a typing indicator then streaming AI response?
  Expected: AI response streams in without error.

**PREVIOUSLY PASSED TESTS (quick re-confirm):**
  Test 1 (Password Reset): Navigate to /forgot-password, submit email -- console shows reset link?
  Test 18 (Profile Update): Navigate to /settings, change name, Save -- nav updates immediately?

Report each test result. If any test fails, describe what you see.
  </how-to-verify>
  <resume-signal>Report results for each test. Type "all pass" if everything works, or describe specific issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update UAT-RETEST with results and close phase</name>
  <files>.planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md</files>
  <action>
Based on the user's test results from the checkpoint:

1. Read the current 06-UAT-RETEST.md
2. Update the frontmatter:
   - If all critical tests pass: set `status: complete`
   - If issues remain: set `status: diagnosed` and add new gap entries
   - Update `updated` timestamp
   - Add `06-15` to source list

3. For each test in the UAT-RETEST, update with new results from user feedback:
   - Tests that were previously "issue" (2, 3): update with new result
   - Tests that were "skipped": update with result if tested, or "skipped" with new reason if still blocked
   - Tests that were "pass" (1, 18): keep as pass unless regression reported

4. Update Summary section with new totals

5. If ALL critical tests pass:
   - Update summary to reflect full resolution
   - Note which specific fix resolved each issue
   - Record: "All upload flow issues resolved by plan 06-15"

6. If any test still fails:
   - Add new Gap entry with detailed analysis
   - Set status to diagnosed

7. Write the updated 06-UAT-RETEST.md
  </action>
  <verify>
Read updated 06-UAT-RETEST.md and confirm: status reflects actual outcomes, all tested items have results, summary totals are accurate.
  </verify>
  <done>06-UAT-RETEST.md updated with definitive results reflecting actual user testing outcomes.</done>
</task>

</tasks>

<verification>
- Backend and frontend running and accessible
- Plan 06-15 fixes confirmed in source code (react-markdown, modal sizing, FILE-05, FILE-06, refetch fix)
- User has tested critical upload flow scenarios
- UAT-RETEST.md updated with definitive results
</verification>

<success_criteria>
- Test 2 passes: analysis displays as formatted markdown in large readable area with context textarea
- Test 3 passes: sidebar populates quickly after Continue to Chat (no 5s delay)
- Test 5 passes: file info modal shows markdown analysis with feedback form
- Tests 1, 18 pass: no regressions
- UAT-RETEST.md has definitive status for all tested items
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-interactive-data-cards/06-16-SUMMARY.md`
</output>
