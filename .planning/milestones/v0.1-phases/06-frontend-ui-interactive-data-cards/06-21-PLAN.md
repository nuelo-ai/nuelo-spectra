---
phase: 06-frontend-ui-interactive-data-cards
plan: 21
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/ChatMessage.tsx
  - frontend/src/components/chat/DataCard.tsx
  - backend/app/config/prompts.yaml
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Data Card always displays interactive sortable/filterable table when query returns tabular data"
    - "No more static markdown tables appearing instead of interactive DataTable"
    - "Empty result sets show empty table UI, not null"
  artifacts:
    - path: "frontend/src/components/chat/ChatMessage.tsx"
      provides: "Robust parseExecutionResult handling all pandas formats"
      contains: "df.to_dict()"
    - path: "frontend/src/components/chat/DataCard.tsx"
      provides: "Clean component without dead code"
    - path: "backend/app/config/prompts.yaml"
      provides: "Coding agent instructions for consistent output format"
      contains: "to_dict('records')"
  key_links:
    - from: "backend/app/config/prompts.yaml"
      to: "frontend/src/components/chat/ChatMessage.tsx"
      via: "execution_result JSON format"
      pattern: "to_dict.*records"
---

<objective>
Fix inconsistent Data Card table rendering to always show interactive DataTable.

Purpose: UAT Round 5 Test 10 failed because Data Card sometimes shows static markdown table instead of interactive TanStack table. Root cause: parseExecutionResult doesn't handle df.to_dict() default format or empty arrays.

Output: Consistent interactive table rendering for all tabular data queries.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/data-card-table-inconsistent.md

Reference files (read before modifying):
@frontend/src/components/chat/ChatMessage.tsx
@frontend/src/components/chat/DataCard.tsx
@backend/app/config/prompts.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update coding agent prompt to specify consistent DataFrame format</name>
  <files>backend/app/config/prompts.yaml</files>
  <action>
Update the coding agent prompt to explicitly require `.to_dict('records')` format for DataFrame results.

Find the `coding` section in prompts.yaml and update the Rules section.

BEFORE (around line 27-32):
```yaml
      **Rules:**
      - Use exact column names from the dataset (case-sensitive)
      - Convert results to Python types: int(), float(), str()
      - End with: print(json.dumps({"result": result}))
      - Libraries allowed: {allowed_libraries}
      - Use .fillna(0) for missing values
      - NEVER check if df exists or use locals()/globals()
```

AFTER:
```yaml
      **Rules:**
      - Use exact column names from the dataset (case-sensitive)
      - Convert results to Python types: int(), float(), str()
      - For DataFrame/Series results: use .to_dict('records') to get list of row dicts
      - End with: print(json.dumps({"result": result}))
      - Libraries allowed: {allowed_libraries}
      - Use .fillna(0) for missing values
      - NEVER check if df exists or use locals()/globals()
```

Also update the example to show DataFrame output format. After the existing example (around line 34-39), add another example:

```yaml
      **Example with DataFrame result:**
      ```python
      import json
      result = df.groupby('category')['sales'].sum().reset_index().to_dict('records')
      print(json.dumps({"result": result}))
      ```
```
  </action>
  <verify>
grep -A 20 "coding:" backend/app/config/prompts.yaml | grep -q "to_dict('records')"
  </verify>
  <done>
Coding agent prompt explicitly instructs LLM to use .to_dict('records') for DataFrame results, ensuring consistent [{"col": val}, ...] format.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance parseExecutionResult to handle all pandas output formats</name>
  <files>frontend/src/components/chat/ChatMessage.tsx</files>
  <action>
Update the parseExecutionResult function (lines 45-91) to handle:
1. df.to_dict() default format: {column: {index: value}}
2. Empty arrays: return empty table structure instead of null
3. df.to_dict('split') format: {columns: [...], data: [...], index: [...]}

Replace the parseExecutionResult function with this improved version:

```typescript
  // Parse execution result for table data - handles multiple pandas output formats
  const parseExecutionResult = (result: any): { columns: string[]; rows: Record<string, any>[] } | null => {
    if (!result) return null;

    // If result is a string, try to parse as JSON
    let parsed = result;
    if (typeof result === "string") {
      try {
        parsed = JSON.parse(result);
      } catch {
        // Not valid JSON, return null
        return null;
      }
    }

    // Format 1: Array of objects (df.to_dict('records') - preferred format)
    // Example: [{"col1": "a", "col2": 1}, {"col1": "b", "col2": 2}]
    if (Array.isArray(parsed)) {
      // Handle empty array - show empty table
      if (parsed.length === 0) {
        return { columns: [], rows: [] };
      }
      // Array of objects
      if (typeof parsed[0] === "object" && parsed[0] !== null) {
        return {
          columns: Object.keys(parsed[0]),
          rows: parsed,
        };
      }
      // Array of scalars - not tabular data
      return null;
    }

    // Format 2: Object with columns and rows/data keys (df.to_dict('split') or custom)
    // Example: {columns: ["col1", "col2"], data: [["a", 1], ["b", 2]]}
    // Example: {columns: ["col1", "col2"], rows: [{col1: "a"}, {col2: "b"}]}
    if (parsed && typeof parsed === "object") {
      if (Array.isArray(parsed.columns)) {
        // If rows is array of objects, use directly
        if (Array.isArray(parsed.rows)) {
          return {
            columns: parsed.columns,
            rows: parsed.rows,
          };
        }
        // If data is array of arrays (split format), convert to array of objects
        if (Array.isArray(parsed.data)) {
          const rows = parsed.data.map((row: any[]) => {
            const obj: Record<string, any> = {};
            parsed.columns.forEach((col: string, i: number) => {
              obj[col] = row[i];
            });
            return obj;
          });
          return { columns: parsed.columns, rows };
        }
      }

      // Format 3: df.to_dict() default format: {column: {index: value}}
      // Example: {"col1": {"0": "a", "1": "b"}, "col2": {"0": 1, "1": 2}}
      // Detect by checking if all values are objects with numeric string keys
      const keys = Object.keys(parsed);
      if (keys.length > 0) {
        const firstValue = parsed[keys[0]];
        if (firstValue && typeof firstValue === "object" && !Array.isArray(firstValue)) {
          // Check if keys of nested object look like indices (numeric strings)
          const indexKeys = Object.keys(firstValue);
          const looksLikeDefaultFormat = indexKeys.length > 0 &&
            indexKeys.every(k => /^\d+$/.test(k));

          if (looksLikeDefaultFormat) {
            // Convert {col: {idx: val}} to [{col: val}, ...]
            const rows: Record<string, any>[] = [];
            indexKeys.forEach(idx => {
              const row: Record<string, any> = {};
              keys.forEach(col => {
                row[col] = parsed[col][idx];
              });
              rows.push(row);
            });
            return { columns: keys, rows };
          }
        }
      }
    }

    // Not a recognized tabular format
    return null;
  };
```
  </action>
  <verify>
1. TypeScript compiles: cd frontend && npm run build (no errors)
2. Function handles all formats correctly (will verify in integration test)
  </verify>
  <done>
parseExecutionResult now handles: (1) df.to_dict('records') arrays, (2) empty arrays, (3) df.to_dict('split') with data arrays, (4) df.to_dict() default {col: {idx: val}} format.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove dead code from DataCard component</name>
  <files>frontend/src/components/chat/DataCard.tsx</files>
  <action>
Remove the unused parseTableData function and fix line 75 logic.

1. Delete the parseTableData function (lines 52-73) - it's dead code that's never called effectively.

2. Change line 75 from:
```typescript
const displayTableData = tableData || parseTableData(tableData);
```

To simply:
```typescript
const displayTableData = tableData;
```

Since ChatMessage.tsx now handles all parsing, DataCard just receives the pre-parsed tableData prop.
  </action>
  <verify>
1. grep -c "parseTableData" frontend/src/components/chat/DataCard.tsx returns 0 (function removed)
2. TypeScript compiles: cd frontend && npm run build (no errors)
  </verify>
  <done>
DataCard component cleaned up - dead parseTableData function removed, simpler prop passthrough.
  </done>
</task>

</tasks>

<verification>
1. Start backend: `cd backend && source venv/bin/activate && uvicorn app.main:app --reload`
2. Start frontend: `cd frontend && npm run dev`
3. Upload a data file and test these queries:
   - "Show me all rows" - should show interactive table
   - "Group by category and sum sales" - should show interactive table
   - "Show empty result" (query that returns no rows) - should show empty table UI
4. All queries that return tabular data show interactive DataTable with:
   - Sortable column headers
   - Filter/search box
   - Pagination controls
   - Download CSV button
5. No static markdown tables appear in place of interactive tables
</verification>

<success_criteria>
UAT Test 10 passes: Data Card consistently displays interactive sortable/filterable table with download capability for all tabular queries.
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-interactive-data-cards/06-21-SUMMARY.md`
</output>
