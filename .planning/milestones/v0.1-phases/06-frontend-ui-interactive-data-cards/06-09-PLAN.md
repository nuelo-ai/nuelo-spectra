---
phase: 06-frontend-ui-interactive-data-cards
plan: 09
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - frontend/src/app/(dashboard)/dashboard/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can ask questions about their data in chat interface and see AI responses stream"
    - "ChatInterface renders inside dashboard when a file tab is active"
    - "Dashboard shows empty state (upload prompt) when no tab is selected"
  artifacts:
    - path: "frontend/src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard page with integrated ChatInterface"
      contains: "ChatInterface"
  key_links:
    - from: "frontend/src/app/(dashboard)/dashboard/page.tsx"
      to: "frontend/src/components/chat/ChatInterface.tsx"
      via: "import and render"
      pattern: "import.*ChatInterface.*from.*components/chat/ChatInterface"
---

<objective>
Wire ChatInterface component into the dashboard page to close the final Phase 6 gap.

Purpose: The ChatInterface component (277 lines, SSE streaming, Data Cards, typing indicators) is fully built but the dashboard page still renders a placeholder div with text "Chat interface will be implemented in Plan 04" instead of the actual component. This blocks the primary user workflow -- users cannot ask questions or see AI responses even though all infrastructure is complete.

Output: Dashboard page renders ChatInterface when a file tab is active, enabling the complete chat experience.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-04-SUMMARY.md
@frontend/src/app/(dashboard)/dashboard/page.tsx
@frontend/src/components/chat/ChatInterface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ChatInterface into Dashboard Page</name>
  <files>frontend/src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
    In `frontend/src/app/(dashboard)/dashboard/page.tsx`:

    1. Add import at the top (after the existing imports, around line 7):
       ```tsx
       import { ChatInterface } from "@/components/chat/ChatInterface";
       ```

    2. Replace lines 88-100 (the placeholder div block inside `{currentTab ? (...)}`):
       ```tsx
       {currentTab ? (
         <ChatInterface
           fileId={currentTab.fileId}
           fileName={currentTab.fileName}
         />
       ) : (
       ```
       Remove the wrapping `<div className="p-6">`, `<div className="max-w-4xl mx-auto">`, and the entire placeholder content (FileSpreadsheet icon, "Chat for {currentTab.fileName}" heading, "Chat interface will be implemented in Plan 04" text). The ChatInterface component manages its own layout, padding, and scroll behavior internally.

    3. Keep the empty state (the `: (` branch for when no tab is selected) exactly as-is. That's the "Get started / Select a file" prompt which is correct.

    The FileSpreadsheet import can remain since it's used in the tab bar icons (line 62). The Upload import is used in the empty state (line 105). No imports need to be removed.
  </action>
  <verify>
    Run TypeScript check and build:
    ```bash
    cd frontend && npx tsc --noEmit --pretty && npm run build
    ```
    Both must pass with zero errors. Grep the built file to confirm:
    ```bash
    grep -c "Plan 04" frontend/src/app/\(dashboard\)/dashboard/page.tsx
    ```
    Must return 0 (placeholder text is gone).
    ```bash
    grep -c "ChatInterface" frontend/src/app/\(dashboard\)/dashboard/page.tsx
    ```
    Must return at least 2 (import + usage).
  </verify>
  <done>
    Dashboard page imports and renders ChatInterface when currentTab exists. The placeholder text "Chat interface will be implemented in Plan 04" is completely removed. TypeScript compiles without errors. The build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `grep "ChatInterface" frontend/src/app/\(dashboard\)/dashboard/page.tsx` returns import line and JSX usage
2. `grep "Plan 04" frontend/src/app/\(dashboard\)/dashboard/page.tsx` returns nothing (placeholder removed)
3. `cd frontend && npx tsc --noEmit --pretty` passes with zero errors
4. `cd frontend && npm run build` compiles successfully
</verification>

<success_criteria>
- ChatInterface component renders in dashboard when a file tab is active
- Placeholder "Chat interface will be implemented in Plan 04" is removed
- Empty state ("Get started / Select a file") still renders when no tab is selected
- TypeScript and build both pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-interactive-data-cards/06-09-SUMMARY.md`
</output>
