---
phase: 06-frontend-ui-interactive-data-cards
plan: 05
type: execute
wave: 3
depends_on: ["06-04"]
files_modified:
  - frontend/src/components/chat/DataCard.tsx
  - frontend/src/components/data/DataTable.tsx
  - frontend/src/components/chat/ChatMessage.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/components/ui/collapsible.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/table.tsx
autonomous: true

must_haves:
  truths:
    - "Query results display as Data Cards inline with chat messages"
    - "Data Cards show Query Brief section at the top"
    - "Data Cards show Data Table section in the middle when table data is available"
    - "Data Cards show AI Explanation section at the bottom"
    - "Data tables are sortable by clicking column headers"
    - "Data tables are filterable with a global search input"
    - "Data tables paginate with Previous/Next controls"
    - "Sections appear progressively during streaming (brief first, table second, explanation last)"
    - "Older Data Cards are collapsible to show just the Query Brief"
  artifacts:
    - path: "frontend/src/components/chat/DataCard.tsx"
      provides: "Progressive-rendering Data Card with 3 sections and collapsible behavior"
      contains: "Collapsible"
    - path: "frontend/src/components/data/DataTable.tsx"
      provides: "TanStack Table with sorting, filtering, and pagination"
      contains: "useReactTable"
  key_links:
    - from: "frontend/src/components/chat/DataCard.tsx"
      to: "frontend/src/components/data/DataTable.tsx"
      via: "renders DataTable for table data section"
      pattern: "DataTable"
    - from: "frontend/src/components/chat/ChatMessage.tsx"
      to: "frontend/src/components/chat/DataCard.tsx"
      via: "renders DataCard for assistant messages with structured data"
      pattern: "DataCard"
    - from: "frontend/src/components/chat/ChatInterface.tsx"
      to: "frontend/src/components/chat/DataCard.tsx"
      via: "passes streaming state to DataCard for progressive rendering"
      pattern: "isStreaming.*DataCard|DataCard.*streaming"
---

<objective>
Build interactive Data Cards with progressive rendering and sortable/filterable data tables using TanStack Table.

Purpose: Data Cards are the primary way users consume analysis results. Progressive rendering (brief -> table -> explanation) provides immediate feedback during AI processing. TanStack Table enables data exploration within the cards.

Output: DataCard component with 3 progressive sections (Query Brief, Data Table, AI Explanation), collapsible behavior for older cards, and fully interactive data tables with sorting, filtering, and pagination.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-CONTEXT.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-RESEARCH.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-04-SUMMARY.md
@backend/app/schemas/chat.py
@frontend/src/types/chat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DataTable component with TanStack Table</name>
  <files>
    frontend/src/components/data/DataTable.tsx
    frontend/src/components/ui/table.tsx
  </files>
  <action>
    1. Install shadcn/ui table component:
       ```
       cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend
       npx shadcn@latest add table
       ```

    2. Create `frontend/src/components/data/DataTable.tsx` (Client Component):
       - Props interface:
         ```typescript
         interface DataTableProps {
           columns: string[]           // Column names from backend
           data: Record<string, any>[] // Row data
         }
         ```
       - Uses TanStack Table v8 with these features:
         - **Sorting:** Click column header to sort asc/desc/unsorted. Show arrow indicator.
         - **Global filter:** Search input above table that filters across all columns.
         - **Pagination:** 10 rows per page, Previous/Next buttons, "Page X of Y" indicator.
       - Implementation:
         - Convert `columns` string array to TanStack column definitions:
           ```typescript
           const tableColumns = columns.map(col => ({
             accessorKey: col,
             header: col,
             cell: (info) => String(info.getValue() ?? '')
           }))
           ```
         - Use `getCoreRowModel`, `getSortedRowModel`, `getFilteredRowModel`, `getPaginationRowModel`
         - Sorting state: `const [sorting, setSorting] = useState<SortingState>([])`
         - Filter state: `const [globalFilter, setGlobalFilter] = useState('')`
       - Visual styling:
         - Use shadcn/ui Table, TableHeader, TableRow, TableCell, TableBody components
         - Filter input: shadcn Input with search icon (Lucide `Search`) and placeholder "Search..."
         - Sort indicators: up arrow, down arrow, or none based on sort state
         - Column headers clickable with cursor-pointer
         - Pagination bar below table with page info and nav buttons
         - Alternate row backgrounds for readability (even rows slightly different)
         - Table container with horizontal scroll for wide tables (overflow-x-auto)
         - Rounded borders matching card aesthetic
       - Handle edge cases:
         - Empty data: show "No data available" message
         - Empty columns: show "No columns defined"
         - Null/undefined cell values: render as empty string
  </action>
  <verify>
    ```
    cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend
    npx tsc --noEmit --pretty
    ```
    TypeScript compilation succeeds for DataTable.tsx.
  </verify>
  <done>
    DataTable component renders TanStack Table with column sorting (click headers), global search filter, pagination (10 rows/page with Previous/Next), and handles edge cases (empty data, null values). Uses shadcn/ui Table components for consistent styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build DataCard with progressive rendering and integrate into chat</name>
  <files>
    frontend/src/components/chat/DataCard.tsx
    frontend/src/components/chat/ChatMessage.tsx
    frontend/src/components/chat/ChatInterface.tsx
    frontend/src/components/ui/collapsible.tsx
    frontend/src/components/ui/badge.tsx
  </files>
  <action>
    1. Install shadcn/ui components:
       ```
       cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend
       npx shadcn@latest add collapsible badge
       ```

    2. Create `frontend/src/components/chat/DataCard.tsx` (Client Component):
       - Props interface:
         ```typescript
         interface DataCardProps {
           queryBrief?: string            // Section 1: user's query summary
           tableData?: {                  // Section 2: data table
             columns: string[]
             rows: Record<string, any>[]
           }
           explanation?: string           // Section 3: AI explanation
           generatedCode?: string         // Code for code display (Plan 06)
           isStreaming?: boolean           // Whether this card is still receiving data
           isCollapsed?: boolean          // Whether card starts collapsed (older cards)
           onToggleCollapse?: () => void  // Callback for collapse toggle
         }
         ```
       - Structure using shadcn/ui Collapsible:
         - CollapsibleTrigger: shows Query Brief as card header with chevron icon
         - CollapsibleContent: contains Data Table and AI Explanation sections
       - Progressive rendering logic:
         - Section 1 (Query Brief): always visible in header, renders immediately
         - Section 2 (Data Table): renders only when `tableData` is provided
           - If tableData exists: render `<DataTable columns={...} data={...} />`
           - If tableData is null and isStreaming: show skeleton loading placeholder
         - Section 3 (AI Explanation): renders only when `explanation` is provided
           - If streaming and no explanation yet: show TypingIndicator
           - If explanation exists: render as formatted text
       - Card styling:
         - Rounded-lg border with subtle shadow
         - Padding 4-6 for comfortable spacing
         - Badge showing "Data Card" or step count at top
         - Smooth collapse/expand animation
         - Chevron icon rotates on collapse/expand
       - Collapse behavior:
         - Cards default to expanded when first created
         - Older cards can be collapsed (show only Query Brief header)
         - Click header to toggle expand/collapse
       - Leave placeholder for download buttons (Plan 06 will add) and code display (Plan 06)
         - Comment: `{/* Download buttons added by Plan 06 */}`
         - Comment: `{/* Code display added by Plan 06 */}`

    3. Update `frontend/src/components/chat/ChatMessage.tsx`:
       - For assistant messages with metadata_json that contains structured data:
         - Parse metadata_json to extract: `generated_code`, `execution_result`, `analysis`, `user_query`
         - If these fields exist: render `<DataCard>` instead of plain text
         - Pass appropriate props to DataCard:
           - queryBrief: user_query or first line of content
           - tableData: parse execution_result if it contains tabular data (JSON with columns/rows)
           - explanation: analysis text
           - generatedCode: generated_code
         - Non-structured assistant messages: continue rendering as plain text
       - For streaming messages:
         - Parse streaming events to determine which DataCard sections are ready
         - Map SSE events to DataCard sections:
           - After `coding_started`/`validation_started`: show Query Brief
           - After `node_complete` with execution data: show Data Table
           - During `content_chunk` for analysis: stream explanation text
           - After `completed`: full Data Card rendered

    4. Update `frontend/src/components/chat/ChatInterface.tsx`:
       - Manage collapse state for Data Cards:
         - Track which cards are collapsed in local state
         - When new card completes: auto-collapse previous cards
         - User can manually toggle any card
       - Parse completed stream data to create DataCard props:
         - Extract query brief, table data, and explanation from completed event
         - Handle case where execution result is not tabular (no Data Table section)
       - Update auto-scroll behavior to account for DataCard expand/collapse
  </action>
  <verify>
    ```
    cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend
    npm run build
    ```
    Build succeeds. DataCard component renders with all three sections. DataTable renders with sorting and filtering.
  </verify>
  <done>
    Data Cards appear inline with chat messages showing Query Brief, Data Table, and AI Explanation sections. Sections render progressively during streaming. Older cards collapse to show just the header. Data tables support sorting, filtering, and pagination. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes in frontend/
2. DataCard renders 3 sections progressively during streaming
3. DataTable sorting works by clicking column headers
4. DataTable global filter searches across all columns
5. DataTable pagination shows 10 rows per page with Previous/Next
6. Older Data Cards collapse to show Query Brief only
7. ChatMessage correctly decides between plain text and DataCard rendering
8. Empty data tables show "No data available"
</verification>

<success_criteria>
- Data Cards display inline with chat messages (CARD-01, CARD-02)
- Data tables are sortable and filterable (CARD-03)
- Results stream progressively (CARD-04)
- Sections appear in order: Query Brief -> Data Table -> AI Explanation
- Older cards collapsible to reduce visual clutter
- Pagination works for large result sets
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-interactive-data-cards/06-05-SUMMARY.md`
</output>
