---
phase: 51-frontend-migration
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - frontend/src/components/sidebar/UnifiedSidebar.tsx
  - frontend/src/components/sidebar/ChatSidebar.tsx
  - frontend/src/app/(workspace)/layout.tsx
  - frontend/src/app/(workspace)/workspace/page.tsx
  - frontend/src/app/(dashboard)/layout.tsx
autonomous: false
requirements:
  - NAV-01
must_haves:
  truths:
    - "Unified sidebar shows Pulse Analysis, Chat, Files, Settings, Admin Panel nav entries"
    - "Pulse Analysis links to /workspace, Chat to /sessions/new, Files to /my-files, Settings to /settings"
    - "Admin Panel entry only visible to admin users"
    - "Chat history list appears in sidebar only on chat routes (/sessions/*)"
    - "(workspace) route group uses unified sidebar without ChatSidebar"
    - "(dashboard) route group uses unified sidebar instead of ChatSidebar"
    - "Existing chat pages still work with sidebar navigation"
  artifacts:
    - path: "frontend/src/components/sidebar/UnifiedSidebar.tsx"
      provides: "Single sidebar for entire app"
      min_lines: 80
    - path: "frontend/src/app/(workspace)/layout.tsx"
      provides: "Workspace route group layout with auth guard"
      min_lines: 20
    - path: "frontend/src/app/(workspace)/workspace/page.tsx"
      provides: "Placeholder workspace page (replaced in plan 03)"
      min_lines: 5
  key_links:
    - from: "frontend/src/components/sidebar/UnifiedSidebar.tsx"
      to: "frontend/src/components/sidebar/ChatList.tsx"
      via: "conditional render on chat routes"
      pattern: "pathname.*sessions"
    - from: "frontend/src/app/(dashboard)/layout.tsx"
      to: "frontend/src/components/sidebar/UnifiedSidebar.tsx"
      via: "import and render"
      pattern: "UnifiedSidebar"
---

<objective>
Build a unified sidebar component replacing ChatSidebar, create (workspace) route group with its own layout, and refactor (dashboard) layout to use the unified sidebar.

Purpose: Navigation foundation — the sidebar is shared by all authenticated pages and must exist before workspace pages can be built. The (workspace) route group isolates workspace pages from the chat layout.
Output: UnifiedSidebar component, (workspace) route group with layout, refactored (dashboard) layout.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-frontend-migration/51-CONTEXT.md
@.planning/phases/51-frontend-migration/51-RESEARCH.md
@.planning/phases/51-frontend-migration/51-01-SUMMARY.md

<interfaces>
<!-- Existing sidebar components to integrate -->
From frontend/src/components/sidebar/ChatList.tsx — renders chat session list, used by ChatSidebar
From frontend/src/components/sidebar/ChatListItem.tsx — individual chat session row
From frontend/src/components/sidebar/UserSection.tsx — user avatar + logout at sidebar bottom
From pulse-mockup/src/components/layout/sidebar.tsx — mockup sidebar design to base UnifiedSidebar on

<!-- Auth hook for admin check -->
From frontend/src/hooks/useAuth.ts:
- useAuth() returns { user, isAuthenticated, isLoading } where user has role field
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UnifiedSidebar + route group layouts</name>
  <files>
    frontend/src/components/sidebar/UnifiedSidebar.tsx,
    frontend/src/app/(workspace)/layout.tsx,
    frontend/src/app/(workspace)/workspace/page.tsx,
    frontend/src/app/(dashboard)/layout.tsx
  </files>
  <action>
    **Part A: UnifiedSidebar component**
    Create `frontend/src/components/sidebar/UnifiedSidebar.tsx` based on mockup's `sidebar.tsx` design:
    - Nav items: Pulse Analysis (/workspace, Sparkles icon), Chat (/sessions/new, MessageSquare icon), Files (/my-files, FolderOpen icon), Settings (/settings, Settings icon), Admin Panel (admin-frontend URL, Shield icon — only visible when user.role === "admin")
    - Use `usePathname()` for active state detection — highlight current route's nav item
    - Chat history section: render `ChatList` component conditionally when `pathname.startsWith('/sessions')` or `pathname === '/dashboard'`
    - User section at bottom: reuse existing `UserSection` component
    - Collapsible state: use local state or mockup's pattern
    - Apply Hex.tech palette: sidebar bg #070b14, border #1e293b, active accent #3b82f6
    - Admin Panel: check existing admin-frontend URL pattern in the codebase (likely `window.location.origin` on port 3001 or `/admin` route). Use `<a href>` with target="_blank" if external.

    **Part B: (workspace) route group layout**
    Create `frontend/src/app/(workspace)/layout.tsx`:
    - "use client" directive
    - Auth guard: redirect to /login if not authenticated (follow existing (dashboard)/layout.tsx pattern)
    - Render UnifiedSidebar + main content area
    - Do NOT include ChatSidebar, LinkedFilesPanel, or any chat-specific wrappers

    Create placeholder `frontend/src/app/(workspace)/workspace/page.tsx`:
    - Simple "Collections" heading — will be replaced in plan 03
    - Enough to verify the route group works

    **Part C: Refactor (dashboard) layout**
    Read and modify `frontend/src/app/(dashboard)/layout.tsx`:
    - Replace ChatSidebar with UnifiedSidebar
    - Keep all existing functionality (auth guard, providers, etc.)
    - Remove ChatSidebar import, add UnifiedSidebar import
    - Ensure LinkedFilesPanel and any chat-specific panels remain for chat routes only

    Do NOT delete ChatSidebar.tsx yet — keep it as backup until regression check passes. Just stop importing it in the layout.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>UnifiedSidebar renders with all nav items, (workspace) route group exists with auth guard, (dashboard) layout uses UnifiedSidebar, /workspace route is accessible, chat pages still work</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify sidebar navigation</name>
  <action>Human verification checkpoint</action>
  <what-built>Unified sidebar replacing ChatSidebar across all authenticated pages, (workspace) route group with own layout</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev`
    2. Visit http://localhost:3000/sessions/new — verify unified sidebar shows: Pulse Analysis, Chat (active), Files, Settings. Chat history list should appear below nav items.
    3. Click "Pulse Analysis" in sidebar — should navigate to /workspace and show placeholder page
    4. Click "Files" — should navigate to /my-files
    5. Click "Settings" — should navigate to /settings
    6. Navigate back to a chat session — verify chat history list reappears in sidebar
    7. If logged in as admin, verify "Admin Panel" link appears in sidebar
    8. Verify sidebar styling matches Hex.tech palette (dark background, blue accents)
  </how-to-verify>
  <resume-signal>Type "approved" if sidebar navigation works correctly across all routes, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` passes
- /workspace route renders with UnifiedSidebar
- /sessions/new renders with UnifiedSidebar + ChatList
- /my-files renders with UnifiedSidebar
- Nav items have correct active states
- Admin Panel only visible to admin users
</verification>

<success_criteria>
- Single unified sidebar used by both route groups
- Chat history only appears on chat routes
- /workspace route accessible and authenticated
- No regression on existing dashboard/chat pages
</success_criteria>

<output>
After completion, create `.planning/phases/51-frontend-migration/51-02-SUMMARY.md`
</output>
