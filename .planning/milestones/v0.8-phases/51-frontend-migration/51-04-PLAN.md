---
phase: 51-frontend-migration
plan: 04
type: execute
wave: 3
depends_on: ["51-01", "51-02"]
files_modified:
  - frontend/src/app/(workspace)/workspace/collections/[id]/signals/page.tsx
  - frontend/src/components/workspace/signal-list-panel.tsx
  - frontend/src/components/workspace/signal-detail-panel.tsx
  - frontend/src/components/workspace/signal-chart-renderer.tsx
autonomous: false
requirements:
  - SIGNAL-03
  - SIGNAL-04
must_haves:
  truths:
    - "Detection Results page shows split-panel layout: SignalListPanel (left) + SignalDetailPanel (right)"
    - "Signals sorted by severity (critical -> warning -> info) in list panel"
    - "Highest-severity signal auto-selected on page load"
    - "Signal detail panel shows title, severity/category badges, Plotly chart, analysis text, 2x2 evidence grid"
    - "Signal chart type driven by chartType field: bar->BarChart, line->AreaChart with fill, scatter->ScatterChart via Plotly"
    - "Investigation and What-If buttons visible but disabled (opacity-50) with tooltip 'Coming in a future update'"
  artifacts:
    - path: "frontend/src/app/(workspace)/workspace/collections/[id]/signals/page.tsx"
      provides: "Detection Results page"
      min_lines: 30
    - path: "frontend/src/components/workspace/signal-list-panel.tsx"
      provides: "Signal list sorted by severity"
      min_lines: 40
    - path: "frontend/src/components/workspace/signal-detail-panel.tsx"
      provides: "Signal detail with chart, evidence, disabled buttons"
      min_lines: 60
    - path: "frontend/src/components/workspace/signal-chart-renderer.tsx"
      provides: "Plotly chart renderer for signals"
      min_lines: 40
  key_links:
    - from: "frontend/src/app/(workspace)/workspace/collections/[id]/signals/page.tsx"
      to: "frontend/src/hooks/useWorkspace.ts"
      via: "useCollectionSignals hook"
      pattern: "useCollectionSignals"
    - from: "frontend/src/components/workspace/signal-chart-renderer.tsx"
      to: "frontend/src/components/chart/ChartRenderer.tsx"
      via: "reuses existing Plotly wrapper"
      pattern: "ChartRenderer"
    - from: "frontend/src/components/workspace/signal-detail-panel.tsx"
      to: "frontend/src/stores/workspaceStore.ts"
      via: "selectedSignalId from store"
      pattern: "useWorkspaceStore"
---

<objective>
Build the Detection Results page with signal split-panel layout, Plotly charts driven by chartType field, and disabled Investigation/What-If buttons.

Purpose: This is the final page users see after running Pulse detection — the signal display is the core value delivery of v0.8. Charts must use Plotly (not Recharts per user decision).
Output: Working /workspace/collections/[id]/signals page with split-panel, severity-sorted signals, Plotly charts, and evidence grid.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-frontend-migration/51-CONTEXT.md
@.planning/phases/51-frontend-migration/51-RESEARCH.md
@.planning/phases/51-frontend-migration/51-01-SUMMARY.md
@.planning/phases/51-frontend-migration/51-02-SUMMARY.md

<interfaces>
<!-- Signal type from plan 01 workspace types -->
SignalDetail: { id, title, severity: "critical"|"warning"|"info", category, analysis, evidence: { metric, context, benchmark, impact } | null, chart_data: object | null, chart_type: "bar"|"line"|"scatter"|null, created_at }

<!-- Existing chart component to reuse -->
From frontend/src/components/chart/ChartRenderer.tsx:
- Accepts plotlyJSON string prop, renders Plotly chart with dark theme
- Handles dynamic import of plotly.js-dist-min with ssr: false
- Has ResizeObserver for responsive sizing

<!-- Severity colors for charts -->
SEVERITY_COLORS = { critical: "#ef4444", warning: "#f59e0b", info: "#22c55e" }

<!-- Plotly chartType mapping -->
bar -> Plotly type: "bar"
line -> Plotly type: "scatter" with mode: "lines", fill: "tozeroy" (area chart)
scatter -> Plotly type: "scatter" with mode: "markers"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SignalChartRenderer + SignalListPanel + SignalDetailPanel</name>
  <files>
    frontend/src/components/workspace/signal-chart-renderer.tsx,
    frontend/src/components/workspace/signal-list-panel.tsx,
    frontend/src/components/workspace/signal-detail-panel.tsx
  </files>
  <action>
    **SignalChartRenderer** (`signal-chart-renderer.tsx`):
    Create a component that converts a SignalDetail's chart_data + chart_type into Plotly JSON and renders via ChartRenderer.
    - Accept props: { signal: SignalDetail }
    - Build `buildSignalPlotlyJSON(signal)` function per research code example:
      - chart_type "bar" -> Plotly bar trace
      - chart_type "line" -> Plotly scatter with mode "lines" + fill "tozeroy" (area fill matching mockup style)
      - chart_type "scatter" -> Plotly scatter with mode "markers"
    - Use severity color for primary trace: SEVERITY_COLORS[signal.severity]
    - Dark theme layout: paper_bgcolor "transparent", plot_bgcolor "transparent", font color "#94a3b8", grid color "#1e293b"
    - Pass generated JSON string to existing ChartRenderer component
    - Handle null chart_data gracefully (show "No chart data" message)
    - Use `next/dynamic` with `ssr: false` if not already handled by ChartRenderer

    **SignalListPanel** (`signal-list-panel.tsx`):
    Adapt from mockup's signal-list-panel.tsx:
    - Accept props: { signals: SignalDetail[], selectedId: string | null, onSelect: (id: string) => void }
    - Sort signals by severity: severityOrder = { critical: 0, warning: 1, info: 2 }
    - Render each signal as a clickable card: severity badge (color-coded), title, category, truncated analysis
    - Highlight selected signal card (border or bg change)
    - Empty state: "No signals found" with message to run detection
    - Scrollable list with ScrollArea component

    **SignalDetailPanel** (`signal-detail-panel.tsx`):
    Adapt from mockup's signal-detail-panel.tsx:
    - Accept props: { signal: SignalDetail | null }
    - If no signal selected: show empty state "Select a signal to view details"
    - Header: signal title + severity badge + category badge
    - Chart section: render SignalChartRenderer with the signal
    - Analysis text section: render signal.analysis as formatted text
    - Evidence grid: 2x2 grid showing signal.evidence fields (metric, context, benchmark, impact) — each in a card with label + value. Handle null evidence gracefully.
    - Action buttons section at bottom:
      - "Investigate" button: disabled (opacity-50), with Tooltip wrapping it: "Coming in a future update"
      - "What-If Analysis" button: disabled (opacity-50), with Tooltip: "Coming in a future update"
      - Both buttons use the disabled prop + className="opacity-50 cursor-not-allowed"
    - All text uses Hex.tech palette colors (muted-foreground for labels, foreground for values)
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>SignalChartRenderer converts chart_data+chart_type to Plotly, SignalListPanel sorts by severity, SignalDetailPanel shows full detail with disabled action buttons</done>
</task>

<task type="auto">
  <name>Task 2: Detection Results page wiring</name>
  <files>
    frontend/src/app/(workspace)/workspace/collections/[id]/signals/page.tsx
  </files>
  <action>
    Create the Detection Results page at `/workspace/collections/[id]/signals`:
    - "use client" directive
    - Extract collection id from route params
    - Use `useCollectionSignals(id)` hook to fetch signals
    - Use `useWorkspaceStore` for selectedSignalId + setSelectedSignalId
    - Sort signals by severity (same severityOrder as SignalListPanel)
    - Auto-select highest severity signal on initial load: useEffect that sets selectedSignalId to sortedSignals[0]?.id when data first loads and no signal is selected (SIGNAL-02)
    - Layout: flex row, SignalListPanel on left (~350px width, fixed), SignalDetailPanel on right (flex-1)
    - Back button in header: "Back to Collection" linking to `/workspace/collections/${id}`
    - Collection name in header (use useCollectionDetail to get name)
    - Skeleton loading state while signals are loading
    - Error state with retry
    - Empty state when no signals: friendly message + "Run Detection" button linking back to Files tab
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Detection Results page renders split-panel with signals from API, highest-severity auto-selected, Plotly charts render per chartType, Investigation/What-If buttons disabled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify detection results</name>
  <action>Human verification checkpoint</action>
  <what-built>Detection Results page with signal split-panel, Plotly charts, severity sorting, disabled Investigation/What-If buttons</what-built>
  <how-to-verify>
    1. Run backend and frontend dev servers
    2. Navigate to a collection that has signals (run Pulse detection first if needed)
    3. Click "Open Signals View" from Signals tab, or navigate to /workspace/collections/[id]/signals
    4. Verify split-panel layout: signal list on left, detail on right
    5. Verify signals are sorted by severity (critical first, then warning, then info)
    6. Verify highest-severity signal is auto-selected on page load
    7. Click different signals — detail panel should update
    8. Verify Plotly chart renders in detail panel matching the signal's chartType
    9. Verify evidence grid shows metric, context, benchmark, impact
    10. Verify "Investigate" and "What-If Analysis" buttons are visible but grayed out with tooltip
    11. Verify back button returns to collection detail page
  </how-to-verify>
  <resume-signal>Type "approved" if Detection Results page works correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` passes
- /workspace/collections/[id]/signals renders split-panel layout
- Signals sorted by severity
- Highest severity auto-selected
- Plotly charts render correctly per chartType
- Evidence grid displays 2x2
- Investigation + What-If buttons disabled with tooltip
</verification>

<success_criteria>
- SIGNAL-01: Signals sorted by severity on Detection Results page
- SIGNAL-02: Highest-severity auto-selected on load
- SIGNAL-03: Detail panel shows chart, evidence, disabled Investigate/What-If buttons
- SIGNAL-04: Chart type driven by chartType field via Plotly
</success_criteria>

<output>
After completion, create `.planning/phases/51-frontend-migration/51-04-SUMMARY.md`
</output>
