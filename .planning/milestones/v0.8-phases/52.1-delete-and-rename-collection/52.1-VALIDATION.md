---
phase: 52.1
slug: delete-and-rename-collection
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-09
---

# Phase 52.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | No automated test framework detected (no jest.config, vitest.config, or test directories) |
| **Config file** | none — manual QA is current approach |
| **Quick run command** | N/A — manual verification |
| **Full suite command** | N/A — manual verification |
| **Estimated runtime** | ~5 minutes manual QA per feature |

---

## Sampling Rate

- **After every task commit:** Manual smoke test of the changed surface (API or UI)
- **After every plan wave:** Full manual QA of the wave's feature set
- **Before `/gsd:verify-work`:** All manual verifications in the map below must be confirmed green
- **Max feedback latency:** ~5 minutes per task

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Behavior | Test Type | Manual Steps | Status |
|---------|------|------|----------|-----------|--------------|--------|
| backend-01 | 01 | 1 | `delete_collection()` service method exists | manual | `grep delete_collection backend/app/services/collection.py` | ⬜ pending |
| backend-02 | 01 | 1 | `DELETE /collections/{id}` returns 204 for owner | manual | curl/API client: owner deletes own collection | ⬜ pending |
| backend-03 | 01 | 1 | `DELETE /collections/{id}` returns 404 for non-owner | manual | curl/API client: attempt delete with wrong user token | ⬜ pending |
| backend-04 | 01 | 1 | Cascade: child rows removed on delete | manual | Query signals/reports/collection_files after delete | ⬜ pending |
| hooks-01 | 02 | 2 | `useUpdateCollection` hook exists and calls PATCH | manual | Browser devtools — check network on rename save | ⬜ pending |
| hooks-02 | 02 | 2 | `useDeleteCollection` hook exists and calls DELETE | manual | Browser devtools — check network on delete confirm | ⬜ pending |
| dialog-01 | 02 | 2 | Edit dialog pre-fills current name and description | manual | Open rename dialog, verify fields populated | ⬜ pending |
| dialog-02 | 02 | 2 | Edit dialog title shows "Edit Collection", button shows "Save" | manual | Visual verification in UI | ⬜ pending |
| dialog-03 | 02 | 2 | Delete dialog shows collection name bolded + cascade warning | manual | Visual verification in UI | ⬜ pending |
| dialog-04 | 02 | 2 | Delete button is red (destructive) and shows "Deleting..." when pending | manual | Click delete, observe button state during API call | ⬜ pending |
| list-01 | 03 | 3 | Kebab menu (⋮) visible in top-right of collection card | manual | Visual check on /workspace list page | ⬜ pending |
| list-02 | 03 | 3 | Clicking ⋮ opens dropdown without navigating to detail page | manual | Click ⋮, verify URL unchanged, dropdown visible | ⬜ pending |
| list-03 | 03 | 3 | "Rename" in card menu opens rename dialog pre-filled | manual | Click Rename, verify dialog opens with values | ⬜ pending |
| list-04 | 03 | 3 | "Delete" in card menu opens delete confirmation dialog | manual | Click Delete, verify confirmation dialog appears | ⬜ pending |
| list-05 | 03 | 3 | Successful delete from list: card disappears, URL unchanged | manual | Confirm delete, verify card gone, URL still /workspace | ⬜ pending |
| list-06 | 03 | 3 | Successful rename from list: card shows updated name | manual | Rename, verify card text updates | ⬜ pending |
| detail-01 | 04 | 3 | Kebab menu (⋮) in detail page header | manual | Navigate to /workspace/collections/[id], check header | ⬜ pending |
| detail-02 | 04 | 3 | "Rename" in detail menu opens rename dialog pre-filled | manual | Click Rename, verify dialog opens with values | ⬜ pending |
| detail-03 | 04 | 3 | "Delete" in detail menu opens delete confirmation dialog | manual | Click Delete, verify confirmation dialog appears | ⬜ pending |
| detail-04 | 04 | 3 | Successful delete from detail: `router.push('/workspace')` | manual | Confirm delete, verify navigation to /workspace | ⬜ pending |
| detail-05 | 04 | 3 | Successful rename from detail: page header updates | manual | Rename, verify `<h1>` heading shows new name | ⬜ pending |
| toast-01 | 03-04 | 3 | Toast "Collection deleted" appears on successful delete | manual | Visual verification — sonner toast visible | ⬜ pending |
| toast-02 | 03-04 | 3 | Toast "Collection updated" appears on successful rename | manual | Visual verification — sonner toast visible | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- No automated test infrastructure detected — no Wave 0 setup required
- All validation is manual QA for this phase

*Existing infrastructure covers all phase requirements (manual QA approach).*

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| DELETE endpoint ownership check | No test framework | `curl -X DELETE /api/collections/{id}` with wrong user JWT — expect 404 |
| Cascade child row cleanup | No test framework | After delete, query `SELECT * FROM signals WHERE collection_id = '{id}'` — expect 0 rows |
| Link + DropdownMenu click isolation | UI interaction | Click ⋮ button on card — verify dropdown opens, URL does NOT change to /collections/[id] |
| Dialog state reset between collections | UI interaction | Rename collection A, close, open rename for collection B — verify B's name in field |
| Detail page header update after rename | UI interaction | Rename from detail page — verify `<h1>` heading reflects new name without page reload |
| Post-delete navigation from detail | UI interaction | Delete from detail page — verify navigation to /workspace, collection no longer in list |

---

## Validation Sign-Off

- [ ] All tasks have manual verify steps defined above
- [ ] Sampling continuity: each wave has full manual QA before advancing
- [ ] No Wave 0 dependencies (no automated test framework to set up)
- [ ] No watch-mode flags
- [ ] Feedback latency < 5 minutes per task
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
