---
phase: 51.1-pipeline-refactor
task: post-UAT fixes complete — viz failures + UI scroll/layout
status: complete
last_updated: 2026-03-08T23:59:00.000Z
---

<current_state>
Phase 51.1 is fully complete. All post-UAT issues resolved.
</current_state>

<completed_work>

## Chart Spinner Fix (Rounds 1–3)
- key={signal.id} on SignalChartRenderer: forces remount on signal switch
- min-h-[300px] on wrapper: prevents spinner overflow
- Double requestAnimationFrame in ChartRenderer: guarantees browser paint before Plotly runs

## Viz Chart Generation Failures — FIXED
Root causes found via diagnostic logging added to pulse.py:
1. `tick_font` → `tickfont` (wrong property name in viz system prompt)
2. `title_font` inside `title=dict()` → correct is `font=dict()` inside title dict
3. Dual-axis attempts (`secondary_y`, `yaxis='y2'` on traces, `yaxis='y1'/'y2'` params) → prohibited with normalization fallback instruction
4. `titlefont` in axis dicts → `title_font` (magic underscore)

All fixes in `backend/app/config/prompts.yaml` viz system prompt.

## UI Layout Fixes
- Signal list panel: replaced Radix ScrollArea with plain `overflow-y-auto min-h-0` div — scrolling works
- Report detail page: reverted main to overflow-y-auto — page scrolls again
- Collection detail tabs/files: removed max-w-7xl mx-auto — content fills full width, tabs no longer visually centered
- Signals page: uses h-screen for concrete bounded height — split panel works correctly

</completed_work>

<remaining_work>
- Move to Phase 52 (Admin and QA)
</remaining_work>
