---
phase: 51.1-pipeline-refactor
plan: 02
subsystem: api
tags: [langgraph, structured-output, pydantic, orchestrator, asyncio]

requires:
  - phase: 51.1-pipeline-refactor
    provides: "Pydantic structured output models, Signal.generated_code field, sub-agent prompts"
provides:
  - "Orchestrator Pulse pipeline with 4 nodes and structured output"
  - "Re-run signal/report overwrite in PulseService"
  - "generated_code persistence for audit trail"
  - "Alembic migration for generated_code column"
affects: [51.1-03-frontend-ux]

tech-stack:
  added: []
  patterns: ["with_structured_output() on all non-code-generating LLM calls", "Fan-out per-signal validation with asyncio.gather"]

key-files:
  created:
    - backend/alembic/versions/357a798917d0_add_generated_code_to_signals.py
  modified:
    - backend/app/agents/pulse.py
    - backend/app/services/pulse.py

key-decisions:
  - "Coder and Viz agents do NOT use with_structured_output since they produce code, not structured data"
  - "Re-run deletes existing signals AND reports before persisting new ones"
  - "PulseRun records kept on re-run (audit trail per user decision)"

patterns-established:
  - "Orchestrator pattern: brain agent generates hypotheses, tool agents validate independently"
  - "Token efficiency: each agent receives only minimum context needed"

requirements-completed: [PIPE-03, PIPE-04, PIPE-05]

duration: 3min
completed: 2026-03-08
---

# Phase 51.1 Plan 02: Pipeline Rewrite Summary

**Orchestrator Pulse pipeline with 4 LangGraph nodes, structured output via with_structured_output(), fan-out per-signal validation, re-run overwrite, and generated_code audit trail**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-08T19:05:14Z
- **Completed:** 2026-03-08T19:08:34Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Complete rewrite of pulse.py from 3-node monolith to 4-node orchestrator pattern
- with_structured_output() on hypothesis, interpreter, and report writer LLM calls
- Per-signal fan-out: Coder -> Validator -> Sandbox -> Interpreter -> Visualization
- PulseService re-run deletes old signals/reports before persisting new ones
- generated_code saved to Signal model + Alembic migration

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite pulse.py -- orchestrator pipeline with structured output** - `c0dfbf9` (feat)
2. **Task 2: Update PulseService for re-run overwrite + generated_code + Alembic migration** - `d15b337` (feat)

## Files Created/Modified
- `backend/app/agents/pulse.py` - Complete rewrite: 4-node orchestrator with structured output, _get_llm_for_agent helper, _process_single_signal fan-out
- `backend/app/services/pulse.py` - Re-run signal/report deletion, new pipeline output shape mapping, generated_code persistence
- `backend/alembic/versions/357a798917d0_add_generated_code_to_signals.py` - Adds generated_code Text column to signals table

## Decisions Made
- Coder and Viz agents produce code (not structured data), so they use raw LLM invocation instead of with_structured_output()
- Re-run deletes both signals AND reports for the collection before persisting new results
- PulseRun records are kept (never deleted) for audit trail
- Deep profile context passed to Coder so it knows data schema; data_summary NOT passed (business intent already encoded in code_instructions)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Pipeline fully rewritten with orchestrator pattern
- All structured output models from Plan 01 are wired into LLM calls
- Ready for Plan 03 (frontend UX improvements)

---
*Phase: 51.1-pipeline-refactor*
*Completed: 2026-03-08*
