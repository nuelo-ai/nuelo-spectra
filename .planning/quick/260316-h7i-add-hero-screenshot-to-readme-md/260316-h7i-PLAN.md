---
phase: quick
plan: 260316-h7i
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QUICK-h7i
must_haves:
  truths:
    - "README.md displays spectra-screenshot.png between the description paragraph and the Features section"
    - "The image renders correctly on GitHub (relative path, alt text present)"
  artifacts:
    - path: "README.md"
      provides: "Hero screenshot inserted at correct location"
      contains: "spectra-screenshot.png"
  key_links:
    - from: "README.md"
      to: "spectra-screenshot.png"
      via: "Markdown image reference"
      pattern: "!\\[.*\\]\\(spectra-screenshot\\.png\\)"
---

<objective>
Insert the hero screenshot into README.md immediately after the opening description paragraph and before the ## Features section.

Purpose: Give visitors a visual impression of the product before reading the feature list.
Output: README.md with one added Markdown image line.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert hero screenshot into README.md</name>
  <files>README.md</files>
  <action>
    Open README.md. After the closing sentence of the "What is Spectra?" paragraph (the paragraph ending "...or the Pulse Analysis workspace.") and before the blank line that precedes `## Features`, insert the following two lines:

    ```

    ![Spectra screenshot](spectra-screenshot.png)
    ```

    The result should be:

    ```
    ...or the Pulse Analysis workspace.

    ![Spectra screenshot](spectra-screenshot.png)

    ## Features
    ```

    Do not change any other content in the file.
  </action>
  <verify>
    <automated>grep -n "spectra-screenshot.png" README.md</automated>
  </verify>
  <done>README.md contains exactly one `![Spectra screenshot](spectra-screenshot.png)` line, positioned between the "What is Spectra?" paragraph and the `## Features` heading. No other content is altered.</done>
</task>

</tasks>

<verification>
- `grep -n "spectra-screenshot.png" README.md` returns exactly one match
- Line number of the match is between the "What is Spectra?" paragraph end and the `## Features` heading
- `grep -c "## Features" README.md` still returns 1 (heading not duplicated or removed)
</verification>

<success_criteria>
README.md has the hero image inserted at the correct location. The file is otherwise identical to the original.
</success_criteria>

<output>
After completion, create `.planning/quick/260316-h7i-add-hero-screenshot-to-readme-md/260316-h7i-SUMMARY.md` with what was done, the exact line inserted, and the commit hash.
</output>
