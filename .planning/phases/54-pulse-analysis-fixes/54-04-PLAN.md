---
phase: 54-pulse-analysis-fixes
plan: "04"
type: execute
wave: 3
depends_on:
  - "54-01"
  - "54-02"
  - "54-03"
files_modified: []
autonomous: false
requirements:
  - PULSE-01
  - PULSE-02
  - PULSE-03
  - PULSE-04
  - PULSE-05

must_haves:
  truths:
    - "All 5 PULSE requirements visually confirmed by the user"
    - "v0.8.1 milestone is ready to ship after this checkpoint passes"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of all five Phase 54 requirements before closing v0.8.1.

Purpose: Requirements PULSE-02, PULSE-03, PULSE-04, and PULSE-05 have no automated assertions — they require visual inspection in a running browser. PULSE-01 has backend tests but also needs visual confirmation that the Credits Used card shows real data. This is the v0.8.1 gate.
Output: User confirmation that all 5 requirements pass, or a list of issues to address.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of all 5 PULSE requirements</name>
  <files>N/A — verification only, no files modified</files>
  <action>No automated action — this checkpoint verifies the output of plans 54-01, 54-02, and 54-03. The executor presents the verification steps to the user and waits for approval.</action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>User approves all 5 PULSE requirements as visually confirmed.</done>
  <what-built>
    Plan 54-01: Credits Used card now shows actual cumulative spend (sum of completed pulse run credit_cost) instead of the cost-per-run config value.
    Plan 54-02: Activity history and Files Added entries now show date AND time (e.g. "Mar 10, 2026, 02:34 PM").
    Plan 54-03: Signal View is mobile-responsive (panel toggle) and has a Chat with Spectra bridge button.
  </what-built>
  <how-to-verify>
    Start the dev server if not running. Open the Pulse workspace in a browser.

    **PULSE-01 — Credits Used accuracy:**
    1. Open a Collection that has had at least one completed pulse run
    2. Go to the Overview tab
    3. Verify the Credits Used card shows a non-zero value that reflects actual run history (not the cost-per-run config value "5" or similar flat number)
    4. Expected: the number matches the sum of credit costs from completed runs

    **PULSE-02 — Mobile Signal View:**
    1. Open any Collection with signals, navigate to the Detection Results (Signals) page
    2. Resize the browser window to below 640px width (or use browser DevTools responsive mode)
    3. Verify: only the signals list is visible initially
    4. Click on any signal in the list
    5. Verify: the detail panel appears (full width) and the list is hidden
    6. Click "Back to Signals" (visible only on mobile)
    7. Verify: the list reappears and the detail panel is hidden
    8. Resize to desktop width — verify both panels show side by side as before

    **PULSE-03 — Chat bridge button:**
    1. On the Signal detail panel, find the "Chat with Spectra" button (should appear between the Analysis text and the Statistical Evidence card)
    2. Verify its placement is correct
    3. Click the "Chat with Spectra" button
    4. Verify: a loading spinner appears on the button while creating the session
    5. Verify: the page navigates to a new Chat session (/sessions/{id})
    6. Verify: the collection's files are pre-linked in the new session

    **PULSE-04 — Activity history timestamps:**
    1. Open a Collection with signals or reports (to populate the Activity section)
    2. Go to the Overview tab, scroll to the Activity History section
    3. Verify each entry shows both date AND time, e.g. "Mar 10, 2026, 02:34 PM"
    4. Expected: NOT "Mar 10, 2026" (date-only)

    **PULSE-05 — Files Added timestamps:**
    1. On the same Collection, go to the Files tab
    2. Look at the "Added" column in the files table
    3. Verify each entry shows both date AND time, e.g. "Mar 10, 2026, 02:34 PM"
    4. Expected: NOT "Mar 10, 2026" (date-only)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 pass, or describe which requirements failed and what was observed.
  </resume-signal>
</task>

</tasks>

<verification>
User approval of all 5 PULSE requirements. After approval, v0.8.1 milestone is complete.
</verification>

<success_criteria>
- PULSE-01: Credits Used card shows actual spend (not flat config value)
- PULSE-02: Signal detail panel accessible on mobile via toggle
- PULSE-03: Chat with Spectra button present and functional
- PULSE-04: Activity history timestamps include time
- PULSE-05: Files Added timestamps include time
</success_criteria>

<output>
After completion, create `.planning/phases/54-pulse-analysis-fixes/54-04-SUMMARY.md`
</output>
