---
phase: 54-pulse-analysis-fixes
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/workspace/activity-feed.tsx
  - frontend/src/components/workspace/file-table.tsx
  - frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx
autonomous: true
requirements:
  - PULSE-04
  - PULSE-05

must_haves:
  truths:
    - "Activity history entries display both date and time (e.g. 'Mar 10, 2026, 02:34 PM')"
    - "Files Added list entries display both date and time"
    - "The UserFileTable inside collections/[id]/page.tsx also shows date and time"
  artifacts:
    - path: "frontend/src/components/workspace/activity-feed.tsx"
      provides: "formatTimestamp with hour/minute options"
      contains: "toLocaleString"
    - path: "frontend/src/components/workspace/file-table.tsx"
      provides: "formatDate with hour/minute options"
      contains: "toLocaleString"
    - path: "frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx"
      provides: "inline formatDate with hour/minute options"
      contains: "toLocaleString"
  key_links:
    - from: "frontend/src/components/workspace/activity-feed.tsx"
      to: "DOM"
      via: "formatTimestamp(item.timestamp)"
      pattern: "formatTimestamp"
    - from: "frontend/src/components/workspace/file-table.tsx"
      to: "DOM"
      via: "formatDate(file.added_at)"
      pattern: "formatDate"
---

<objective>
Fix all timestamp display functions in the Pulse workspace to include both date AND time.

Purpose: Activity history and Files Added list show date-only strings. Users cannot distinguish between events on the same day. The fix changes three formatDate/formatTimestamp functions from `toLocaleDateString` (date only) to `toLocaleString` with hour/minute options.
Output: Three files updated, each timestamp showing e.g. "Mar 10, 2026, 02:34 PM".
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-pulse-analysis-fixes/54-RESEARCH.md

<interfaces>
<!-- Exact current code to replace — verified from codebase inspection. -->

From frontend/src/components/workspace/activity-feed.tsx (lines 16-23):
```typescript
function formatTimestamp(iso: string): string {
  const date = new Date(iso);
  return date.toLocaleDateString("en-US", {
    month: "short",
    day: "numeric",
    year: "numeric",
  });
}
```

From frontend/src/components/workspace/file-table.tsx (lines 19-26):
```typescript
function formatDate(dateStr: string): string {
  const date = new Date(dateStr);
  return date.toLocaleDateString("en-US", {
    month: "short",
    day: "numeric",
    year: "numeric",
  });
}
```

From frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx (lines 686-692):
```typescript
function formatDate(dateStr: string): string {
  return new Date(dateStr).toLocaleDateString("en-US", {
    month: "short",
    day: "numeric",
    year: "numeric",
  });
}
```

Target output format: "Mar 10, 2026, 02:34 PM"
Key: Must use toLocaleString (NOT toLocaleDateString) — toLocaleDateString ignores hour/minute options in all browsers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix formatTimestamp in activity-feed.tsx (PULSE-04)</name>
  <files>frontend/src/components/workspace/activity-feed.tsx</files>
  <action>
    Replace the `formatTimestamp` function body. Change from `toLocaleDateString` to `toLocaleString` and add `hour` and `minute` options.

    Replace:
    ```typescript
    function formatTimestamp(iso: string): string {
      const date = new Date(iso);
      return date.toLocaleDateString("en-US", {
        month: "short",
        day: "numeric",
        year: "numeric",
      });
    }
    ```

    With:
    ```typescript
    function formatTimestamp(iso: string): string {
      const date = new Date(iso);
      return date.toLocaleString("en-US", {
        month: "short",
        day: "numeric",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit",
      });
    }
    ```

    No other changes to this file.
  </action>
  <verify>
    <automated>grep -n "toLocaleString\|hour.*2-digit" /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend/src/components/workspace/activity-feed.tsx</automated>
  </verify>
  <done>activity-feed.tsx formatTimestamp uses toLocaleString with hour and minute options. No toLocaleDateString remains in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix formatDate in file-table.tsx and collections/[id]/page.tsx (PULSE-05)</name>
  <files>
    frontend/src/components/workspace/file-table.tsx
    frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx
  </files>
  <action>
    **file-table.tsx** — Replace the `formatDate` function (lines 19-26):

    Replace:
    ```typescript
    function formatDate(dateStr: string): string {
      const date = new Date(dateStr);
      return date.toLocaleDateString("en-US", {
        month: "short",
        day: "numeric",
        year: "numeric",
      });
    }
    ```

    With:
    ```typescript
    function formatDate(dateStr: string): string {
      const date = new Date(dateStr);
      return date.toLocaleString("en-US", {
        month: "short",
        day: "numeric",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit",
      });
    }
    ```

    **collections/[id]/page.tsx** — Find the inline `formatDate` function (around line 686). This is inside the `UserFileTable` inner component. Replace it with the same pattern:

    Replace:
    ```typescript
    function formatDate(dateStr: string): string {
      return new Date(dateStr).toLocaleDateString("en-US", {
        month: "short",
        day: "numeric",
        year: "numeric",
      });
    }
    ```

    With:
    ```typescript
    function formatDate(dateStr: string): string {
      return new Date(dateStr).toLocaleString("en-US", {
        month: "short",
        day: "numeric",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit",
      });
    }
    ```

    Do NOT touch any other code in these files.
  </action>
  <verify>
    <automated>grep -n "toLocaleString\|hour.*2-digit" /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend/src/components/workspace/file-table.tsx /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend/src/app/\(workspace\)/workspace/collections/\[id\]/page.tsx</automated>
  </verify>
  <done>Both file-table.tsx and collections/[id]/page.tsx formatDate functions use toLocaleString with hour and minute options. No toLocaleDateString remains for date formatting in either file.</done>
</task>

</tasks>

<verification>
- `grep -rn "toLocaleDateString" frontend/src/components/workspace/activity-feed.tsx` returns empty (no remaining instances)
- `grep -rn "toLocaleDateString" frontend/src/components/workspace/file-table.tsx` returns empty
- `grep -c "toLocaleString" frontend/src/components/workspace/activity-feed.tsx` returns 1
- `grep -c "toLocaleString" frontend/src/components/workspace/file-table.tsx` returns 1
- Three total formatDate/formatTimestamp locations updated
</verification>

<success_criteria>
- Activity history entries show date + time (e.g. "Mar 10, 2026, 02:34 PM")
- Files Added entries show date + time
- UserFileTable in collections page shows date + time for uploaded files
- No regressions — all other code in these files untouched
</success_criteria>

<output>
After completion, create `.planning/phases/54-pulse-analysis-fixes/54-02-SUMMARY.md`
</output>
