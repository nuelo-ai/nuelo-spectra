---
phase: 51.1-pipeline-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/pulse.py
  - backend/app/models/signal.py
  - backend/app/config/prompts.yaml
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02

must_haves:
  truths:
    - "Pydantic models exist for each agent step's structured output (hypothesis, business finding, chart instruction, report)"
    - "Signal model has generated_code Text field for audit trail"
    - "prompts.yaml has entries for pulse_coder, pulse_interpreter, pulse_viz, pulse_report_writer"
  artifacts:
    - path: "backend/app/schemas/pulse.py"
      provides: "Structured output Pydantic models for all Pulse agent steps"
      contains: "SignalHypothesis"
    - path: "backend/app/models/signal.py"
      provides: "generated_code column on Signal model"
      contains: "generated_code"
    - path: "backend/app/config/prompts.yaml"
      provides: "Agent config entries for new Pulse sub-agents"
      contains: "pulse_coder"
  key_links:
    - from: "backend/app/schemas/pulse.py"
      to: "backend/app/agents/pulse.py"
      via: "Structured output models used with with_structured_output()"
      pattern: "with_structured_output"
---

<objective>
Define all Pydantic structured output models, add generated_code field to Signal model, and add prompts.yaml config entries for the new Pulse sub-agents.

Purpose: Establish contracts and configuration that the pipeline rewrite (Plan 02) builds against.
Output: Extended schemas, updated Signal model, new prompts.yaml entries.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51.1-pipeline-refactor/51.1-CONTEXT.md
@.planning/phases/51.1-pipeline-refactor/51.1-RESEARCH.md
@backend/app/schemas/pulse.py
@backend/app/models/signal.py
@backend/app/config/prompts.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pydantic structured output models to schemas/pulse.py</name>
  <files>backend/app/schemas/pulse.py</files>
  <action>
Add the following Pydantic models to `backend/app/schemas/pulse.py` (keep all existing models intact):

1. **SignalHypothesis** — Output from Pulse brain's hypothesis generation step:
   - `title: str` (Field: "Business-language title, no statistical jargon")
   - `severity_hint: Literal["critical", "warning", "info"]`
   - `category: str` (Field: "anomaly, trend, correlation, distribution")
   - `code_instructions: str` (Field: "Prescriptive: 'Calculate X, aggregate by Y, return Z as JSON'")
   - `chart_hint: str` (Field: "What business story the chart should tell")

2. **HypothesisOutput** — Wrapper for structured output from hypothesis LLM call:
   - `hypotheses: list[SignalHypothesis]`

3. **CoderResult** — Expected JSON structure from sandbox stdout:
   - `analysis_text: str` (Field: "Summary of what the code computed")
   - `statistical_evidence: dict` (Field: "Metrics dict with metric, context, benchmark, impact keys")
   - `chart_data: dict | None = None`
   - `chart_type: str | None = None`

4. **BusinessFinding** — Output from interpretation LLM call:
   - `title: str` (Field: "Business-language finding title")
   - `finding: str` (Field: "Business-language finding for leadership audience")
   - `severity: Literal["critical", "warning", "info"]`
   - `category: str`
   - `evidence: SignalEvidence`

5. **FindingsOutput** — Wrapper for batch interpretation:
   - `findings: list[BusinessFinding]`

6. **ChartInstruction** — Output from visualization LLM call:
   - `chart_type: Literal["bar", "line", "scatter"]`
   - `title: str`
   - `description: str` (Field: "What business story the chart tells")
   - `code_instructions: str` (Field: "Prescriptive Plotly code generation instructions")

7. **ReportOutput** — Output from report writer LLM call:
   - `executive_summary: str`
   - `content: str` (Field: "Full markdown report")

All models use Pydantic v2 BaseModel. Import Field from pydantic where needed.
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev && python -c "from app.schemas.pulse import SignalHypothesis, HypothesisOutput, CoderResult, BusinessFinding, FindingsOutput, ChartInstruction, ReportOutput; print('OK')" 2>&1 || PYTHONPATH=backend python -c "from app.schemas.pulse import SignalHypothesis, HypothesisOutput, CoderResult, BusinessFinding, FindingsOutput, ChartInstruction, ReportOutput; print('OK')"</automated>
  </verify>
  <done>All 7 new Pydantic models importable and validate correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add generated_code field to Signal model + prompts.yaml entries</name>
  <files>backend/app/models/signal.py, backend/app/config/prompts.yaml</files>
  <action>
**Signal model** (`backend/app/models/signal.py`):
Add `generated_code: Mapped[str | None] = mapped_column(Text, nullable=True)` field after the `chart_type` field. This stores the Python code generated by the Coder agent for audit trail. No Alembic migration needed yet — will be created as part of Plan 02 or handled manually.

**prompts.yaml** (`backend/app/config/prompts.yaml`):
Add 4 new agent entries after the existing `pulse_agent` entry. Each entry needs: provider, model, temperature, max_tokens, system_prompt. Use the same provider/model as pulse_agent (anthropic, claude-sonnet-4-20250514) but with agent-specific temperatures and prompts:

1. `pulse_coder`:
   - temperature: 0.0 (deterministic code generation)
   - max_tokens: 4096
   - system_prompt: "You are a Python data analysis coder. You receive prescriptive instructions about what to compute and return. Write clean Python code that: 1) Reads the specified data files using pandas, 2) Performs exactly the requested calculations, 3) Prints a single JSON object as the LAST line of stdout with keys: analysis_text, statistical_evidence, chart_data, chart_type. Use only pandas, numpy, scipy.stats, and json. Never print raw dataframes. Always return pre-aggregated compact results."

2. `pulse_interpreter`:
   - temperature: 0.7 (creative business language)
   - max_tokens: 2048
   - system_prompt: "You are a senior business analyst interpreting data findings for executive leadership. Convert statistical results into clear business language. Never use statistical jargon (no 'Z-score', 'p-value', 'standard deviation'). Instead use business impact language ('revenue concentration risk', 'customer churn acceleration', 'seasonal demand shift'). Be specific with numbers and percentages."

3. `pulse_viz`:
   - temperature: 0.3
   - max_tokens: 4096
   - system_prompt: "You are a data visualization specialist. Given a business finding and its data, write Plotly Python code that generates a chart telling the business story. The code must: 1) Read the data file(s), 2) Create a Plotly figure, 3) Print fig.to_json() as the LAST line of stdout. Use dark theme colors (#0a0e1a background, #3b82f6 accent). Keep charts clean and focused on the key insight."

4. `pulse_report_writer`:
   - temperature: 0.7
   - max_tokens: 4096
   - system_prompt: "You are an executive report writer. Create concise, impactful detection reports summarizing data analysis findings. Write in business language for leadership. Structure: executive summary (2-3 sentences), then signal-by-signal breakdown with key metrics. You may include tables and bullet points at your discretion. Focus on actionable insights and business impact."

Also add a `code_gen_prompt` field to `pulse_coder` (not pulse_agent) — this is the template for the coder's user message. Move the existing `code_gen_prompt` from `pulse_agent` to `pulse_coder`.
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev && PYTHONPATH=backend python -c "from app.models.signal import Signal; assert hasattr(Signal, 'generated_code'); print('Signal OK')" && PYTHONPATH=backend python -c "from app.agents.config import get_agent_prompt; p = get_agent_prompt('pulse_coder'); assert len(p) > 10; print('prompts OK')"</automated>
  </verify>
  <done>Signal.generated_code field exists; pulse_coder, pulse_interpreter, pulse_viz, pulse_report_writer entries all present in prompts.yaml with system prompts</done>
</task>

</tasks>

<verification>
- All new Pydantic models import without error
- Signal model has generated_code field
- All 4 new prompts.yaml entries are loadable via get_agent_prompt()
</verification>

<success_criteria>
Contracts and configuration are in place for Plan 02 (pipeline rewrite) to build against. No runtime behavior changes yet.
</success_criteria>

<output>
After completion, create `.planning/phases/51.1-pipeline-refactor/51.1-01-SUMMARY.md`
</output>
