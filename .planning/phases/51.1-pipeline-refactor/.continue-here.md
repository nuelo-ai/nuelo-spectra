---
phase: 51.1-pipeline-refactor
task: post-UAT hotfixes
total_tasks: complete
status: in_progress
last_updated: 2026-03-08T22:13:20.054Z
---

<current_state>
Phase 51.1 execution and UAT are fully complete. This session was post-UAT hotfixing — fixing bugs discovered during live testing and optimizing the pipeline. All fixes are committed. No uncommitted changes. Ready to move to Phase 52.
</current_state>

<completed_work>

## Phase 51.1 UAT — COMPLETE (7/8 passed, 1 fixed inline)
- All 8 UAT tests run and recorded in 51.1-UAT.md
- Test 6 (signal card navigation highlight) — fixed inline (d53ff57)

## Post-UAT Hotfixes (all committed to develop)
1. **Signal highlight on navigation** (d53ff57) — signals/page.tsx missing useSearchParams(); overview tab passes ?selected=id but page never read it
2. **Report button opens report directly** (7fe2eda) — changed from setActiveTab("reports") to Link href to /reports/{id}
3. **Report generation token overflow** (2ede297) — write_report_node sent chart_data + generated_code to LLM (216k tokens > 200k limit); stripped to title/finding/severity/category/evidence only
4. **LLM token bloat — compact profiles** (939325b) — deep_profile JSON (1-5k tokens/file) was serialized into Coder and Viz for every signal; replaced with _compact_profile_for_coder() (~200-500 tokens) and _compact_profile_for_viz() (column names + dtypes only ~100-200 tokens); viz retry now rebuilds message fresh instead of appending; report writer also strips analysis_text
5. **LangSmith 34MB payload** (6273b8a) — file_bytes in LangGraph state caused 34MB serialization per node transition; moved to _pulse_file_bytes ContextVar (async-safe); service layer sets before ainvoke, nodes rehydrate as needed
6. **Chart spinner UX** (b1fc1c6) — signal switch showed stale chart + stale left panel; replaced useDeferredValue with: chartReady state reset on signal.id change, spinner shown until Plotly.react() Promise resolves via onReady callback, left panel + content update immediately

## Documentation
- 51.1-UAT.md updated with Post-UAT Findings A and B (token bloat)
- .continue-here.md from previous session deleted (UAT was complete)
</completed_work>

<remaining_work>

## Nothing remaining in Phase 51.1
Phase is fully done. Ready for Phase 52 (Admin and QA).

## Pending todos captured in STATE.md
- Disable "Run Detection" button while Pulse is running, show tooltip "Pulse analysis is currently running" (ui)
- Create Dokploy Docker deployment package for spectra-api service (deployment)
- Query safety filter in Manager Agent (security)
- Show suggestions in Data Summary sidebar panel (ui)
- Use Pydantic structured output for agent JSON responses (consistency)
- Plan production environment variable cleanup and validation (deployment)
</remaining_work>

<decisions_made>

- _compact_profile_for_coder: col names, dtypes, key numeric stats (min/max/mean), top categorical values, strong correlations (|r|≥0.5)
- _compact_profile_for_viz: column names + dtypes only (avoid hallucination, not full stats)
- file_bytes in ContextVar (not state): asyncio propagates to sub-tasks including asyncio.gather() fan-out
- Chart spinner: onReady fires after Plotly.react() Promise resolves — ChartRenderer, not useDeferredValue
- Report writer strips analysis_text too (finding field is already interpreted business language)
</decisions_made>

<blockers>
None. All fixes working.
</blockers>

<context>
Long session: started with UAT for Phase 51.1, fixed bugs live, then investigated and fixed LLM token efficiency issues, LangSmith payload bloat, and chart rendering lag. All clean, all committed. Codebase is in good shape for Phase 52.
</context>

<next_action>
/clear then /gsd:resume-work → should route to Phase 52 (Admin and QA)
</next_action>
