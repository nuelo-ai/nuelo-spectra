---
phase: 51.1-pipeline-refactor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx
  - frontend/src/components/workspace/detection-progress-banner.tsx
  - frontend/src/components/workspace/rerun-detection-dialog.tsx
autonomous: true
requirements:
  - PIPE-06
  - PIPE-07
  - PIPE-08

must_haves:
  truths:
    - "Detection shows inline progress banner on Overview tab instead of full-page takeover"
    - "Global toast notification appears when detection completes with link to signals"
    - "Run Detection button + user_context text input always visible, not hidden after detection"
    - "Re-run shows confirmation dialog with credit cost warning before triggering"
    - "'Recent Signals' text changed to 'Signals Identified'"
    - "'View Signal Report' button visible above Files section"
    - "Signal cards on Overview page navigate to Detection Results page on click"
  artifacts:
    - path: "frontend/src/components/workspace/detection-progress-banner.tsx"
      provides: "Inline progress banner component"
      contains: "DetectionProgressBanner"
    - path: "frontend/src/components/workspace/rerun-detection-dialog.tsx"
      provides: "Re-run confirmation dialog with credit cost"
      contains: "RerunDetectionDialog"
    - path: "frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx"
      provides: "Updated collection detail page with all UX improvements"
      contains: "Signals Identified"
  key_links:
    - from: "frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx"
      to: "frontend/src/components/workspace/detection-progress-banner.tsx"
      via: "Component import and conditional render"
      pattern: "DetectionProgressBanner"
    - from: "frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx"
      to: "sonner"
      via: "toast() call on detection completion"
      pattern: "toast\\.success"
---

<objective>
Replace the full-page DetectionLoading overlay with inline progress, add global toast notifications, re-run confirmation dialog, and all UI text/navigation improvements.

Purpose: Users stay on the page during detection, can re-run with confirmation, and navigate signals from Overview.
Output: New components + updated collection detail page.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51.1-pipeline-refactor/51.1-CONTEXT.md
@.planning/phases/51.1-pipeline-refactor/51.1-RESEARCH.md
@frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx
@frontend/src/stores/workspaceStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DetectionProgressBanner and RerunDetectionDialog components</name>
  <files>frontend/src/components/workspace/detection-progress-banner.tsx, frontend/src/components/workspace/rerun-detection-dialog.tsx</files>
  <action>
**DetectionProgressBanner** (`detection-progress-banner.tsx`):
- Props: `{ status: string }` where status is "pending" | "profiling" | "analyzing"
- Renders a rounded border banner (not full-page) with:
  - Spinner icon (Loader2 from lucide-react, animate-spin)
  - Status text: "Preparing detection..." / "Profiling data files..." / "Analyzing patterns..."
  - Hex.tech palette: `border-primary/30 bg-primary/5` styling
- Component sits inline within the Overview tab content, does NOT replace the page

**RerunDetectionDialog** (`rerun-detection-dialog.tsx`):
- Props: `{ creditCost: number, onConfirm: () => void, children: React.ReactNode }` where children is the trigger button
- Uses shadcn `AlertDialog` (AlertDialogTrigger wraps children)
- Title: "Re-run Detection?"
- Description: "Running detection again will replace your current signals and report. This will cost {creditCost} credits. Continue?"
- Cancel and Continue buttons
- Continue calls onConfirm
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Both components exist, type-check, and follow Hex.tech palette conventions</done>
</task>

<task type="auto">
  <name>Task 2: Update collection detail page with all UX improvements</name>
  <files>frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx</files>
  <action>
Read the current page.tsx first, then make these changes:

1. **Remove full-page DetectionLoading takeover**: Find `if (detectionStatus === "running") return <DetectionLoading />` (or similar early return). Remove this early return entirely.

2. **Add inline progress banner**: Import `DetectionProgressBanner`. Within the Overview tab content, render `<DetectionProgressBanner status={pulseRunStatus} />` when detection is running (status is "pending" | "profiling" | "analyzing"). Place it at the top of the Overview tab content, before existing content.

3. **Global toast on completion**: Import `{ toast } from "sonner"` and `{ useRouter } from "next/navigation"`. Add useEffect that watches pulseRun status — when it transitions to "completed", fire:
   ```tsx
   toast.success("Detection complete", {
     description: `${signalCount} signals identified`,
     action: { label: "View Signals", onClick: () => router.push(`/workspace/collections/${id}/signals`) }
   })
   ```

4. **Run Detection always visible**: Find the RunDetectionBanner or Run Detection button. Remove any conditional that hides it after detection (e.g., `hasFilesNoSignals` check). It should always render when files exist. Include the `user_context` text input field (a simple textarea or Input) that passes value to the pulse trigger API call.

5. **Re-run confirmation**: Import `RerunDetectionDialog`. When signals already exist (signalCount > 0), wrap the Run Detection button with `<RerunDetectionDialog creditCost={creditCost} onConfirm={handleRunDetection}>`. When no signals exist, render button directly without dialog.

6. **Text changes**: Find "Recent Signals" text and change to "Signals Identified".

7. **"View Signal Report" button**: Add a prominent button above the Files section (or in the Overview header area). Style: `variant="default"` (primary blue), with a FileText icon from lucide-react. Text: "View Signal Report". onClick navigates to the Reports tab or the report detail.

8. **Signal card navigation**: Find where signal cards are rendered on the Overview tab. Make them clickable — onClick navigates to `/workspace/collections/${id}/signals?selected=${signalId}` (or just `/workspace/collections/${id}/signals` with that signal pre-selected).

9. **409 conflict handling**: When the pulse trigger API returns 409, show a toast error: "A detection run is already in progress" instead of a generic error.
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Collection detail page shows inline progress (not full-page), toast on completion, always-visible Run Detection with re-run confirmation, "Signals Identified" text, "View Signal Report" button, clickable signal cards</done>
</task>

</tasks>

<verification>
- No TypeScript errors in the workspace frontend code
- DetectionProgressBanner renders inline (no full-page takeover)
- RerunDetectionDialog uses shadcn AlertDialog
- "Recent Signals" text no longer appears (replaced with "Signals Identified")
- sonner toast imported and used for completion notification
</verification>

<success_criteria>
All frontend UX improvements from CONTEXT.md locked decisions are implemented. User stays on the page during detection, gets toast notification on completion, can re-run with confirmation, and navigate to signals from Overview.
</success_criteria>

<output>
After completion, create `.planning/phases/51.1-pipeline-refactor/51.1-03-SUMMARY.md`
</output>
