---
status: complete
phase: 51.1-pipeline-refactor
source: [51.1-01-SUMMARY.md, 51.1-02-SUMMARY.md, 51.1-03-SUMMARY.md]
started: 2026-03-08T19:30:00Z
updated: 2026-03-08T21:20:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Detection Produces Structured Signals
expected: Run detection on a collection with uploaded data. Pipeline completes and produces signals with hypotheses, code, findings, and visualizations. Signals appear in the Detection Results tab.
result: pass

### 2. Re-run Overwrites Previous Signals
expected: Run detection a second time on a collection that already has signals. Previous signals and reports are deleted and replaced with new results. No duplicates.
result: pass

### 3. Inline Progress Banner
expected: When detection is running, an inline progress banner appears at the top of the collection page (not a full-page overlay). The rest of the page remains visible and interactive.
result: pass

### 4. Toast on Detection Completion
expected: When detection finishes, a sonner toast notification appears showing the number of signals found and a "View Signals" action button. Clicking the action navigates to Detection Results.
result: pass

### 5. Re-run Confirmation Dialog
expected: When signals already exist for a collection, clicking "Run Detection" shows an AlertDialog confirmation with credit cost warning before proceeding.
result: pass

### 6. Signal Card Navigation
expected: On the Overview tab, clicking a signal card navigates to the Detection Results page for that signal.
result: issue
reported: "when button clicked it redirects to the signal result page but the clicked signal did not get highlighted. It highlights the top list only"
severity: minor

### 7. View Signal Report Button
expected: On the Overview tab, a "View Signal Report" button is visible. Section is labeled "Signals Identified" (not "Recent Signals").
result: pass

### 8. 409 Conflict Toast
expected: If detection is already running and you try to run it again, a specific toast error appears indicating detection is already in progress.
result: pass

## Summary

total: 8
passed: 7
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "Clicking a signal card on Overview tab navigates to Detection Results and highlights that specific signal"
  status: failed
  reason: "User reported: when button clicked it redirects to the signal result page but the clicked signal did not get highlighted. It highlights the top list only"
  severity: minor
  test: 6
  root_cause: "Signals page never reads ?selected= query param from URL. Overview tab passes signal ID via router.push(?selected=id) but signals/page.tsx has no useSearchParams() — auto-select effect defaults to top signal."
  artifacts:
    - path: "frontend/src/app/(workspace)/workspace/collections/[id]/signals/page.tsx"
      issue: "Missing useSearchParams() to read ?selected= query param and set selectedSignalId in store"
  missing:
    - "Add useSearchParams() hook to read ?selected= param on page load and call setSelectedSignalId"
  debug_session: ""
