---
phase: 39-api-key-management-ui-deployment-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/api_key.py
  - backend/app/services/api_key.py
  - backend/app/schemas/api_key.py
  - backend/app/routers/admin/api_keys.py
  - backend/app/routers/admin/__init__.py
  - backend/app/routers/api_v1/health.py
  - backend/app/routers/api_v1/__init__.py
autonomous: true
requirements:
  - APIKEY-06
  - APIKEY-07
  - APIKEY-08
  - APIINFRA-03

must_haves:
  truths:
    - "Admin can list all API keys (active and revoked) for any user via GET /api/admin/users/{user_id}/api-keys"
    - "Admin can create an API key on behalf of any user via POST /api/admin/users/{user_id}/api-keys, with created_by_admin_id tracked"
    - "Admin can revoke any user's API key via DELETE /api/admin/users/{user_id}/api-keys/{key_id} without ownership check"
    - "GET /api/v1/health returns 200 with service status and DB connectivity check"
    - "ApiKey model has created_by_admin_id, revoked_at, and total_credits_used columns"
  artifacts:
    - path: "backend/app/routers/admin/api_keys.py"
      provides: "Admin API key CRUD endpoints"
      exports: ["router"]
    - path: "backend/app/routers/api_v1/health.py"
      provides: "API v1 health endpoint with DB check"
      exports: ["router"]
    - path: "backend/app/schemas/api_key.py"
      provides: "Admin-specific Pydantic schemas"
      contains: "AdminApiKeyListItem"
  key_links:
    - from: "backend/app/routers/admin/api_keys.py"
      to: "backend/app/services/api_key.py"
      via: "ApiKeyService.list_for_user, ApiKeyService.create, ApiKeyService.admin_revoke"
      pattern: "ApiKeyService\\."
    - from: "backend/app/routers/admin/__init__.py"
      to: "backend/app/routers/admin/api_keys.py"
      via: "admin_router.include_router"
      pattern: "include_router.*api_keys"
    - from: "backend/app/routers/api_v1/__init__.py"
      to: "backend/app/routers/api_v1/health.py"
      via: "api_v1_router.include_router"
      pattern: "include_router.*health"
---

<objective>
Backend infrastructure for admin API key management and API service health endpoint.

Purpose: Provide the backend endpoints and data model extensions that the admin frontend (Plan 03) needs to manage any user's API keys, and the health endpoint that SPECTRA_MODE=api Dokploy service needs for monitoring.

Output: Admin API key CRUD endpoints under /api/admin/users/{user_id}/api-keys, extended ApiKey model with admin tracking fields, and /api/v1/health endpoint with DB connectivity check.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-api-key-management-ui-deployment-mode/39-RESEARCH.md
@backend/app/models/api_key.py
@backend/app/services/api_key.py
@backend/app/schemas/api_key.py
@backend/app/routers/admin/__init__.py
@backend/app/routers/admin/users.py
@backend/app/routers/api_v1/__init__.py
@backend/app/dependencies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ApiKey model and create Alembic migration</name>
  <files>
    backend/app/models/api_key.py
  </files>
  <action>
Add three new columns to the ApiKey model in backend/app/models/api_key.py:

1. `created_by_admin_id: Mapped[UUID | None]` — nullable UUID ForeignKey to users.id with ondelete="SET NULL". Comment: "Admin who created this key on behalf of user (NULL = self-created)".

2. `revoked_at: Mapped[datetime | None]` — nullable DateTime(timezone=True). Set when is_active transitions to False. Used to display revocation date in admin UI.

3. `total_credits_used: Mapped[float]` — Float with default 0.0. Denormalized counter to be populated by Phase 40 credit deduction. Display as "0.0" for now.

Add a relationship for `created_by_admin` pointing to User model (use TYPE_CHECKING import pattern already in file).

Then create the Alembic migration:
```bash
cd backend && uv run alembic revision --autogenerate -m "add created_by_admin_id revoked_at total_credits_used to api_keys"
```

Review the generated migration to ensure it includes:
- `add_column('api_keys', Column('created_by_admin_id', UUID(), nullable=True))`
- `create_foreign_key('fk_api_keys_created_by_admin', 'api_keys', 'users', ['created_by_admin_id'], ['id'], ondelete='SET NULL')`
- `add_column('api_keys', Column('revoked_at', DateTime(timezone=True), nullable=True))`
- `add_column('api_keys', Column('total_credits_used', Float(), server_default='0', nullable=False))`

Run the migration:
```bash
cd backend && uv run alembic upgrade head
```
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run python -c "from app.models.api_key import ApiKey; print('created_by_admin_id' in ApiKey.__table__.columns, 'revoked_at' in ApiKey.__table__.columns, 'total_credits_used' in ApiKey.__table__.columns)"</automated>
    <manual>All three column checks print True</manual>
  </verify>
  <done>ApiKey model has created_by_admin_id (UUID FK nullable), revoked_at (DateTime nullable), total_credits_used (Float default 0.0). Migration applied successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ApiKeyService, schemas, admin router, health endpoint, and register routers</name>
  <files>
    backend/app/services/api_key.py
    backend/app/schemas/api_key.py
    backend/app/routers/admin/api_keys.py
    backend/app/routers/admin/__init__.py
    backend/app/routers/api_v1/health.py
    backend/app/routers/api_v1/__init__.py
  </files>
  <action>
**1. Extend ApiKeyService** (backend/app/services/api_key.py):

- Update `create()` to accept optional `created_by_admin_id: UUID | None = None` parameter. Pass it to the ApiKey constructor.
- Update `revoke()` to also set `revoked_at = datetime.now(timezone.utc)` when deactivating.
- Add `admin_revoke(db, key_id)` static method — same as revoke but does NOT filter by user_id (admin can revoke any key). Also sets `revoked_at`.

**2. Extend schemas** (backend/app/schemas/api_key.py):

- Add `AdminApiKeyListItem` schema extending ApiKeyListItem with additional fields:
  - `revoked_at: datetime | None`
  - `total_credits_used: float`
  - `created_by_admin_id: UUID | None`
  - Keep `model_config = {"from_attributes": True}`

- Update `ApiKeyListItem` to include `total_credits_used: float` (regular users see this too per CONTEXT decision).

**3. Create admin API keys router** (backend/app/routers/admin/api_keys.py):

Create new file with:
```python
router = APIRouter(prefix="/users/{user_id}/api-keys", tags=["admin-api-keys"])
```

Three endpoints following the pattern in backend/app/routers/admin/users.py:

- `GET ""` — list_user_api_keys: Uses `CurrentAdmin` + `DbSession`. Calls `ApiKeyService.list_for_user(db, user_id)`. Returns `list[AdminApiKeyListItem]`.

- `POST ""` — create_user_api_key: Uses `CurrentAdmin` + `DbSession` + `Request`. Calls `ApiKeyService.create(db, user_id, body.name, body.description, created_by_admin_id=current_admin.id)`. Logs admin action via `log_admin_action` (action="create_api_key", target_type="api_key"). Commits. Returns `ApiKeyCreateResponse` with status 201.

- `DELETE "/{key_id}"` — revoke_user_api_key: Uses `CurrentAdmin` + `DbSession` + `Request`. Calls `ApiKeyService.admin_revoke(db, key_id)`. If False, raise 404. Logs admin action (action="revoke_api_key"). Commits. Returns 204.

Import pattern: `from app.dependencies import CurrentAdmin, DbSession`, `from app.services.api_key import ApiKeyService`, `from app.services.admin.audit import log_admin_action`.

**4. Register admin router** (backend/app/routers/admin/__init__.py):

Add import and include_router for admin_api_keys:
```python
from app.routers.admin import api_keys as admin_api_keys
admin_router.include_router(admin_api_keys.router)
```

**5. Create health endpoint** (backend/app/routers/api_v1/health.py):

New file:
```python
router = APIRouter(tags=["API v1 Health"])

@router.get("/health")
async def api_v1_health(db: DbSession):
    settings = get_settings()
    try:
        await db.execute(text("SELECT 1"))
        db_status = "connected"
    except Exception:
        db_status = "disconnected"
    status = "healthy" if db_status == "connected" else "degraded"
    return {
        "status": status,
        "service": "spectra-api",
        "version": settings.app_version,
        "database": db_status,
    }
```

Import: `from sqlalchemy import text`, `from app.dependencies import DbSession`, `from app.config import get_settings`.

**6. Register health router** (backend/app/routers/api_v1/__init__.py):

Add: `from app.routers.api_v1 import health` and `api_v1_router.include_router(health.router)`.
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run python -c "
from app.routers.admin import admin_router
from app.routers.api_v1 import api_v1_router
admin_routes = [r.path for r in admin_router.routes]
api_routes = [r.path for r in api_v1_router.routes]
print('admin api-keys:', any('api-keys' in p for p in admin_routes))
print('health:', any('health' in p for p in api_routes))
print('admin route count:', len(admin_routes))
print('api v1 route count:', len(api_routes))
" && uv run pytest tests/test_api_key_service.py -x -q 2>/dev/null; echo "exit: $?"</automated>
    <manual>Admin routes include api-keys paths, health endpoint registered, existing API key tests pass</manual>
  </verify>
  <done>Admin API key endpoints registered under /api/admin/users/{user_id}/api-keys (GET, POST, DELETE). Health endpoint at /api/v1/health with DB connectivity check. All existing API key service tests still pass.</done>
</task>

</tasks>

<verification>
1. Admin API key endpoints are registered and reachable under /api/admin/users/{user_id}/api-keys
2. Health endpoint returns JSON with status, service, version, database fields
3. ApiKey model has 3 new columns (created_by_admin_id, revoked_at, total_credits_used)
4. ApiKeyService.create accepts created_by_admin_id parameter
5. ApiKeyService.admin_revoke works without user_id check
6. Existing API key service tests pass
</verification>

<success_criteria>
- Admin can list/create/revoke API keys for any user via admin endpoints
- Health endpoint responds at /api/v1/health with DB status
- ApiKey model tracks admin creation and revocation timestamps
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/39-api-key-management-ui-deployment-mode/39-01-SUMMARY.md`
</output>
