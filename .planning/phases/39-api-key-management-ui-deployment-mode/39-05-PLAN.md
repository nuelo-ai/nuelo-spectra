---
phase: 39-api-key-management-ui-deployment-mode
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/api_key.py
  - frontend/src/hooks/useApiKeys.ts
  - frontend/src/components/settings/ApiKeySection.tsx
autonomous: true
requirements: [APIKEY-06, APIKEY-07]
gap_closure: true

must_haves:
  truths:
    - "Public frontend API key metadata shows 'Credit Usage:' label (not 'Credits:')"
    - "Public frontend shows 'Last used: Never' when last_used_at is null"
    - "Admin-created keys show Admin badge on public frontend"
  artifacts:
    - path: "backend/app/schemas/api_key.py"
      provides: "created_by_admin_id field in ApiKeyListItem"
      contains: "created_by_admin_id"
    - path: "frontend/src/hooks/useApiKeys.ts"
      provides: "created_by_admin_id in TS interface"
      contains: "created_by_admin_id"
    - path: "frontend/src/components/settings/ApiKeySection.tsx"
      provides: "Admin badge, Last used Never, Credit Usage label"
  key_links:
    - from: "frontend/src/components/settings/ApiKeySection.tsx"
      to: "frontend/src/hooks/useApiKeys.ts"
      via: "ApiKeyListItem.created_by_admin_id"
      pattern: "created_by_admin_id"
    - from: "backend/app/schemas/api_key.py"
      to: "frontend/src/hooks/useApiKeys.ts"
      via: "JSON response field"
      pattern: "created_by_admin_id"
---

<objective>
Fix three UAT gaps on the public frontend: (1) add created_by_admin_id to backend ApiKeyListItem schema so public frontend receives the field, (2) show "Last used: Never" instead of hiding when null, (3) rename "Credits:" to "Credit Usage:", (4) render Admin badge for admin-created keys.

Purpose: Close minor UAT issues in public frontend API key display and full-stack data flow.
Output: Public frontend shows complete key metadata with admin badge support.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-api-key-management-ui-deployment-mode/39-02-SUMMARY.md
@.planning/phases/39-api-key-management-ui-deployment-mode/39-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add created_by_admin_id to ApiKeyListItem schema and frontend interface</name>
  <files>
    backend/app/schemas/api_key.py
    frontend/src/hooks/useApiKeys.ts
  </files>
  <action>
    1. In `backend/app/schemas/api_key.py`: Add `created_by_admin_id: UUID | None` field to the `ApiKeyListItem` class (currently only `AdminApiKeyListItem` has it). Place it after `total_credits_used`. Import UUID is already present. The `model_config = {"from_attributes": True}` will auto-map from the SQLAlchemy model which already has this column.

    2. In `frontend/src/hooks/useApiKeys.ts`: Add `created_by_admin_id: string | null;` to the `ApiKeyListItem` interface, after `total_credits_used`.
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev && python -c "from app.schemas.api_key import ApiKeyListItem; assert 'created_by_admin_id' in ApiKeyListItem.model_fields" 2>/dev/null || (cd backend && python -c "from app.schemas.api_key import ApiKeyListItem; assert 'created_by_admin_id' in ApiKeyListItem.model_fields") && grep -q 'created_by_admin_id' frontend/src/hooks/useApiKeys.ts && echo "PASS"</automated>
  </verify>
  <done>ApiKeyListItem schema and TS interface both include created_by_admin_id field. Public frontend GET /v1/keys response now includes the field.</done>
</task>

<task type="auto">
  <name>Task 2: Fix public frontend key metadata display and add Admin badge</name>
  <files>frontend/src/components/settings/ApiKeySection.tsx</files>
  <action>
    In the key list metadata row (the `div` with `className="flex items-center gap-3 text-xs text-muted-foreground"`), make three changes:

    1. **Last Used display:** Replace the conditional `{key.last_used_at && (<span>Last used ...</span>)}` with an always-visible version:
       ```
       <span>Last used {key.last_used_at ? new Date(key.last_used_at).toLocaleDateString() : "Never"}</span>
       ```

    2. **Credit Usage label:** Change `<span>Credits: {key.total_credits_used.toFixed(1)}</span>` to `<span>Credit Usage: {key.total_credits_used.toFixed(1)}</span>`.

    3. **Admin badge:** In the name row (the `div` with `className="flex items-center gap-2"` containing the key name and Active/Revoked badge), add an Admin badge after the status badge when `key.created_by_admin_id` is not null:
       ```tsx
       {key.created_by_admin_id && (
         <Badge variant="outline" className="gap-1">
           <Shield className="size-3" />
           Admin
         </Badge>
       )}
       ```
       Import `Shield` from `lucide-react` (add to existing import). Import `Badge` is already present.
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev && grep -q 'Credit Usage:' frontend/src/components/settings/ApiKeySection.tsx && grep -q 'Never' frontend/src/components/settings/ApiKeySection.tsx && grep -q 'Shield' frontend/src/components/settings/ApiKeySection.tsx && echo "PASS"</automated>
    <manual>On Settings page, each API key shows "Last used: Never", "Credit Usage: 0.0", and Admin badge for admin-created keys.</manual>
  </verify>
  <done>Public frontend key list shows "Last used: Never" when null, "Credit Usage:" label, and Admin badge with Shield icon for admin-created keys.</done>
</task>

</tasks>

<verification>
- Public frontend Settings page: each key shows "Credit Usage: 0.0" (not "Credits:")
- Public frontend Settings page: each key shows "Last used: Never" (not hidden)
- Admin-created keys display Shield + "Admin" badge on public frontend
- Backend GET /v1/keys response includes created_by_admin_id field
</verification>

<success_criteria>
All three UAT gaps (tests 2, 7) resolved: credit usage label corrected, last used always visible, admin badge rendered on public frontend for admin-created keys.
</success_criteria>

<output>
After completion, create `.planning/phases/39-api-key-management-ui-deployment-mode/39-05-SUMMARY.md`
</output>
