---
phase: 18-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/ChatInput.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/components/session/FileCard.tsx
  - frontend/src/components/session/LinkedFilesPanel.tsx
autonomous: true

must_haves:
  truths:
    - "User can type messages freely even with no files linked"
    - "Sending a message with no files linked shows toast error AND persistent inline warning below input"
    - "Inline warning disappears automatically when user links a file"
    - "User cannot unlink the last file from a session — remove button is disabled when only 1 file remains"
    - "Attempting to remove the last file shows a warning toast explaining why"
  artifacts:
    - path: "frontend/src/components/chat/ChatInput.tsx"
      provides: "File requirement validation with dual feedback (toast + inline warning)"
      contains: "noFilesWarning"
    - path: "frontend/src/components/chat/ChatInterface.tsx"
      provides: "Passes linkedFileIds to ChatInput for validation"
      contains: "linkedFileIds"
    - path: "frontend/src/components/session/FileCard.tsx"
      provides: "Disabled unlink when only 1 file linked"
      contains: "isLastFile"
    - path: "frontend/src/components/session/LinkedFilesPanel.tsx"
      provides: "Passes file count context to FileCard"
      contains: "isLastFile"
  key_links:
    - from: "frontend/src/components/chat/ChatInterface.tsx"
      to: "frontend/src/components/chat/ChatInput.tsx"
      via: "linkedFileIds prop derived from sessionDetail.files"
      pattern: "linkedFileIds.*sessionDetail"
    - from: "frontend/src/components/session/LinkedFilesPanel.tsx"
      to: "frontend/src/components/session/FileCard.tsx"
      via: "isLastFile prop computed from files.length"
      pattern: "isLastFile.*files\\.length"
---

<objective>
Enforce file requirement before sending messages and prevent unlinking the last file from a session.

Purpose: Users must have at least one file linked to chat meaningfully (CHAT-03). Input stays enabled for composing, but sending is blocked with dual feedback (toast + inline warning). The last file in a session cannot be removed.
Output: Updated ChatInput with validation, ChatInterface passing file context, FileCard/LinkedFilesPanel with last-file protection.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-integration-polish/18-RESEARCH.md
@frontend/src/components/chat/ChatInput.tsx
@frontend/src/components/chat/ChatInterface.tsx
@frontend/src/components/session/FileCard.tsx
@frontend/src/components/session/LinkedFilesPanel.tsx
@frontend/src/hooks/useSessionMutations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add file requirement validation to ChatInput with dual feedback</name>
  <files>
    frontend/src/components/chat/ChatInput.tsx
    frontend/src/components/chat/ChatInterface.tsx
  </files>
  <action>
**ChatInput.tsx changes:**
1. Add `linkedFileIds: string[]` prop to ChatInputProps interface
2. Add `const [noFilesWarning, setNoFilesWarning] = useState(false);` state
3. In `handleSend()`: before the existing `onSend(trimmed)` call, add file check:
   - If `linkedFileIds.length === 0`: call `toast.error("Link at least one file before sending a message")`, set `setNoFilesWarning(true)`, and return early
   - Otherwise: set `setNoFilesWarning(false)` before proceeding with send
4. In `handleKeyDown()`: add the same file check before the `onSend(trimmed)` + `setMessage("")` call (the Enter key path). If no files linked, call `toast.error(...)`, set warning, and return early (don't send or clear input)
5. Add `useEffect` that watches `linkedFileIds` — when `linkedFileIds.length > 0`, call `setNoFilesWarning(false)` to auto-clear warning
6. Add inline warning JSX below the toolbar row (after the existing `</div>` that closes the toolbar):
   ```
   {noFilesWarning && (
     <div className="flex items-center gap-2 text-sm text-destructive px-1">
       <AlertCircle className="h-4 w-4 shrink-0" />
       <span>Link at least one file to start chatting</span>
     </div>
   )}
   ```
7. Import `AlertCircle` from `lucide-react`, `toast` from `sonner`, and `useEffect` from `react`

**ChatInterface.tsx changes:**
1. Pass `linkedFileIds` prop to ChatInput, derived from sessionDetail:
   ```
   linkedFileIds={sessionDetail?.files?.map((f) => f.id) ?? []}
   ```
   This goes on the `<ChatInput ... />` component in the render section (around line 594).

Per user decision: input stays enabled (user can type), only send is blocked. WelcomeScreen stays as-is (no file enforcement there).
  </action>
  <verify>
Run `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx next build 2>&1 | tail -20` — build succeeds with no type errors. Grep ChatInput.tsx for `noFilesWarning` and `linkedFileIds` to confirm both are present.
  </verify>
  <done>
ChatInput blocks sending when no files linked, shows toast + inline warning, auto-clears warning when files are linked. ChatInterface passes linkedFileIds to ChatInput. Input textarea remains enabled for typing at all times.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prevent unlinking the last file from a session</name>
  <files>
    frontend/src/components/session/FileCard.tsx
    frontend/src/components/session/LinkedFilesPanel.tsx
  </files>
  <action>
**LinkedFilesPanel.tsx changes:**
1. Pass `isLastFile={files.length === 1}` as a prop to each `<FileCard>` component in the file list map
2. This is a simple addition to the existing `files.map((file) => (...))` block

**FileCard.tsx changes:**
1. Add `isLastFile?: boolean` to FileCardProps interface
2. Accept `isLastFile` in the component destructuring
3. In the remove AlertDialog trigger `<Button>`:
   - Add `disabled={isLastFile}` attribute
   - Update `title` to show `{isLastFile ? "Cannot remove last file — at least one file must be linked" : "Remove from session"}`
4. In `handleUnlink()`: add early return guard at the top:
   ```
   if (isLastFile) {
     toast.warning("At least one file must be linked to the session");
     return;
   }
   ```
5. Import `toast` from `sonner` (if not already imported)

The AlertDialog trigger button being disabled prevents the confirmation dialog from opening. The toast in handleUnlink is a safety net in case disabled state is bypassed. Per user decision: show warning "At least one file must be linked" when user tries to unlink the only remaining file.
  </action>
  <verify>
Run `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx next build 2>&1 | tail -20` — build succeeds. Grep FileCard.tsx for `isLastFile` and `toast.warning` to confirm both are present.
  </verify>
  <done>
Remove button on last remaining file is disabled with explanatory title. Attempting to remove via handleUnlink shows warning toast. LinkedFilesPanel passes isLastFile boolean to each FileCard based on file count.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx next build` — compiles without errors
2. ChatInput.tsx contains `noFilesWarning` state, `linkedFileIds` prop, `AlertCircle` inline warning, and toast.error call
3. ChatInterface.tsx passes `linkedFileIds={sessionDetail?.files?.map(...) ?? []}` to ChatInput
4. FileCard.tsx contains `isLastFile` prop, disabled button logic, toast.warning fallback
5. LinkedFilesPanel.tsx passes `isLastFile={files.length === 1}` to each FileCard
</verification>

<success_criteria>
- Sending a message with 0 files linked produces toast error AND visible inline warning below input
- Inline warning disappears when a file is linked (no manual action needed)
- Input textarea stays enabled at all times (user can type freely)
- Last file in session cannot be unlinked — remove button is disabled
- WelcomeScreen is unaffected (no file validation enforced there)
</success_criteria>

<output>
After completion, create `.planning/phases/18-integration-polish/18-01-SUMMARY.md`
</output>
