---
phase: 51-frontend-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/components/ui/avatar.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/progress.tsx
  - frontend/src/components/ui/scroll-area.tsx
  - frontend/src/components/ui/separator.tsx
  - frontend/src/components/ui/sheet.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/table.tsx
  - frontend/src/components/ui/tabs.tsx
  - frontend/src/components/ui/textarea.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/types/workspace.ts
  - frontend/src/hooks/useWorkspace.ts
  - frontend/src/stores/workspaceStore.ts
autonomous: false
requirements:
  - SIGNAL-04
must_haves:
  truths:
    - "Dark mode uses Hex.tech palette (#0a0e1a background, #111827 cards, #1e293b borders, #3b82f6 accent)"
    - "All shared UI components match pulse-mockup versions"
    - "Existing pages (/sessions, /my-files, /settings, /dashboard) still render correctly after component swap"
    - "TypeScript workspace types match backend API schema"
    - "TanStack Query hooks exist for collections, signals, pulse status"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Hex.tech dark palette tokens"
      contains: "#0a0e1a"
    - path: "frontend/src/types/workspace.ts"
      provides: "TypeScript types matching backend schemas"
      contains: "SignalDetailResponse"
    - path: "frontend/src/hooks/useWorkspace.ts"
      provides: "TanStack Query hooks for workspace API"
      exports: ["useCollections", "useCollectionDetail", "useCollectionSignals", "usePulseStatus"]
    - path: "frontend/src/stores/workspaceStore.ts"
      provides: "Zustand store for workspace UI state"
      exports: ["useWorkspaceStore"]
  key_links:
    - from: "frontend/src/hooks/useWorkspace.ts"
      to: "/collections API"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*collections"
    - from: "frontend/src/types/workspace.ts"
      to: "backend/app/schemas/pulse.py"
      via: "matching field names"
      pattern: "chart_type|chart_data|evidence"
---

<objective>
Apply Hex.tech dark palette to globals.css, replace all shared UI components with pulse-mockup versions, create workspace TypeScript types matching backend API schemas, create TanStack Query hooks and Zustand store for workspace data, and verify existing pages still work after the swap.

Purpose: Establish the visual foundation and data layer that all subsequent workspace plans depend on. Component swap + palette must happen first because every page uses these primitives.
Output: Updated globals.css, replaced UI components, new workspace types/hooks/store, passing build + existing page regression.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-frontend-migration/51-CONTEXT.md
@.planning/phases/51-frontend-migration/51-RESEARCH.md

<interfaces>
<!-- Backend API schemas the TypeScript types must match -->

From backend/app/schemas/collection.py:
```python
class CollectionListItem(BaseModel):
    id: UUID
    name: str
    description: str | None
    created_at: datetime
    updated_at: datetime
    file_count: int
    signal_count: int

class CollectionDetailResponse(BaseModel):
    id: UUID
    name: str
    description: str | None
    created_at: datetime
    updated_at: datetime
    file_count: int
    signal_count: int
    report_count: int

class CollectionFileResponse(BaseModel):
    id: UUID
    file_id: UUID
    added_at: datetime
    filename: str
    file_size: int | None = None
    data_summary: str | None = None

class ReportListItem(BaseModel):
    id: UUID
    title: str
    report_type: str
    created_at: datetime
    pulse_run_id: UUID | None

class ReportDetailResponse(BaseModel):
    id: UUID
    title: str
    report_type: str
    content: str | None
    created_at: datetime
    pulse_run_id: UUID | None
    signal_count: int
```

From backend/app/schemas/pulse.py:
```python
class SignalEvidence(BaseModel):
    metric: str
    context: str
    benchmark: str
    impact: str

class SignalDetailResponse(BaseModel):
    id: UUID
    title: str
    severity: str
    category: str
    analysis: str | None
    evidence: dict | None
    chart_data: dict | None
    chart_type: str | None
    created_at: datetime

class PulseRunTriggerResponse(BaseModel):
    pulse_run_id: UUID
    status: str
    credit_cost: float

class PulseRunDetailResponse(PulseRunResponse):
    signal_count: int
    signals: list[SignalDetailResponse] = []

class PulseRunCreate(BaseModel):
    file_ids: list[UUID]
    user_context: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Palette swap, UI component replacement, workspace types + hooks + store</name>
  <files>
    frontend/src/app/globals.css,
    frontend/src/components/ui/avatar.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/checkbox.tsx,
    frontend/src/components/ui/dialog.tsx,
    frontend/src/components/ui/dropdown-menu.tsx,
    frontend/src/components/ui/input.tsx,
    frontend/src/components/ui/progress.tsx,
    frontend/src/components/ui/scroll-area.tsx,
    frontend/src/components/ui/separator.tsx,
    frontend/src/components/ui/sheet.tsx,
    frontend/src/components/ui/skeleton.tsx,
    frontend/src/components/ui/table.tsx,
    frontend/src/components/ui/tabs.tsx,
    frontend/src/components/ui/textarea.tsx,
    frontend/src/components/ui/tooltip.tsx,
    frontend/src/types/workspace.ts,
    frontend/src/hooks/useWorkspace.ts,
    frontend/src/stores/workspaceStore.ts
  </files>
  <action>
    **Part A: Palette swap**
    Update `frontend/src/app/globals.css` `.dark {}` block to use Hex.tech palette from 51-RESEARCH.md Code Examples section. Key tokens: --background: #0a0e1a, --card: #111827, --border: #1e293b, --primary: #3b82f6. Add sidebar tokens and severity tokens. Keep :root (light mode) unchanged.

    **Part B: UI component replacement**
    For each of the 17 UI components listed in files_modified (avatar through tooltip):
    1. Read the current frontend version AND the pulse-mockup version
    2. Diff the two — identify prop signature differences
    3. Replace the frontend version with the mockup version
    4. If the mockup version has different exports or props, ensure backward compatibility: keep existing export names, add any missing variants that existing pages depend on
    5. Ensure all components import from the same relative paths (e.g., `@/lib/utils` for `cn`)

    Important: The mockup has 17 UI components. The frontend has 21 (includes alert-dialog, collapsible, label, sidebar, sonner which the mockup does NOT have). Do NOT touch alert-dialog.tsx, collapsible.tsx, label.tsx, sidebar.tsx, or sonner.tsx — leave those as-is.

    **Part C: Workspace types**
    Create `frontend/src/types/workspace.ts` with TypeScript interfaces matching the backend schemas shown in the interfaces block above. Key types:
    - CollectionListItem: { id, name, description, created_at, updated_at, file_count, signal_count }
    - CollectionDetail: { ...CollectionListItem, report_count }
    - CollectionFile: { id, file_id, added_at, filename, file_size, data_summary }
    - SignalDetail: { id, title, severity: "critical"|"warning"|"info", category, analysis, evidence: SignalEvidence|null, chart_data, chart_type: "bar"|"line"|"scatter"|null, created_at }
    - SignalEvidence: { metric, context, benchmark, impact }
    - PulseRunTrigger: { pulse_run_id, status, credit_cost }
    - PulseRunDetail: { id, collection_id, status, credit_cost, error_message, started_at, completed_at, created_at, signal_count, signals }
    - ReportListItem, ReportDetail
    - PulseRunCreate: { file_ids: string[], user_context?: string }

    **Part D: TanStack Query hooks**
    Create `frontend/src/hooks/useWorkspace.ts` following existing hook patterns (check useFileManager.ts or similar). Hooks:
    - useCollections(): GET /collections/
    - useCollectionDetail(id): GET /collections/{id}, enabled: !!id
    - useCollectionFiles(collectionId): GET /collections/{id}/files
    - useCollectionSignals(collectionId): GET /collections/{id}/signals
    - useCollectionReports(collectionId): GET /collections/{id}/reports
    - usePulseStatus(collectionId, pulseRunId): GET /collections/{id}/pulse/{pulseRunId}, refetchInterval 3000ms until status is "complete" or "failed"
    - useTriggerPulse(): useMutation POST /collections/{id}/pulse with PulseRunCreate body
    - useCreateCollection(): useMutation POST /collections/ with { name } body, invalidates ["collections"]
    - useUploadFile(collectionId): useMutation POST /collections/{id}/files (FormData)
    - useRemoveFile(collectionId): useMutation DELETE /collections/{id}/files/{fileId}
    All hooks use apiClient from @/lib/api-client and return typed responses from workspace.ts.

    **Part E: Zustand store**
    Create `frontend/src/stores/workspaceStore.ts` with:
    - selectedSignalId: string | null + setter
    - selectedFileIds: string[] + toggleFileSelection + clearFileSelection
    - detectionStatus: "idle" | "running" | "complete" | "failed" + setter
    - pulseRunId: string | null + setter
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>globals.css has Hex.tech dark palette, all 17 shared UI components replaced with mockup versions, workspace types/hooks/store created, build passes with no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify regression</name>
  <action>Human verification checkpoint</action>
  <what-built>Hex.tech dark palette applied to globals.css, all 17 shared UI components replaced with pulse-mockup versions, workspace TypeScript types + TanStack Query hooks + Zustand store created</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev`
    2. Visit http://localhost:3000/sessions/new — verify chat page renders correctly with new dark palette (darker background #0a0e1a, blue accents #3b82f6)
    3. Visit http://localhost:3000/my-files — verify file manager page renders, upload button works visually
    4. Visit http://localhost:3000/settings — verify settings forms render with updated styling
    5. Visit http://localhost:3000/dashboard — verify dashboard cards render
    6. Check that buttons, cards, badges, dialogs all have consistent styling across pages
    7. Open browser console — verify no React errors or import failures
  </how-to-verify>
  <resume-signal>Type "approved" if all existing pages render correctly with new palette, or describe any visual regressions</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` completes without errors
- globals.css contains #0a0e1a, #111827, #1e293b, #3b82f6 in .dark block
- All 17 UI component files match mockup versions
- frontend/src/types/workspace.ts exports all workspace types
- frontend/src/hooks/useWorkspace.ts exports all hooks
- frontend/src/stores/workspaceStore.ts exports useWorkspaceStore
- Existing pages render without console errors
</verification>

<success_criteria>
- Hex.tech dark palette is live across entire app
- UI components are refreshed to mockup versions
- All existing pages (chat, files, settings, dashboard) still work
- Workspace data layer (types, hooks, store) ready for consumption by plans 02-04
</success_criteria>

<output>
After completion, create `.planning/phases/51-frontend-migration/51-01-SUMMARY.md`
</output>
