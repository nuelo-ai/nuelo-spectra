---
phase: 51-frontend-migration
plan: 03
type: execute
wave: 3
depends_on: ["51-01", "51-02"]
files_modified:
  - frontend/src/app/(workspace)/workspace/page.tsx
  - frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx
  - frontend/src/components/workspace/collection-card.tsx
  - frontend/src/components/workspace/collection-list.tsx
  - frontend/src/components/workspace/create-collection-dialog.tsx
  - frontend/src/components/workspace/overview-stat-cards.tsx
  - frontend/src/components/workspace/run-detection-banner.tsx
  - frontend/src/components/workspace/signal-card.tsx
  - frontend/src/components/workspace/file-table.tsx
  - frontend/src/components/workspace/file-upload-zone.tsx
  - frontend/src/components/workspace/data-summary-panel.tsx
  - frontend/src/components/workspace/sticky-action-bar.tsx
  - frontend/src/components/workspace/activity-feed.tsx
  - frontend/src/components/workspace/empty-state.tsx
  - frontend/src/components/workspace/detection-loading.tsx
autonomous: false
requirements:
  - NAV-02
  - NAV-03
  - NAV-04
  - SIGNAL-01
  - SIGNAL-02
must_haves:
  truths:
    - "Collection list page shows collection cards from live API with name, file count, signal count, created date"
    - "Create Collection dialog creates a collection via API and card appears in list"
    - "Collection detail page has 4 tabs: Overview, Files, Signals, Reports"
    - "Overview tab shows stat cards, Run Detection banner, signal card previews, file table, activity feed"
    - "Files tab shows file rows with upload zone, checkbox selection, sticky action bar, data summary slide-out"
    - "Signals tab shows signal cards with Open Signals View button"
    - "Collection header shows credit usage pill with Zap icon"
    - "Detection loading state shows 4 animated steps when pulse is running"
  artifacts:
    - path: "frontend/src/app/(workspace)/workspace/page.tsx"
      provides: "Collection list page with API data"
      min_lines: 30
    - path: "frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx"
      provides: "Collection detail page with 4 tabs"
      min_lines: 80
    - path: "frontend/src/components/workspace/collection-card.tsx"
      provides: "Collection card component"
      min_lines: 20
  key_links:
    - from: "frontend/src/app/(workspace)/workspace/page.tsx"
      to: "frontend/src/hooks/useWorkspace.ts"
      via: "useCollections hook"
      pattern: "useCollections"
    - from: "frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx"
      to: "frontend/src/hooks/useWorkspace.ts"
      via: "useCollectionDetail, useCollectionFiles, useCollectionSignals, useCollectionReports"
      pattern: "useCollectionDetail"
    - from: "frontend/src/components/workspace/sticky-action-bar.tsx"
      to: "frontend/src/hooks/useWorkspace.ts"
      via: "useTriggerPulse mutation"
      pattern: "useTriggerPulse"
---

<objective>
Migrate collection list page and collection detail page (4 tabs) from pulse-mockup to main frontend, replacing all mock data with live API calls via TanStack Query hooks.

Purpose: These are the core workspace pages users interact with — creating collections, uploading files, viewing signals/reports, and triggering Pulse detection. They consume the types/hooks/store from plan 01 and the route group from plan 02.
Output: Working /workspace and /workspace/collections/[id] pages with live API data.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-frontend-migration/51-CONTEXT.md
@.planning/phases/51-frontend-migration/51-RESEARCH.md
@.planning/phases/51-frontend-migration/51-01-SUMMARY.md
@.planning/phases/51-frontend-migration/51-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collection list page + workspace components (card, list, create dialog, empty state)</name>
  <files>
    frontend/src/app/(workspace)/workspace/page.tsx,
    frontend/src/components/workspace/collection-card.tsx,
    frontend/src/components/workspace/collection-list.tsx,
    frontend/src/components/workspace/create-collection-dialog.tsx,
    frontend/src/components/workspace/empty-state.tsx
  </files>
  <action>
    Copy-and-adapt from pulse-mockup/src/components/workspace/:
    - collection-card.tsx, collection-list.tsx, create-collection-dialog.tsx, empty-state.tsx

    For each component:
    1. Read mockup version, identify all MOCK_* data imports and remove them
    2. Replace mock data with props typed using workspace.ts types (CollectionListItem etc.)
    3. Update imports: `@/components/ui/*` (already replaced in plan 01), `@/lib/utils` for cn
    4. Keep visual layout identical to mockup

    **Collection list page** (`/workspace`):
    - Use `useCollections()` hook to fetch collection data
    - Render CollectionList with real data
    - "Create Collection" button opens CreateCollectionDialog
    - CreateCollectionDialog uses `useCreateCollection()` mutation
    - Empty state when no collections with CTA to create first collection
    - Skeleton loader while collections are loading (use Skeleton component)
    - Error state with "Try again" button on fetch failure
    - Each collection card links to `/workspace/collections/${id}`
    - Collection card shows: name, description (truncated), file_count, signal_count, created_at formatted
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Collection list page renders from API data, create dialog works via mutation, empty/loading/error states present</done>
</task>

<task type="auto">
  <name>Task 2: Collection detail page with 4 tabs + remaining workspace components</name>
  <files>
    frontend/src/app/(workspace)/workspace/collections/[id]/page.tsx,
    frontend/src/components/workspace/overview-stat-cards.tsx,
    frontend/src/components/workspace/run-detection-banner.tsx,
    frontend/src/components/workspace/signal-card.tsx,
    frontend/src/components/workspace/file-table.tsx,
    frontend/src/components/workspace/file-upload-zone.tsx,
    frontend/src/components/workspace/data-summary-panel.tsx,
    frontend/src/components/workspace/sticky-action-bar.tsx,
    frontend/src/components/workspace/activity-feed.tsx,
    frontend/src/components/workspace/detection-loading.tsx
  </files>
  <action>
    Copy-and-adapt remaining workspace components from pulse-mockup, replacing all mock data with API hooks:

    **Components to migrate:**
    - overview-stat-cards.tsx: receives { file_count, signal_count, report_count } from CollectionDetail
    - run-detection-banner.tsx: shows "Run Detection" CTA, links to Files tab
    - signal-card.tsx: renders a single signal card with severity badge and title (non-interactive in Overview/Signals tabs)
    - file-table.tsx: file rows with checkbox selection, uses `useWorkspaceStore` for selectedFileIds
    - file-upload-zone.tsx: drag-drop upload zone, uses `useUploadFile()` mutation
    - data-summary-panel.tsx: slide-out panel showing file column profile from data_summary field (parse JSON string)
    - sticky-action-bar.tsx: appears when files selected, shows "N files selected" + "Run Detection (5 credits)" button. Triggers `useTriggerPulse()` mutation. Credit cost: fetch from platform settings or hardcode 5 for now.
    - activity-feed.tsx: static/simple activity list (can use created_at timestamps from signals/reports)
    - detection-loading.tsx: full-page loading overlay with 4 animated steps (Profiling data, Detecting anomalies, Analyzing trends, Generating signals)

    **Collection detail page** (`/workspace/collections/[id]`):
    - Use `useCollectionDetail(id)`, `useCollectionFiles(id)`, `useCollectionSignals(id)`, `useCollectionReports(id)` hooks
    - Header: collection name, back button to /workspace, credit usage pill (NAV-04) showing total credits from pulse runs with Zap icon
    - 4 tabs using Tabs component:
      - **Overview**: OverviewStatCards + RunDetectionBanner + up to 4 SignalCard previews in 2-col grid + compact FileTable (3 rows) with "View all files" link + ActivityFeed
      - **Files**: FileUploadZone at top + full FileTable with checkboxes + StickyActionBar when files selected + DataSummaryPanel slide-out on file click
      - **Signals** (NAV-03): all SignalCards in a grid + "Open Signals View" button linking to `/workspace/collections/${id}/signals`
      - **Reports**: report list rows with type badge, title, date, "View Report" button linking to report detail (use existing report viewer pattern or simple markdown render)
    - Skeleton loaders for each tab content while loading
    - When detection is running (detectionStatus === "running" in workspaceStore): show DetectionLoading overlay. Use `usePulseStatus()` to poll. On complete: refetch signals, navigate to signals tab or signals page.
    - Signal sort by severity: const severityOrder = { critical: 0, warning: 1, info: 2 }. Sort signals array before rendering in Overview and Signals tabs (SIGNAL-01).
    - Auto-select highest severity signal on load: set `selectedSignalId` in workspaceStore to sorted signals[0].id when signals data loads (SIGNAL-02) — this is consumed by plan 04's Detection Results page.

    Skip these mockup components (v0.9+): add-to-collection-modal.tsx, investigation-qa-thread.tsx, whatif-refinement-chat.tsx, investigation-checkpoint.tsx
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Collection detail page renders with 4 tabs all showing live API data; file upload/selection works; detection trigger via sticky action bar works; credit pill shows in header; signals sorted by severity</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify workspace pages</name>
  <action>Human verification checkpoint</action>
  <what-built>Collection list page and collection detail page with all 4 tabs wired to live API data, file upload, detection trigger, detection loading state</what-built>
  <how-to-verify>
    1. Run backend and frontend dev servers
    2. Visit http://localhost:3000/workspace — should show collection list (empty state if no collections)
    3. Click "Create Collection" — dialog should appear, create a collection, card appears in list
    4. Click a collection card — should navigate to detail page with 4 tabs
    5. Files tab: try uploading a CSV file, verify it appears in file table
    6. Click a file row — data summary panel should slide out showing column profile
    7. Select files via checkboxes — sticky action bar should appear with "Run Detection (5 credits)"
    8. Overview tab: verify stat cards show correct counts, signal previews if any
    9. Signals tab: verify signal cards display if any exist
    10. Reports tab: verify report list renders
    11. Verify credit usage pill in collection header
  </how-to-verify>
  <resume-signal>Type "approved" if workspace pages work with live data, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` passes
- /workspace shows collection cards from API
- /workspace/collections/[id] shows 4 tabs with live data
- File upload creates collection_file via API
- Detection trigger calls POST /collections/{id}/pulse
- Signals sorted by severity in Overview and Signals tabs
- Credit usage pill visible in header
</verification>

<success_criteria>
- Users can create collections, upload files, view collection details
- All 4 tabs render with live API data
- Detection trigger flow works (button -> loading -> results)
- NAV-02, NAV-03, NAV-04, SIGNAL-01, SIGNAL-02 requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/51-frontend-migration/51-03-SUMMARY.md`
</output>
