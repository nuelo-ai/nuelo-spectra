---
phase: 51-frontend-migration
task: pulse-pipeline-debugging
total_tasks: 4 plans (51-01 through 51-04) + pipeline fixes
status: in_progress
last_updated: 2026-03-08T00:04:20.597Z
---

<current_state>
Phase 51 code is all committed. Owner is now testing the full E2E flow (create collection -> add files -> run detection -> view signals). The Pulse pipeline had multiple bugs preventing successful execution. Most have been fixed this session. The pipeline now reaches sandbox execution but some candidates may still fail due to code generation issues. Last test showed progress: candidates pass code validation, sandbox executes, but need to confirm full end-to-end signal persistence and display.
</current_state>

<completed_work>

- Plan 51-01 through 51-04: All code committed and prior fixes applied
- **This session's bug fixes (all uncommitted on develop):**

1. **MissingGreenlet error on POST /pulse** (500): `pulse_run.files = list(files)` triggered sync lazy-load in async context. Fixed by using direct `INSERT INTO pulse_run_files` via association table instead of ORM relationship assignment. Also removed redundant `await db.commit()` from router (service already commits).

2. **Wrong polling URL** (404 on all polls): Frontend polled `/collections/{id}/pulse/{runId}` but backend route is `/collections/{id}/pulse-runs/{runId}`. Fixed in `useWorkspace.ts`.

3. **Status mismatch**: Frontend checked `status === "complete"` but backend sends `"completed"`. Fixed in both `page.tsx` (monitor effect) and `useWorkspace.ts` (refetchInterval).

4. **Sticky loading page**: `detectionStatus` in Zustand store persisted `"running"` across navigations. Added reset effect on mount when no active `pulseRunId`.

5. **CreditService.refund missing balance_after** (NOT NULL violation on 409 conflict): `CreditTransaction` created without `balance_after` field. Fixed in `credit.py`.

6. **User context input missing**: Added `Textarea` to `RunDetectionBanner` for optional user guidance text. Wired through `handleRunDetection` -> `triggerPulse.mutateAsync` -> backend `PulseRunCreate.user_context`.

7. **Double-click required for Run Detection**: `collectionFiles` async data not loaded when banner first renders. Changed `hasFilesNoSignals`/`hasNoFiles` to use `collectionFiles.length` (same data source as handler) and added `loadingCollectionFiles` guard.

8. **All Pulse candidates rejected — scipy not in allowlist**: LLM generates `import scipy` for statistical analysis. Added `scipy`, `scipy.stats`, `openpyxl` to `allowlist.yaml`.

9. **Sandbox FileNotFoundError**: LLM generated `pd.read_csv('/home/user/data.csv')` but file was uploaded as `maruna-inventory-1.xlsx`. Updated code gen prompt to include actual filenames and correct `read_csv`/`read_excel` function.

10. **Added extensive pipeline logging**: `************ PULSE` markers at every status transition and per-candidate validation results for easy debugging.
</completed_work>

<remaining_work>

1. Run detection end-to-end and confirm signals actually persist and display on Signals tab
2. Verify detection loading page shows properly during pipeline execution
3. Verify signals page renders signal cards with chart data
4. 51-04 Detection Results page (`/workspace/collections/[id]/signals`) still needs owner UAT
5. Phase verification (gsd-verifier) has NOT been run
6. ROADMAP.md and STATE.md final updates pending
7. Commit all this session's fixes
8. Remove debug logging (************ PULSE markers) before final commit
</remaining_work>

<decisions_made>

- Direct INSERT into pulse_run_files association table instead of ORM relationship assignment (avoids MissingGreenlet in async context)
- Polling URL: `/pulse-runs/{id}` matches backend route (not `/pulse/{id}`)
- Backend status value is `"completed"` (not `"complete"`) — frontend aligned to match
- Detection state resets to idle on page mount if no active pulseRunId
- `collectionFiles` is the single source of truth for both banner visibility and handler file IDs
- scipy, scipy.stats, openpyxl added to allowlist for Pulse statistical analysis
- Code gen prompt now includes actual filenames and correct pandas read function
- User context textarea added to RunDetectionBanner (optional, guides but doesn't limit analysis)
</decisions_made>

<blockers>
- Pipeline may still have issues with specific file types or LLM code generation edge cases — need full successful run to confirm
- Debug logging should be removed before production commit
</blockers>

<context>
This session focused on debugging the Pulse detection pipeline end-to-end. The pipeline architecture (LangGraph: profile -> analyze -> generate) is sound but had many integration bugs: async/ORM incompatibility, URL mismatches, status string mismatches, missing allowlist entries, wrong file paths in generated code. Each bug was identified via backend logs and fixed incrementally. The pipeline is now much closer to working — candidates generate, pass code validation, and reach sandbox execution. Next session should start with a clean test run to verify full signal persistence and frontend display.
</context>

<next_action>
Resume with `/gsd:resume-work`. The resuming agent should:
1. Clean collection tables (script in session history)
2. Run detection end-to-end and check logs for `************ PULSE ... completed` with signal count > 0
3. Verify signals display on the Signals tab
4. If working, commit all fixes with descriptive message
5. Continue with 51-04 UAT verification and phase completion
</next_action>
