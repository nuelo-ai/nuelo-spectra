---
phase: 51-frontend-migration
task: pulse-pipeline-fixes
total_tasks: 4 plans (51-01 through 51-04) + pipeline fixes
status: in_progress
last_updated: 2026-03-08T02:57:45.382Z
---

<current_state>
Phase 51 plans 01-03 complete. Plan 04 needs UAT. Pulse pipeline is functional end-to-end (signals persist, charts render, reports viewable). However, the pipeline architecture does NOT match the Phase 49 CONTEXT.md design — _validate_single_candidate bypasses Coding Agent and Visualization Agent, doing everything inline with pulse_agent config. This needs refactoring before milestone completion.
</current_state>

<completed_work>

**Previous session (10 bug fixes — uncommitted on develop):**
1. MissingGreenlet fix: direct INSERT into pulse_run_files association table
2. Polling URL fix: `/pulse-runs/{id}` matches backend route
3. Status mismatch fix: `"completed"` not `"complete"`
4. Sticky loading page: reset detectionStatus on mount
5. CreditService.refund: added missing balance_after field
6. User context textarea added to RunDetectionBanner
7. Double-click fix: collectionFiles as single source of truth
8. scipy/openpyxl added to allowlist.yaml
9. Sandbox FileNotFoundError: correct filenames in code gen prompt
10. Extensive pipeline logging added

**This session:**
11. Re-fetch pulse_run after graph.ainvoke() to avoid stale session
12. Per-signal try/catch in persistence loop
13. Full traceback logging in error handler
14. Signal title fix: merge candidate metadata (title, severity, category) into validated results
15. Chart renderer rewrite: multi-strategy extraction (Plotly native, points arrays, histograms, bubble charts, grouped data, generic fallback)
16. Analysis markdown rendering: ReactMarkdown + remarkGfm in signal-detail-panel
17. Report detail page created: `/workspace/collections/[id]/reports/[reportId]/page.tsx` matching pulse-mockup design (sticky header, document paper area, prose-slate)
18. Type error fix: onClick handler wrapping for handleRunDetection
19. Moved hardcoded code_gen_prompt to prompts.yaml under pulse_agent.code_gen_prompt
20. Added get_agent_config_field() helper to config.py
21. Added plotly/plotly.graph_objects/plotly.express to allowlist.yaml
22. Updated code_gen_prompt to use Plotly fig.to_json() for chart output
</completed_work>

<remaining_work>

1. **CRITICAL — Refactor _validate_single_candidate to reuse existing agents (Phase 49 CONTEXT.md violation):**
   - Currently: pulse_agent LLM does code gen + chart in one script
   - Should be: Coding Agent → Code Checker → E2B → Visualization Agent → E2B (per candidate)
   - See .planning/phases/49-pulse-agent/49-CONTEXT.md for the planned architecture
   - Fan-out parallel execution should remain (all candidates concurrently)

2. Remove debug logging (`************ PULSE` markers) before final commit

3. Phase 51 Plan 04 UAT verification

4. Phase verification (gsd-verifier) has NOT been run

5. Commit all uncommitted fixes (20+ fixes across backend and frontend)

6. ROADMAP.md and STATE.md final updates
</remaining_work>

<decisions_made>

- Direct INSERT into pulse_run_files association table (avoids MissingGreenlet in async context)
- Polling URL: `/pulse-runs/{id}` matches backend route
- Backend status value is `"completed"` — frontend aligned
- Detection state resets to idle on page mount if no active pulseRunId
- scipy, scipy.stats, openpyxl, plotly, plotly.graph_objects, plotly.express added to allowlist
- Code gen prompt moved from hardcoded to prompts.yaml (pulse_agent.code_gen_prompt)
- Report page design: matches pulse-mockup (sticky header, white paper on muted bg, prose-slate)
- Signal chart_data should be Plotly fig.to_json() output, not raw dicts
- get_agent_config_field() added for accessing arbitrary YAML config fields
</decisions_made>

<blockers>
- Pipeline architecture mismatch: _validate_single_candidate must be refactored to reuse Coding Agent + Visualization Agent per the Phase 49 plan. Owner flagged this — do NOT ship without fixing.
</blockers>

<context>
The Pulse pipeline works end-to-end but with the wrong architecture. The owner reviewed and identified that _validate_single_candidate does its own LLM code generation (using pulse_agent config) instead of reusing the existing Coding Agent, and generates charts inline instead of using the Visualization Agent. The Phase 49 CONTEXT.md explicitly specifies reusing these agents. This refactor is the top priority for next session before any other work.
</context>

<next_action>
Resume with `/gsd:resume-work`. Priority:
1. Refactor `_validate_single_candidate` in `backend/app/agents/pulse.py` to call existing Coding Agent → Code Checker → E2B → Visualization Agent → E2B per candidate
2. Reference `backend/app/agents/graph.py` for how chat pipeline wires these agents
3. Reference `.planning/phases/49-pulse-agent/49-CONTEXT.md` for the planned architecture
4. After refactor works, commit all fixes, remove debug logging, run phase verification
</next_action>
