---
phase: 06-frontend-ui-interactive-data-cards
plan: 08
type: execute
wave: 4
depends_on: ["06-03", "06-04", "06-05", "06-06", "06-07"]
files_modified:
  - frontend/src/app/(dashboard)/dashboard/page.tsx
  - frontend/src/app/(dashboard)/layout.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/components/chat/ChatMessage.tsx
  - frontend/src/components/chat/DataCard.tsx
  - frontend/src/components/file/FileSidebar.tsx
  - frontend/src/components/file/FileUploadZone.tsx
  - frontend/src/app/globals.css
  - frontend/tailwind.config.ts
autonomous: false

must_haves:
  truths:
    - "Loading states use skeleton screens or spinners that feel polished"
    - "Transitions between states are smooth (no jarring jumps)"
    - "Empty states have clear messaging and appropriate icons"
    - "Error states show actionable messages"
    - "File sidebar has smooth hover and active state transitions"
    - "Data Cards have subtle entrance animations"
    - "Chat messages have gentle fade-in on appearance"
    - "Upload progress bar has smooth gradient animation"
    - "Dashboard layout integrates sidebar, tabs, and chat cleanly"
    - "Full user flow works end-to-end: login -> upload -> chat -> Data Card -> settings"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Global animation classes and transition utilities"
      contains: "transition"
    - path: "frontend/tailwind.config.ts"
      provides: "Custom animation keyframes and timing functions"
      contains: "keyframes"
  key_links:
    - from: "frontend/src/app/(dashboard)/dashboard/page.tsx"
      to: "frontend/src/components/chat/ChatInterface.tsx"
      via: "renders ChatInterface for active tab"
      pattern: "ChatInterface.*fileId|currentTab"
    - from: "frontend/src/app/(dashboard)/layout.tsx"
      to: "frontend/src/components/file/FileSidebar.tsx"
      via: "renders sidebar in dashboard layout"
      pattern: "FileSidebar"
---

<objective>
Apply visual polish, animations, loading states, and final integration wiring across all frontend components. Verify end-to-end user flow.

Purpose: This plan transforms functional components into a polished, production-quality interface. The CONTEXT.md specifies "modern & vibrant style" with "subtle & smooth transitions" and "gentle fades, slides, ease-in-out timing." This plan delivers that finish and ensures all plans integrate seamlessly.

Output: Visually polished frontend with smooth animations, loading skeletons, error states, empty states, and a complete end-to-end user flow from login through Data Card interaction.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-CONTEXT.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-03-SUMMARY.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-04-SUMMARY.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-05-SUMMARY.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-06-SUMMARY.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add animations, loading states, and visual polish</name>
  <files>
    frontend/src/app/globals.css
    frontend/tailwind.config.ts
    frontend/src/components/chat/ChatMessage.tsx
    frontend/src/components/chat/DataCard.tsx
    frontend/src/components/chat/ChatInterface.tsx
    frontend/src/components/file/FileSidebar.tsx
    frontend/src/components/file/FileUploadZone.tsx
  </files>
  <action>
    1. Update `frontend/tailwind.config.ts` with additional animations:
       - `fadeIn`: opacity 0 -> 1, 300ms ease-in-out (for chat messages)
       - `slideUp`: translateY(10px) + opacity 0 -> translateY(0) + opacity 1, 400ms ease-out (for Data Cards)
       - `slideRight`: translateX(-10px) + opacity 0 -> translateX(0) + opacity 1, 300ms ease-out (for sidebar items)
       - `pulse-gentle`: opacity 0.5 -> 1 -> 0.5, 2s infinite (for loading states)
       - `shimmer`: background position animation for skeleton loading effect
       - Add corresponding animation utility classes

    2. Update `frontend/src/app/globals.css`:
       - Add gradient utility classes for the vibrant aesthetic:
         - `.gradient-primary`: blue-to-purple gradient (for user message bubbles, buttons)
         - `.gradient-bg`: subtle light gradient for page backgrounds
         - `.gradient-accent`: colorful accent gradient for highlights
       - Add skeleton loading class with shimmer animation
       - Add smooth scrollbar styling (webkit and Firefox)
       - Add focus-visible ring styling for accessibility
       - Ensure all transitions use ease-in-out timing

    3. Update `frontend/src/components/chat/ChatMessage.tsx`:
       - Add `animate-fadeIn` class to message wrapper for gentle entrance
       - User messages: gradient background (blue-to-purple), white text, rounded-2xl
       - Assistant messages: light background (bg-muted), rounded-2xl, subtle left border accent
       - Add smooth transition for streaming text appearance
       - Timestamps: small, muted text with relative time formatting

    4. Update `frontend/src/components/chat/DataCard.tsx`:
       - Add `animate-slideUp` class for entrance animation
       - Card border: subtle gradient border or accent left border
       - Section transitions: each section fades in with slight delay
       - Collapse/expand: smooth height animation (not instant toggle)
       - Loading skeletons for table and explanation sections during streaming:
         - Table skeleton: 3 rows of animated shimmer rectangles
         - Explanation skeleton: 2-3 lines of shimmer text blocks

    5. Update `frontend/src/components/file/FileSidebar.tsx`:
       - Hover state: smooth background transition (200ms ease)
       - Active file: accent color left border or background highlight
       - File items: `animate-slideRight` with staggered delays on initial load
       - Delete/info buttons: opacity 0 by default, opacity 100 on row hover (smooth 200ms transition)
       - Loading state: skeleton shimmer for file list while fetching
       - Empty state: centered icon + "No files yet. Upload your first file!" message

    6. Update `frontend/src/components/file/FileUploadZone.tsx`:
       - Drag-active: scale(1.02) transform + border color change (smooth 200ms)
       - Progress bar: gradient animation that shifts during upload
       - Stage transitions: text fades between stages (opacity transition)
       - Success state: green checkmark with scale-up animation

    7. Update `frontend/src/components/chat/ChatInterface.tsx`:
       - Loading state: skeleton chat messages (3 placeholder bubbles) while messages load
       - Empty state: centered illustration/icon with "Ask a question about your data to get started" text, styled with muted colors and gentle opacity
       - Scroll behavior: smooth scrolling (scroll-behavior: smooth)
       - Status bar during streaming: subtle background with progress dots, slides up from bottom
  </action>
  <verify>
    ```
    cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend
    npm run build
    ```
    Build succeeds. Verify tailwind config includes new animations. Verify globals.css includes gradient utilities.
  </verify>
  <done>
    All components have smooth animations, loading skeletons, and visual polish. Chat messages fade in, Data Cards slide up, sidebar items have hover transitions, upload shows animated progress. Vibrant modern aesthetic with gradients applied consistently.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete frontend UI with visual polish:
    - Auth flow: login, register, forgot/reset password pages with gradient styling
    - File management: sidebar with file list, drag-drop upload with progress stages, info modal, delete confirmation
    - Chat interface: message display, auto-expanding input (Enter/Shift+Enter), SSE streaming with typing indicator
    - Data Cards: progressive rendering (Brief -> Code -> Table -> Explanation), collapsible, sortable/filterable tables
    - Downloads: CSV export from tables, Markdown export of analysis reports
    - Code display: Python code viewer with copy button
    - Settings: profile editing, password change, account info
    - Visual polish: animations, loading skeletons, empty/error states, gradient aesthetic
  </what-built>
  <how-to-verify>
    Prerequisites:
    - Backend running: `cd backend && uvicorn app.main:app --reload` (port 8000)
    - Frontend running: `cd frontend && npm run dev` (port 3000)

    1. **Auth flow:**
       - Navigate to http://localhost:3000 -- should see login page with gradient background
       - Click "Sign up" -- register page renders with email, password, name fields
       - Create account -- redirected to dashboard
       - Log out (user menu) -- returns to login
       - Log back in -- session works

    2. **File management:**
       - Click "Upload File" in sidebar -- drag-drop zone appears in dialog
       - Upload a CSV file -- progress shows Uploading -> Analyzing -> Ready
       - File appears in sidebar after upload
       - Click file -- tab opens on right side
       - Upload 5 more files -- 6th tab shows alert "Maximum 5 tabs"
       - Click info icon -- modal shows AI analysis summary
       - Click delete -- confirmation dialog appears

    3. **Chat interface:**
       - Type a question in chat input -- auto-expands
       - Press Enter -- message sends, typing indicator appears
       - AI response streams in character-by-character
       - Press Shift+Enter -- adds newline (does not send)
       - Errors show inline in chat

    4. **Data Cards:**
       - After AI processes query -- Data Card appears inline
       - Sections appear progressively: Query Brief first, then Table, then Explanation
       - Click column header -- table sorts
       - Type in search -- table filters
       - Click "View code" -- Python code expands
       - Click copy icon -- code copied to clipboard
       - Click "Download CSV" -- downloads table data
       - Click "Download Markdown" -- downloads analysis report

    5. **Settings:**
       - Navigate to Settings from user menu
       - Edit first name, click Save -- success toast
       - Change password with correct current password -- success toast
       - Change password with wrong current password -- error message

    6. **Visual polish:**
       - Check animations are smooth (no jankiness)
       - Loading states show skeletons (not blank screens)
       - Empty states have helpful messages
       - Transitions feel subtle and polished
  </how-to-verify>
  <resume-signal>Type "approved" if the UI looks and works correctly, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes in frontend/
2. All animations render smoothly (no layout shifts, no flashing)
3. Loading skeletons appear during data fetching
4. Empty states shown when no files/messages
5. Error states show actionable messages
6. Gradient aesthetic is consistent across all pages
7. Full user flow works end-to-end
</verification>

<success_criteria>
- Visual polish includes smooth animations, loading states, and transitions (CARD-05)
- All 12 Phase 6 requirements verified through end-to-end user flow
- No jarring visual transitions or blank loading states
- Modern vibrant aesthetic consistent across all pages
- Human verification confirms UI quality meets expectations
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-interactive-data-cards/06-08-SUMMARY.md`
</output>
