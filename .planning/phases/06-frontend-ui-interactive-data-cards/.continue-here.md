---
phase: 06-frontend-ui-interactive-data-cards
status: critical_fixes_applied_needs_testing
last_updated: 2026-02-04T21:00:00Z
---

# Phase 6 UAT Gap Closure - Paused After Critical Fixes

## Current State

**Just completed 3 critical ad-hoc fixes** after UAT round 3 (plan 06-19) identified root causes. Code is committed but **NOT TESTED YET**.

**Location:** Phase 6 gap closure, post-UAT round 3
**Last action:** Committed fixes for dialog width, scroll UX, and **critically** - bypassed broken Next.js proxy causing sidebar auth failures
**Next action:** User needs to refresh browser and test if sidebar populates with files

## Completed Work

### Plans Executed (Wave 1 & 2):
- ✓ 06-17: Markdown rendering + dialog sizing (typography plugin, remark-gfm)
- ✓ 06-18: Button handler resilience + sidebar refresh fallback
- ✓ 06-19: UAT retest round 3 (checkpoint - user tested, found 3 issues)

### Ad-hoc Fixes Applied (Not in formal plans):
1. ✓ **Dialog width CSS specificity fix** (commit 0c95172)
   - Changed `max-w-4xl` to `sm:max-w-4xl` to override DialogContent default `sm:max-w-lg`
   - Root cause: shadcn/ui DialogContent has default className with breakpoint-specific max-width

2. ✓ **Dialog overflow fix** (commit 9b5c76b)
   - Added `overflow-y-auto` back to DialogContent to prevent Chrome viewport overflow
   - Keeps `max-h-[85vh]` to constrain dialog height

3. ✓ **Next.js proxy bypass - CRITICAL AUTH FIX** (commit 045ed33)
   - Changed ALL API calls from `/api/*` to `http://localhost:8000/*` in:
     - `frontend/src/lib/api-client.ts` (5 locations)
     - `frontend/src/hooks/useSSEStream.ts` (1 location)
   - Root cause: Next.js rewrite was doing **307 redirect** instead of proxying
   - Browser dropped Authorization header on redirect (security policy)
   - Backend returned 401 Unauthorized
   - CORS already configured on backend, so direct calls work

4. ✓ **Debug logging** (commit 4a65aa8)
   - Added detailed error logging to useFiles hook to diagnose failures

## Root Cause Analysis (Completed)

**UAT Round 3 Results:**
- ✓ Markdown rendering WORKS (remark-gfm + @tailwindcss/typography confirmed)
- ✓ Button handler WORKS (try/catch/finally resilience confirmed)
- ✗ Dialog width not applying → **Fixed** (CSS specificity)
- ✗ Dialog overflow on Chrome → **Fixed** (added overflow-y-auto)
- ✗ Sidebar empty with 401 errors → **Fixed** (bypassed broken proxy)

**The 307 Redirect Problem:**
```
Frontend: GET /api/files/
   ↓
Next.js: 307 Redirect → http://localhost:8000/files/
   ↓ (Browser drops Authorization header on redirect)
Backend: Receives request without auth → 401

SOLUTION: Direct calls to http://localhost:8000/files/ with CORS
```

## Remaining Work

### Immediate (Next Session):
1. **TEST THE FIXES** - User must:
   - Refresh browser (Ctrl+Shift+R / Cmd+Shift+R)
   - Check if sidebar populates with 13 files (backend confirmed files exist)
   - Test upload flow end-to-end
   - Check if dialog width is now 896px (max-w-4xl)
   - Verify dialog doesn't overflow viewport on Chrome

2. **If tests pass:**
   - Consider UAT round 4 to verify all fixes
   - Update 06-UAT-RETEST.md with results
   - Consider phase verification

3. **If tests still fail:**
   - Debug with browser DevTools (check Network tab, Console)
   - May need additional gap closure plans

## Decisions Made

1. **Bypass Next.js proxy instead of fixing it**
   - Next.js rewrites doing 307 redirect in dev mode
   - Browsers drop auth headers on cross-origin redirects (security)
   - Faster to use direct backend calls with CORS than debug Next.js config
   - CORS already configured on backend for localhost:3000

2. **Add overflow-y-auto back to dialog**
   - User complained about whole dialog scrolling
   - But without it, dialog overflows viewport on Chrome
   - Compromise: Keep dialog scrollable with max-h-[85vh], analysis area also scrollable at max-h-[40vh]

3. **Use sm:max-w-4xl for dialog width**
   - DialogContent has default `sm:max-w-lg` that wins in CSS specificity
   - Need breakpoint prefix to match specificity and override

## Blockers

**NONE currently** - All blockers resolved:
- ✓ CSS specificity blocking dialog width → Fixed with sm: prefix
- ✓ 307 redirect blocking auth → Fixed by bypassing proxy
- ✓ Dialog overflow → Fixed with overflow-y-auto

**Waiting on:**
- User testing to confirm fixes work in browser

## Context & Mental State

**The debugging journey:**
1. User reported sidebar empty after UAT round 3
2. Thought it was stale dev server → user restarted, still broken
3. Checked backend directly with curl → works fine, returns 13 files
4. User provided Network tab screenshot → saw **307 Temporary Redirect**
5. Realized Next.js proxy is redirecting instead of proxying
6. Browser security drops Authorization header on redirects
7. Backend gets request without auth → 401 Unauthorized
8. Solution: Bypass proxy entirely, use direct backend URLs with CORS

**Architecture:**
- Backend: FastAPI on port 8000 with CORS enabled for localhost:3000
- Frontend: Next.js on port 3000
- ~~Proxy: Next.js rewrites /api/* → http://localhost:8000/*~~ (BROKEN, now bypassed)
- Direct calls: All apiClient calls go to http://localhost:8000/* with CORS

**Files modified (all committed):**
- frontend/src/lib/api-client.ts (changed 5 fetch URLs)
- frontend/src/hooks/useSSEStream.ts (changed 1 fetch URL)
- frontend/src/components/file/FileSidebar.tsx (dialog className fixes)
- frontend/src/hooks/useFileManager.ts (debug logging)

## Next Action

**When resuming:**

1. Ask user: "Have you tested the fixes? Please refresh browser and report if sidebar shows files."

2. **If user hasn't tested yet:**
   ```
   The critical fix is applied (bypassed broken Next.js proxy).

   Please:
   1. Refresh browser (Ctrl+Shift+R / Cmd+Shift+R)
   2. Look at the sidebar on the left
   3. Do you see your uploaded files listed?
   4. Try uploading a new file - does it appear in sidebar?

   Share results and I'll proceed accordingly.
   ```

3. **If sidebar works:**
   - Celebrate! The auth issue is fixed.
   - Check other items: dialog width (should be ~896px), no overflow
   - Consider running UAT round 4 to verify all tests
   - Update STATE.md and 06-UAT-RETEST.md
   - Proceed to phase verification if all tests pass

4. **If sidebar still empty:**
   - Ask for browser Console errors
   - Ask for Network tab screenshot of GET request to localhost:8000/files/
   - Check if CORS error or different issue
   - Debug from there

## Testing Checklist (For User)

When testing, verify:
- [ ] Sidebar shows list of files (should show ~13 files)
- [ ] Upload new file → appears in sidebar within seconds
- [ ] Dialog width looks good (~896px, not narrow)
- [ ] Dialog doesn't overflow viewport on Chrome
- [ ] Continue to Chat button works and closes dialog
- [ ] File tab opens after clicking Continue to Chat

## Git State

**Commits:**
- 70bf665: config(gsd): set model profile to quality
- 0c95172: fix(06): dialog width CSS specificity + remove whole-dialog scroll
- 9b5c76b: fix(06): add overflow-y-auto back to dialog to prevent viewport overflow on Chrome
- 4a65aa8: debug(06): add detailed error logging for GET /files/ failures
- 045ed33: fix(06): bypass Next.js proxy, call backend directly via CORS to fix 307 redirect auth header loss

**Branch:** master
**Working tree:** Clean (all fixes committed)

## Important Context for Next Claude

- Backend has 13 files for user ms@marwazi.id (verified with curl)
- Backend auth works (tested login with password123)
- The issue was purely frontend: Next.js proxy breaking auth headers
- CORS is configured on backend: allows localhost:3000, credentials=True
- frontend/src/lib directory was gitignored, had to force add with -f flag
- User credentials: ms@marwazi.id / password123
