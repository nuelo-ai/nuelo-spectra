---
phase: 06-frontend-ui-interactive-data-cards
plan: 16
type: execute
wave: 2
depends_on: ["06-15"]
files_modified:
  - .planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md
autonomous: false

must_haves:
  truths:
    - "Upload dialog shows analysis text after onboarding completes"
    - "Continue to Chat button appears and works (closes dialog, opens tab, populates sidebar)"
    - "Sidebar file list shows uploaded files"
    - "AI chat streaming works without error (if unblocked by sidebar fix)"
    - "All previously-passed UAT tests still pass"
  artifacts:
    - path: ".planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md"
      provides: "Updated UAT retest with definitive results for all tests"
      contains: "status:"
  key_links:
    - from: "06-15-SUMMARY.md"
      to: "UAT-RETEST Tests 2-7"
      via: "Hardened FileUploadZone with local state and timeout"
      pattern: "result: pass"
---

<objective>
Verify that plan 06-15 fixes resolve the remaining UAT failures and re-test all blocked tests.

Purpose: Two prior rounds of fixes (06-11, 06-14) failed to resolve the upload flow issues in actual user testing despite code-level verification passing. Plan 06-15 took a defensive approach with local state storage, error timeouts, and debugging instrumentation. This plan verifies those fixes work in practice and records definitive test results.

Output: Updated 06-UAT-RETEST.md with pass/fail results for all tests.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

## Fix applied in prior plan
@.planning/phases/06-frontend-ui-interactive-data-cards/06-15-SUMMARY.md

## Current test state
@.planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks -- verify services running and fixes deployed</name>
  <files></files>
  <action>
Before asking user to test, verify everything is ready:

1. Check backend is running on port 8000:
   ```bash
   curl -s http://localhost:8000/health | head -20
   ```
   If not running, start it:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && .venv/bin/uvicorn app.main:app --reload --port 8000 &
   ```

2. Check frontend is running on port 3000:
   ```bash
   curl -s http://localhost:3000 | head -5
   ```
   If not running, start it:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npm run dev &
   ```

3. Verify 06-15 fixes are deployed by checking code:
   a. FileUploadZone.tsx has `analysisText` useState (local state for analysis text)
   b. FileUploadZone.tsx has onboarding timeout (60 second timer)
   c. FileUploadZone.tsx has console.log statements with "[FileUploadZone]" prefix
   d. FileSummaryResponse in files.py has `onboarding_status` field
   e. useFileManager.ts stops polling on `onboarding_status === "failed"`
   f. frontend/src/types/file.ts has `onboarding_status` in FileSummaryResponse

4. Quick smoke test -- verify graph initialization:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && .venv/bin/python -c "from app.agents.graph import get_or_create_graph; g = get_or_create_graph(); print('Graph OK:', type(g))"
   ```

5. Quick smoke test -- verify summary endpoint returns onboarding_status:
   ```bash
   # This will 401 without auth but should NOT 500
   curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/files/00000000-0000-0000-0000-000000000000/summary
   ```
   Expected: 401 or 404 (not 500)

If any check fails, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
All five checks pass: backend running, frontend running, 06-15 code fixes confirmed in source, graph initializes, summary endpoint doesn't 500.
  </verify>
  <done>Infrastructure verified. Plan 06-15 fixes confirmed deployed. Services running and responsive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 06-15 hardened the upload flow with three defensive measures:
1. Analysis text stored in local React state (survives React Query data reference changes)
2. 60-second onboarding timeout with error message (prevents infinite wait)
3. Console logging at all state transitions (enables runtime debugging)
4. Backend now returns onboarding_status (pending/complete/failed) in summary endpoint

These address the persistent failure where upload reaches "Ready" but analysis text and Continue to Chat button never appear.
  </what-built>
  <how-to-verify>
Please test the following. Open browser developer console (F12 > Console tab) to see [FileUploadZone] debug logs during upload.

**CRITICAL TESTS (previously failing):**

Test A: File Upload + Analysis Display
  1. Navigate to /dashboard
  2. Click "Upload File" button in sidebar
  3. Drag a CSV or Excel file onto the dropzone (or click browse to select)
  4. Watch the progress bar: Uploading (0-50%) then Analyzing (50-80%) then Ready (100%)
  5. CRITICAL CHECK: After "Ready!", do you see the AI analysis text in a scrollable box?
  6. CRITICAL CHECK: Do you see the "Continue to Chat" button below the analysis?
  7. Report what you see. Also paste any [FileUploadZone] console log lines.
  Expected: Analysis text appears in gray box, Continue to Chat button below it.

Test B: Sidebar Population + Tab Opening
  1. From Test A, click "Continue to Chat"
  2. CRITICAL CHECK: Does the dialog close?
  3. CRITICAL CHECK: Does the file appear in the sidebar file list?
  4. CRITICAL CHECK: Does a new tab open with the file name?
  Expected: Dialog closes, file in sidebar, tab opens with ChatInterface.

Test C: Sidebar File Persistence
  1. Refresh the page (F5)
  2. CRITICAL CHECK: Does the sidebar still show the uploaded file?
  Expected: File persists in sidebar after refresh.

Test D: AI Chat Response
  1. With a file tab open, type "What columns are in this data?" and press Enter
  2. CRITICAL CHECK: Do you see a typing indicator then streaming AI response?
  3. Or do you see "Something went wrong. Please try again."?
  Expected: AI response streams in. If error occurs, paste the browser console output.

**PREVIOUSLY PASSED TESTS (quick re-confirm):**

Test E: Close Tab -- click X on tab, tab closes
Test F: Settings -- navigate to Settings, change name, confirm nav updates immediately
Test G: Password Change -- change password with correct current password
Test H: Logout -- click Logout, redirected to /login

Report each test as: "Test X: pass" or "Test X: issue - [description]"
If any test fails, include any console output or error messages you see.
  </how-to-verify>
  <resume-signal>Report results for Tests A through H. Include any [FileUploadZone] console log output for Tests A and B.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update UAT-RETEST with results and assess remaining gaps</name>
  <files>.planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md</files>
  <action>
Based on the user's test results from the checkpoint:

1. Read the current 06-UAT-RETEST.md
2. Update the frontmatter:
   - If all tests pass: set `status: complete`
   - If issues remain: set `status: diagnosed` and add new gap entries
   - Update `updated` timestamp
   - Add `06-15-SUMMARY.md` to source list

3. For each test, update with new results from user feedback

4. Update Summary section with new totals

5. If ALL critical tests (A-D) pass:
   - Add note: "Upload flow fully resolved after plan 06-15 defensive hardening"
   - Document which specific fix resolved the issue (local state, timeout, or other)

6. If any critical test still fails:
   - Add new Gap entry with detailed root cause analysis using the console log output
   - The [FileUploadZone] console logs should reveal exactly where the state machine breaks

7. Write the updated 06-UAT-RETEST.md

Important: Use the console log output from the user to inform the gap analysis. The logs should show the exact sequence of state transitions and where it diverges from expected.
  </action>
  <verify>
Read updated 06-UAT-RETEST.md and confirm status reflects actual test outcomes, all tests have results, and summary totals are accurate.
  </verify>
  <done>06-UAT-RETEST.md updated with definitive results. If issues remain, gap entries include console log analysis for targeted next fix.</done>
</task>

</tasks>

<verification>
- Backend and frontend running and accessible
- Plan 06-15 fixes confirmed in source code
- User has tested all critical upload flow scenarios
- UAT-RETEST.md updated with definitive results
- Console logs provide debugging data if issues persist
</verification>

<success_criteria>
- Tests A and B pass: upload dialog shows analysis + Continue to Chat works
- Test C passes: sidebar shows files after page refresh
- Test D passes (or has specific error info for next fix): AI chat responds
- Tests E-H pass: no regressions in previously-working features
- UAT-RETEST.md has definitive status for all tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-interactive-data-cards/06-16-SUMMARY.md`
</output>
