---
phase: 06-frontend-ui-interactive-data-cards
plan: 19
type: execute
wave: 2
depends_on: ["06-17", "06-18"]
files_modified:
  - .planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md
autonomous: false

must_haves:
  truths:
    - "Upload dialog displays analysis as formatted markdown with proper table rendering"
    - "Upload dialog is a well-proportioned centered rectangle that does not overflow the viewport"
    - "Continue to Chat button works: closes dialog, populates sidebar, opens file tab"
    - "Sidebar file list populates after clicking Continue to Chat"
    - "All previously-passed tests (password reset, profile update) still pass"
    - "Skipped tests (5-17, 19-20) can be reached and tested because upload/sidebar flow works"
  artifacts:
    - path: ".planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md"
      provides: "Definitive UAT results after plans 06-17 and 06-18 gap closure"
      contains: "status:"
  key_links:
    - from: "06-17-SUMMARY.md"
      to: "UAT Test 2 (analysis display)"
      via: "@tailwindcss/typography + remark-gfm + dialog sizing"
      pattern: "result: pass"
    - from: "06-18-SUMMARY.md"
      to: "UAT Tests 3-4 (sidebar + button)"
      via: "try/catch/finally + refetchInterval"
      pattern: "result: pass"
---

<objective>
Re-verify all 20 UAT tests after plans 06-17 (markdown + dialog sizing) and 06-18 (button handler + sidebar resilience) and record definitive results.

Purpose: Plans 06-17 and 06-18 fix the four diagnosed root causes from the UAT retest: (1) @tailwindcss/typography + remark-gfm for markdown rendering, (2) dialog max-w-3xl + max-h-[85vh] for proper sizing, (3) try/catch/finally on button handler for fault tolerance, (4) refetchInterval + isError for sidebar resilience. This plan verifies those fixes work in actual user testing.

Output: Updated 06-UAT-RETEST.md with pass/fail results for all 20 tests.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

## Fixes applied in prior plans
@.planning/phases/06-frontend-ui-interactive-data-cards/06-17-SUMMARY.md
@.planning/phases/06-frontend-ui-interactive-data-cards/06-18-SUMMARY.md

## Current test state
@.planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks -- verify services running and fixes deployed</name>
  <files></files>
  <action>
Before asking the user to re-test, verify everything is ready:

1. Check backend is running on port 8000:
   ```bash
   curl -s http://localhost:8000/health | head -20
   ```
   If not running, start it:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && .venv/bin/uvicorn app.main:app --reload --port 8000 &
   ```

2. Check frontend is running on port 3000:
   ```bash
   curl -s http://localhost:3000 | head -5
   ```
   If not running, start it:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npm run dev &
   ```

3. Verify ALL six critical fixes from 06-17 and 06-18 are in place:
   a. @tailwindcss/typography installed: `grep "@tailwindcss/typography" frontend/package.json`
   b. remark-gfm installed: `grep "remark-gfm" frontend/package.json`
   c. Typography import in CSS: `grep "@tailwindcss/typography" frontend/src/app/globals.css`
   d. remarkGfm used in FileUploadZone: `grep "remarkGfm" frontend/src/components/file/FileUploadZone.tsx`
   e. Dialog sizing in FileSidebar: `grep "max-w-3xl" frontend/src/components/file/FileSidebar.tsx`
   f. finally block in FileUploadZone: `grep "finally" frontend/src/components/file/FileUploadZone.tsx`
   g. refetchInterval in useFiles: `grep "refetchInterval" frontend/src/hooks/useFileManager.ts`
   h. isError in FileSidebar: `grep "isError" frontend/src/components/file/FileSidebar.tsx`

4. Quick build smoke test:
   ```bash
   cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx tsc --noEmit
   ```

If any check fails, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
All eight code checks pass. TypeScript compiles. Backend and frontend are running.
  </verify>
  <done>Infrastructure verified. Plans 06-17 and 06-18 fixes confirmed deployed. Services running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plans 06-17 and 06-18 made six fixes to resolve the upload flow failures:

06-17 (Markdown + Dialog Sizing):
1. Installed @tailwindcss/typography -- prose classes now style markdown HTML elements (headings, tables, lists, bold)
2. Installed remark-gfm -- markdown table syntax now parses into HTML tables
3. Added @import "@tailwindcss/typography" to globals.css for Tailwind v4
4. Added remarkPlugins={[remarkGfm]} to ReactMarkdown in both FileUploadZone and FileInfoModal
5. Widened upload dialog from max-w-lg (512px) to max-w-3xl (768px) -- well-proportioned rectangle
6. Added max-h-[85vh] overflow-y-auto to upload dialog -- button always accessible
7. Reduced analysis area from max-h-[60vh] to max-h-[40vh] -- fits within constrained dialog

06-18 (Button Handler + Sidebar Resilience):
1. Rewrote button handler with try/catch/finally -- dialog ALWAYS closes via finally block
2. Made context POST non-blocking -- failure shows toast warning, does NOT block dialog close/tab open
3. Added exact: true to invalidateQueries/refetchQueries -- no over-refetching
4. Added refetchInterval: 10s to useFiles -- sidebar refreshes independently as fallback
5. Added isError state to FileSidebar -- shows "Failed to load files" instead of misleading "No files yet"
  </what-built>
  <how-to-verify>
Please test these scenarios. Report each as "Test X: pass" or "Test X: issue - [description]".

**CRITICAL TESTS (previously failing -- verify fixes):**

Test 2: File Upload Analysis Display
  1. Navigate to /dashboard
  2. Click "Upload File" in sidebar
  3. Upload a CSV or Excel file (ideally one with tabular data)
  4. Wait for progress: Uploading -> Analyzing -> Ready
  5. CHECK: Is the analysis text rendered as formatted markdown? Look for:
     - Headings appear larger/bold (not raw # characters)
     - Tables render with borders and cells (not pipe | characters)
     - Bold text appears bold (not raw ** characters)
     - Lists have bullet points or numbers (not raw - or 1. characters)
  6. CHECK: Is the dialog a nice centered rectangle (not tall/narrow, not overlapping screen edges)?
  7. CHECK: Is the "Add context about your data" textarea visible below the analysis?
  8. CHECK: Is the "Continue to Chat" button visible WITHOUT scrolling?
  Expected: Formatted markdown with styled tables, well-proportioned dialog, textarea and button all visible.

Test 3: Sidebar File List Population
  1. From Test 2, click "Continue to Chat"
  2. CHECK: Does the dialog close promptly (not hanging)?
  3. CHECK: Does the uploaded file appear in the sidebar file list?
  4. CHECK: Does a new tab open with the file name?
  Expected: Dialog closes, file appears in sidebar, tab opens.

Test 4: Continue to Chat Works
  1. Upload another file
  2. In the "Add context" textarea, type some text (e.g., "test context")
  3. Click "Continue to Chat"
  4. CHECK: Dialog closes (even with context text entered)?
  5. CHECK: File appears in sidebar? Tab opens?
  Expected: Everything works even when context text is provided.

**FILE FEATURE TESTS:**

Test 5: File Info Modal
  1. Click the info icon (i) next to a file in sidebar
  2. CHECK: Modal shows AI analysis as formatted markdown (with tables if present)?
  3. CHECK: "Refine AI Understanding" feedback form visible?
  Expected: Large modal with markdown analysis and feedback form.

Test 6: Delete File
  1. Click trash icon next to a file in sidebar
  2. Click Delete in confirmation dialog
  3. CHECK: File removed from sidebar?
  Expected: Clean delete with UI update.

**DOWNSTREAM TESTS (if Tests 2-4 pass):**

Test 7: Max 5 Tabs -- open multiple files, try 6th
Test 8: AI Chat Streaming -- type question in file tab, get AI response
Test 9-15: Data Card features (code, table, explanation, downloads, collapse, history)

**PREVIOUSLY PASSED (quick re-confirm):**
Test 1: Password Reset -- /forgot-password, submit email, check console
Test 18: Profile Update -- /settings, change name, Save, check nav

Report each test result. If any test fails, describe exactly what you see and include a screenshot if possible.
  </how-to-verify>
  <resume-signal>Report results for each test. Type "all pass" if everything works, or describe specific issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update UAT-RETEST with results</name>
  <files>.planning/phases/06-frontend-ui-interactive-data-cards/06-UAT-RETEST.md</files>
  <action>
Based on the user's test results from the checkpoint:

1. Read the current 06-UAT-RETEST.md
2. Update the frontmatter:
   - If all critical tests pass: set `status: complete`
   - If issues remain: set `status: diagnosed` and add new gap entries
   - Update `updated` timestamp
   - Add `06-17` and `06-18` to source list

3. For each test in the UAT-RETEST, update with new results from user feedback:
   - Tests 2, 3, 4: update with new result (these were the blockers)
   - Previously-skipped tests (5-17, 19-20): update with result if tested
   - Previously-passed tests (1, 18): keep as pass unless regression reported

4. Update Summary section with new totals

5. If ALL critical tests pass:
   - Update gaps to reflect closure
   - Record which plan fixed each issue:
     - "Markdown rendering: fixed by 06-17 (@tailwindcss/typography + remark-gfm)"
     - "Dialog sizing: fixed by 06-17 (max-w-3xl + max-h-[85vh])"
     - "Button handler: fixed by 06-18 (try/catch/finally)"
     - "Sidebar refresh: fixed by 06-18 (refetchInterval + isError)"

6. If any test still fails:
   - Add new Gap entry with detailed root cause analysis
   - Set status to diagnosed

7. Write the updated 06-UAT-RETEST.md
  </action>
  <verify>
Read updated 06-UAT-RETEST.md and confirm: status reflects actual outcomes, all tested items have results, summary totals are accurate, gap entries reflect closure or new issues.
  </verify>
  <done>06-UAT-RETEST.md updated with definitive results reflecting actual user testing outcomes after plans 06-17 and 06-18.</done>
</task>

</tasks>

<verification>
- Backend and frontend running and accessible
- All six fixes from 06-17 and 06-18 confirmed in source code
- User has tested critical upload flow scenarios (Tests 2, 3, 4)
- User has tested downstream scenarios where possible (Tests 5-17, 19-20)
- Previously-passed tests re-confirmed (Tests 1, 18)
- UAT-RETEST.md updated with definitive results
</verification>

<success_criteria>
- Test 2 passes: analysis displays as formatted markdown with styled tables in well-proportioned dialog
- Test 3 passes: sidebar populates after Continue to Chat
- Test 4 passes: button works even with context text entered
- Tests 1, 18 pass: no regressions
- UAT-RETEST.md has definitive status for all tested items
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-interactive-data-cards/06-19-SUMMARY.md`
</output>
