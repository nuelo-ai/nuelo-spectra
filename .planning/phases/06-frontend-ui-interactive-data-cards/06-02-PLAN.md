---
phase: 06-frontend-ui-interactive-data-cards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/auth.py
  - backend/app/schemas/auth.py
  - backend/app/services/auth.py
autonomous: true

must_haves:
  truths:
    - "User can update first_name and last_name via PATCH /auth/me"
    - "User can change password via POST /auth/change-password with current password verification"
    - "Invalid current password returns 401"
    - "Updated profile fields persist to database"
  artifacts:
    - path: "backend/app/routers/auth.py"
      provides: "PATCH /auth/me and POST /auth/change-password endpoints"
      contains: "change-password"
    - path: "backend/app/schemas/auth.py"
      provides: "ProfileUpdateRequest and ChangePasswordRequest schemas"
      contains: "ProfileUpdateRequest"
    - path: "backend/app/services/auth.py"
      provides: "update_user_profile and change_password service functions"
      contains: "update_user_profile"
  key_links:
    - from: "backend/app/routers/auth.py"
      to: "backend/app/services/auth.py"
      via: "service function calls for profile update and password change"
      pattern: "auth_service\\.update_user_profile|auth_service\\.change_password"
---

<objective>
Add backend API endpoints for user profile editing and password change to support Phase 6 settings page requirements (SETT-01, SETT-03).

Purpose: The backend currently has no endpoints for updating user profile information or changing password. The frontend settings page (Plan 07) requires these endpoints to exist. Adding them now in Wave 1 allows parallel execution with frontend scaffolding.

Output: Two new backend endpoints: PATCH /auth/me (update profile) and POST /auth/change-password (change password with current password verification).
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/routers/auth.py
@backend/app/schemas/auth.py
@backend/app/schemas/user.py
@backend/app/services/auth.py
@backend/app/models/user.py
@backend/app/dependencies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add profile update and password change schemas</name>
  <files>
    backend/app/schemas/auth.py
  </files>
  <action>
    Add two new Pydantic schemas to `backend/app/schemas/auth.py`:

    1. `ProfileUpdateRequest`:
       - `first_name: str | None = None` -- optional, max length 100
       - `last_name: str | None = None` -- optional, max length 100
       - Use `Field(None, max_length=100)` for both fields
       - At least one field must be provided (add model validator)

    2. `ChangePasswordRequest`:
       - `current_password: str` -- required, user's current password for verification
       - `new_password: str = Field(..., min_length=8)` -- required, min 8 chars (matches signup validation)

    Do NOT modify existing schemas. Append these to the end of the file.
  </action>
  <verify>
    ```
    cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend
    python -c "from app.schemas.auth import ProfileUpdateRequest, ChangePasswordRequest; print('Schemas imported successfully')"
    ```
  </verify>
  <done>
    ProfileUpdateRequest and ChangePasswordRequest schemas exist and import without errors. ProfileUpdateRequest has first_name and last_name fields. ChangePasswordRequest has current_password and new_password fields with min_length=8.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add profile update and password change endpoints</name>
  <files>
    backend/app/routers/auth.py
    backend/app/services/auth.py
  </files>
  <action>
    1. Add service function `update_user_profile` to `backend/app/services/auth.py`:
       - Parameters: `db: AsyncSession`, `user_id: UUID`, `first_name: str | None`, `last_name: str | None`
       - Query user by id, update only provided (non-None) fields
       - Commit and refresh
       - Return updated user
       - Raise ValueError if user not found

    2. Add service function `change_password` to `backend/app/services/auth.py`:
       - Parameters: `db: AsyncSession`, `user_id: UUID`, `current_password: str`, `new_password: str`
       - Query user by id
       - Verify current_password against stored hash using `verify_password()` from utils.security
       - If verification fails: return False (caller raises 401)
       - Hash new_password and update user.hashed_password
       - Commit
       - Return True

    3. Add PATCH /auth/me endpoint to `backend/app/routers/auth.py`:
       - Requires CurrentUser dependency (authenticated)
       - Accepts ProfileUpdateRequest body
       - Calls auth_service.update_user_profile
       - Returns UserResponse with updated fields
       - Import ProfileUpdateRequest from schemas

    4. Add POST /auth/change-password endpoint to `backend/app/routers/auth.py`:
       - Requires CurrentUser dependency (authenticated)
       - Accepts ChangePasswordRequest body
       - Calls auth_service.change_password
       - If returns False: raise HTTPException 401 "Current password is incorrect"
       - If returns True: return MessageResponse "Password changed successfully"
       - Import ChangePasswordRequest from schemas

    Follow existing patterns in the codebase:
    - Use `Annotated[AsyncSession, Depends(get_db)]` for db sessions
    - Use `CurrentUser` type alias for authenticated user
    - Return Pydantic response models
  </action>
  <verify>
    1. Start backend server:
       ```
       cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend
       python -c "from app.routers.auth import router; print(f'Routes: {[r.path for r in router.routes]}')"
       ```
       Verify `/auth/me` appears twice (GET and PATCH) and `/auth/change-password` appears.

    2. Verify imports:
       ```
       python -c "from app.services.auth import update_user_profile, change_password; print('Service functions imported')"
       ```
  </verify>
  <done>
    PATCH /auth/me endpoint accepts {first_name, last_name} and returns updated UserResponse. POST /auth/change-password accepts {current_password, new_password}, verifies current password, and returns success/failure. Both endpoints require authentication.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from app.schemas.auth import ProfileUpdateRequest, ChangePasswordRequest"` succeeds
2. `python -c "from app.services.auth import update_user_profile, change_password"` succeeds
3. PATCH /auth/me route registered in router
4. POST /auth/change-password route registered in router
5. Both endpoints require CurrentUser dependency (authentication)
</verification>

<success_criteria>
- ProfileUpdateRequest schema validates first_name/last_name with max_length=100
- ChangePasswordRequest schema validates new_password min_length=8
- PATCH /auth/me updates user profile fields and returns updated UserResponse
- POST /auth/change-password verifies current password before allowing change
- Wrong current password returns 401
- All existing auth endpoints remain unchanged and functional
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui-interactive-data-cards/06-02-SUMMARY.md`
</output>
