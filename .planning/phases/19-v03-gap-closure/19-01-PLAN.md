---
phase: 19-v03-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/sidebar/ChatListItem.tsx
  - frontend/src/components/sidebar/ChatSidebar.tsx
  - frontend/src/components/sidebar/ChatList.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Double-clicking a session title in the sidebar enters inline rename mode"
    - "Sidebar shows Spectra product logo/name in the header"
    - "Sidebar empty state text is hidden when sidebar is collapsed to icon mode"
  artifacts:
    - path: "frontend/src/components/sidebar/ChatListItem.tsx"
      provides: "Double-click rename on session title"
      contains: "onDoubleClick"
    - path: "frontend/src/components/sidebar/ChatSidebar.tsx"
      provides: "Product logo in sidebar header"
      contains: "Spectra"
    - path: "frontend/src/components/sidebar/ChatList.tsx"
      provides: "Collapsed-aware empty state"
      contains: "group-data-[collapsible=icon]:hidden"
  key_links:
    - from: "ChatListItem.tsx"
      to: "handleStartRename"
      via: "onDoubleClick handler on SidebarMenuButton"
      pattern: "onDoubleClick.*handleStartRename"
---

<objective>
Fix sidebar UX bugs found during v0.3 UAT: double-click to rename sessions (GAP 2) and sidebar cosmetic issues (GAP 9).

Purpose: Close 2 of 9 UAT gaps related to sidebar usability and appearance.
Output: Patched sidebar components with double-click rename and cosmetic fixes.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/sidebar-dblclick-rename.md
@.planning/debug/sidebar-cosmetic-issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add double-click rename to ChatListItem</name>
  <files>frontend/src/components/sidebar/ChatListItem.tsx</files>
  <action>
Add onDoubleClick handler to SidebarMenuButton (line ~142) that triggers handleStartRename.

Specifically:
1. Add `onDoubleClick={handleStartRename}` prop to the SidebarMenuButton element.
2. To prevent single-click navigation from firing on double-click, add a click-delay guard:
   - Use a `clickTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)` to track pending single clicks.
   - In handleClick, instead of navigating immediately, set a 250ms timeout. Store the timer in clickTimerRef.
   - In handleStartRename (when triggered via double-click), clear the clickTimerRef timeout first to cancel the pending navigation.
3. This ensures: single-click navigates (after 250ms), double-click enters rename mode (cancels navigation).

Do NOT change the dropdown menu Rename item -- it must continue to work as before.
  </action>
  <verify>
Run `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx next build 2>&1 | tail -20` to verify no TypeScript errors.
  </verify>
  <done>Double-clicking a session title in the expanded sidebar enters inline rename mode. Single-click still navigates to the session. Dropdown menu rename still works.</done>
</task>

<task type="auto">
  <name>Task 2: Fix sidebar cosmetics — logo and collapsed empty state</name>
  <files>frontend/src/components/sidebar/ChatSidebar.tsx, frontend/src/components/sidebar/ChatList.tsx</files>
  <action>
**ChatSidebar.tsx — Add Spectra product name to SidebarHeader:**
1. Before the SidebarMenu in SidebarHeader, add a product identity element:
   ```tsx
   <div className="flex items-center gap-2 px-2 py-1.5 group-data-[collapsible=icon]:hidden">
     <div className="h-7 w-7 rounded-lg gradient-primary flex items-center justify-center">
       <span className="text-sm font-bold text-white">S</span>
     </div>
     <span className="font-semibold text-sm tracking-tight">Spectra</span>
   </div>
   ```
   The `group-data-[collapsible=icon]:hidden` class hides it when sidebar is collapsed. The gradient-primary class uses the existing gradient from globals.css. When collapsed, only the New Chat and My Files icon buttons remain visible (as designed).

**ChatList.tsx — Hide empty state text when collapsed:**
1. On the empty state container div (line ~53, the one with `className="flex flex-col items-center gap-2 px-2 py-8 text-center"`), add `group-data-[collapsible=icon]:hidden` to the className.
   This hides the "No conversations yet" text and icon when the sidebar is collapsed to icon mode, preventing squeezed/overlapping text.
  </action>
  <verify>
Run `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npx next build 2>&1 | tail -20` to verify no TypeScript errors.
  </verify>
  <done>Sidebar header shows "S" logo + "Spectra" text when expanded, hidden when collapsed. Empty state message in ChatList is hidden when sidebar is in icon mode.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx next build` completes without errors
2. ChatListItem.tsx contains onDoubleClick handler wired to handleStartRename
3. ChatSidebar.tsx contains "Spectra" product name in SidebarHeader
4. ChatList.tsx empty state has group-data-[collapsible=icon]:hidden class
</verification>

<success_criteria>
- GAP 2 closed: Double-click on session title in sidebar triggers inline rename
- GAP 9 closed: Sidebar shows Spectra branding and empty state hides when collapsed
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-v03-gap-closure/19-01-SUMMARY.md`
</output>
