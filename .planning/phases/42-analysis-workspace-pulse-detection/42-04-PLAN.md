---
phase: 42-analysis-workspace-pulse-detection
plan: 04
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - pulse-mockup/src/app/workspace/collections/[id]/signals/page.tsx
  - pulse-mockup/src/components/workspace/signal-list-panel.tsx
  - pulse-mockup/src/components/workspace/signal-detail-panel.tsx
  - pulse-mockup/src/components/workspace/signal-card.tsx
  - pulse-mockup/src/components/workspace/signal-chart.tsx
autonomous: false
requirements:
  - PULSE-06
  - PULSE-07

must_haves:
  truths:
    - "Reviewer can see a signal list panel on the left with scrollable signal cards showing title, severity badge, and category tag"
    - "Reviewer can see the highest severity signal auto-selected on page load"
    - "Reviewer can click a signal card to see its full detail in the main panel"
    - "Reviewer can see a signal detail view with title, description, severity badge, category tag, chart visualization, and statistical evidence"
    - "Reviewer can see a rendered Recharts chart with realistic mock data in the signal detail"
  artifacts:
    - path: "pulse-mockup/src/app/workspace/collections/[id]/signals/page.tsx"
      provides: "Signal results page with 3-column layout"
      min_lines: 40
    - path: "pulse-mockup/src/components/workspace/signal-list-panel.tsx"
      provides: "Scrollable signal list panel (~280px)"
      min_lines: 40
    - path: "pulse-mockup/src/components/workspace/signal-detail-panel.tsx"
      provides: "Signal detail with chart, description, evidence"
      min_lines: 60
    - path: "pulse-mockup/src/components/workspace/signal-chart.tsx"
      provides: "Recharts chart rendering for signal visualization"
      min_lines: 40
  key_links:
    - from: "pulse-mockup/src/app/workspace/collections/[id]/signals/page.tsx"
      to: "pulse-mockup/src/lib/mock-data.ts"
      via: "Imports mock signals array"
      pattern: "import.*mock-data"
    - from: "pulse-mockup/src/components/workspace/signal-chart.tsx"
      to: "recharts"
      via: "Renders LineChart, BarChart, or ScatterChart based on signal chartType"
      pattern: "from.*recharts"
---

<objective>
Build the Signal results page with the 3-column layout -- signal list panel + signal detail panel with chart visualizations.

Purpose: The payoff screen of the Pulse Detection flow -- reviewer sees detected signals with severity color-coding, interactive selection, and realistic chart visualizations showing statistical evidence.
Output: Fully styled /workspace/collections/[id]/signals page with signal list, detail panel, and Recharts visualizations.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-analysis-workspace-pulse-detection/42-CONTEXT.md
@.planning/phases/42-analysis-workspace-pulse-detection/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Signal list panel and signal card components</name>
  <files>
    pulse-mockup/src/app/workspace/collections/[id]/signals/page.tsx
    pulse-mockup/src/components/workspace/signal-list-panel.tsx
    pulse-mockup/src/components/workspace/signal-card.tsx
  </files>
  <action>
    1. Create src/components/workspace/signal-card.tsx:
       - Compact card for the signal list panel (NOT the full detail view)
       - Shows: signal title (1-2 lines, truncated), severity badge (color-coded: red=Critical, amber=Warning, green=Info/Opportunity), category tag (small muted text or chip)
       - Active/selected state: highlighted border or background accent
       - Clickable -- calls onSelect callback with signal ID
       - No description snippet per CONTEXT.md (compact cards)
       - Hover state: subtle background change

    2. Create src/components/workspace/signal-list-panel.tsx:
       - Fixed width panel (~280px) with shadcn ScrollArea for vertical scrolling
       - Header: "Signals" title + count badge (e.g., "8 signals")
       - Optional filter/sort controls (severity dropdown or tabs for All/Critical/Warning/Info -- Claude's discretion, keep simple)
       - Renders list of SignalCard components
       - Props: signals array, selectedSignalId, onSelectSignal callback
       - Sorted by severity by default (critical first, then warning, then info)

    3. Create src/app/workspace/collections/[id]/signals/page.tsx:
       - 3-column layout per CONTEXT.md: main sidebar (~240px, already from app shell) + signal list panel (~280px, fixed) + signal detail (flex-1, remaining width)
       - The page itself arranges signal list panel + signal detail side by side (the app shell sidebar is handled by the workspace layout)
       - State: selectedSignalId -- initialized to the highest severity signal on mount
       - Imports mock signals from mock-data.ts (filtered by collection ID if mock data supports it, or just use all signals)
       - Page header: collection name + "Detection Results" breadcrumb area
       - Renders SignalListPanel on left, SignalDetailPanel on right
       - "Back to Collection" link at top
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Signal results page renders 2-panel layout (signal list + detail). Signal cards show title, severity badge (color-coded), and category tag. Highest severity signal is auto-selected on page load. Clicking a card updates the selected signal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Signal detail panel with chart visualizations and evidence</name>
  <files>
    pulse-mockup/src/components/workspace/signal-detail-panel.tsx
    pulse-mockup/src/components/workspace/signal-chart.tsx
  </files>
  <action>
    1. Create src/components/workspace/signal-chart.tsx:
       - Renders a Recharts chart based on the signal's chartType field:
         - "line": LineChart with area fill (for time series anomalies, trend signals)
         - "bar": BarChart (for category comparisons, distribution signals)
         - "scatter": ScatterChart (for correlation signals)
       - Uses the signal's chartData array from mock-data
       - Recharts components: ResponsiveContainer (width="100%" height={300}), XAxis, YAxis, Tooltip, CartesianGrid (subtle gridlines)
       - Color scheme: primary data in accent blue, anomaly/highlight areas in severity color (red for critical signals, amber for warning)
       - Dark theme compatible: axis labels in muted text color, grid in subtle border color, tooltip with dark background
       - Realistic chart appearance -- not placeholder or trivial data

    2. Create src/components/workspace/signal-detail-panel.tsx:
       - Takes the full Signal object as prop
       - Layout (scrollable, flex-1 width):
         - Header section: signal title (large), severity badge (color-coded with text: "Critical", "Warning", "Informational"), category tag
         - Chart section: SignalChart component rendered in a Card container with subtle border
         - Description section: signal description paragraph (2-4 sentences of realistic analytical text from mock data)
         - Statistical Evidence section: Card with key metrics displayed as a small grid/list:
           - "Confidence": percentage (e.g., "94.2%")
           - "P-value": scientific notation (e.g., "< 0.001")
           - "Deviation": percentage or value (e.g., "+23.4% from baseline")
           - "Affected Period": date range (e.g., "Jul 2024 - Sep 2024")
           - These are all hardcoded in the mock signal data
         - Actions section (bottom): "Investigate" button (muted/outline, this is a Phase 44 feature -- just show the button as a teaser, disabled with tooltip "Coming soon" or simply styled but non-functional)

    3. Wire into signals/page.tsx:
       - SignalDetailPanel receives the signal object matching selectedSignalId
       - When selectedSignalId changes, detail panel updates
       - If no signal selected (edge case): show a centered placeholder "Select a signal to view details"
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Signal detail panel shows title, severity badge, category tag, rendered Recharts chart with realistic mock data, description text, and statistical evidence metrics. Chart type varies by signal (line/bar/scatter). Switching selected signal updates the entire detail panel including chart.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual review of complete Phase 42 mockup flow</name>
  <action>
    Human verifies the complete Analysis Workspace and Pulse Detection mockup flow end-to-end.
    What was built across all 4 plans: app shell with dark Hex.tech-inspired theme, Collection list with cards, Create Collection dialog, Collection detail with file table and sticky action bar, Run Detection loading animation, and Signal results with chart visualizations.
  </action>
  <verify>
    Human verification steps:
    1. Run: cd pulse-mockup and npm run dev
    2. Open http://localhost:3000 -- should redirect to /workspace
    3. Verify app shell: dark sidebar with 5 nav items (Analysis Workspace active), header with credit balance and theme toggle
    4. Toggle theme between dark and light -- both should look polished
    5. Verify collection cards grid: name, status badge, created date, signal count visible
    6. Click "Create Collection" button -- dialog opens with name + description fields
    7. Click a collection card -- navigates to /workspace/collections/[id]
    8. Verify collection detail: file dropzone at top, file table with checkboxes below
    9. Click a file name -- data summary side panel slides in from right
    10. Select some files via checkboxes -- sticky bar shows count, credit estimate, enabled Run Detection button
    11. Deselect all files -- Run Detection disabled with hint text
    12. Click Run Detection -- full-page loading with 4 animated steps
    13. After loading completes (~12-15s) -- auto-navigates to signals page
    14. Verify signal results: left panel with scrollable signal cards (severity color-coded), right panel with chart + detail
    15. Click different signal cards -- detail panel updates with different chart types
    16. Overall: does the design feel Hex.tech-inspired, dark, data-dense, analytical, and production-quality?
  </verify>
  <done>Human approves the complete Phase 42 flow or provides specific issues to fix. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- /workspace/collections/1/signals renders the 2-panel signal results layout
- Signal list shows sorted cards with severity badges (red/amber/green)
- Highest severity signal auto-selected on load
- Signal detail shows title, description, severity, category, chart, and statistical evidence
- Charts render with realistic data using Recharts (line, bar, scatter types)
- Clicking different signals updates the detail panel
- Full end-to-end flow: /workspace -> collection card -> collection detail -> run detection -> loading -> signals
</verification>

<success_criteria>
- Signal results page renders with scrollable signal list and detail panel
- Severity color scheme: red=Critical, amber=Warning, green=Info
- Recharts charts render with realistic mock data (not placeholder boxes)
- Statistical evidence section shows confidence, p-value, deviation, period
- Human reviewer approves the complete Phase 42 flow end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/42-analysis-workspace-pulse-detection/42-04-SUMMARY.md`
</output>
