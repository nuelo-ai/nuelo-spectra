---
phase: 16-frontend-restructure
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - frontend/src/app/(dashboard)/layout.tsx
  - frontend/src/app/(dashboard)/sessions/new/page.tsx
  - frontend/src/app/(dashboard)/sessions/[sessionId]/page.tsx
  - frontend/src/app/page.tsx
  - frontend/src/hooks/useAuth.tsx
  - frontend/src/hooks/useChatMessages.ts
  - frontend/src/hooks/useSSEStream.ts
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/types/chat.ts
autonomous: true

must_haves:
  truths:
    - "Root URL redirects to /sessions/new instead of /dashboard"
    - "Login and signup redirect to /sessions/new instead of /dashboard"
    - "Dashboard layout renders ChatSidebar (left) + main content without top nav header"
    - "/sessions/new route renders a page that will host welcome screen"
    - "/sessions/[sessionId] route renders ChatInterface with session data"
    - "ChatInterface accepts sessionId instead of fileId and fetches messages via session endpoint"
    - "SSE streaming uses session-based endpoint /chat/sessions/{sessionId}/stream"
    - "useChatMessages fetches from /sessions/{sessionId}/messages"
    - "File-tab navigation (tab bar, tabStore references) is completely removed from dashboard"
  artifacts:
    - path: "frontend/src/app/(dashboard)/layout.tsx"
      provides: "Session-aware dashboard layout with SidebarProvider"
      contains: "SidebarProvider"
    - path: "frontend/src/app/(dashboard)/sessions/new/page.tsx"
      provides: "New session page route"
      contains: "NewSessionPage"
    - path: "frontend/src/app/(dashboard)/sessions/[sessionId]/page.tsx"
      provides: "Session chat page route"
      contains: "SessionPage"
    - path: "frontend/src/components/chat/ChatInterface.tsx"
      provides: "Session-based chat interface (migrated from fileId)"
      contains: "sessionId"
    - path: "frontend/src/hooks/useChatMessages.ts"
      provides: "Session-based message fetching"
      contains: "sessions.*messages"
    - path: "frontend/src/hooks/useSSEStream.ts"
      provides: "Session-based SSE streaming"
      contains: "sessions.*stream"
  key_links:
    - from: "frontend/src/app/(dashboard)/sessions/[sessionId]/page.tsx"
      to: "frontend/src/components/chat/ChatInterface.tsx"
      via: "ChatInterface component with sessionId prop"
      pattern: "ChatInterface.*sessionId"
    - from: "frontend/src/hooks/useSSEStream.ts"
      to: "/chat/sessions/{sessionId}/stream"
      via: "fetch POST for SSE streaming"
      pattern: "sessions.*stream"
    - from: "frontend/src/hooks/useChatMessages.ts"
      to: "/sessions/{sessionId}/messages"
      via: "apiClient.get for message fetching"
      pattern: "sessions.*messages"
    - from: "frontend/src/app/(dashboard)/layout.tsx"
      to: "frontend/src/components/sidebar/ChatSidebar.tsx"
      via: "ChatSidebar component render"
      pattern: "ChatSidebar"
---

<objective>
Restructure the dashboard layout to use session-centric routing with the left sidebar from Plan 01, and migrate ChatInterface + hooks from file-based to session-based API endpoints.

Purpose: This is the structural backbone of the Phase 16 restructure. It replaces the file-tab layout with session routes, wires the sidebar into the layout, and migrates all chat data flow from file-based to session-based endpoints.

Output: Working session-based navigation where clicking a session in the sidebar loads that session's chat. File tabs are fully removed. All chat data flows through session endpoints.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-frontend-restructure/16-CONTEXT.md
@.planning/phases/16-frontend-restructure/16-RESEARCH.md
@.planning/phases/16-frontend-restructure/16-01-SUMMARY.md

Key references (read these during execution):
@frontend/src/app/(dashboard)/layout.tsx (current layout — rewrite this)
@frontend/src/app/(dashboard)/dashboard/page.tsx (current dashboard page — replaced by session routes)
@frontend/src/components/chat/ChatInterface.tsx (migrate from fileId to sessionId)
@frontend/src/hooks/useChatMessages.ts (migrate to session endpoint)
@frontend/src/hooks/useSSEStream.ts (migrate to session endpoint)
@frontend/src/hooks/useAuth.tsx (update redirect targets)
@frontend/src/types/chat.ts (update ChatMessageResponse)
@frontend/src/app/page.tsx (update root redirect)
@backend/app/routers/chat_sessions.py (session API — GET /sessions/{id}/messages)
@backend/app/routers/chat.py (session stream endpoint — POST /chat/sessions/{id}/stream)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dashboard layout, create session routes, update redirects</name>
  <files>
    frontend/src/app/(dashboard)/layout.tsx
    frontend/src/app/(dashboard)/sessions/new/page.tsx
    frontend/src/app/(dashboard)/sessions/[sessionId]/page.tsx
    frontend/src/app/page.tsx
    frontend/src/hooks/useAuth.tsx
  </files>
  <action>
    **1. Rewrite dashboard layout (frontend/src/app/(dashboard)/layout.tsx):**
    Replace the current layout (top nav + FileSidebar + main content) with a session-centric layout:
    - Wrap entire layout in `<SidebarProvider defaultOpen={true}>` from shadcn sidebar
    - Remove the top `<header>` nav bar entirely (user menu moves to sidebar UserSection from Plan 01)
    - Remove FileSidebar import and usage
    - Remove tabStore import and usage
    - Structure: `<div className="flex h-screen overflow-hidden">` containing:
      - `<ChatSidebar />` (from Plan 01)
      - `<main className="flex-1 overflow-hidden">{children}</main>`
    - Keep the auth guard logic (useAuth, isAuthenticated check, loading spinner, redirect to /login)
    - Remove getInitials and Avatar from layout (moved to UserSection)
    - Do NOT remove the import of user/logout from useAuth — UserSection handles these but the auth guard still needs isAuthenticated/isLoading

    **2. Create new session page (frontend/src/app/(dashboard)/sessions/new/page.tsx):**
    Simple page that will host the WelcomeScreen (Plan 03). For now, create a placeholder:
    ```tsx
    'use client'
    export default function NewSessionPage() {
      return (
        <div className="flex items-center justify-center h-full">
          <div className="text-center space-y-2">
            <h1 className="text-2xl font-semibold">New Chat</h1>
            <p className="text-muted-foreground">Welcome screen coming soon</p>
          </div>
        </div>
      )
    }
    ```
    This placeholder will be replaced by WelcomeScreen in Plan 03.

    **3. Create session chat page (frontend/src/app/(dashboard)/sessions/[sessionId]/page.tsx):**
    Page that renders ChatInterface for a specific session:
    - Use `useParams()` to get sessionId from URL
    - Use `useSessionDetail(sessionId)` from Plan 01 hooks to fetch session data
    - Loading state: show spinner while session loads
    - Error/not found state: redirect to /sessions/new
    - When loaded: render `<ChatInterface sessionId={session.id} sessionTitle={session.title} />` (sessionId and sessionTitle props — ChatInterface migration in Task 2)
    - Update sessionStore.currentSessionId when this page mounts (useEffect)

    **4. Update root redirect (frontend/src/app/page.tsx):**
    Change `redirect("/dashboard")` to `redirect("/sessions/new")`.

    **5. Update auth redirects (frontend/src/hooks/useAuth.tsx):**
    - In `login()`: change `router.push("/dashboard")` to `router.push("/sessions/new")`
    - In `signup()`: change `router.push("/dashboard")` to `router.push("/sessions/new")`
    This implements CHAT-09: user is greeted with new chat session upon login.

    **6. Note on old dashboard page:**
    Do NOT delete `frontend/src/app/(dashboard)/dashboard/page.tsx` yet — leave it in place to avoid broken imports during transition. It will be unused since root now redirects to /sessions/new. Can be cleaned up in Phase 18.
  </action>
  <verify>
    Run `cd frontend && npx tsc --noEmit` — no TypeScript errors.
    Verify new files exist: sessions/new/page.tsx, sessions/[sessionId]/page.tsx.
    Verify layout.tsx no longer imports FileSidebar or tabStore.
    Verify page.tsx redirects to /sessions/new.
    Verify useAuth.tsx login/signup redirect to /sessions/new.
  </verify>
  <done>
    Dashboard layout renders ChatSidebar with session routes. Root URL and auth flow redirect to /sessions/new. Session page loads session detail and renders ChatInterface. File-tab navigation completely removed from layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ChatInterface, useChatMessages, and useSSEStream to session-based</name>
  <files>
    frontend/src/components/chat/ChatInterface.tsx
    frontend/src/hooks/useChatMessages.ts
    frontend/src/hooks/useSSEStream.ts
    frontend/src/types/chat.ts
  </files>
  <action>
    **1. Update ChatMessageResponse type (frontend/src/types/chat.ts):**
    - Change `file_id: string` to `file_id: string | null` (session-based messages have null file_id, per Phase 14 decision)
    - Add `session_id?: string` field (optional, for future use)

    **2. Migrate useChatMessages (frontend/src/hooks/useChatMessages.ts):**
    Change from file-based to session-based endpoint:
    - Rename parameter from `fileId` to `sessionId` in `useChatMessages(sessionId: string | null)`
    - Change queryKey from `["chat", "messages", fileId]` to `["session", "messages", sessionId]`
    - Change fetch URL from `/chat/${fileId}/messages` to `/sessions/${sessionId}/messages`
    - Update `useAddLocalMessage`: parameter from fileId to sessionId, queryKey update, optimistic message uses `file_id: null` (session messages have no file_id)
    - Update `useInvalidateChatMessages`: parameter from fileId to sessionId, queryKey update

    **3. Migrate useSSEStream (frontend/src/hooks/useSSEStream.ts):**
    Change from file-based to session-based streaming endpoint:
    - In `startStream`: rename first parameter from `fileId` to `sessionId`
    - Change fetch URL from `http://localhost:8000/chat/${fileId}/stream` to `http://localhost:8000/chat/sessions/${sessionId}/stream`
    - The rest of the SSE parsing logic stays identical — event types, state management unchanged

    **4. Migrate ChatInterface (frontend/src/components/chat/ChatInterface.tsx):**
    Change props and internal references from file-based to session-based:
    - Update interface: `ChatInterfaceProps { sessionId: string; sessionTitle: string; }` (remove fileId, fileName)
    - Update destructuring: `{ sessionId, sessionTitle }`
    - Update `useChatMessages(sessionId)` call (was fileId)
    - Update `addLocalMessage(sessionId, message)` call (was fileId)
    - Update `startStream(sessionId, message, ...)` call (was fileId)
    - Update header: display `sessionTitle` instead of `fileName`
    - Update ContextUsage: pass `sessionId` instead of `fileId` (note: ContextUsage may need sessionId — if it uses fileId for context checking, pass sessionId. If ContextUsage breaks, stub it out with a TODO comment for Phase 18 fix)
    - Update trim context URL: `/chat/${fileId}/trim-context` → this is file-based and may not work with sessions yet. Comment it out with TODO for Phase 18.
    - Update `useFileSummary(fileId)` call: This was for query suggestions on the file. For sessions, suggestion logic moves to WelcomeScreen (Plan 03). Remove useFileSummary import and usage. Remove summaryData references. The empty-state query suggestions block should show a simple "Ask a question about your data" message instead (the WelcomeScreen from Plan 03 handles the proper greeting).
    - Update streaming message `file_id` references: change `file_id: fileId` to `file_id: null` in all inline message objects (streaming placeholders)
    - Keep all existing streaming logic, DataCard rendering, message rendering, follow-up suggestions, search sources — these are all preserved.
    - Remove `useTabCloseWarning` import and usage (tabs are gone)
    - Remove `useFileSummary` import
  </action>
  <verify>
    Run `cd frontend && npx tsc --noEmit` — no TypeScript errors.
    Run `cd frontend && npm run build` — build succeeds.
    Verify ChatInterface accepts sessionId/sessionTitle props.
    Verify useChatMessages fetches from /sessions/{id}/messages.
    Verify useSSEStream streams from /chat/sessions/{id}/stream.
    Grep for "fileId" in ChatInterface.tsx — should return zero matches (all migrated).
  </verify>
  <done>
    ChatInterface fully migrated from file-based to session-based. Messages fetch from session endpoint. SSE streaming uses session endpoint. All file_id references replaced with sessionId. Tab-related hooks removed. Build passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes with zero errors
2. `cd frontend && npm run build` completes successfully
3. Root URL (page.tsx) redirects to /sessions/new
4. Auth login/signup redirect to /sessions/new (CHAT-09)
5. Layout renders ChatSidebar without top nav header or FileSidebar
6. /sessions/new route renders placeholder page
7. /sessions/[sessionId] route renders ChatInterface with session data
8. ChatInterface accepts sessionId prop, not fileId
9. useChatMessages fetches from /sessions/{id}/messages endpoint
10. useSSEStream POSTs to /chat/sessions/{id}/stream endpoint
11. No references to tabStore in layout.tsx or ChatInterface.tsx
</verification>

<success_criteria>
- Dashboard layout restructured: ChatSidebar in left, main content area, no top nav bar
- Session routes operational: /sessions/new and /sessions/[sessionId]
- ChatInterface fully migrated from file-based to session-based endpoints
- SSE streaming works with session-based endpoint
- File-tab navigation completely removed from active flow
- Auth flow redirects to /sessions/new (CHAT-09)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/16-frontend-restructure/16-02-SUMMARY.md`
</output>
