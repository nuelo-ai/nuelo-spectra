---
phase: 14-database-foundation-migration
plan: 04
type: execute
wave: 4
depends_on: ["14-02", "14-03"]
files_modified:
  - backend/app/routers/chat.py
  - backend/app/services/agent_service.py
  - backend/app/agents/graph.py
autonomous: true

must_haves:
  truths:
    - "Chat query endpoint accepts session_id and uses session-based thread_id for LangGraph"
    - "Stream endpoint accepts session_id and uses session-based thread_id"
    - "Context usage endpoint uses session-based thread_id"
    - "Context trim endpoint uses session-based thread_id"
    - "Agent service generates thread_id as session_{session_id}_user_{user_id}"
    - "Old file-based endpoints still work during transition period"
    - "Messages created by agent are saved with session_id"
  artifacts:
    - path: "backend/app/routers/chat.py"
      provides: "Updated chat router with session-based query endpoints alongside file-based ones"
      contains: "session_id"
    - path: "backend/app/services/agent_service.py"
      provides: "Updated agent service using session-based thread_ids"
      contains: "session_{session_id}_user_{user_id}"
  key_links:
    - from: "backend/app/routers/chat.py"
      to: "backend/app/services/agent_service.py"
      via: "passes session_id to agent service"
      pattern: "run_chat_query.*session_id"
    - from: "backend/app/services/agent_service.py"
      to: "LangGraph checkpointer"
      via: "session-based thread_id in config"
      pattern: "session_.*_user_"
    - from: "backend/app/services/agent_service.py"
      to: "backend/app/services/chat.py"
      via: "saves messages with session_id"
      pattern: "create_session_message|session_id"
---

<objective>
Update the chat router and agent service to support session-based queries with session-based LangGraph thread_ids, while maintaining backward compatibility with file-based endpoints during the transition period.

Purpose: Completes the backend transition from file-centric to session-centric conversation flow. After this plan, the agent system uses session-based thread_ids matching the migrated checkpoint data, and messages are saved with session_id. This is the final wiring that makes DATA-03 (messages belong to sessions) and DATA-05 (LangGraph memory preserved) fully functional at the application level.

Output: 2-3 updated files (chat router, agent_service, optionally graph.py)
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-database-foundation-migration/14-RESEARCH.md
@.planning/phases/14-database-foundation-migration/14-01-SUMMARY.md
@.planning/phases/14-database-foundation-migration/14-02-SUMMARY.md
@.planning/phases/14-database-foundation-migration/14-03-SUMMARY.md

@backend/app/routers/chat.py
@backend/app/routers/chat_sessions.py
@backend/app/services/agent_service.py
@backend/app/services/chat.py
@backend/app/services/chat_session.py
@backend/app/agents/graph.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session-based query endpoints to chat router</name>
  <files>backend/app/routers/chat.py</files>
  <action>
    Add NEW session-based endpoints to the existing chat router. Keep ALL existing file-based endpoints intact (backward compatibility during transition).

    Add these endpoints (they mirror the file-based ones but use session_id):

    1. **POST /chat/sessions/{session_id}/query** -> ChatAgentResponse
       - Same as existing /{file_id}/query but:
       - Validates session ownership (using ChatSessionService.get_user_session)
       - Gets linked files from session
       - Passes session_id (not file_id) to agent_service.run_chat_query
       - If session has no linked files, return 400 "No files linked to this session"

    2. **POST /chat/sessions/{session_id}/stream** -> EventSourceResponse
       - Same as existing /{file_id}/stream but session-based
       - Validates session ownership
       - Gets linked files from session
       - Passes session_id to agent_service.run_chat_query_stream
       - If session has no linked files, return 400 "No files linked to this session"

    3. **GET /chat/sessions/{session_id}/context-usage** -> context usage dict
       - Same logic as /{file_id}/context-usage but uses session-based thread_id
       - thread_id = f"session_{session_id}_user_{current_user.id}"

    4. **POST /chat/sessions/{session_id}/trim-context** -> trim result dict
       - Same logic as /{file_id}/trim-context but uses session-based thread_id
       - thread_id = f"session_{session_id}_user_{current_user.id}"

    Import ChatSessionService and needed schemas. Add imports at top of file.

    IMPORTANT: Do NOT remove or modify existing file-based endpoints. They remain for backward compatibility. Phase 16 (frontend) will switch to session-based endpoints, and the file-based ones can be deprecated later.
  </action>
  <verify>
    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "
    from app.main import app
    routes = [r.path for r in app.routes if hasattr(r, 'path')]
    session_chat = [r for r in routes if '/chat/sessions/' in r]
    file_chat = [r for r in routes if '/chat/{file_id}' in r]
    print('Session-based chat routes:', session_chat)
    print('File-based chat routes (preserved):', file_chat)
    assert len(session_chat) >= 4, 'Missing session chat routes'
    assert len(file_chat) >= 4, 'File-based routes should still exist'
    print('All routes present')
    "`

    Expected: Both session-based and file-based chat routes present.
  </verify>
  <done>
    Four new session-based endpoints added to chat router: query, stream, context-usage, trim-context. All use session_id path parameter and session-based thread_ids. Existing file-based endpoints preserved for backward compatibility.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update agent_service to support session-based thread_ids and message saving</name>
  <files>
    backend/app/services/agent_service.py
    backend/app/agents/graph.py
  </files>
  <action>
    Update `backend/app/services/agent_service.py`:

    1. **Update `run_chat_query` function signature** to accept optional `session_id: UUID | None = None`:
       - If session_id is provided: use `thread_id = f"session_{session_id}_user_{user_id}"`
       - If session_id is None (backward compat): use old `thread_id = f"file_{file_id}_user_{user_id}"`
       - This allows both file-based and session-based callers during transition

    2. **Update `run_chat_query_stream` function signature** same way:
       - Accept optional session_id parameter
       - Use session-based thread_id when provided

    3. **Update message saving in both functions**:
       - When session_id is provided, use `ChatService.create_session_message(db, session_id, user_id, ...)` for saving messages
       - When session_id is None, use existing `ChatService.create_message(db, file_id, user_id, ...)` for backward compatibility

    4. **Update initial_state** passed to the graph:
       - Add `"session_id": str(session_id) if session_id else None` to initial_state
       - When session_id provided, get ALL linked files' data_summaries and file_paths:
         ```python
         # For multi-file sessions, concatenate file contexts
         # (Phase 15 will build proper Context Assembler - for now basic concat)
         if session_id:
             from app.services.chat_session import ChatSessionService
             session = await ChatSessionService.get_user_session(db, session_id, user_id)
             # Use first file's data for now (multi-file support is Phase 15)
             if session.files:
                 file_record = session.files[0]
         ```
       - This means for v0.3 initially, the agent uses the first linked file's context. Phase 15 will add proper multi-file context assembly.

    5. **Keep the old file-based code path working**: Use `if session_id:` / `else:` branching so the function works in both modes.

    ---

    Update `backend/app/agents/graph.py` (minimal change):
    - Update the docstring/example that references thread_id format to mention session-based format
    - No structural changes needed - the graph doesn't generate thread_ids, it receives them via config

    IMPORTANT NOTES:
    - Do NOT break existing file-based flow. The session_id parameter is optional with default None.
    - The agent graph itself doesn't change - only the thread_id and message saving change.
    - Multi-file context assembly is Phase 15 scope. This plan just uses first file's context.
    - All 4 places in agent_service.py that generate thread_id must be updated (search for "file_{file_id}_user_{user_id}").
  </action>
  <verify>
    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && python -c "
    import inspect
    from app.services.agent_service import run_chat_query, run_chat_query_stream

    # Check that session_id parameter exists
    sig1 = inspect.signature(run_chat_query)
    sig2 = inspect.signature(run_chat_query_stream)
    print('run_chat_query params:', list(sig1.parameters.keys()))
    print('run_chat_query_stream params:', list(sig2.parameters.keys()))
    assert 'session_id' in sig1.parameters, 'session_id not in run_chat_query'
    assert 'session_id' in sig2.parameters, 'session_id not in run_chat_query_stream'
    print('session_id parameter present in both functions')
    "`

    Also grep to confirm thread_id format update:
    ```bash
    cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend
    grep -n "session_.*_user_" app/services/agent_service.py
    ```

    Expected: session_id parameter present in both functions, and "session_{session_id}_user_{user_id}" pattern found in agent_service.py.
  </verify>
  <done>
    Agent service supports both session-based and file-based thread_ids via optional session_id parameter. When session_id provided: uses session-based thread_id, saves messages with session_id, loads file context from session's linked files. When session_id is None: falls back to file-based behavior (backward compatibility). Graph.py docstring updated. All 4 thread_id generation points updated with conditional branching.
  </done>
</task>

</tasks>

<verification>
- Session-based chat query creates session-based thread_id (session_X_user_Y)
- File-based chat query still creates file-based thread_id (backward compat)
- Messages saved via session-based endpoints have session_id set, file_id null
- Messages saved via file-based endpoints still have file_id set (backward compat)
- LangGraph checkpointer receives correct thread_id format matching migrated data
- Application starts without errors
- Both old and new endpoint paths are reachable
</verification>

<success_criteria>
- POST /api/chat/sessions/{session_id}/query works end-to-end with session-based thread_id
- POST /api/chat/sessions/{session_id}/stream works end-to-end with session-based thread_id
- GET /api/chat/sessions/{session_id}/context-usage returns correct token counts using session thread_id
- POST /api/chat/sessions/{session_id}/trim-context trims context using session thread_id
- Old file-based endpoints (/{file_id}/query, /stream, etc.) still work unchanged
- Agent service run_chat_query and run_chat_query_stream accept optional session_id
- LangGraph conversation memory works with session-based thread IDs (migrated checkpoints accessible)
</success_criteria>

<output>
After completion, create `.planning/phases/14-database-foundation-migration/14-04-SUMMARY.md`
</output>
