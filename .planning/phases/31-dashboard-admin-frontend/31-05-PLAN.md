---
phase: 31-dashboard-admin-frontend
plan: 05
type: execute
wave: 3
depends_on: ["31-02", "31-03", "31-04"]
files_modified:
  - admin-frontend/src/app/(admin)/credits/page.tsx
  - admin-frontend/src/app/(admin)/settings/page.tsx
  - admin-frontend/src/app/(admin)/audit-log/page.tsx
  - admin-frontend/src/hooks/useCredits.ts
  - admin-frontend/src/hooks/useSettings.ts
  - admin-frontend/src/hooks/useAuditLog.ts
  - admin-frontend/src/types/credit.ts
  - admin-frontend/src/types/settings.ts
  - admin-frontend/src/types/audit.ts
  - admin-frontend/src/components/credits/CreditOverview.tsx
  - admin-frontend/src/components/settings/SettingsForm.tsx
  - admin-frontend/src/components/audit/AuditLogTable.tsx
autonomous: false
requirements:
  - ARCH-06
  - ARCH-07
  - ARCH-09

must_haves:
  truths:
    - "Credits page shows platform-wide credit overview with tier breakdown and bulk adjustment capabilities"
    - "Settings page displays all platform settings in an editable form (signup toggle, default tier, invite expiry, credit policy, cost per message)"
    - "Audit log page displays paginated, filterable log entries with action, admin, target, and timestamp"
    - "All admin pages are accessible via sidebar navigation and render within the admin shell"
  artifacts:
    - path: "admin-frontend/src/app/(admin)/credits/page.tsx"
      provides: "Credit management overview page"
      contains: "CreditOverview"
    - path: "admin-frontend/src/app/(admin)/settings/page.tsx"
      provides: "Platform settings form page"
      contains: "SettingsForm"
    - path: "admin-frontend/src/app/(admin)/audit-log/page.tsx"
      provides: "Audit log viewer page"
      contains: "AuditLogTable"
  key_links:
    - from: "admin-frontend/src/hooks/useSettings.ts"
      to: "/api/admin/settings"
      via: "adminApiClient.get/patch"
      pattern: "adminApiClient.*settings"
    - from: "admin-frontend/src/hooks/useAuditLog.ts"
      to: "/api/admin/audit-log"
      via: "adminApiClient.get"
      pattern: "adminApiClient.*audit-log"
---

<objective>
Build the remaining Platform section pages (Credits, Settings, Audit Log) and perform visual verification of the complete admin frontend.

Purpose: These pages complete the admin frontend by providing credit management overview, platform configuration, and audit trail viewing. After building them, the entire admin portal is feature-complete and ready for visual verification.

Output: Three fully functional pages plus a verified, polished admin frontend.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard-admin-frontend/31-RESEARCH.md
@.planning/phases/31-dashboard-admin-frontend/31-02-SUMMARY.md
@.planning/phases/31-dashboard-admin-frontend/31-04-SUMMARY.md
@admin-frontend/src/lib/admin-api-client.ts
@admin-frontend/src/app/(admin)/layout.tsx
@admin-frontend/src/components/shared/DataTableShell.tsx
@backend/app/routers/admin/credits.py
@backend/app/routers/admin/settings.py
@backend/app/schemas/credit.py
@backend/app/schemas/platform_settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Credits, Settings, and Audit Log pages</name>
  <files>
    admin-frontend/src/types/credit.ts
    admin-frontend/src/types/settings.ts
    admin-frontend/src/types/audit.ts
    admin-frontend/src/hooks/useCredits.ts
    admin-frontend/src/hooks/useSettings.ts
    admin-frontend/src/hooks/useAuditLog.ts
    admin-frontend/src/components/credits/CreditOverview.tsx
    admin-frontend/src/components/settings/SettingsForm.tsx
    admin-frontend/src/components/audit/AuditLogTable.tsx
    admin-frontend/src/app/(admin)/credits/page.tsx
    admin-frontend/src/app/(admin)/settings/page.tsx
    admin-frontend/src/app/(admin)/audit-log/page.tsx
  </files>
  <action>
    **MANDATORY: Use /frontend-design skill for all layout and design implementation.**

    **Credits Page:**

    1. Create `src/types/credit.ts`:
       - `CreditOverviewData`: { total_users: number, total_credits_distributed: number, total_credits_used: number, total_credits_remaining: number, distribution_by_tier: { tier: string, user_count: number, total_balance: number }[] }
       - `CreditTransaction`: { id: string, user_id: string, type: string, amount: number, balance_after: number, description: string, admin_id: string|null, created_at: string }

    2. Create `src/hooks/useCredits.ts`:
       - `useCreditDistribution()`: GET /api/admin/credits/distribution
       - `useBulkCreditAdjust()`: POST mutation for bulk credit operations
       - Reuses TanStack Query patterns

    3. Create `src/components/credits/CreditOverview.tsx`:
       - Summary cards: total credits distributed, total used, total remaining
       - Tier breakdown table: tier name, user count, total balance, avg balance per user
       - Bulk credit adjustment section: select tier, enter amount, confirm with note

    4. Assemble `src/app/(admin)/credits/page.tsx`:
       - CreditOverview component with data from hooks
       - Loading/error states

    **Settings Page:**

    5. Create `src/types/settings.ts`:
       - `PlatformSettings`: { allow_public_signup: boolean, default_user_class: string, invite_expiry_hours: number, credit_reset_policy: string, cost_per_message: number, tier_credit_overrides: Record<string, number> }

    6. Create `src/hooks/useSettings.ts`:
       - `usePlatformSettings()`: GET /api/admin/settings
       - `useUpdateSettings()`: PATCH mutation

    7. Create `src/components/settings/SettingsForm.tsx`:
       - Form with sections:
         - **Signup Control:** Toggle switch for allow_public_signup, dropdown for default_user_class
         - **Invitations:** Number input for invite_expiry_hours
         - **Credits:** Dropdown for credit_reset_policy (weekly/monthly), number input for cost_per_message
         - **Tier Credit Overrides:** Editable fields for each tier's credit allocation
       - Save button with loading state
       - Show success toast on save
       - Form validation: cost_per_message > 0, invite_expiry_hours > 0

    8. Assemble `src/app/(admin)/settings/page.tsx`:
       - SettingsForm with data from usePlatformSettings
       - Loading/error states

    **Audit Log Page:**

    9. Create `src/types/audit.ts`:
       - `AuditLogEntry`: { id: string, admin_id: string, admin_email: string, action: string, target_type: string|null, target_id: string|null, details: Record<string, any>|null, created_at: string }
       - `AuditLogListResponse`: { items: AuditLogEntry[], total: number, page: number, page_size: number, total_pages: number }

    10. Create `src/hooks/useAuditLog.ts`:
        - `useAuditLog(params)`: paginated query with filters (action, admin_id, target_type, date range)

    11. Create `src/components/audit/AuditLogTable.tsx`:
        - TanStack Table with columns: Timestamp (formatted), Admin (email), Action (badge), Target Type, Target ID, Details (expandable or tooltip)
        - Filter bar: action type dropdown, date range picker
        - Server-side pagination

    12. Assemble `src/app/(admin)/audit-log/page.tsx`:
        - Filter bar at top
        - AuditLogTable with data from useAuditLog
        - Loading/error states
  </action>
  <verify>
    - `npm run build` passes
    - Navigate to /credits -- overview displays tier breakdown and summary cards
    - Navigate to /settings -- form loads with current values, save updates backend
    - Navigate to /audit-log -- table shows audit entries, filters work, pagination works
    - All three pages render within admin shell (sidebar + header visible)
  </verify>
  <done>Credits, Settings, and Audit Log pages are fully functional with real backend data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete admin frontend</name>
  <what-built>
    Complete admin frontend application with:
    - Login page with admin authentication
    - Dashboard with 6 metric cards, 2 trend charts, credit distribution, low-credit users
    - Users list with search, filters, bulk operations, row actions
    - User detail with 4 tabbed sections (Overview, Credits, Activity, Sessions)
    - Invitations page with create, revoke, resend
    - Credits page with tier overview and bulk adjustment
    - Settings page with editable platform configuration
    - Audit log page with filterable, paginated entries
    - Collapsible sidebar navigation with grouped sections
    - Dark/light theme toggle
    - Distinctive admin accent color (indigo/blue)
  </what-built>
  <how-to-verify>
    1. Start backend: `cd backend && SPECTRA_MODE=dev uvicorn app.main:app --reload`
    2. Start admin frontend: `cd admin-frontend && npm run dev`
    3. Visit http://localhost:3001 -- should redirect to /login
    4. Log in with admin credentials
    5. **Dashboard** (/dashboard):
       - Verify 6 metric cards show data
       - Verify 2 trend charts render with lines/areas
       - Verify credit section shows tier chart and low-credit table
       - Toggle dark mode -- verify charts adapt
    6. **Users** (/users):
       - Verify table loads with user data
       - Test search (type a name/email)
       - Test filters (status, tier)
       - Select rows -- verify bulk action bar appears
       - Click three-dot menu on a row -- verify dropdown shows actions
       - Click "Delete" on a user -- verify challenge code dialog appears with paste disabled
    7. **User Detail** (/users/:id):
       - Click a user name -- opens detail page
       - Verify 4 tabs render: Overview, Credits, Activity, Sessions
       - Click back button -- returns to /users
    8. **Invitations** (/invitations):
       - Verify table loads
       - Click "Create Invitation" -- verify dialog opens
       - Test revoke action on a pending invite
    9. **Credits** (/credits):
       - Verify tier breakdown displays
    10. **Settings** (/settings):
        - Verify form loads with current values
        - Toggle signup mode -- verify it saves
    11. **Audit Log** (/audit-log):
        - Verify table loads with entries
        - Test filters
    12. **Sidebar**: Click collapse button -- verify icons-only mode
    13. **Session**: Wait 5 minutes -- verify you stay logged in (sliding window token)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Credits page shows tier-level credit overview
2. Settings page allows editing all platform configuration
3. Audit log page displays filterable, paginated entries
4. All pages accessible via sidebar and render within admin shell
5. Complete admin frontend is visually polished and functional
6. Three-process local dev works: backend:8000, frontend:3000, admin:3001
</verification>

<success_criteria>
- All 7 admin pages are functional (login, dashboard, users, user detail, invitations, credits, settings, audit log)
- Navigation works across all pages via sidebar
- Dark/light mode works throughout
- Human verifies visual quality and functionality
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-admin-frontend/31-05-SUMMARY.md`
</output>
