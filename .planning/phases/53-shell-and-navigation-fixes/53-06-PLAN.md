---
phase: 53-shell-and-navigation-fixes
plan: 06
type: execute
wave: 4
depends_on:
  - 53-05
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - LBAR-01
  - LBAR-02

must_haves:
  truths:
    - "User visually confirms SidebarTrigger is present and functional in all three workspace sub-views"
    - "User visually confirms sidebar nav icon alignment is correct in both expanded and collapsed states"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of the two gap closure fixes: LBAR-01 (SidebarTrigger in workspace sub-views) and LBAR-02 (sidebar nav icon alignment).

Purpose: Both gaps require browser rendering to confirm — code inspection cannot verify pixel-level position or that toggle placement matches the visual design. This checkpoint closes Phase 53.
Output: User confirms both gaps are resolved, completing all 7 Phase 53 requirements.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-shell-and-navigation-fixes/53-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the frontend dev server</name>
  <files></files>
  <action>
    Run the frontend development server so the user can verify the gap closure changes in a browser.

    ```bash
    cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npm run dev
    ```

    The app will be available at http://localhost:3000. Keep the server running while the user performs visual checks.
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Dev server is running. User can open http://localhost:3000 in a browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of LBAR-01 and LBAR-02 gap fixes</name>
  <what-built>
    Two gap closure fixes:
    - Plan 05 Task 1 (LBAR-01): SidebarTrigger added to Collection Detail page, Signal Detection Results page, and Report Detail page headers — all three workspace sub-views that were missing the toggle
    - Plan 05 Task 2 (LBAR-02): Removed the insufficient `pl-1` from nav item Link and anchor children in UnifiedSidebar — lets the SidebarMenuButton cva default (p-2, flex items-center, size-8 in collapsed mode) render the icons without competing child padding
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 and verify each item:

    **LBAR-01 — Leftbar toggle in workspace sub-views:**

    1. Click "Pulse Analysis" in the sidebar to open the collections list (/workspace)
    2. Click into any collection to open the Collection Detail page
    3. Confirm: a leftbar collapse toggle is visible at the top-left of the main screen (above the ArrowLeft back button row)
    4. Click it to confirm the sidebar collapses and expands

    5. From the collection detail, navigate to the Signals view (either via the Signals tab "Open Signals View" button, or a signal card link) — URL: /workspace/collections/[id]/signals
    6. Confirm: a leftbar collapse toggle is visible at the top-left of the header bar (to the left of the ArrowLeft back link)
    7. Click it to confirm it works

    8. From the collection detail, navigate to a Report (Reports tab → View Report) — URL: /workspace/collections/[id]/reports/[reportId]
    9. Confirm: a leftbar collapse toggle is visible at the top-left of the sticky header (to the left of the Back button)
    10. Click it to confirm it works

    **LBAR-02 — Sidebar nav icon alignment:**

    1. With the sidebar expanded, look at the Pulse Analysis, Chat, and Files nav items
    2. Confirm: the icons (sparkle, message, folder) are at the same horizontal position as each other — visually aligned in a column
    3. Collapse the sidebar to icon-only mode (click the toggle)
    4. Confirm: each icon is horizontally centered within the collapsed sidebar column (not pushed to the left or right)
    5. Expand the sidebar again — confirm icons return to correct alignment
  </how-to-verify>
  <resume-signal>Type "approved" if both LBAR-01 and LBAR-02 are now visually correct. Or describe any remaining issues for further gap closure.</resume-signal>
</task>

</tasks>

<verification>
Human approval received for both LBAR-01 (SidebarTrigger in all workspace sub-views) and LBAR-02 (nav icon alignment in expanded and collapsed modes).
</verification>

<success_criteria>
User types "approved" confirming both gap closure fixes are visually correct, completing all 7 Phase 53 requirements and closing the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/53-shell-and-navigation-fixes/53-06-SUMMARY.md`
</output>
