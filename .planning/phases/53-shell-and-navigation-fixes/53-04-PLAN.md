---
phase: 53-shell-and-navigation-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - 53-01
  - 53-02
  - 53-03
files_modified: []
autonomous: false
requirements:
  - LBAR-01
  - LBAR-02
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - FILES-01
  - FILES-02

must_haves:
  truths:
    - "All 7 success criteria from the phase roadmap are visually confirmed by the user"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of all 7 phase requirements after the Wave 1 plans complete.

Purpose: All Phase 53 changes are pure UI layout fixes that cannot be automatically tested without a component test framework. The build passes after each plan, but correctness requires visual confirmation in a running browser.
Output: User confirms all 7 behaviors are correct, or describes issues for gap closure.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-shell-and-navigation-fixes/53-01-SUMMARY.md
@.planning/phases/53-shell-and-navigation-fixes/53-02-SUMMARY.md
@.planning/phases/53-shell-and-navigation-fixes/53-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the frontend dev server</name>
  <files></files>
  <action>
    Run the frontend development server so the user can verify the changes in a browser.

    ```bash
    cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npm run dev
    ```

    The app will be available at http://localhost:3000. Keep the server running while the user performs visual checks.
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Dev server is running. User can open http://localhost:3000 in a browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three sets of UI fixes across 4 files:
    - Plan 01: WorkspacePage now has a fixed SidebarTrigger header (LBAR-01); UnifiedSidebar nav items have adjusted left padding (LBAR-02)
    - Plan 02: WelcomeScreen logo removed, floating expand button added when rightbar is collapsed (CHAT-01, CHAT-02); ChatInterface rightbar toggle pinned to right edge (CHAT-03)
    - Plan 03: MyFilesPage logo removed, SidebarTrigger moved to fixed header above scroll (FILES-01, FILES-02)
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 and verify each item:

    **LBAR-01 — Leftbar toggle in Pulse Analysis:**
    1. Click "Pulse Analysis" in the sidebar
    2. Confirm a collapse/expand toggle button is visible at the top-left of the main screen, above the collections list
    3. Click it — sidebar should collapse/expand

    **LBAR-02 — Menu item icon padding:**
    1. With the sidebar expanded, confirm Pulse Analysis, Chat, Files icon+text items are horizontally aligned (icons at same x position)
    2. Collapse the sidebar to icon-only — confirm icons are centered in the sidebar

    **CHAT-01 — No logo in Chat panel:**
    1. Click "Chat" in the sidebar (opens /sessions/new)
    2. Confirm there is NO gradient "S" logo block and NO "Spectra" text in the top-left of the main chat area
    3. Confirm the leftbar collapse toggle (hamburger/chevron) is still visible at the top-left

    **CHAT-02 — Rightbar expand toggle in new/existing chat:**
    1. Navigate to an existing chat session (/sessions/[id])
    2. Confirm the rightbar (linked files panel) can be collapsed — click the Files toggle to close it
    3. After collapsing, confirm a small expand button is visible at the top-right corner of the chat area
    4. Click it — the linked files panel should reopen

    **CHAT-03 — Rightbar toggle at true right edge in existing chat:**
    1. In an existing chat session, look at the header bar
    2. The "Files (N)" toggle button should be at the far right edge of the screen, NOT centered at the conversation column
    3. On a wide screen this is most visible — toggle should be ~16px from the right edge

    **FILES-01 — No logo in Files panel:**
    1. Click "Files" in the sidebar (opens /my-files)
    2. Confirm there is NO gradient "S" logo block and NO "Spectra" text in the top area
    3. Confirm "My Files" heading and subtitle are still shown

    **FILES-02 — Leftbar toggle at top of Files screen:**
    1. In the Files section, confirm the collapse toggle is visible at the top of the screen in a fixed header bar (not inside the scrollable file list)
    2. Scroll down if files are present — the SidebarTrigger should remain fixed at the top (not scroll away)
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass. Or describe which items failed (e.g., "LBAR-02 padding still misaligned") for gap closure.</resume-signal>
</task>

</tasks>

<verification>
Human approval received for all 7 requirements, or specific failures identified for gap closure planning.
</verification>

<success_criteria>
User types "approved" confirming all 7 visual requirements are met, completing Phase 53.
</success_criteria>

<output>
After completion, create `.planning/phases/53-shell-and-navigation-fixes/53-04-SUMMARY.md`
</output>
