---
phase: 53-shell-and-navigation-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/(dashboard)/my-files/page.tsx
autonomous: true
requirements:
  - FILES-01
  - FILES-02

must_haves:
  truths:
    - "Selecting the Files section shows no Spectra logo (gradient S block + 'Spectra' text) in the top-left of the Files main panel"
    - "The leftbar collapse toggle (SidebarTrigger) is visible at the top of the Files screen in a fixed header above the scrollable file list"
  artifacts:
    - path: "frontend/src/app/(dashboard)/my-files/page.tsx"
      provides: "MyFilesPage with SidebarTrigger in a fixed shrink-0 header above scrollable content, logo removed"
      contains: "shrink-0"
  key_links:
    - from: "frontend/src/app/(dashboard)/my-files/page.tsx"
      to: "SidebarTrigger"
      via: "shrink-0 header strip above overflow-y-auto content wrapper"
      pattern: "shrink-0"
---

<objective>
Fix two Files-section bugs: remove the Spectra logo from the Files panel header (FILES-01) and move the SidebarTrigger from inside the scrollable content area into a fixed header above it (FILES-02).

Purpose: MyFilesPage renders its entire header (including SidebarTrigger and the Spectra logo) inside the scrollable `flex-1 overflow-y-auto` wrapper, so the SidebarTrigger scrolls out of view and an unwanted logo is shown. The outer div already uses `flex flex-col h-full overflow-hidden` — it just needs a `shrink-0` header extracted above the scrollable section.
Output: MyFilesPage with a fixed `shrink-0` header bar containing only the SidebarTrigger, the page title and subtitle; logo removed; remaining content unchanged in the scrollable area.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/53-shell-and-navigation-fixes/53-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/app/(dashboard)/my-files/page.tsx (current structure, lines 80-98):
```tsx
// Outer div (line 81) — already correct:
<div className="flex flex-col h-full overflow-hidden">
  {/* PROBLEM: entire header is inside the scrollable wrapper */}
  <div className="flex-1 overflow-y-auto">
    <div className="max-w-6xl mx-auto px-6 py-8 space-y-6">
      {/* Page header — scrolls out of view */}
      <div className="flex items-center gap-3">
        <SidebarTrigger className="-ml-1" />
        <div className="h-7 w-7 rounded-lg gradient-primary flex items-center justify-center shrink-0">
          <span className="text-sm font-bold text-white">S</span>
        </div>
        <span className="font-semibold text-sm tracking-tight shrink-0">Spectra</span>
        <span className="text-muted-foreground/40 shrink-0">|</span>
        <div>
          <h1 className="text-2xl font-bold tracking-tight">My Files</h1>
          <p className="text-sm text-muted-foreground mt-1">Manage your uploaded data files</p>
        </div>
      </div>
      {/* drag zone, upload button, file table — remain inside scrollable area */}
    </div>
  </div>
  {/* Upload dialog — outside scrollable area, keep as-is */}
</div>
```

SidebarTrigger usage pattern (consistent with other pages):
```tsx
import { SidebarTrigger } from "@/components/ui/sidebar";
// Already imported in my-files/page.tsx
<SidebarTrigger className="-ml-1" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract header to fixed strip and remove logo (FILES-01 + FILES-02)</name>
  <files>frontend/src/app/(dashboard)/my-files/page.tsx</files>
  <action>
    Restructure MyFilesPage to extract the page header out of the scrollable content area and into a `shrink-0` header strip. Remove the Spectra logo from the header.

    The outer div already has `className="flex flex-col h-full overflow-hidden"` — do not change it.

    **New structure:**
    ```tsx
    <div className="flex flex-col h-full overflow-hidden">
      {/* Fixed header strip — above scrollable content */}
      <div className="px-6 py-3 shrink-0 border-b flex items-center gap-3">
        <SidebarTrigger className="-ml-1" />
        {/* Logo REMOVED per FILES-01 — no gradient S block, no "Spectra" text */}
        <div>
          <h1 className="text-2xl font-bold tracking-tight">My Files</h1>
          <p className="text-sm text-muted-foreground mt-1">
            Manage your uploaded data files
          </p>
        </div>
      </div>

      {/* Scrollable content — starts BELOW the fixed header */}
      <div className="flex-1 overflow-y-auto">
        <div className="max-w-6xl mx-auto px-6 py-8 space-y-6">
          {/* drag-and-drop zone — unchanged */}
          {/* upload button — unchanged */}
          {/* file table — unchanged */}
        </div>
      </div>

      {/* Upload dialog — unchanged, keep outside scrollable area */}
    </div>
    ```

    Remove from the header:
    - `<div className="h-7 w-7 rounded-lg gradient-primary ..."><span>S</span></div>`
    - `<span className="font-semibold text-sm tracking-tight shrink-0">Spectra</span>`
    - `<span className="text-muted-foreground/40 shrink-0">|</span>`

    Keep in the header:
    - `<SidebarTrigger className="-ml-1" />` (already imported)
    - The h1 "My Files" and the subtitle paragraph

    The inner `<div className="max-w-6xl mx-auto px-6 py-8 space-y-6">` previously contained the header + all content. After this change, it contains ONLY the content that was below the header (drag zone, upload button, file table). Remove the old `<div className="flex items-center gap-3">` header block from inside it.

    DO NOT change: drag zone, upload button, file table, upload dialog, any hooks or state logic.
    DO NOT remove the `SidebarTrigger` import (it remains in use in the new fixed header).
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>MyFilesPage builds without errors. The outer div still has `flex flex-col h-full overflow-hidden`. A new `shrink-0 border-b` header strip sits above the `flex-1 overflow-y-auto` content wrapper. The header contains SidebarTrigger and the page title/subtitle; no gradient logo or "Spectra" text is present. The drag zone and file table are inside the scrollable area unchanged.</done>
</task>

</tasks>

<verification>
After the task:
- `cd frontend && npm run build` — zero TypeScript errors
- `cd frontend && npm run lint` — zero lint errors
- my-files/page.tsx: outer div retains `flex flex-col h-full overflow-hidden`
- my-files/page.tsx: `shrink-0 border-b` div is a direct child of outer div, before the `flex-1 overflow-y-auto` div
- my-files/page.tsx: no `gradient-primary` class in the header section
- my-files/page.tsx: `SidebarTrigger` still imported and used in the fixed header
</verification>

<success_criteria>
1. `npm run build` exits 0 with no TypeScript errors
2. Fixed header strip (`shrink-0 border-b`) with SidebarTrigger and page title is above the scrollable content
3. No gradient logo div or "Spectra" text in the header
4. Drag zone, upload button, file table remain inside the scrollable area and unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/53-shell-and-navigation-fixes/53-03-SUMMARY.md`
</output>
