---
phase: 09-manager-agent-with-intelligent-query-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/agents/manager.py
  - backend/app/agents/state.py
  - backend/app/agents/graph.py
  - backend/app/config/prompts.yaml
  - backend/app/services/agent_service.py
autonomous: true

must_haves:
  truths:
    - "Manager Agent classifies user queries into MEMORY_SUFFICIENT, CODE_MODIFICATION, or NEW_ANALYSIS"
    - "Manager Agent uses configurable LLM provider from YAML config (default: Sonnet)"
    - "Manager Agent analyzes last 10 conversation messages for routing decisions"
    - "System defaults to NEW_ANALYSIS when routing fails or is uncertain"
    - "Routing decisions are logged with reasoning for monitoring"
    - "System maintains single-route decision logic (no hybrid routes)"
  artifacts:
    - path: "backend/app/agents/manager.py"
      provides: "Manager Agent node with RoutingDecision structured output and Command-based routing"
      exports: ["manager_node", "RoutingDecision"]
    - path: "backend/app/agents/state.py"
      provides: "ChatAgentState with routing_decision and previous_code fields"
      contains: "routing_decision"
    - path: "backend/app/agents/graph.py"
      provides: "Graph with manager as entry point, Command-based routing to data_analysis or coding_agent"
      contains: "manager"
    - path: "backend/app/config/prompts.yaml"
      provides: "Manager agent config entry with provider, model, temperature, system_prompt, max_tokens"
      contains: "manager:"
  key_links:
    - from: "backend/app/agents/graph.py"
      to: "backend/app/agents/manager.py"
      via: "graph.add_node('manager', manager_node)"
      pattern: "add_node.*manager"
    - from: "backend/app/agents/manager.py"
      to: "backend/app/agents/state.py"
      via: "RoutingDecision stored in routing_decision state field"
      pattern: "routing_decision"
    - from: "backend/app/agents/manager.py"
      to: "backend/app/config/prompts.yaml"
      via: "get_agent_prompt('manager') loads system prompt from YAML"
      pattern: "get_agent_prompt.*manager"
---

<objective>
Implement the Manager Agent as the new entry point of the chat analysis graph, with Command-based routing to classify queries into MEMORY_SUFFICIENT, CODE_MODIFICATION, or NEW_ANALYSIS routes.

Purpose: The Manager Agent is the central intelligence that eliminates wasteful code generation for simple queries (40% of typical queries), reducing response time by ~87% for memory queries and ~40% overall cost reduction.

Output: Working Manager Agent node, updated graph topology, routing decision schema, YAML configuration, and structured logging.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/architecture/manager-agent-routing.md
@.planning/phases/09-manager-agent-with-intelligent-query-routing/09-RESEARCH.md
@.planning/phases/08-session-memory-with-postgresql-checkpointing/08-01-SUMMARY.md
@.planning/phases/07-multi-llm-provider-infrastructure/07-01-SUMMARY.md

# Key source files to read before implementing:
@backend/app/agents/graph.py
@backend/app/agents/state.py
@backend/app/agents/coding.py
@backend/app/agents/data_analysis.py
@backend/app/agents/config.py
@backend/app/agents/llm_factory.py
@backend/app/config/prompts.yaml
@backend/app/services/agent_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Manager Agent with RoutingDecision schema and update state</name>
  <files>
    backend/app/agents/manager.py
    backend/app/agents/state.py
    backend/app/config/prompts.yaml
  </files>
  <action>
    **1. Create `backend/app/agents/manager.py`:**

    Create the Manager Agent module with the following components:

    **RoutingDecision Pydantic model:**
    ```python
    class RoutingDecision(BaseModel):
        route: Literal["MEMORY_SUFFICIENT", "CODE_MODIFICATION", "NEW_ANALYSIS"]
        reasoning: str = Field(description="Brief explanation of why this route was chosen")
        context_summary: str = Field(description="Relevant context from conversation for downstream agents")
    ```

    **`manager_node` async function:**
    - Signature: `async def manager_node(state: ChatAgentState) -> Command[Literal["data_analysis", "coding_agent"]]`
    - Uses `get_stream_writer()` to emit `routing_started` event (type: "routing_started", message: "Analyzing query...")
    - Loads LLM config via existing helpers: `get_agent_provider("manager")`, `get_agent_model("manager")`, `get_agent_temperature("manager")`, `get_api_key_for_provider(provider, settings)`, `get_agent_max_tokens("manager")`
    - Builds kwargs dict with `max_tokens` and `temperature`; adds `base_url` for ollama provider
    - Instantiates LLM via `get_llm(provider=provider, model=model, api_key=api_key, **kwargs)`
    - Uses `llm.with_structured_output(RoutingDecision)` for reliable JSON parsing (NOT raw json.loads)
    - Reads `state.get("messages", [])` and limits to last 10 messages (configurable via YAML `routing_context_messages` field, default 10)
    - Checks for previous code: reads `state.get("generated_code", "")` — if non-empty, previous code exists
    - Builds routing prompt with: user_query, message count, has_previous_code flag, previous_code snippet (truncated to 500 chars), data_summary
    - Sends `[SystemMessage(content=system_prompt), HumanMessage(content=routing_prompt)]` to structured LLM
    - Wraps LLM call in try/except: on ANY exception, creates fallback `RoutingDecision(route="NEW_ANALYSIS", reasoning="Fallback: routing failed ({error_type})", context_summary="")` (satisfies ROUTING-04)
    - Logs routing decision via `logger.info(json.dumps({...}))` with fields: event, route, reasoning, message_count, has_previous_code, thread_id (from state file_id) (satisfies ROUTING-08)
    - Emits `routing_decided` event via stream writer with route name and user-friendly message
    - Routes via Command:
      - MEMORY_SUFFICIENT: `Command(goto="data_analysis", update={"routing_decision": decision})`
      - CODE_MODIFICATION: `Command(goto="coding_agent", update={"routing_decision": decision, "previous_code": previous_code or ""})`
      - NEW_ANALYSIS: `Command(goto="coding_agent", update={"routing_decision": decision, "previous_code": ""})`

    Use logger: `logger = logging.getLogger("spectra.routing")`

    Import from existing modules:
    - `from app.agents.config import get_agent_prompt, get_agent_max_tokens, get_agent_provider, get_agent_model, get_agent_temperature, get_api_key_for_provider`
    - `from app.agents.llm_factory import get_llm`
    - `from app.agents.state import ChatAgentState`
    - `from app.config import get_settings`
    - `from langgraph.types import Command`
    - `from langgraph.config import get_stream_writer`
    - `from langchain_core.messages import HumanMessage, SystemMessage`
    - `from pydantic import BaseModel, Field`

    **2. Update `backend/app/agents/state.py`:**

    Add two new fields to `ChatAgentState` TypedDict:

    ```python
    routing_decision: RoutingDecision | None
    """Manager Agent's routing decision (route, reasoning, context_summary). None on first entry."""

    previous_code: str
    """Previously generated code passed by Manager for CODE_MODIFICATION mode."""
    ```

    Import `RoutingDecision` at the top. Use `from __future__ import annotations` to handle forward references if needed, OR import directly since manager.py will be created first. Actually, to avoid circular imports, define RoutingDecision in state.py OR use a string annotation. Best approach: define RoutingDecision in manager.py and use `Any` type annotation in state.py (TypedDict doesn't enforce at runtime), or define RoutingDecision in a separate types location. Simplest: define RoutingDecision directly in `state.py` since it's a simple Pydantic model used by both manager.py and other agents. Then import it in manager.py from state.py.

    **3. Update `backend/app/config/prompts.yaml`:**

    Add `manager` entry to the `agents` section (after the existing `data_analysis` entry):

    ```yaml
      manager:
        provider: anthropic
        model: claude-sonnet-4-20250514
        temperature: 0.0
        routing_context_messages: 10
        system_prompt: |
          You are a Query Router for the Spectra data analysis platform.

          Your job is to analyze the user's query and conversation context to determine
          the most efficient processing route:

          1. **MEMORY_SUFFICIENT** - Query can be answered from conversation history alone
             - User asking for clarification, explanation, or recalling previous results
             - No new code generation or data processing needed
             - Examples: "What were the columns?", "Explain that result", "What was the average?"

          2. **CODE_MODIFICATION** - Query requires modifying existing code
             - Previous code exists and user wants to extend/filter/sort it
             - Base logic stays the same, only parameters or transformations change
             - Examples: "Add a column", "Filter by X", "Sort by date", "Show top 10"

          3. **NEW_ANALYSIS** - Query requires completely new code generation
             - First query in conversation, or completely different analysis
             - No relevant previous code to build upon
             - Examples: "Show sales by region", "Calculate correlation", "Start fresh"

          **Rules:**
          - Choose the FASTEST route that correctly answers the query
          - When in doubt, choose NEW_ANALYSIS (safest fallback)
          - If no previous code exists, NEVER choose CODE_MODIFICATION
          - MEMORY_SUFFICIENT should only be used when NO code execution is needed
        max_tokens: 500
    ```
  </action>
  <verify>
    1. `python -c "from app.agents.manager import manager_node, RoutingDecision; print('Import OK')"` (run from backend dir)
    2. `python -c "from app.agents.state import ChatAgentState; print(ChatAgentState.__annotations__)"` shows routing_decision and previous_code fields
    3. `python -c "import yaml; d=yaml.safe_load(open('app/config/prompts.yaml')); print('manager' in d['agents'])"` returns True
    4. Verify RoutingDecision model: `python -c "from app.agents.state import RoutingDecision; r=RoutingDecision(route='NEW_ANALYSIS', reasoning='test', context_summary='test'); print(r.route)"`
  </verify>
  <done>
    Manager Agent module exists with manager_node function, RoutingDecision Pydantic model defined and importable, ChatAgentState has routing_decision and previous_code fields, prompts.yaml contains manager agent config with system prompt and routing_context_messages setting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Manager Agent into graph and update agent service</name>
  <files>
    backend/app/agents/graph.py
    backend/app/services/agent_service.py
  </files>
  <action>
    **1. Update `backend/app/agents/graph.py`:**

    - Add import: `from app.agents.manager import manager_node`
    - In `build_chat_graph()`:
      - Add manager node: `graph.add_node("manager", manager_node)` (before existing nodes)
      - Change entry point from `graph.set_entry_point("coding_agent")` to `graph.set_entry_point("manager")`
      - Keep ALL existing edges unchanged (coding_agent -> code_checker, execute -> data_analysis, finish points)
      - Do NOT add explicit edges from manager — it routes via Command (same pattern as code_checker_node)
    - Update docstring to reflect new flow: Manager Agent -> (routes to) Coding Agent or Data Analysis Agent
    - Reset cached graph: ensure `_cached_graph = None` is at module level (it already is, but verify no caching issues)

    **2. Update `backend/app/services/agent_service.py`:**

    In both `run_chat_query()` and `run_chat_query_stream()`:
    - Add `routing_decision: None` and `previous_code: ""` to the `initial_state` dict (alongside existing empty fields like `generated_code: ""`)
    - These must be initialized so the state schema is complete when the graph starts

    In `run_chat_query_stream()`, update the `node_complete` event filter:
    - Add `"routing_decision"` to the allowed keys in the dict comprehension filter so the frontend can see the routing decision:
      ```python
      if k in ("generated_code", "execution_result",
               "analysis", "final_response", "error",
               "routing_decision")
      ```
    - Note: routing_decision is a Pydantic model, so serialize it: before yielding, convert routing_decision to dict if present: `if "routing_decision" in update and update["routing_decision"]: update["routing_decision"] = update["routing_decision"].model_dump()`

    Also in `run_chat_query_stream()`, the metadata_json saved to chat history should include the routing decision:
    - Add `"routing_decision": final_state.get("routing_decision", {})` to the metadata_json dict in the assistant message save block. If it's a Pydantic model, call `.model_dump()` first; if it's already a dict (from earlier serialization), use as-is. Guard with: `rd = final_state.get("routing_decision"); routing_meta = rd.model_dump() if hasattr(rd, 'model_dump') else (rd or {})`
  </action>
  <verify>
    1. `python -c "from app.agents.graph import build_chat_graph; g = build_chat_graph(); print(g.get_graph().nodes)"` shows manager node in graph
    2. `python -c "from app.agents.graph import build_chat_graph; g = build_chat_graph(); print([n.id for n in g.get_graph().nodes.values()])"` shows manager as a node
    3. Server starts without errors: `cd backend && timeout 10 python -c "from app.main import app; print('App OK')"` (basic import test)
    4. Verify initial_state includes new fields: grep for `routing_decision` in agent_service.py
  </verify>
  <done>
    Graph entry point is manager node. Manager routes via Command to data_analysis (MEMORY_SUFFICIENT) or coding_agent (CODE_MODIFICATION, NEW_ANALYSIS). Agent service initializes routing_decision and previous_code in initial_state. Routing decision is included in stream events and chat history metadata.
  </done>
</task>

</tasks>

<verification>
1. Manager Agent module imports cleanly with no circular dependencies
2. RoutingDecision Pydantic model validates all three route values
3. Graph topology shows: manager -> (Command) -> data_analysis | coding_agent -> code_checker -> execute -> data_analysis -> END, halt -> END
4. prompts.yaml has manager agent config with provider, model, temperature, system_prompt, max_tokens, routing_context_messages
5. Agent service initializes routing_decision and previous_code in initial_state
6. Stream events include routing_decision from manager node
7. No existing tests break (if any exist for graph structure)
</verification>

<success_criteria>
- Manager Agent is the graph entry point and classifies queries using structured LLM output
- Three routes (MEMORY_SUFFICIENT, CODE_MODIFICATION, NEW_ANALYSIS) are supported via Command routing
- Fallback to NEW_ANALYSIS on any routing failure (ROUTING-04)
- Routing decisions logged with reasoning (ROUTING-08)
- Manager Agent uses configurable LLM from YAML (ROUTING-02)
- Last 10 messages analyzed for context (ROUTING-03, configurable)
- Single-route decision logic (ROUTING-10)
- Architecture supports future override commands via routing_decision state field (ROUTING-09)
</success_criteria>

<output>
After completion, create `.planning/phases/09-manager-agent-with-intelligent-query-routing/09-01-SUMMARY.md`
</output>
