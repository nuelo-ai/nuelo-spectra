---
phase: 36-dokploy-deployment-and-deployment-md
task: 1
total_tasks: 3
plan: 36-02
status: in_progress
last_updated: 2026-02-19T20:35:03.658Z
---

<current_state>
Phase 36 execution in progress. Plan 36-01 is complete (public backend deployed). Plan 36-02 is in progress — Task 1 (admin backend deployment) just completed. The user confirmed admin backend health check returns 200 via Tailscale. Now need to present Task 2 (deploy both frontends) and Task 3 (verify split-horizon routing).

Executing via `/gsd:execute-phase 36` orchestrator with wave-based execution. Currently in Wave 2.
</current_state>

<completed_work>

## Plan 36-01: Prerequisites + Public Backend Deployment (COMPLETE)
- Task 1: Created Dokploy PostgreSQL database (spectra-db), generated SECRET_KEY, constructed asyncpg DATABASE_URL — Done
- Task 2: Installed Tailscale on Dokploy host (IP: 100.115.125.119), configured iptables DOCKER-USER rules blocking ports 8001 and 3001 — Done
- Task 3: Deployed spectra-public-backend to Dokploy with spectra-uploads volume at /app/uploads, Alembic migrations applied, health returning 200 — Done
- Task 4: Verified all foundational components — Done
- SUMMARY.md created, STATE.md and ROADMAP.md updated, committed as `63365c0`

## Plan 36-02: Admin Backend + Both Frontends (IN PROGRESS)
- Task 1: Deployed spectra-admin-backend with SPECTRA_MODE=admin, published port 8001 (Ingress mode), no domain assigned. Health check confirmed: `{"status":"healthy","version":"0.1.0"}` via `http://100.115.125.119:8001/health` — Done
- Task 2: Deploy public frontend and admin frontend — NOT STARTED
- Task 3: Verify split-horizon routing end-to-end — NOT STARTED
</completed_work>

<remaining_work>

## Plan 36-02 (current)
- Task 2: Deploy both frontends in Dokploy
  - Public frontend: Dockerfile.frontend, branch develop, BACKEND_URL needs to point to public backend container (or use Traefik internal URL)
  - Admin frontend: Dockerfile.admin, branch develop, BACKEND_URL=http://100.115.125.119:8001, published port 3001 (Ingress), no domain
- Task 3: Verify split-horizon routing — 5 tests confirming public/admin isolation

## Plan 36-03: HTTPS Domains, Smoke Test, DEPLOYMENT.md
- Task 1: Configure DNS A records and add HTTPS domains in Dokploy (Let's Encrypt)
- Task 2: Full production smoke test (8 tests)
- Task 3: Write DEPLOYMENT.md (auto task — code generation)
- Task 4: Final verification and DEPLOYMENT.md review

## After all plans
- Phase verification (gsd-verifier)
- Mark phase/milestone complete
</remaining_work>

<decisions_made>

- iptables DOCKER-USER chain instead of VPS cloud firewall — Hostinger VPS doesn't have robust cloud firewall; Docker bypasses UFW entirely
- Tailscale subnet routing NOT needed — dokploy-network is Swarm overlay (VXLAN), container IPs not routable; published host ports via Tailscale IP are the access pattern
- Dokploy branch set to `develop` (not master) — test deployment before final merge
- Admin backend port mode: Ingress (not Host) — Host mode causes Swarm update issues requiring manual stop/start on redeploy
- Committed prompts.yaml change (title_generator switched from ollama to openrouter gpt-4o-mini) as `47f20b5`
- Admin backend does NOT need spectra-uploads volume (only public backend handles file uploads)
</decisions_made>

<blockers>
- None currently. All infrastructure prerequisites resolved.
</blockers>

<context>
The orchestrator is running `/gsd:execute-phase 36` with 3 plans across 3 waves (sequential, each plan depends on the previous). All tasks are human-action or human-verify checkpoints — this is a deployment phase, not a code phase. The user performs manual steps in Dokploy UI and on the server, then confirms completion.

Key infrastructure facts for resumption:
- Tailscale IP: 100.115.125.119
- Public backend container: nuelo-spectra-publicbackend-ltiq2m
- Admin backend container: check with `docker ps | grep admin-backend`
- spectra-db container: nuelo-spectra-spectradb-5bsjfd
- Dokploy uses Docker Swarm (overlay network, not bridge)
- Git remote `develop` branch is up to date

For the public frontend BACKEND_URL, need to determine how to route to the public backend. Options:
1. Use the internal Docker overlay IP (may not work reliably with Swarm)
2. Use the Dokploy Traefik internal URL
3. Wait for HTTPS domain (plan 36-03) and use that

This needs to be figured out when resuming Task 2.
</context>

<next_action>
Resume `/gsd:execute-phase 36` — continue from Plan 36-02, Task 2 (deploy both frontends).

Present the checkpoint for Task 2 to the user:
- Public frontend: Create Dokploy app with Dockerfile.frontend, branch develop, determine BACKEND_URL for public backend
- Admin frontend: Create Dokploy app with Dockerfile.admin, branch develop, BACKEND_URL=http://100.115.125.119:8001, published port 3001, no domain

After Task 2, run Task 3 (split-horizon verification), then create 36-02-SUMMARY.md and proceed to Wave 3 (Plan 36-03).
</next_action>
