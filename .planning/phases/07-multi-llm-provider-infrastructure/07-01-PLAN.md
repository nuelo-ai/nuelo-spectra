---
phase: 07-multi-llm-provider-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/config.py
  - backend/app/config/llm_providers.yaml
  - backend/app/config/prompts.yaml
  - backend/app/agents/llm_factory.py
  - backend/app/agents/config.py
autonomous: true

must_haves:
  truths:
    - "System recognizes 5 LLM providers: anthropic, openai, google, ollama, openrouter"
    - "Each agent in prompts.yaml can specify provider and model independently"
    - "Agents without explicit provider/model fall back to the registry default"
    - "Ollama provider accepts base_url for local or remote deployment"
    - "OpenRouter provider routes through OpenAI-compatible API at openrouter.ai"
  artifacts:
    - path: "backend/app/config/llm_providers.yaml"
      provides: "Central provider registry with 5 providers and default flag"
      contains: "default: true"
    - path: "backend/app/config/prompts.yaml"
      provides: "Per-agent provider and model fields for all 4 agents"
      contains: "provider:"
    - path: "backend/app/agents/llm_factory.py"
      provides: "Factory supporting ollama and openrouter providers"
      contains: "ChatOllama"
    - path: "backend/app/agents/config.py"
      provides: "Per-agent provider/model loading and registry functions"
      exports: ["get_agent_provider", "get_agent_model", "load_provider_registry", "get_default_provider"]
    - path: "backend/app/config.py"
      provides: "Settings with ollama_base_url and openrouter_api_key"
      contains: "ollama_base_url"
  key_links:
    - from: "backend/app/agents/config.py"
      to: "backend/app/config/llm_providers.yaml"
      via: "load_provider_registry() reads YAML"
      pattern: "llm_providers\\.yaml"
    - from: "backend/app/agents/config.py"
      to: "backend/app/config/prompts.yaml"
      via: "get_agent_provider() reads provider field from prompts"
      pattern: "get_agent_provider"
    - from: "backend/app/agents/llm_factory.py"
      to: "langchain_ollama"
      via: "lazy import for ollama provider"
      pattern: "from langchain_ollama import ChatOllama"
---

<objective>
Create the multi-LLM provider infrastructure foundation: central provider registry, per-agent YAML configuration, extended LLM factory with Ollama and OpenRouter support, and config loading functions.

Purpose: Establish the configuration layer that all agents and validation systems will build upon. This plan creates the data structures and factories; subsequent plans wire agents and add validation.
Output: Provider registry YAML, extended prompts.yaml, updated llm_factory.py, extended config.py (both Settings and agents/config.py)
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multi-llm-provider-infrastructure/07-CONTEXT.md
@.planning/phases/07-multi-llm-provider-infrastructure/07-RESEARCH.md
@.planning/phases/03-ai-agents---orchestration/03-01-SUMMARY.md

Key existing files to reference:
@backend/app/config.py
@backend/app/agents/llm_factory.py
@backend/app/agents/config.py
@backend/app/config/prompts.yaml
@backend/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create provider registry and extend Settings</name>
  <files>
    backend/app/config/llm_providers.yaml
    backend/app/config.py
    backend/pyproject.toml
  </files>
  <action>
    **1. Create `backend/app/config/llm_providers.yaml`** — central provider registry:
    ```yaml
    providers:
      anthropic:
        type: anthropic
        default: true
        # API key from env: ANTHROPIC_API_KEY

      google:
        type: google
        # API key from env: GOOGLE_API_KEY

      openai:
        type: openai
        # API key from env: OPENAI_API_KEY

      ollama:
        type: ollama
        base_url_env: OLLAMA_BASE_URL
        # No API key needed (local/remote Ollama)

      openrouter:
        type: openrouter
        base_url: https://openrouter.ai/api/v1
        # API key from env: OPENROUTER_API_KEY
    ```

    Per user decision: Anthropic is the default provider. Each entry has `type` and optionally `base_url_env` (for env var resolution) or `base_url` (fixed endpoint). Comments document which env var provides credentials.

    **2. Extend `backend/app/config.py` Settings class:**
    Add two new fields after existing LLM Provider section:
    - `ollama_base_url: str = "http://localhost:11434"` — defaults to local Ollama
    - `openrouter_api_key: str = ""` — empty string default (optional provider)

    These env vars are referenced by the provider registry and used by `llm_factory.py`.

    **3. Add `langchain-ollama` to `backend/pyproject.toml`:**
    Add `"langchain-ollama>=0.2.0"` to the dependencies list (after `langchain-google-genai`).

    Do NOT install structlog — use Python's standard `logging` module (existing pattern). Structured logging will use `json.dumps()` in Plan 03.

    Run `cd backend && uv pip install langchain-ollama` to install the new dependency.
  </action>
  <verify>
    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run python -c "from app.config import get_settings; s = get_settings(); print(f'ollama_base_url={s.ollama_base_url}'); print(f'openrouter_api_key={repr(s.openrouter_api_key)}')"` — should print default values.

    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run python -c "import yaml; data = yaml.safe_load(open('app/config/llm_providers.yaml')); providers = list(data['providers'].keys()); print(f'providers={providers}'); default = [k for k,v in data['providers'].items() if v.get('default')]; print(f'default={default}')"` — should show 5 providers and anthropic as default.

    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run python -c "from langchain_ollama import ChatOllama; print('langchain-ollama installed successfully')"` — should import without error.
  </verify>
  <done>
    Provider registry YAML exists at `backend/app/config/llm_providers.yaml` with 5 providers (anthropic marked default). Settings has `ollama_base_url` and `openrouter_api_key` fields. `langchain-ollama` is installed and importable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend LLM factory and per-agent config loading</name>
  <files>
    backend/app/agents/llm_factory.py
    backend/app/agents/config.py
    backend/app/config/prompts.yaml
  </files>
  <action>
    **1. Extend `backend/app/agents/llm_factory.py`** with Ollama and OpenRouter:

    Add two new `elif` branches to `get_llm()`:

    - **Ollama:** `elif provider == "ollama":` — lazy import `from langchain_ollama import ChatOllama`. Create with `model=model`, `base_url=kwargs.pop("base_url", None)` (pop to avoid passing as extra kwarg), and remaining `**kwargs`. Ollama does not need an `api_key` parameter — ignore it if passed.
    - **OpenRouter:** `elif provider == "openrouter":` — reuse `from langchain_openai import ChatOpenAI` (already available). Create with `model=model`, `api_key=api_key`, `base_url="https://openrouter.ai/api/v1"`, and `**kwargs`. Add default headers for attribution: `default_headers={"HTTP-Referer": "https://spectra.app", "X-Title": "Spectra"}`.

    Update the `ValueError` message to list all 5 providers. Update docstring examples to include Ollama and OpenRouter.

    **2. Extend `backend/app/agents/config.py`** with new functions:

    Add `load_provider_registry()` — LRU-cached function that reads `llm_providers.yaml` from the config directory. Same pattern as `load_prompts()`.

    Add `get_default_provider()` — reads registry, returns the name of the provider with `default: true`. Raises `ValueError` if no default found.

    Add `get_agent_provider(agent_name: str) -> str` — reads the `provider` field from the agent's config in `prompts.yaml`. If no `provider` field, falls back to `get_default_provider()`.

    Add `get_agent_model(agent_name: str) -> str` — reads the `model` field from the agent's config in `prompts.yaml`. Raises `KeyError` with clear message if agent has no `model` field.

    **3. Add `provider` and `model` fields to `backend/app/config/prompts.yaml`:**

    Add to each agent's config (maintaining Sonnet 4.0 as default per user decision LLM-05):
    ```yaml
    agents:
      onboarding:
        provider: anthropic
        model: claude-sonnet-4-20250514
        system_prompt: |
          ...existing prompt...
        max_tokens: 1500

      coding:
        provider: anthropic
        model: claude-sonnet-4-20250514
        system_prompt: |
          ...existing prompt...
        max_tokens: 10000

      code_checker:
        provider: anthropic
        model: claude-sonnet-4-20250514
        system_prompt: |
          ...existing prompt...
        max_tokens: 500

      data_analysis:
        provider: anthropic
        model: claude-sonnet-4-20250514
        system_prompt: |
          ...existing prompt...
        max_tokens: 2000
    ```

    Add YAML comments explaining that `provider` and `model` are optional (fall back to default from `llm_providers.yaml`), and showing OpenRouter naming example (`provider: openrouter, model: anthropic/claude-3.5-sonnet`).
  </action>
  <verify>
    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run python -c "
from app.agents.config import get_agent_provider, get_agent_model, get_default_provider, load_provider_registry
print(f'default_provider={get_default_provider()}')
for agent in ['onboarding', 'coding', 'code_checker', 'data_analysis']:
    print(f'{agent}: provider={get_agent_provider(agent)}, model={get_agent_model(agent)}')
"` — should print anthropic as default and each agent's provider/model.

    Run: `cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/backend && uv run python -c "
from app.agents.llm_factory import get_llm
# Test Ollama factory (won't connect but should instantiate)
try:
    llm = get_llm('ollama', 'llama3.1', '', base_url='http://localhost:11434')
    print(f'ollama: {type(llm).__name__}')
except Exception as e:
    print(f'ollama error: {e}')

# Test OpenRouter factory (won't connect but should instantiate)
try:
    llm = get_llm('openrouter', 'anthropic/claude-3.5-sonnet', 'test-key')
    print(f'openrouter: {type(llm).__name__}')
except Exception as e:
    print(f'openrouter error: {e}')
"` — should print ChatOllama and ChatOpenAI class names.
  </verify>
  <done>
    LLM factory supports 5 providers (anthropic, openai, google, ollama, openrouter). Config module provides `get_agent_provider()`, `get_agent_model()`, `get_default_provider()`, `load_provider_registry()`. All 4 agents in `prompts.yaml` have explicit `provider: anthropic` and `model: claude-sonnet-4-20250514` fields (maintaining current behavior).
  </done>
</task>

</tasks>

<verification>
1. All 5 providers listed in `llm_providers.yaml` with anthropic as default
2. `prompts.yaml` has provider/model for all 4 agents
3. `get_llm()` factory creates instances for all 5 providers without errors
4. `get_agent_provider()` and `get_agent_model()` return correct values for all agents
5. `get_default_provider()` returns "anthropic"
6. `langchain-ollama` installed and importable
7. Settings has `ollama_base_url` and `openrouter_api_key` fields
8. No existing tests broken (run `uv run pytest` if tests exist)
</verification>

<success_criteria>
- Provider registry YAML created with 5 providers
- LLM factory extended with Ollama (ChatOllama) and OpenRouter (ChatOpenAI with base_url)
- Per-agent config functions working (provider, model, default fallback)
- prompts.yaml updated with provider/model for all agents (anthropic/sonnet-4 default)
- Settings extended with new env vars
- langchain-ollama dependency installed
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-llm-provider-infrastructure/07-01-SUMMARY.md`
</output>
