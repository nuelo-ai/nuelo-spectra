---
phase: 43-collections-reports
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - pulse-mockup/src/app/workspace/collections/[id]/reports/[reportId]/page.tsx
autonomous: true
requirements: [COLL-03, COLL-04, COLL-05]

must_haves:
  truths:
    - "Clicking 'View Report' from the Reports tab navigates to a full-page report reader"
    - "Report reader displays a paper-white document centered against the dark app shell"
    - "Report content renders as formatted markdown (headings, paragraphs, tables, bold/italic)"
    - "A sticky header bar remains visible while scrolling, showing the report title and download buttons"
    - "Two download buttons are present: 'Download as Markdown' and 'Download as PDF'"
    - "Clicking Download as Markdown triggers a browser file download of the report content"
    - "Download as PDF button is present (static mock — shows a toast or no-op, no actual PDF generation needed)"
  artifacts:
    - path: "pulse-mockup/src/app/workspace/collections/[id]/reports/[reportId]/page.tsx"
      provides: "Full-page document-style report reader with sticky header and download buttons"
      contains: "markdownContent"
  key_links:
    - from: "pulse-mockup/src/app/workspace/collections/[id]/reports/[reportId]/page.tsx"
      to: "pulse-mockup/src/lib/mock-data.ts"
      via: "imports MOCK_REPORTS, finds report by reportId param"
      pattern: "MOCK_REPORTS"
    - from: "sticky header bar"
      to: "download handler"
      via: "onClick triggers Blob download or toast"
      pattern: "Download|download"
---

<objective>
Build the full-page document-style report reader at `/workspace/collections/[id]/reports/[reportId]`. The reader renders the report's markdown content with proper typography on a paper-white document panel centered against the dark app shell. A sticky header bar at the top always shows the report title plus "Download as Markdown" and "Download as PDF" buttons. Download as Markdown triggers a real browser file download; Download as PDF shows a disabled state or no-op for this static mockup.

Purpose: Delivers COLL-03 (report list + in-page reader) and COLL-04 (download options) — the full report reading experience.

Output: Single new route file for the report reader page.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-collections-reports/43-CONTEXT.md

@pulse-mockup/src/lib/mock-data.ts
@pulse-mockup/src/components/layout/header.tsx
@pulse-mockup/src/app/workspace/layout.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From pulse-mockup/src/lib/mock-data.ts (after Plan 43-01 adds these):
```typescript
export type ReportType = "Detection Summary" | "Investigation Report" | "What-If Scenario Report" | "Chat Report" | "Custom Report";
export interface Report {
  id: string;
  collectionId: string;
  type: ReportType;
  title: string;
  generatedAt: string;
  markdownContent: string;
}
export const MOCK_REPORTS: Report[];
```

Design system:
- Dark app shell: #0a0e1a background, #111827 cards, #1e293b borders
- Report document: paper-white background (bg-white text-gray-900) to visually stand out
- Sticky header: bg-[#111827] border-b border-[#1e293b] backdrop-blur-md
- The workspace layout already provides the sidebar + top header shell. This page renders WITHIN the workspace layout, so it gets the shell automatically. No need to re-render sidebar.

Markdown rendering: Do NOT install react-markdown or any new packages. Instead, render a simplified markdown-to-HTML conversion inline using a helper function that handles:
- # Heading 1 -> <h1>
- ## Heading 2 -> <h2>
- ### Heading 3 -> <h3>
- **bold** -> <strong>
- *italic* -> <em>
- | table | -> <table> with <tr><td> (detect lines starting with |)
- --- -> <hr>
- - list item -> <li> in <ul>
- Remaining lines -> <p>
Wrap the rendered HTML using `dangerouslySetInnerHTML`. Use a custom CSS class `prose-report` or inline Tailwind prose-like styles on the container.

Alternatively, use the `prose` typography from Tailwind if @tailwindcss/typography is already installed. Check package.json first — if @tailwindcss/typography is in dependencies, use `className="prose prose-slate max-w-none"` on the content container. If not installed, implement the simple markdown parser helper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build full-page report reader with sticky header and download actions</name>
  <files>pulse-mockup/src/app/workspace/collections/[id]/reports/[reportId]/page.tsx</files>
  <action>
Create the directory `pulse-mockup/src/app/workspace/collections/[id]/reports/[reportId]/` and the `page.tsx` file.

**Component structure:**

```typescript
"use client";

import { useParams } from "next/navigation";
import Link from "next/link";
import { ArrowLeft, Download, FileText } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { MOCK_REPORTS } from "@/lib/mock-data";
```

The component reads `useParams()` to get `id` (collectionId) and `reportId`. It finds the report: `const report = MOCK_REPORTS.find(r => r.id === reportId) ?? MOCK_REPORTS[0]`.

**Layout structure:**

```
<div className="relative min-h-screen">
  {/* Sticky header bar */}
  <div className="sticky top-0 z-30 flex items-center justify-between px-6 py-3 bg-[#111827] border-b border-[#1e293b] backdrop-blur-md">
    <div className="flex items-center gap-3">
      <Link href={`/workspace/collections/${id}`}>
        <Button variant="ghost" size="sm" className="gap-1.5">
          <ArrowLeft className="h-4 w-4" /> Back
        </Button>
      </Link>
      <div className="h-4 w-px bg-border" /> {/* divider */}
      <FileText className="h-4 w-4 text-muted-foreground" />
      <span className="text-sm font-medium truncate max-w-xs">{report.title}</span>
    </div>
    <div className="flex items-center gap-2">
      <Button variant="outline" size="sm" className="gap-1.5" onClick={handleDownloadMarkdown}>
        <Download className="h-3.5 w-3.5" /> Download as Markdown
      </Button>
      <Button variant="outline" size="sm" className="gap-1.5 opacity-60 cursor-not-allowed" disabled>
        <Download className="h-3.5 w-3.5" /> Download as PDF
      </Button>
    </div>
  </div>

  {/* Document paper area */}
  <div className="flex justify-center py-10 px-4 bg-[#0a0e1a] min-h-screen">
    <div className="w-full max-w-3xl bg-white rounded-lg shadow-2xl shadow-black/40 px-16 py-12 text-gray-900">
      {/* Report metadata header within the paper */}
      <div className="mb-8 pb-6 border-b border-gray-200">
        <Badge className="mb-3 bg-blue-100 text-blue-700 border-0 text-xs">{report.type}</Badge>
        <h1 className="text-3xl font-bold text-gray-900 mb-2">{report.title}</h1>
        <p className="text-sm text-gray-500">Generated {report.generatedAt}</p>
      </div>
      {/* Markdown content */}
      <div className="report-content">
        {renderMarkdown(report.markdownContent)}
      </div>
    </div>
  </div>
</div>
```

**Download handler:**

```typescript
const handleDownloadMarkdown = () => {
  const blob = new Blob([report.markdownContent], { type: "text/markdown" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = `${report.title.replace(/\s+/g, "-").toLowerCase()}.md`;
  a.click();
  URL.revokeObjectURL(url);
};
```

**Markdown renderer (inline helper function):**

Check if `@tailwindcss/typography` is in `pulse-mockup/package.json`. If yes, use `dangerouslySetInnerHTML` with the markdown converted using a simple regex approach and wrap in `<div className="prose prose-slate max-w-none">`.

If typography plugin is not installed, implement `renderMarkdown(content: string): React.ReactNode` that:
1. Splits content by `\n`
2. Identifies heading lines (`# `, `## `, `### `), table lines (starting with `|`), list items (`- `), horizontal rules (`---`), empty lines
3. Groups consecutive table rows into a `<table>` element with `<thead>` (first row) and `<tbody>` (remaining rows), skipping separator rows (`|---|`)
4. Returns a React fragment with rendered elements
5. Applies inline Tailwind classes for typography:
   - h1: `text-2xl font-bold mt-8 mb-4 text-gray-900`
   - h2: `text-xl font-semibold mt-6 mb-3 text-gray-800`
   - h3: `text-lg font-medium mt-4 mb-2 text-gray-800`
   - p: `mb-4 text-gray-700 leading-relaxed`
   - ul: `mb-4 ml-6 list-disc space-y-1`
   - li: `text-gray-700`
   - hr: `my-6 border-gray-200`
   - table: `w-full mb-4 text-sm border-collapse`
   - th: `border border-gray-300 px-3 py-2 bg-gray-50 font-semibold text-left text-gray-700`
   - td: `border border-gray-300 px-3 py-2 text-gray-700`
   - Strong (**text**): parse inline using regex and render `<strong className="font-semibold">`
  </action>
  <verify>
    <automated>cd /Users/marwazisiagian/Documents/ms-dev/spectra-project/spectra-dev/pulse-mockup && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Visiting http://localhost:3000/workspace/collections/col-001/reports/rep-001 shows a paper-white document centered on the dark background. Sticky header bar is visible with "Back", the report title, "Download as Markdown" button (functional), and "Download as PDF" button (disabled/muted). Report content renders with proper headings, paragraphs, and table formatting. Clicking Download as Markdown downloads a .md file. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Route `/workspace/collections/col-001/reports/rep-001` loads without 404
- Paper-white document visible against dark app shell background
- Sticky header stays fixed while scrolling (test with long content)
- "Download as Markdown" button triggers file download with .md extension
- "Download as PDF" button is present but visually disabled (opacity-60, cursor-not-allowed, disabled attr)
- Report title and type badge visible in both the sticky header and the document itself
- Markdown content renders formatted: headings, bold text, table with borders, list items, horizontal rules
</verification>

<success_criteria>
- Full-page report reader exists at `/workspace/collections/[id]/reports/[reportId]`
- Document-style: paper white (bg-white) panel centered on dark (#0a0e1a) background with shadow
- Sticky header: Back link, report title, two download buttons always visible
- Download as Markdown: triggers real browser download of .md file
- Download as PDF: present but disabled (static mockup)
- Markdown content rendered with proper typography (headings, tables, lists, bold)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/43-collections-reports/43-02-SUMMARY.md`
</output>
