---
phase: 11-web-search-tool-integration
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - frontend/src/components/chat/ChatInput.tsx
  - frontend/src/components/chat/DataCard.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/hooks/useSearchToggle.ts
  - frontend/src/hooks/useSSEStream.ts
  - frontend/src/types/chat.ts
  - frontend/src/lib/api.ts
autonomous: false

must_haves:
  truths:
    - "User sees a search toggle below the chat input area"
    - "Toggle is grayed out with tooltip when search is not configured (no API key)"
    - "Toggle defaults to OFF and resets to OFF after each query"
    - "User sees real-time search activity indicator showing actual query text while searching"
    - "DataCard displays Sources section with clickable links at the end of response"
    - "Toggle auto-disables with message when daily quota is exceeded"
    - "Search works end-to-end: toggle ON -> send query -> see search activity -> see sources in DataCard"
  artifacts:
    - path: "frontend/src/hooks/useSearchToggle.ts"
      provides: "Search toggle state management hook"
      contains: "useSearchToggle"
    - path: "frontend/src/components/chat/ChatInput.tsx"
      provides: "Chat input with search toggle"
      contains: "Switch"
    - path: "frontend/src/components/chat/DataCard.tsx"
      provides: "DataCard with Sources section"
      contains: "searchSources"
    - path: "frontend/src/types/chat.ts"
      provides: "Search event types"
      contains: "search_started"
  key_links:
    - from: "frontend/src/hooks/useSearchToggle.ts"
      to: "backend/app/routers/search.py"
      via: "GET /search/config API call"
      pattern: "/search/config"
    - from: "frontend/src/components/chat/ChatInterface.tsx"
      to: "backend/app/routers/chat.py"
      via: "web_search_enabled in POST body"
      pattern: "web_search_enabled"
    - from: "frontend/src/hooks/useSSEStream.ts"
      to: "backend SSE events"
      via: "search_started event handling"
      pattern: "search_started"
---

<objective>
Build the frontend search UI: search toggle below chat input, real-time search activity indicators, DataCard sources section, and end-to-end integration with ChatInterface. Then verify the complete web search feature works.

Purpose: Connect the user-facing controls to the backend search infrastructure built in Plans 01-02. This delivers the visible user experience: toggling search, seeing search activity, and viewing source citations.

Output: Working search toggle in ChatInput, search activity indicators, DataCard Sources section, human-verified end-to-end flow.
</objective>

<execution_context>
@/Users/marwazisiagian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marwazisiagian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-web-search-tool-integration/11-RESEARCH.md
@.planning/phases/11-web-search-tool-integration/11-CONTEXT.md
@.planning/phases/11-web-search-tool-integration/11-01-SUMMARY.md
@.planning/phases/11-web-search-tool-integration/11-02-SUMMARY.md
@frontend/src/components/chat/ChatInput.tsx
@frontend/src/components/chat/DataCard.tsx
@frontend/src/components/chat/ChatInterface.tsx
@frontend/src/hooks/useSSEStream.ts
@frontend/src/types/chat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search toggle hook, ChatInput toggle, type definitions, and SSE events</name>
  <files>
    frontend/src/hooks/useSearchToggle.ts
    frontend/src/components/chat/ChatInput.tsx
    frontend/src/hooks/useSSEStream.ts
    frontend/src/types/chat.ts
    frontend/src/lib/api.ts
  </files>
  <action>
**1. Update `frontend/src/types/chat.ts`** — add search-related types:
- Add to StreamEventType (or equivalent type union):
  ```typescript
  search_started = "search_started"
  search_completed = "search_completed"
  search_failed = "search_failed"
  search_quota_exceeded = "search_quota_exceeded"
  ```
- Add `SearchSource` interface: `{ title: string; url: string }`
- Add `SearchConfig` interface: `{ configured: boolean; enabled: boolean; daily_quota: number; used_today: number; quota_exceeded: boolean }`

**2. Create `frontend/src/hooks/useSearchToggle.ts`** — toggle state management:
- `useSearchToggle()` hook returns `{ enabled, toggle, isConfigured, isQuotaExceeded, resetToggle, checkConfig }`.
- On mount, fetch `GET /search/config` (with auth token) to check if search is configured and quota status. Use the existing API utility pattern from the project.
- `enabled: boolean` — local toggle state, defaults to `false`.
- `toggle()` — toggles enabled. If `isQuotaExceeded`, do not allow enabling.
- `resetToggle()` — sets enabled back to `false` (called after each query send).
- `isConfigured: boolean` — from API response.
- `isQuotaExceeded: boolean` — from API response.
- `checkConfig()` — re-fetch config (called after quota might change).

**3. Update `frontend/src/components/chat/ChatInput.tsx`** — add search toggle:
- Import `Switch` from `@/components/ui/switch` (shadcn/ui — verify it exists, if not use a simple toggle button styled with Tailwind).
- Import `Globe` icon from `lucide-react` for the toggle label.
- Add new props: `searchEnabled?: boolean`, `onSearchToggle?: () => void`, `searchConfigured?: boolean`, `searchQuotaExceeded?: boolean`.
- Below the existing flex container with textarea and send button, add a new row:
  ```
  <div className="flex items-center gap-2 mt-2">
    <Switch
      checked={searchEnabled}
      onCheckedChange={onSearchToggle}
      disabled={!searchConfigured || searchQuotaExceeded || disabled}
      className="data-[state=unchecked]:bg-muted"
    />
    <label className="text-xs text-muted-foreground flex items-center gap-1">
      <Globe className="h-3 w-3" />
      Search web
    </label>
    {!searchConfigured && (
      <span className="text-xs text-muted-foreground/60 italic" title="Search not configured">
        (not configured)
      </span>
    )}
    {searchQuotaExceeded && (
      <span className="text-xs text-destructive italic">
        Search quota reached
      </span>
    )}
  </div>
  ```
- If Switch component doesn't exist, use a simple `<button>` with conditional styling (bg-primary when on, bg-muted when off, opacity-50 when disabled).

**4. Update `frontend/src/hooks/useSSEStream.ts`** — handle search events:
- In the event handling switch/if block, add handlers for:
  - `search_started`: Store the search query text for display. Add to a `searchActivities` array or emit to a callback.
  - `search_completed`: Clear the active search indicator.
  - `search_failed`: Show brief inline notice.
  - `search_quota_exceeded`: Emit quota exceeded signal.
- The hook should expose search-related state (e.g., `currentSearchQuery: string | null`, `searchSources: SearchSource[]`).
- When a `node_complete` event for `da_response` arrives with `search_sources`, store them for DataCard display.

**5. Update `frontend/src/lib/api.ts`** (or equivalent):
- Ensure the `POST /chat/{file_id}/stream` request body includes `web_search_enabled` field alongside `content`. This may require updating the fetch call in ChatInterface or the API utility.
  </action>
  <verify>
- `cd frontend && npx tsc --noEmit` — no TypeScript errors
- `cd frontend && npm run build` — builds successfully
- The Switch (or toggle button) renders in the ChatInput component
- Search config API call works when backend is running
  </verify>
  <done>
Search toggle renders below chat input. Toggle is disabled when not configured or quota exceeded. SSE stream handles search events. Search sources are captured from stream events. web_search_enabled is sent in the POST body.
  </done>
</task>

<task type="auto">
  <name>Task 2: DataCard sources section, ChatInterface integration, and search activity</name>
  <files>
    frontend/src/components/chat/DataCard.tsx
    frontend/src/components/chat/ChatInterface.tsx
  </files>
  <action>
**1. Update `frontend/src/components/chat/DataCard.tsx`** — add Sources section:
- Add new prop: `searchSources?: { title: string; url: string }[]`
- After the follow-up suggestions section (or after explanation if no follow-ups), add Sources section:
  ```tsx
  {/* Sources section */}
  {searchSources && searchSources.length > 0 && !isStreaming && (
    <div className="space-y-2 pt-3 border-t border-border/50">
      <h4 className="text-xs font-medium text-muted-foreground">Sources</h4>
      <div className="space-y-1">
        {searchSources.map((source, idx) => (
          <a
            key={idx}
            href={source.url}
            target="_blank"
            rel="noopener noreferrer"
            className="block text-xs text-primary hover:underline truncate"
            title={source.url}
          >
            {source.title || source.url}
          </a>
        ))}
      </div>
    </div>
  )}
  ```
- Styling: compact text-xs links, each on its own line, separated from follow-ups by border-t.
- Each source shows page title as clickable link (per CONTEXT.md: "page title as clickable link + URL, no snippet").

**2. Update `frontend/src/components/chat/ChatInterface.tsx`** — full integration:
- Import and use `useSearchToggle` hook.
- Pass search toggle props to ChatInput: `searchEnabled`, `onSearchToggle`, `searchConfigured`, `searchQuotaExceeded`.
- When sending a message:
  - Include `web_search_enabled: searchToggle.enabled` in the POST body to `/chat/{file_id}/stream`.
  - After sending, call `searchToggle.resetToggle()` to reset toggle to OFF (per CONTEXT.md: "toggle resets to OFF after each query").
- Handle search SSE events:
  - On `search_started`: Display a search activity indicator in the streaming message area. Show the actual search query text: "Searching: '{query}'..." with pulsing animation (reuse existing status indicator pattern). This can be a temporary element above or within the DataCard being streamed.
  - On `search_completed`: Clear the search activity indicator.
  - On `search_failed`: Show inline notice: "Web search unavailable -- answering from available data".
  - On `search_quota_exceeded`: Call `searchToggle.checkConfig()` to refresh quota status (toggle will auto-disable).
- Pass `searchSources` to DataCard:
  - From live stream events (node_complete from da_response), extract `search_sources` and pass to the current DataCard.
  - From historical messages loaded from API (metadata_json), extract `search_sources` and pass to DataCard.
- Search activity indicator design:
  - Show below the typing indicator or in the same status area.
  - Format: small Globe icon + "Searching: 'industry revenue benchmarks 2025'..." in text-xs text-muted-foreground with subtle pulsing animation.
  - Multiple searches show sequentially (replace text as each new search starts).
  </action>
  <verify>
- `cd frontend && npx tsc --noEmit` — no TypeScript errors
- `cd frontend && npm run build` — builds successfully
- DataCard renders Sources section when searchSources prop is provided
- ChatInterface passes web_search_enabled in stream request body
- Toggle resets to OFF after each query send
  </verify>
  <done>
DataCard shows Sources section with clickable links. ChatInterface integrates search toggle, sends web_search_enabled, handles search SSE events, displays search activity, and passes sources to DataCard. Toggle resets after each query.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end web search verification</name>
  <files>none</files>
  <action>
CHECKPOINT: Human verification of the complete Web Search Tool Integration feature.

**What was built:** Complete web search integration: search toggle below chat input, LangGraph bind_tools/ToolNode-based search via Serper.dev, real-time search activity indicators, source citations in DataCard, daily quota tracking per user, and graceful degradation on API failure.

**How to verify:**

Prerequisites: Ensure `SERPER_API_KEY` is set in backend `.env` file. Start backend (`cd backend && uvicorn app.main:app --reload`) and frontend (`cd frontend && npm run dev`).

**Test 1: Search toggle visibility and configuration**
1. Open the app, navigate to a file tab with chat
2. Verify search toggle appears below the chat input (small switch with Globe icon and "Search web" label)
3. If SERPER_API_KEY is not set: verify toggle is grayed out with "(not configured)" text
4. If SERPER_API_KEY is set: verify toggle is clickable

**Test 2: Web search end-to-end (SERPER_API_KEY required)**
1. Toggle "Search web" ON
2. Type a benchmarking query like: "How does our revenue compare to industry benchmarks?"
3. Send the message
4. Verify: search activity indicator appears showing "Searching: '...'" with the actual search query text
5. Verify: DataCard renders with Sources section at the bottom showing clickable links
6. Verify: analysis references external data from search results
7. Verify: toggle resets to OFF after the query completes

**Test 3: Toggle reset behavior**
1. Toggle search ON, send a query, verify toggle resets to OFF
2. Send another query WITHOUT toggling search back ON
3. Verify: no search activity, no sources in DataCard (agent should not search)

**Test 4: Memory mode with search toggle (if testable)**
1. Ask a follow-up question that should be answered from memory (e.g., "What columns did we analyze?")
2. Even if search toggle is ON, agent should NOT search for memory-sufficient queries
3. Verify: no search activity indicator appears

**Test 5: Graceful degradation**
1. If API key is invalid or Serper is down: verify inline error notice appears
2. Verify: analysis still completes using only uploaded data (no crash)
  </action>
  <verify>Human confirms all 5 test scenarios pass</verify>
  <done>Web search feature works end-to-end: toggle, search activity, sources, quota, graceful degradation all verified by human.</done>
</task>

</tasks>

<verification>
1. Frontend builds without errors: `cd frontend && npm run build`
2. Search toggle renders and is functional
3. web_search_enabled flows from toggle through API to backend
4. Search activity indicators appear during search
5. DataCard Sources section shows clickable links
6. Toggle resets after each query
7. Graceful degradation when API unavailable
8. Backend tests still pass: `cd backend && python -m pytest tests/ -v --timeout=30`
</verification>

<success_criteria>
- Search toggle visible below chat input with Globe icon
- Toggle grayed out when not configured, with tooltip
- Toggle defaults OFF, resets after each query
- Real-time "Searching: '...'" indicator during search
- DataCard Sources section with clickable page title links
- Toggle auto-disables when quota exceeded
- End-to-end flow: toggle ON -> query -> search activity -> sources in DataCard
- Human verified all test scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/11-web-search-tool-integration/11-03-SUMMARY.md`
</output>
