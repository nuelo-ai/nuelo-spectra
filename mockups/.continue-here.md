---
task: UI/UX Mockups for Analysis Workspace
status: in_progress
completed: 4 of 5 screens done + 1 revised
last_updated: 2026-03-02T00:58:48.665Z
---

<current_state>
Creating 5 standalone HTML mockup files in `mockups/` directory for the Analysis Workspace feature. All 5 screens are created. Screen 2 was redesigned based on owner feedback. Dark mode text contrast was fixed across all files. Owner has more feedback pending.
</current_state>

<completed_work>

- Screen 1: `01-collection-list.html` — DONE. Collection grid, sidebar, search/filter, empty state toggle. Dark mode text-muted fixed to #7B88A1.
- Screen 2: `02-pulse-signals.html` — DONE + REVISED. Originally was full-page signal list. Redesigned to three-panel layout per brainstorm spec: left panel (data sources + signal list), main content (signal detail on click). Follows Step 1 flow from brainstorm-idea-1.md.
- Screen 3: `03-investigation-flow.html` — DONE. Guided Q&A with 3 steps, collapsible exchanges, evidence panels, root cause conclusion card with cross-signal links.
- Screen 4: `04-model-simulation.html` — DONE. Tabbed: Sensitivity (tornado chart), Lever Playground (interactive sliders with mock outcome calc), Scenario Comparison (cards + table + SVG overlay chart).
- Screen 5: `05-admin-workspace.html` — DONE. Admin sidebar, 4 metric cards, collections table with pagination, tier config with toggles, credit cost config.
- Fix: `--text-muted` changed from `var(--nord3)` (#4C566A) to `#7B88A1` across ALL 5 files for better dark mode readability.
</completed_work>

<remaining_work>

- Owner said "im going to provide my feedback in pieces" — more feedback rounds expected
- Visual review of all 5 screens in browser needed (verification step from plan)
- Potential additional revisions based on owner feedback
- No commit made yet for the mockup files
</remaining_work>

<decisions_made>

- Used Inter font (Google Fonts CDN) per existing Spectra design system — plan specified Inter
- Nord palette strictly followed: #2E3440 bg, #3B4252 cards, #ECEFF4 bright text
- Dark mode `--text-muted` changed to #7B88A1 (was #4C566A/nord3 — too low contrast on dark cards)
- Screen 2 redesigned from scroll-list layout to left-panel-signal-list + main-detail-view per brainstorm spec (signals on left, detail in center)
- All files self-contained (inline CSS/JS, no build step, no dependencies except Google Fonts)
- Sidebar shown collapsed (64px) on screens 2-5 to maximize content area
</decisions_made>

<blockers>
- None. Waiting for owner's next feedback round.
</blockers>

<context>
The plan called for 5 mockup screens following the Analysis Workspace brainstorm (requirements/brainstorm-idea-1.md). Design references were gathered from the existing frontend (globals.css, chartTheme.ts, DataCard.tsx, StatusBadge.tsx, AdminSidebar.tsx etc.) by an explore agent. All screens share consistent Nord theme variables, dark/light toggle, and component patterns. The mockups are visual references for engineering, not production code.

Key design reference findings stored in explore agent (add2677acd744e673) — Nord palette constants, admin patterns, card/badge components, layout structures.
</context>

<next_action>
Resume by asking owner for their next piece of feedback. They said feedback would come in pieces. Open each HTML file in browser for visual review if needed. Apply any requested changes. When all feedback rounds complete, commit the mockups directory.
</next_action>
